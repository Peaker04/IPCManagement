---
review: mvp-web-flow
date: 2026-07-19
depth: deep
files_reviewed: 19
critical: 0
warning: 1
info: 3
status: conditional-pass
---

# Code review luồng MVP

## Warning

### CR-WARN-01: Candidate nghiệp vụ bị giới hạn ở 100 dòng đầu

- File: `frontend/src/features/workflow/pages/PurchasingPage.tsx`, `WarehousePage.tsx`
- Evidence: query ingredient demand `pageNumber: 1, pageSize: 100`, sau đó group ở client.
- Risk: chứng từ chỉ xuất hiện sau 100 dòng có thể không được chọn trong dialog.
- Remediation: BE trả distinct actionable material requests có server paging hoặc thêm filter ngày/khách hàng bắt buộc.

## Info

- `CR-INFO-01`: `WeeklyMenuPage.tsx` hơn 4.000 dòng, nên tách import wizard, KHSX, demand và purchase summary.
- `CR-INFO-02`: `PurchasingPage.tsx` chứa nhiều manager component trong một file; nên tách theo tab.
- `CR-INFO-03`: `SwimlaneProgress.tsx` là cleanup candidate nhưng phải xóa cùng commit dashboard dependency để clean checkout không bị vỡ.

## Findings đã sửa trong review

- FE thiếu mutation cho endpoint `POST /api/purchase-workflow/from-demand`.
- `DemandSummary` phân trang local trên dữ liệu đã được phân trang server.
- `WeeklyMenuPage` reset state đồng bộ trong effect, vi phạm `react-hooks/set-state-in-effect`.
- Approval card lặp nhãn next-action cạnh control thật.
- Kho tự chọn nhu cầu/kho đầu tiên thay vì yêu cầu người dùng xác nhận.
- Yêu cầu bổ sung bếp chỉ lưu React state nhưng báo như đã persist.
- Select portal dùng layer thấp hơn dialog overlay nên option không thể click bằng chuột/cảm ứng.

## Security và data integrity

- Mutation mới không làm yếu authorization; BE vẫn áp dụng `PurchaseAccess` và `PurchaseGenerateAccess`.
- Material request ID được BE parse/validate; lỗi API được đưa vào toast danger, không giả lập thành công.
- Không phát hiện secret, unsafe HTML hoặc bỏ qua validation trong các file thay đổi của lát cắt này.
