---
review: mvp-web-flow
date: 2026-07-19
depth: deep
files_reviewed: 26
critical: 0
warning: 1
info: 2
status: conditional-pass
---

# Code review luồng MVP — lần chạy lại

## Kết luận

Không phát hiện lỗi Critical trong endpoint phân trang candidate, mutation yêu cầu bổ sung hoặc các component FE nối API. Backend 273/273 test, frontend 89/89 test, build và lint đều pass. Migration đã áp dụng vào database thật; endpoint candidate và mutation supplemental đã được gọi thành công bằng tài khoản `admin` thật.

## Warning còn mở

### CR-WARN-01: Page container vẫn còn quá lớn sau lát cắt refactor đầu tiên

- `WeeklyMenuPage.tsx`: 4.014 dòng.
- `PurchasingPage.tsx`: 899 dòng.
- `ChefDashboardPage.tsx`: 633 dòng.
- Đã tách `ImportedLayoutMatrix`, `SupplierLineItem` và `chefDashboardTypes`, nên dependency mới có caller rõ ràng và build không vỡ.
- Risk còn lại: khó review theo section, dễ tạo merge conflict và test component nghiệp vụ chưa đủ cô lập.
- Remediation tiếp theo: tách import wizard/KHSX/demand khỏi Weekly Menu; tách quotation/order managers khỏi Purchasing; tách production-plan mapper và shift journal khỏi Chef.

## Findings đã đóng

- Candidate material request không còn bị giới hạn 100 dòng client: BE trả distinct request theo `pageNumber/pageSize`; FE dùng một pager server-side.
- UI yêu cầu bổ sung chỉ báo thành công sau `POST /api/supplemental-material-requests` trả thành công và lưu `PENDING`.
- Mutation supplemental kiểm tra issue/line, phiếu đã được bếp nhận, số lượng dương và warehouse scope trước khi ghi dữ liệu.
- Không xóa `SwimlaneProgress` vì GitNexus vẫn thấy caller ở baseline đã commit; tránh làm vỡ clean checkout.
- Swagger JSON từng trả 500 do hai tham số `[FromForm]`; đã gom `IFormFile` vào form DTO. `/swagger/v1/swagger.json` hiện trả 200.

## Security và data integrity

- Endpoint supplemental giữ `InventoryIssueAccess`, rate limit và actor từ claim; client không truyền `requestedBy`, warehouse hay status.
- Kho/ingredient/unit được suy ra từ issue line, không tin dữ liệu định danh do FE tự gửi.
- Candidate query chỉ đọc, dùng `AsNoTracking`, filter trạng thái nghiệp vụ và phân trang tại database.
- Không phát hiện secret, unsafe HTML hoặc success giả trong scope thay đổi.

## Giới hạn verification

Browser runtime của phiên Codex trả danh sách browser rỗng, nên chưa thể xác nhận thao tác click/keyboard và screenshot trực tiếp. Báo cáo giữ `conditional-pass`; không thay thế bằng một browser tool ngoài workflow.
