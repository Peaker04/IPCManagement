---
quick_id: 260619-t7p
status: in_progress
created: 2026-06-19
---

# Quick Task: R-01/R-05/R-06 Frontend Catalog, Code Splitting, Visual Regression

## Scope

Implement the next residual tasks requested by the user:

- `R-01`: remove primary Weekly Menu/Chef preview dependence on `DEV_FALLBACK_DISHES` and `DEV_FALLBACK_RAW_MATERIALS` where the current backend contract supports it.
- `R-05`: code-split main feature routes to address Vite chunk-size warning.
- `R-06`: verify the route visual regression test runner and baselines.

## Critical Gate

`R-01` depends on the weekly schedule/quantity contract in the tracker. Current backend code has `GET /api/dishes/catalog` with BOM lines and coordination order APIs, but no dedicated frontend weekly schedule endpoint. Do not invent a new hidden contract. If the schedule part still needs SDS, complete the catalog-backed BOM/cost replacement and record the remaining schedule blocker.

## Tasks

1. Add frontend dish catalog RTK Query endpoint/types and map BOM/unit/price fields into UI-friendly objects.
2. Replace Weekly Menu cost calculator and Chef preview BOM/material calculations with catalog data, keeping existing Vietnamese UI and route labels.
3. Apply `React.lazy`/`Suspense` route code splitting in `AppRouter.tsx`.
4. Verify frontend build/lint and route visual regression.
5. Update `.planning/STATE.md`, quick summary/verification, and `Project_Tracking v.xlsx`.

## Verification

- `npm run build --workspace frontend`
- `npm run lint --workspace frontend`
- `npm run test:visual --workspace frontend`
- `git status --short --branch`
- `node .gitnexus/run.cjs status`
