# Risk Register — UI/UX Refactor v2

| ID | Risk | Impact | Trigger | Mitigation | Gate |
|---|---|---:|---|---|---|
| R1 | Dirty user work is mixed into UI commit | Critical | File differs before task | Separate worktree/stash only with verification; stage allowlist only | Before every commit |
| R2 | CRITICAL shared table primitive breaks routes | Critical | GitNexus >15 symbols/processes | Add canonical replacement beside old primitive; migrate pilots; defer deletion | Wave 2 |
| R3 | Local pagination hides server data | High | UI page changes but query payload does not match contract | Explicit controller type and tests for totals/query invariants | Wave 2 |
| R4 | Cursor UI shows numeric/local semantics | High | Cursor endpoint has generic pager | Dedicated cursor copy and stack behavior | Wave 2/3 |
| R5 | Visual fix changes business mutation | High | Action handlers moved or payload changed | Preserve handlers and API hooks; mutation regression tests | Every route |
| R6 | Auth/backend unavailable makes smoke fail | High | Login stays at `/login` | Classify fixture vs environment; run backend/auth preflight; never mask with selector relaxations | Wave 0/4 |
| R7 | Global style dirty blocks trustworthy visual audit | High | `index.css` modified before task | Freeze global style until baseline; use scoped canonical tokens for new work | Wave 0/1 |
| R8 | Duplicate compatibility layers survive indefinitely | Medium | New consumer uses old adapter after migration | Deprecation comments, consumer inventory and cleanup deadline | Wave 5 |
| R9 | Snapshot updates hide regressions | Medium | Snapshot changed without issue record | Require before/after reason, viewport list and UI audit evidence | Wave 4 |
| R10 | Copy refactor removes traceability | Medium | Code identifiers disappear from operational UI | Keep code + semantic label pair where audit/support needs it | Wave 1/3 |
| R11 | Shared HIGH component migration breaks multiple role workflows | High | GitNexus reports 4–6 direct callers | Change only controller/viewport implementation; preserve props/DOM actions; run unit/lint/build/UI audit; commit per component | Wave 3 |
| R12 | Dirty AdminDataPage blocks compatibility cleanup | High | Existing 613-line user-owned diff overlaps six legacy tables | Freeze file; reconcile ownership and diff before migration; do not remove compatibility export while it remains a consumer | Wave 3/5 |
| R13 | Legacy adapter diverges from canonical viewport | Medium | Compatibility wrapper owns separate DOM/CSS behavior | Delegate adapter to `TableViewport`; retain only public prop/class compatibility until final consumer migration | Wave 3/5 |
| R14 | Semantic copy changes confuse exports or operational traceability | Medium | UI label is reused as backend enum/value | Change labels only; retain enum/value fields and technical codes in exported data | Wave 1/3 |
| R15 | Shared copy change affects multiple role workflows | Medium | Shared component has HIGH upstream callers | Keep vocabulary-only change, preserve DOM/action contracts, run shared UI audit and route regression gates | Wave 1/3 |
| R16 | Pagination mode contract is weakened by optional fields | High | A cursor consumer receives numeric total semantics or local consumer mutates query | Use discriminated union factories; keep local math pure; preserve legacy hook only as compatibility API | Wave 2/3 |
| R17 | Critical table shell migration changes interactive checklist behavior | Medium | Component has checkbox/action handlers inside shell | Migrate only wrapper, add caption, preserve row keys/handlers/disabled rules, run component and UI audit gates | Wave 3 |
| R18 | Expandable Chef table loses accessible relationship during viewport migration | Medium | Expanded content has button-controlled region | Preserve `aria-controls`, `aria-expanded`, row keys and nested region label; add viewport caption only | Wave 3 |
| R19 | Chef production action becomes detached from table surface | Medium | Daily plan table shares section-level send action | Change viewport wrapper only; preserve section badge action, row readiness mapping and route-level action callback | Wave 3 |
| R20 | Dashboard lane vocabulary obscures existing action semantics | Medium | Shared lane table uses technical short labels | Centralize semantic labels only; preserve lane IDs, active state and action renderer contract | Wave 1/3 |
| R21 | Global `DataTableShell` bridge changes legacy route geometry | Critical | 10 direct callers / 12 affected flows; visual run 8/20 | Keep shell protected; migrate callers individually; add public-contract test; require explained visual diff before any snapshot update | Next slice |
| R22 | Route refactor overwrites unrelated feature work | Critical | `WeeklyMenuPage` and `AdminDataPage` have pre-existing dirty diffs | Require hunk-level ownership classification and explicit handoff; stage only isolated refactor hunks | Wave 3 |
| R23 | Existing route visual baseline masks a safe isolated migration | High | Legacy and canonical wrapper produce identical visual mismatch | Run A/B wrapper comparison; record exact pixel/height evidence; defer snapshot update until dirty route is reconciled | Wave 3/4 |
| R24 | Legacy pagination helper diverges from canonical local controller | Medium | Two helpers implement page state or slicing separately | Delegate `usePaginatedRows` to `useLocalPagination`; preserve legacy return API; verify unit/lint/build/UI audit and keep AdminDataPage ownership unchanged | Wave 3 |
| R25 | Table caption is visually hidden but not associated with its scroll region | High | Screen reader receives region label without table context | Preserve viewport geometry and public props; add per-instance `aria-describedby` contract; verify with RTL test and route UI audit | Wave 3/4 |
| R26 | Shared pagination primitive changes many role workflows | Critical | GitNexus upstream impact reports 8 direct callers and 13 flows | Keep public props/classes/callbacks; centralize only metadata/copy; add clamp/range tests; require full unit/lint/build/UI audit and staged detect scope before commit | Wave 3 |
| R27 | Cursor pagination is incorrectly treated as numeric pagination | High | Cursor control exposes total pages or sends local page semantics to cursor endpoint | Use a dedicated `CursorPaginationBar` with only page display, hasNext and navigation callbacks; preserve cursor state/request payloads; test absence of numeric totals | Wave 3 |
| R28 | Route-local pagination refactor stages unrelated WeeklyMenu feature work | Critical | Dirty route contains BOM/template/production-plan changes near pagination footer | Classify hunks, stage only import/footer/range hunk, run staged diff check and preserve all feature hunks unstaged | Wave 3 |
| R29 | Feature-added shell is misclassified as a legacy consumer | Critical | `DataTableShell` appears only in the user-added BOM current-table diff | Compare route against clean baseline before migration; if consumer is feature-added, keep it protected and require explicit handoff instead of staging a wrapper-only change | Wave 3 |
| R30 | Grouped domain pages are rendered as misleading row pagination | High | Production-plan pages represent one service-day group, not a slice of rows | Use dedicated `PageStepper` with page count only; keep day/plan/serving summary outside the control; verify grouped-page contract and route gate | Wave 3 |
| R31 | Filtering adjustment rows after a bounded movement page drops valid rows | High | API pages all movement types, then UI filters `adjustment` locally | Add optional case-insensitive `MovementType` API filter before cursor slicing; keep existing unfiltered callers unchanged; add regression assertion and page-parameter verification | Wave 3 |
| R32 | Backend movement filter impact is not represented in GitNexus index | Medium | `GetStockMovementsAsync`, `GetStockMovementPageAsync` and `WorkflowReportQueryDto` return UNKNOWN impact | Treat UNKNOWN as an explicit risk; verify compile, full backend tests and staged diff; do not infer zero blast radius from an empty graph result | Every backend report-contract change |
| R33 | Report-wide kitchen aggregates materialize issue IDs | Medium | Aggregate KPI needs matching return/waste rows across all issue lines | Keep UI payload bounded and preserve quantity semantics now; add DB-side grouped aggregate as a performance follow-up before very large-history release | Wave 3/5 |
| R34 | Parallel Playwright servers race on Vite dependency cache | Medium | Controls, UI audit and smoke start together and Vite reports `EPERM` during dependency rename | Run browser gates sequentially; treat connection-refused failures as environment failures until a serial rerun reproduces them | Wave 4 |
| R35 | CSS cleanup removes a rule still owned by dirty route work | Critical | Global stylesheet or route file has mixed refactor and BOM/production-plan changes | Build a selector/reference inventory, keep dirty route/global CSS protected, delete only isolated proven-dead rules | Wave 4.5 |
| R36 | Browser-native feedback removal hides a required decision | High | `alert`/`confirm` is replaced by passive toast although user must confirm or recover | Use typed toast for transient outcomes, InlineAlert for contextual failures and Dialog for confirmation; test keyboard/focus paths | Wave 4.5 |
| R37 | Shared toast provider creates duplicate announcements or stale messages | High | Multiple route effects publish the same mutation/loading state | One provider at app root, typed event contract, explicit dismissal and route unmount cleanup; assert announcement count in tests | Wave 4.5 |
| R38 | Layout cleanup changes operational action reachability | High | Header/sidebar/table wrappers are collapsed without preserving action ownership | Keep one command region and preserve action callbacks/labels; run controls, UI audit and mobile route smoke per group | Wave 4.5 |
| R39 | Raw technical copy remains after visual cleanup | Medium | User sees enum/code labels instead of meaning | Centralize semantic copy and retain technical identifier only as secondary traceable metadata | Wave 4.5 |
| R40 | Shared document status label change affects four workflow pages | High | `DocumentRail` has direct callers in Warehouse, Chef, Purchasing and Approval | Keep raw status for logic, change only rendered label through `formatWorkflowStatus`, run unit/lint/build plus serial controls/smoke/UI audit before commit; mitigated by `08e7b3f` | Wave 4.5 |
| R41 | Shared role-inbox action copy is coupled to dirty route/API work | High | `RoleInbox` has three callers and its action text is supplied by `workflowApi`/route renderers; Dashboard is user-owned dirty work | Shared boundary now translates only the callback display copy (`7729347`); do not edit dirty mapper/callers until ownership handoff, and preserve raw values for logic | Wave 4.5 |
| R42 | Dashboard/Admin still render raw action/status text outside shared surfaces | High | Dirty Dashboard/Admin route diffs contain direct `nextAction`/status rendering near feature work | Reconcile ownership first, then apply route-local formatter with exact impact and isolated hunk staging; do not overwrite existing feature changes | Wave 4.5 |
| R43 | Alert consolidation changes stale Chef visual geometry | Medium | Chef snapshot predates retained KHSX/table/journal content and now also changes alert grouping | Keep snapshot unchanged, record actual dimensions/root cause, verify controls/smoke/UI audit and reconcile baseline ownership before updating visual fixtures | Wave 4.5 |
| R44 | Price aggregate calculation materializes before paging | Medium | New page endpoints bound the response but still load filtered receipt lines before grouping/counting; very large history can pressure memory/latency | Keep page-number response contract and legacy list compatibility; later push aggregate grouping/count/order into SQL with a separate backend impact gate and performance tests; mitigated for current UI payload by `2946327` | Reports follow-up |
| R45 | Purchasing inline supplier lookup repeats legacy list calls | Medium | The paged quotation manager, request table and order table are migrated, but each supplier-line editor can still fetch the full quotation list for best-price suggestions | Keep legacy list compatibility for inline best-price semantics; make the page endpoint execute `Count` plus database `Skip/Take` and independently preserve the best-price ID. Follow up with a batched ingredient lookup only if unique-ingredient request volume remains high; core table payload risk mitigated by `30b87d6`, `cea09f9` and `3e5365d`, query materialization mitigated in the current slice | Purchasing follow-up |
| R46 | OperationalFrame shared shell change has critical route blast radius | Critical | GitNexus reports 9 direct callers across 7 processes, including dirty WeeklyMenu/AdminData routes | Do not alter shell geometry globally; use caller-local wrappers or scoped classes until route ownership is reconciled. Any shared change requires full route, controls, smoke and UI-audit gates plus staged impact detection | Wave 4.5 |
| R47 | ContextStrip shared KPI geometry change has critical route blast radius | Critical | GitNexus reports 6 direct callers across 5 processes, including dirty AdminData routes | Keep the shared KPI DOM/geometry stable; prefer route-local item grouping or scoped wrappers after visual evidence. Any shared change requires all responsive gates and preservation of `dt`/`dd` semantics | Wave 4.5 |
| R48 | CommandBar shared action layout change breaks multiple route action groups | Critical | GitNexus reports 8 direct callers across 6 processes, including dirty WeeklyMenu/AdminData routes | Keep `CommandBar` as a compatibility boundary; use route-local `actionsClassName` only with concrete visual evidence, preserve action classification/callbacks, and require full controls/smoke/UI-audit gates before any shared change | Wave 4.5 |
| R49 | Approval inbox client-side pager masks a list-only contract | High | `ApprovalPage` previously requested `limit: 100` from list-only `/approvals/inbox`; `ApprovalQueue` sliced the returned array locally, so large inboxes still transferred and rendered too much data | Mitigated by `e0e7ba1`: HTTP now returns a bounded cursor page, the route requests one page at a time, and smoke proves continuation query propagation. Keep R61 open until backend cross-source transition fixtures pass | Wave 4.5 |
| R50 | GitNexus index omits a route symbol during impact/detect analysis | Medium | `ApprovalRulesPage` is present in source and route tests but the refreshed graph returns `UNKNOWN`/no changes for the symbol | Treat graph output as insufficient evidence, keep edits route-local, inspect direct imports/callers with source search, require full UI/type/test gates, and re-run analysis after the next index refresh | Wave 4.5 |
| R51 | GitNexus detect misses a clean route hunk after a valid LOW impact result | Medium | `PurchasingPage` impact resolves LOW, but staged detection reports no changes for the scoped quotation-form hunk | Preserve the exact allowlist, run full route/UI/type gates and source diff review, keep the shared shell untouched, and refresh the index before the next shared-symbol edit | Wave 4.5 |
| R52 | Shared Chef excess-material dialog responsive change has HIGH caller impact | High | `ExcessMaterialDialog` has one direct caller and three Chef execution flows | Keep the change CSS-only, preserve dialog state/submit behavior and API payloads, add a focused component contract test, and require full controls/smoke/UI-audit gates; mitigated by `55dfd63` | Wave 4.5 |
| R53 | Shared Chef supplemental-request copy change affects three flows | High | `SupplementalRequestDialog` has one direct caller and three Chef execution flows | Keep labels/placeholder changes presentation-only, preserve validation text, callback and payload, add a focused unit contract, and require full route gates; mitigated by `986d4dd` | Wave 4.5 |
| R54 | Chef summary/BOM copy normalization affects shared dashboard components | Low | `ChefHeader` and `ActiveDishesGrid` are reused by the Chef dashboard flows | Keep the change presentation-only, remove only the redundant uppercase utility, preserve production-plan data and expand/collapse behavior, and verify focused/full unit plus route gates | Wave 4.5 |
| R55 | Chef checklist copy normalization affects shared signoff surface | Low | `MaterialChecklist` is used by the head-chef and Chef dashboard flows | Keep the change presentation-only, preserve checkbox disabled rules, callback payloads and status values, and verify the focused/full unit and route gates; mitigated by `e8ddfbd` | Wave 4.5 |
| R56 | Shared Chef excess-material dialog copy normalization has HIGH upstream impact | High | `ExcessMaterialDialog` has one direct caller and three Chef execution flows; staged graph detection reports no process mutation because the change is display-only | Preserve dialog state, validation, focus/close behavior, callback and payload; treat upstream impact as authoritative, require full route gates and keep the change limited to labels/utility classes; mitigated by `2da39fe` | Wave 4.5 |
| R57 | Shared Chef operational-action heading style affects the action flow | Medium | `OperationalActions` is used by the head-chef and Chef dashboard flows, and staged detection reports one shared flow | Keep the change display-only, preserve both dialog triggers and callbacks, and require the focused/full unit plus serial controls/smoke/UI-audit gates; mitigated by `334b520` | Wave 4.5 |
| R58 | Shared supplemental-request dialog label style has HIGH upstream impact | High | `SupplementalRequestDialog` has one direct caller and three Chef execution flows; staged detection reports one direct flow | Preserve required markers, validation, focus/close behavior, callback and payload; treat upstream impact as authoritative and keep the change limited to label utility classes with full route gates; mitigated by `9c4532a` | Wave 4.5 |
| R59 | Approval history enum translation can hide an unknown backend decision | Medium | History records may contain values beyond the known approve/reject/submit set | Keep raw values as the formatter fallback through the shared workflow status mapper, preserve API data unchanged, and test known decisions plus full route gates; mitigated by `746de4b` | Wave 4.5 |
| R60 | Purchasing route copy hunk is not mapped by staged GitNexus detection | Medium | `PurchasingPage` impact is exact LOW but `detect_changes --scope staged` reports no change for the clean header hunk | Keep the allowlist to one route file, preserve technical field/API values, use source diff plus full route gates as evidence, and refresh the graph before the next shared-symbol edit; mitigated by `524276a` | Wave 4.5 |
| R61 | Approval cursor pagination can create duplicate/missing records across heterogeneous sources | Critical | `e0e7ba1` uses the complete opaque due-date/code/item-id tuple; `2ab826b` proves a representative four-source replay with `Limit = 1`; `684bc97` makes price-alert selection resumable in keyset batches. Same-code/item-id siblings and production-shaped large-volume behavior are still unproven | Add same-code sibling fixtures, a role-filtered large-volume test and query/batch evidence before closing the risk. Do not claim full lazy-loading correctness from the representative fixture alone | Wave 4.5.2 |
| R62 | Legacy table styles can override route-local mobile width rules | Medium | Dirty global CSS sets `min-width: 100%` with higher specificity, or a route mounts a table only after tab activation | Use a scoped canonical selector on the viewport/table boundary, assert computed width plus local `scrollWidth > clientWidth`, and assert document width remains bounded at 390px; mitigated for Purchasing and Warehouse by `e3f3c27` | Wave 4.5 |
| R63 | Generic empty-state minimum height creates artificial gaps in workflow panels | Medium | A route-specific empty state inherits the legacy 220px reservation, pushing action controls below the fold on mobile | Add a route-scoped empty-state class only where the panel contract permits content-sized height, assert computed `min-height` and action reachability, preserve desktop panel sizing and all mutation handlers; mitigated for Coordination by `eb3528c` | Wave 4.5 |
| R64 | Weekly Menu day matrix collapses into unreadable vertical headers on mobile | High | The 7-day matrix has no explicit table minimum width, so the browser distributes seven columns across 390px instead of using the viewport scroll boundary | Set a route-scoped table minimum width, assert local `scrollWidth > clientWidth` and document no-overflow at 390px, preserve the dirty route’s import/menu data contract; mitigated by `e3d9efe` | Wave 4.5 |
| R65 | AdminData semantic-copy cleanup can accidentally stage adjacent BOM/import feature work | High | `AdminDataPage.tsx` contains a large user-owned dirty diff around the import command surface | Stage only the presentation/inactive-command hunks, inspect the cached source diff, preserve handlers/payloads and rerun the full frontend gates; mitigated by `b9772ce` |
| R66 | Removing a legacy report limit can break request guards or re-expand a long-lived query | Medium | Smoke contract requires a bounded `limit` query on filtered report requests, while page endpoints also carry `pageSize` | Preserve the query parameter at the canonical bounded `reportPageSize` value, verify filtered request propagation and full smoke; mitigated by `05e0e5a` |
| R67 | Re-indexed GitNexus graph overstates or models hidden `DataTableShell` callers beyond direct source inventory | Critical | A fresh analyze reports `16` impacted symbols and `12` flows, while `rg` finds one runtime consumer plus barrel/test references | Treat the graph result as authoritative for destructive cleanup, keep `DataTableShell` and `index.css` protected, and reconcile graph edges against import/runtime ownership before any shared migration |
| R68 | Desktop-only UI audit misses mobile route geometry regressions | Medium | The previous audit covered only `1365px`; the reference defect is a narrow-shell/mobile layout failure | Run the same protected-route/action/dialog/overflow audit at `390×844` and retain separate evidence; mitigated by `6ff9b11` with desktop/mobile `4/4` pass |
| R69 | Token cleanup changes icon contrast or warning semantics on Reports | Low | Replacing repeated literal slate colors can accidentally affect warning/exception icons or inherit an unintended theme value | Replace only the exact neutral `#475569` literals with the existing `--ipc-slate-600` token, preserve `--ipc-danger` warning state, and verify route controls/UI audit; mitigated by `73ea59b` |
| R70 | Coordination zebra-row token cleanup changes operational table contrast | Medium | `OrderTable` feeds Coordination flows and staged detection reports two display/action paths | Replace only the neutral even-row color, preserve hover/status/action classes and payloads, and verify targeted coordination control plus desktop/mobile UI audit; mitigated by `0bf1bda` |
| R71 | Weekly Menu token cleanup stages adjacent production-plan/import feature work | Critical | The route has a 122-line user-owned feature diff surrounding the six neutral icon literals | Use hunk-level staging, inspect cached versus unstaged diff, preserve all feature hunks and run Weekly Menu controls/lint/build; mitigated by `e998feb` |
| R72 | SLA copy normalization can hide or alter technical workflow values | Medium | Approval Rules is not mapped in the current GitNexus index and Reports exports the same labels used in its table | Change visible labels only, preserve enum/API/CSV values, add a focused rendered-label assertion and run lint/build; mitigated by `ea71fb6` |
| R73 | Shared approval deadline copy can alter operational meaning | Medium | `ApprovalQueue` is used by Approval and Admin flows; staged detection reports one shared flow | Change only the visible `SLA` prefix, preserve deadline calculation, status, action callback and API values, and verify focused unit plus lint/build; mitigated by `5b8457b` |
| R74 | Document owner semantic fix affects multiple operational routes | High | `DocumentRail` has six direct callers across Chef, Approval, Purchasing, Warehouse, Weekly Menu and Admin flows | Preserve the existing class/layout, copy action, toast behavior and document actions; change only the wrapper to a valid `dl`, add semantic unit coverage and run full route gates; mitigated by `d5e8db1` with unit `87/87`, controls `19/19`, smoke `15/15`, UI audit `4/4`, lint/build |
| R75 | Removing duplicate shell token changes variable resolution | Low | `ui-redesign.css` repeated `--ipc-shell-width` while `index.css`, imported first, already owns the canonical value | Remove only the duplicate declaration, verify exactly one source definition and run build/lint/UI audit; mitigated by `deaa5cf` |
| R76 | Approval next-action translation hides an action value used by callbacks | Medium | `ApprovalQueue` renders `nextAction` beside action controls and has direct Admin/Approval callers | Translate only the rendered text through `formatWorkflowStatus`, preserve the original record passed to callbacks, add a raw-key assertion and run full route gates; mitigated by `638b3a5` |
| R77 | Route next-action translation changes handoff label or destination | Low | Warehouse and Reports render next-action values directly inside links while the route destination depends on row tone | Translate only link text, preserve tone-based destinations and raw row data, and run controls/smoke/UI audit plus lint/build; mitigated by `fe0b57a` |
| R78 | Handoff/NVL copy normalization breaks smoke selectors or user navigation vocabulary | Medium | Reports and Purchasing expose these labels in tabs, KPI context and table headers; staged detection reports one Reports flow | Change visible copy only, preserve tab ids, route links, API values and pagination, update exact smoke expectation and rerun all route gates; mitigated by `da73ff5` |
| R79 | Operational document/report labels remain technical in shared and route-local surfaces | Medium | `DocumentRail` has six callers; Reports and Warehouse also expose workflow terminology in visible titles/descriptions | Change visible copy only, preserve DOM/CSS/API/action contracts, run full controls/smoke/UI-audit gates and staged detection; mitigated by `4cf73d8` |
| R80 | Admin statistics keeps English handoff terminology inside a dirty route file | Medium | `AdminDataPage` has a large user-owned BOM/import diff, so a broad cleanup could overwrite feature work | Stage only the isolated header-copy hunk, preserve all route logic and feature hunks, run lint/build/controls/smoke and detect staged scope; mitigated by `c689af0` |
| R81 | Shared shell vocabulary change propagates to every protected route context | Medium | `MainLayout` feeds the App shell and staged detection reports four affected execution flows | Change only the visible overview label, preserve route context keys, lane resolution and action state, and run lint/build plus desktop/mobile UI audit; mitigated by `d4d48fd` |
