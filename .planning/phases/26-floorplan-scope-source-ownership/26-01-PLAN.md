---
phase: 26-floorplan-scope-source-ownership
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/uiFloorplanScopeRegistry.ts
  - frontend/tests/uiFloorplanScopeContract.test.ts
autonomous: true
requirements:
  - FLOOR-01
must_haves:
  truths:
    - "Per D-01, every production route, tab, nested view, and declared role/data-state combination in desktop v1.4 has exactly one canonical identity key."
    - "Missing, duplicate, orphan, and stale scope identities each cause a source-aware automated failure rather than being hidden by aggregate counts."
    - "Per D-06, scope closure is test-owned and changes no production behavior, policy, API, cache, lifecycle, route access, rendered UI, database state, or roadmap scope."
    - "Downstream Phase 26 plans can import one authoritative typed scope projection instead of copying route or state vocabulary."
  artifacts:
    - path: "frontend/tests/uiFloorplanScopeRegistry.ts"
      provides: "Typed canonical scope identities and deterministic exact-set comparison helpers"
      exports:
        - "UiFloorplanScopeEntry"
        - "UiFloorplanScopeKey"
        - "uiFloorplanScopeRegistry"
        - "buildUiFloorplanScopeKey"
        - "compareUiFloorplanScopeSets"
    - path: "frontend/tests/uiFloorplanScopeContract.test.ts"
      provides: "Source-aware route/tab/nested-view/role/data-state closure and negative drift probes"
      contains: "missing, duplicate, orphan, stale"
  key_links:
    - from: "frontend/tests/uiFloorplanScopeContract.test.ts"
      to: "frontend/src/routes/AppRouter.tsx and frontend/src/lib/routeConfig.ts"
      via: "source-aware extraction of mounted routes and route configuration"
      pattern: "AppRouter|routeConfig"
    - from: "frontend/tests/uiFloorplanScopeContract.test.ts"
      to: "frontend/tests/uiFloorplanScopeRegistry.ts"
      via: "exact key-set comparison, never count-only comparison"
      pattern: "compareUiFloorplanScopeSets"
    - from: "frontend/tests/uiFloorplanScopeContract.test.ts"
      to: "frontend/tests/uiCanonSourceInventory.ts and frontend/tests/uiStatePurityInventory.ts"
      via: "imports/reuses existing declared source and state inventories rather than duplicating their vocabulary"
      pattern: "uiCanonSourceInventory|uiStatePurityInventory"
---

<objective>
Establish the exact machine-readable identity set for every production route, tab, nested view, and declared role/data state in the Phase 26 desktop scope per D-01, while enforcing the no-behavior-change fence in D-06.

Purpose: Give every later floorplan, capability, source-ownership, DOM-join, and leakage contract one authoritative set of canonical keys, with deterministic failure for missing, duplicate, orphan, or stale identities.
Output: A test-owned typed scope registry plus a source-aware exact-set contract test; no production file, runtime, database, route, policy, API, cache, lifecycle, or rendered behavior changes.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-floorplan-scope-source-ownership/CONTEXT.md
@.planning/phases/26-floorplan-scope-source-ownership/26-PLAN-OUTLINE.md
@frontend/src/routes/AppRouter.tsx
@frontend/src/lib/routeConfig.ts
@frontend/tests/uiCanonSourceInventory.ts
@frontend/tests/uiCanonSourceInventory.test.ts
@frontend/tests/uiStatePurityInventory.ts
@frontend/tests/uiStatePurityContract.test.ts
@frontend/tests/ui-audit.spec.ts
@frontend/tests/visual-routes.spec.ts

<interfaces>
Create `UiFloorplanScopeEntry` as the complete identity record with route identity/path, surface kind (`route`, `tab`, or `nested-view`), stable surface ID, nullable parent-surface ID, declared role-state ID, and declared data-state ID. `UiFloorplanScopeKey` must be derived deterministically from every identity dimension. Export `uiFloorplanScopeRegistry`, `buildUiFloorplanScopeKey`, and a pure `compareUiFloorplanScopeSets` result that reports separate `missing`, `duplicates`, `orphans`, and `stale` key collections. These exports are the only scope-key contract later Phase 26 plans consume.
</interfaces>
</context>

## Artifacts this phase produces

- `frontend/tests/uiFloorplanScopeRegistry.ts` — new test-owned contract exporting `UiFloorplanScopeEntry`, `UiFloorplanScopeKey`, `uiFloorplanScopeRegistry`, `buildUiFloorplanScopeKey`, and `compareUiFloorplanScopeSets`.
- `frontend/tests/uiFloorplanScopeContract.test.ts` — new Vitest contract containing the source-aware production-scope extractor and explicit missing/duplicate/orphan/stale negative probes.
- No production symbol, package manifest, lockfile, runtime artifact, evidence artifact, database object, or file outside the two paths above is created or modified.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the canonical exact-scope identity contract and registry</name>
  <files>frontend/tests/uiFloorplanScopeRegistry.ts, frontend/tests/uiFloorplanScopeContract.test.ts</files>
  <read_first>
    <file>.planning/phases/26-floorplan-scope-source-ownership/CONTEXT.md</file>
    <file>.planning/phases/26-floorplan-scope-source-ownership/26-PLAN-OUTLINE.md</file>
    <file>frontend/src/routes/AppRouter.tsx</file>
    <file>frontend/src/lib/routeConfig.ts</file>
    <file>frontend/tests/uiCanonSourceInventory.ts</file>
    <file>frontend/tests/uiStatePurityInventory.ts</file>
    <file>frontend/tests/ui-audit.spec.ts</file>
    <file>frontend/tests/visual-routes.spec.ts</file>
  </read_first>
  <behavior>
    - Test 1: a canonical key changes when any route, surface-kind, surface, parent, role-state, or data-state identity dimension changes.
    - Test 2: the registry contains no blank identity dimension and no duplicate canonical key.
    - Test 3: every tab or nested-view entry resolves to a declared parent on the same route; a broken parent is reported as an orphan.
    - Test 4: registry ordering and exact-set diagnostics are deterministic so downstream contracts and diffs remain stable.
  </behavior>
  <action>Before editing, capture `git status --short --branch`, obtain the current branch with `git branch --show-current`, and apply the D-07/outline Lightweight graph gate: call GitNexus `list_repos` once, confirm the indexed `IPCManagement` slot for that explicit branch, and record raw/effective risk as Lightweight; do not run per-symbol impact because this task creates only test-owned shared vocabulary. Write the failing contract cases first, then create `uiFloorplanScopeRegistry.ts` with the exact exports declared above. Build each registry row as a complete canonical identity rather than as a route count or representative sample. Inventory mounted production routes from `AppRouter.tsx` and `routeConfig.ts`, tab and nested-view identities from route-loaded screen sources and every `ViewSwitcher` callsite, and declared role/data-state identities from the existing state/visual inventories. Preserve source distinctions: do not merge identities by label, path caption, or aggregate count. Reuse/import existing test-owned inventory values where they are already authoritative; do not create a second unguarded route, role, or data-state vocabulary. Keep the implementation under `frontend/tests/`, use only installed dependencies or Node APIs, and do not edit package manifests or lockfiles. Per D-06, do not edit production code, add a route/tab/view/action, change access or rendering, start an application runtime, or touch any database.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiFloorplanScopeContract.test.ts --maxWorkers=1</automated>
    <automated>git diff --check -- frontend/tests/uiFloorplanScopeRegistry.ts frontend/tests/uiFloorplanScopeContract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The two declared files exist and no task-created file exists outside the declared scope.
    - All five contract exports are present and `UiFloorplanScopeKey` depends on every identity dimension.
    - Registry rows are nonblank, unique, deterministic, and retain route/tab/nested/role/data-state distinctions.
    - No production, package, lockfile, runtime, evidence, or database artifact changes.
  </acceptance_criteria>
  <done>The typed registry is a complete deterministic candidate projection with internal key and hierarchy invariants, ready for independent source-derived exact-set closure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove source-aware exact-set closure and finish the Lightweight graph gate</name>
  <files>frontend/tests/uiFloorplanScopeRegistry.ts, frontend/tests/uiFloorplanScopeContract.test.ts</files>
  <read_first>
    <file>frontend/tests/uiFloorplanScopeRegistry.ts</file>
    <file>frontend/tests/uiFloorplanScopeContract.test.ts</file>
    <file>frontend/src/routes/AppRouter.tsx</file>
    <file>frontend/src/lib/routeConfig.ts</file>
    <file>frontend/tests/uiCanonSourceInventory.test.ts</file>
    <file>frontend/tests/uiStatePurityContract.test.ts</file>
  </read_first>
  <behavior>
    - Test 1: removing one source-derived identity from the registry reports that exact canonical key as `missing`.
    - Test 2: repeating one registry identity reports that exact canonical key as `duplicates` even when unique counts would otherwise match.
    - Test 3: breaking a tab/nested-view parent relationship reports the child canonical key as `orphans`.
    - Test 4: retaining an old route, tab, nested-view, role-state, or data-state token not declared by current production/test authorities reports that exact canonical key as `stale`.
    - Test 5: the unmodified current-source projection and registry are bidirectionally equal with all four diagnostic collections empty.
  </behavior>
  <action>Implement an independent source-aware discovery path in `uiFloorplanScopeContract.test.ts` using the repository's existing source-reading/AST conventions and already-installed dependencies; do not make the registry its own oracle. Resolve mounted routes bidirectionally across `AppRouter.tsx` and `routeConfig.ts`, follow static/lazy route loaders into their current screen sources, enumerate every `ViewSwitcher` callsite and declared option identity, and join the existing role/data-state inventories without copying their literals. Normalize only syntax-level equivalences such as import resolution or path parameter notation; never merge semantically distinct screens or states. Compare full canonical key sets with `compareUiFloorplanScopeSets`, and create negative probes by cloning the current-source set and separately dropping, duplicating, orphaning, and staling one identity so each diagnostic is demonstrated by behavior. Define the categories precisely: `missing` is current-source identity absent from the registry; `duplicates` is repeated registry identity; `orphans` is a child whose declared parent is absent on the same route; `stale` is registry identity absent from all current source authorities. Fail with sorted key-level diagnostics, not counts. Prove that production source does not import either new test-owned file. Run the focused scope, source-inventory, and state-purity tests, then the existing root verification/count-preservation gate. Finish D-07 by calling GitNexus `detect_changes` once with `repo=IPCManagement` and the explicit current branch; reconcile every changed shared test contract and require zero undispositioned production process/edge. If detection exposes a production process/edge, public contract, unresolved mapping, partial/truncated result, or confidence below 0.8, stop and escalate to the Full-analysis lane rather than lowering the evidence. Compare final status to the captured pre-edit baseline, preserve all unrelated worktree changes, and do not commit or push from this plan unless the owning GSD executor explicitly performs its normal atomic plan commit.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiFloorplanScopeContract.test.ts tests/uiCanonSourceInventory.test.ts tests/uiStatePurityContract.test.ts --maxWorkers=1</automated>
    <automated>npm run verify</automated>
    <automated>git diff --check -- frontend/tests/uiFloorplanScopeRegistry.ts frontend/tests/uiFloorplanScopeContract.test.ts</automated>
    <automated>$matches = Select-String -Path 'frontend/tests/uiFloorplanScopeRegistry.ts','frontend/tests/uiFloorplanScopeContract.test.ts' -Pattern '(?i)(password\s*=|api[_-]?key\s*=|secret\s*=|TODO|FIXME|placeholder|hardcoded for now)' -AllMatches; if ($matches) { $matches | ForEach-Object { $_.ToString() }; exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - Current production route/tab/nested-view and declared role/data-state identities equal registry identities bidirectionally by exact canonical key.
    - Missing, duplicate, orphan, and stale mutants each fail in the named diagnostic bucket with the offending key.
    - Focused Vitest, existing source/state inventory gates, root verification/count preservation, whitespace, and secret/stub scans pass.
    - Explicit-branch final `detect_changes` has no undispositioned production process/edge; otherwise the plan stops at the required escalation gate.
    - Final diff attributable to this plan contains only the two declared test-owned files and preserves D-06, `ipc_lane1`, and the five-phase roadmap.
  </acceptance_criteria>
  <done>FLOOR-01 is executable: one authoritative registry exactly equals current production and declared state authorities, every required drift class is proven to fail, and Lightweight graph evidence is reconciled without behavior or scope mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production route/view source → test-owned extractor | Repository source is parsed as data to derive the actual identity set; the checker must not execute page code or trust a copied list. |
| Existing state inventories → canonical scope registry | Shared role/data-state vocabulary crosses into a downstream-consumed contract and must remain source-linked. |
| Test-owned registry → later Phase 26 contracts | Downstream plans trust exported keys, so ambiguity or stale identities would contaminate floorplan and ownership evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | Tampering | `uiFloorplanScopeRegistry.ts` | mitigate | Derive an independent actual set from production/state authorities and enforce bidirectional key equality plus explicit missing/duplicate/orphan/stale mutants. |
| T-26-01-02 | Spoofing | Canonical identity construction | mitigate | Include every route, surface, parent, role-state, and data-state dimension in a deterministic key; reject blanks and collisions. |
| T-26-01-03 | Repudiation | Drift diagnostics | mitigate | Emit sorted offending keys in named buckets so failures identify the exact source/registry mismatch rather than only a changed count. |
| T-26-01-04 | Information disclosure | Test-owned registry boundary | mitigate | Keep source-reading and contracts under `frontend/tests/`; assert production source imports neither new test file and emit no source paths into runtime code. |
| T-26-01-05 | Denial of service | Source-aware scan | mitigate | Restrict reads to declared frontend source/test authorities and resolved route/view modules; exclude `node_modules`, `dist`, artifacts, and recursive workspace-wide scanning. |
| T-26-01-SC | Tampering | Package supply chain | accept | No package install or dependency change is authorized; package manifests and lockfiles must remain outside the diff. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID / item | Plan | Status | Notes |
|--------|-----------|------|--------|-------|
| GOAL | Exact expectation set for every existing screen | 26-01 | COVERED | This plan establishes the authoritative identity set consumed by the remaining source-ownership plans. |
| REQ | FLOOR-01 | 26-01 | COVERED | Exact production route/tab/nested-view/declared-state closure with four drift failures. |
| RESEARCH | Exact scope closure before presentation work | 26-01 | COVERED | Uses the already-approved outline; no new research or presentation remediation occurs. |
| CONTEXT | D-01 | 26-01 | COVERED | Full exact-set identity, not sampling or aggregate counts. |
| CONTEXT | D-06 | 26-01 | COVERED | Test-owned contracts only; behavior, policy, API, cache, lifecycle, access, runtime, database, and roadmap stay unchanged. |
| CONTEXT | D-07 | 26-01 | COVERED | Shared route/state vocabulary uses the Lightweight graph gate with explicit-branch final detection and escalation conditions. |

Phase 26 decisions D-02–D-05 and requirements FLOOR-02–FLOOR-04/SOURCE-01–SOURCE-03 remain assigned to Plans 26-02–26-05 exactly as locked by `26-PLAN-OUTLINE.md`; Phase 27 capture/geometry, Phase 28 judging, Phase 29 remediation, Phase 30 rejudge/closeout, deferred VIEW-01, and deferred PROD-01 are deliberate exclusions rather than gaps.

<verification>
1. Run the focused Vitest command and confirm the live registry has empty `missing`, `duplicates`, `orphans`, and `stale` diagnostics while each isolated negative mutant fails in its intended bucket.
2. Run `npm run verify`; existing application/API/frontend and UI-completeness gates must remain green, and discovered test counts must not decrease.
3. Confirm the final attributable diff contains only `frontend/tests/uiFloorplanScopeRegistry.ts` and `frontend/tests/uiFloorplanScopeContract.test.ts`; package manifests, lockfiles, production source, `.planning` outside the executor summary, runtime/evidence files, and database artifacts are unchanged.
4. Run `git diff --check` and the declared secret/stub scan. The planning/implementation graph reason is recorded as Lightweight for behavior-bearing shared test vocabulary, not as a production-symbol edit.
5. Reconcile one final GitNexus `detect_changes` call against `repo=IPCManagement` and the explicit current branch. Accept completion only with no undispositioned production process/edge; otherwise escalate and satisfy Full-analysis Done before continuing.
6. Do not start headed browser capture, mutate `ipc_lane1`, reset/seed/import/restore any database, push, add a route/panel/action, or begin Phase 27–30 work.
</verification>

<success_criteria>
- FLOOR-01 is satisfied by exact source-derived key equality for all production routes, tabs, nested views, and declared role/data states.
- Missing, duplicate, orphan, and stale scope identities have deterministic, key-specific automated failures.
- One typed test-owned registry is the sole downstream Phase 26 scope-key authority, with a source-aware drift guard against copied vocabulary.
- Focused tests, existing count-preservation/root gates, hygiene checks, and the final explicit-branch Lightweight GitNexus gate pass.
- D-01 and D-06 are preserved: no business behavior/policy/API/cache/lifecycle/route-access/rendered behavior change, no database or `ipc_lane1` mutation, no push, and no roadmap or phase expansion.
</success_criteria>

<output>
Create `.planning/phases/26-floorplan-scope-source-ownership/26-01-SUMMARY.md` when this plan is executed.
</output>
