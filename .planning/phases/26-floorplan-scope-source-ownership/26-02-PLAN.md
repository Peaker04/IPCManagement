---
phase: 26-floorplan-scope-source-ownership
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - frontend/tests/uiFloorplanContracts.ts
  - frontend/tests/uiFloorplanContracts.test.ts
autonomous: true
requirements:
  - FLOOR-02
  - FLOOR-03
  - FLOOR-04
must_haves:
  truths:
    - "Per D-02, every canonical scope key from 26-01 has exactly one task-semantic named SAP Fiori floorplan or explicitly named custom dynamic-page composition, with rationale, rejected alternatives, and pinned current official SAP provenance."
    - "Per D-03, every named floorplan has executable mandatory capabilities; an Analytical List Page declaration cannot validate unless real analytical drilldown and chart/table interaction are both rendered and exercised with asserted postconditions."
    - "Per D-04, every table-bearing canonical state declares semantic column data types, preserved row/key identifiers, and explicit responsive plus horizontal/vertical scroll ownership; identifier fields can never be treated as numeric measures."
    - "Missing, duplicate, stale, unsupported, or internally inconsistent floorplan contracts fail with the exact canonical scope key and diagnostic category."
    - "Per D-06, the contracts remain test-owned and change no business behavior, backend policy, API, cache, lifecycle, route access, rendered UI, database state, or roadmap scope."
  artifacts:
    - path: "frontend/tests/uiFloorplanContracts.ts"
      provides: "Typed official-SAP provenance catalog and exact per-state floorplan, capability, and table-semantic contracts"
      exports:
        - "SapFioriFloorplanName"
        - "OfficialSapProvenance"
        - "UiFloorplanCapability"
        - "UiFloorplanDeclaration"
        - "UiTableSemanticContract"
        - "UiFloorplanContract"
        - "sapFioriProvenance"
        - "uiFloorplanContracts"
        - "validateUiFloorplanContracts"
    - path: "frontend/tests/uiFloorplanContracts.test.ts"
      provides: "Exact scope coverage, official-source, false-ALP, capability-evidence, and table-semantics contract tests"
      contains: "analytical-drilldown, chart-table-interaction, identifier, scroll-owner"
  key_links:
    - from: "frontend/tests/uiFloorplanContracts.ts"
      to: "frontend/tests/uiFloorplanScopeRegistry.ts"
      via: "imports UiFloorplanScopeKey and keys every declaration by the authoritative 26-01 identity"
      pattern: "UiFloorplanScopeKey|uiFloorplanScopeRegistry"
    - from: "frontend/tests/uiFloorplanContracts.test.ts"
      to: "frontend/tests/uiFloorplanContracts.ts"
      via: "validates bidirectional scope equality and named diagnostic buckets"
      pattern: "validateUiFloorplanContracts"
    - from: "frontend/tests/uiFloorplanContracts.test.ts"
      to: "current production screen sources and executable render/interaction tests"
      via: "source-aware table discovery plus resolvable ALP render-and-exercise evidence; the registry is not its own oracle"
      pattern: "analytical-drilldown|chart-table-interaction|table"
---

<objective>
Classify every canonical state by task semantics, pin current official SAP provenance, enforce executable mandatory floorplan capabilities including strict real ALP drilldown/chart-table interaction, and declare complete table semantics per D-02, D-03, D-04, and D-06.

Purpose: Turn the exact state identities created by 26-01 into falsifiable SAP Fiori expectations before any DOM instrumentation, capture, image judgment, or presentation remediation begins.
Output: A test-owned typed floorplan/capability/table contract and source-aware negative/closure tests; no production, package, runtime, evidence, route, policy, API, cache, lifecycle, rendered-behavior, or database change.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-floorplan-scope-source-ownership/CONTEXT.md
@.planning/phases/26-floorplan-scope-source-ownership/26-PLAN-OUTLINE.md
@.planning/phases/26-floorplan-scope-source-ownership/26-01-PLAN.md
@.planning/phases/26-floorplan-scope-source-ownership/26-01-SUMMARY.md
@frontend/tests/uiFloorplanScopeRegistry.ts
@frontend/tests/uiFloorplanScopeContract.test.ts
@frontend/tests/uiCanonSourceInventory.ts
@frontend/tests/uiStatePurityInventory.ts
@frontend/tests/ui-audit.spec.ts
@frontend/tests/visual-routes.spec.ts

<interfaces>
Consume the 26-01 exports exactly: `UiFloorplanScopeEntry`, `UiFloorplanScopeKey`, `uiFloorplanScopeRegistry`, and `buildUiFloorplanScopeKey`. Do not reconstruct route, tab, nested-view, role-state, or data-state vocabulary in this plan.

Create `OfficialSapProvenance` with a unique stable reference ID, official page title, HTTPS URL, SAP Fiori for Web version/path provenance, and the date the live official page was checked. Create a discriminated `UiFloorplanDeclaration`: either a typed named SAP Fiori floorplan or an explicitly named custom dynamic-page composition; both variants carry task-semantic rationale, rejected nearby alternatives with reasons, and an official provenance reference.

Create `UiFloorplanCapability` with a stable capability ID, observable requirement, required rendered anchor, executable interaction when applicable, asserted postcondition, and a resolvable evidence reference. Reserve explicit capability IDs for `analytical-drilldown` and `chart-table-interaction`; both are mandatory for every Analytical List Page declaration.

Create `UiTableSemanticContract` as a discriminated table/not-applicable contract. Table contracts contain stable column IDs, semantic data types and roles, row-key fields, identifier-preservation rules, responsive intent, horizontal/vertical scroll intent, and the intended scroll-owner region. Not-applicable entries require a reason so omission cannot masquerade as classification.

Create `UiFloorplanContract` keyed by `UiFloorplanScopeKey` and export `sapFioriProvenance`, `uiFloorplanContracts`, and pure `validateUiFloorplanContracts` diagnostics for missing, duplicate, stale, invalid-provenance, missing-capability, invalid-capability-evidence, missing-table-semantics, invalid-identifier-semantics, and invalid-scroll-intent findings.
</interfaces>
</context>

## Artifacts this phase produces

- `frontend/tests/uiFloorplanContracts.ts` — new test-owned module exporting `SapFioriFloorplanName`, `OfficialSapProvenance`, `UiFloorplanCapability`, `UiFloorplanDeclaration`, `UiTableSemanticContract`, `UiFloorplanContract`, `sapFioriProvenance`, `uiFloorplanContracts`, and `validateUiFloorplanContracts`.
- `frontend/tests/uiFloorplanContracts.test.ts` — new Vitest contract covering exact canonical-key closure, task-semantic classification, pinned official SAP provenance, mandatory capabilities, strict ALP render/exercise evidence, and table semantics.
- No production symbol, package manifest, lockfile, runtime artifact, browser/evidence artifact, database object, or file outside the two paths above is created or modified.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify every canonical state and pin official SAP provenance</name>
  <files>frontend/tests/uiFloorplanContracts.ts, frontend/tests/uiFloorplanContracts.test.ts</files>
  <read_first>
    <file>.planning/phases/26-floorplan-scope-source-ownership/CONTEXT.md</file>
    <file>.planning/phases/26-floorplan-scope-source-ownership/26-PLAN-OUTLINE.md</file>
    <file>.planning/phases/26-floorplan-scope-source-ownership/26-01-SUMMARY.md</file>
    <file>frontend/tests/uiFloorplanScopeRegistry.ts</file>
    <file>frontend/tests/uiFloorplanScopeContract.test.ts</file>
    <file>frontend/tests/uiCanonSourceInventory.ts</file>
    <file>frontend/tests/uiStatePurityInventory.ts</file>
    <file>frontend/tests/ui-audit.spec.ts</file>
    <file>frontend/tests/visual-routes.spec.ts</file>
  </read_first>
  <behavior>
    - Test 1: canonical scope keys and floorplan-contract keys are bidirectionally equal; dropping, duplicating, or inventing one contract reports the exact key as missing, duplicate, or stale.
    - Test 2: every state has exactly one discriminated named-floorplan or custom-dynamic-page declaration with nonblank task rationale and at least one explicitly reasoned exclusion of a plausible alternative.
    - Test 3: every provenance reference resolves to one unique catalog entry whose live-checked URL is HTTPS on the official SAP Fiori for Web design-system host/path and whose title/version/check date are pinned.
    - Test 4: a route label, page title, component name, visual resemblance, or copied registry value alone cannot satisfy task-semantic classification.
  </behavior>
  <action>Before editing, capture `git status --short --branch` and the explicit current branch. Apply the outline and D-07 Lightweight graph implementation gate because these two files define shared behavior-bearing state/floorplan vocabulary: call GitNexus `list_repos` once, confirm `repo=IPCManagement` has the explicit current branch indexed, and record raw/effective risk as Lightweight; per-symbol impact is not required because this plan edits no production executable symbol. Write the failing exact-key, classification, and provenance tests first. Then create `uiFloorplanContracts.ts`, importing the 26-01 scope types/registry rather than copying any route or state literal. Classify every canonical key from the actual user task in that state: distinguish browse/triage, work-object detail, create/edit, overview, approval, analytical, and custom multi-region work; use a named SAP Fiori floorplan only when its task and mandatory structure fit, otherwise use an explicitly named custom dynamic-page composition. Record a concise state-specific rationale and reject the closest plausible alternatives with concrete capability/task mismatches; never infer from route labels, component names, or visual resemblance alone. Build one deduplicated `sapFioriProvenance` catalog. At execution time, open the official SAP Fiori floorplan selector from CONTEXT.md and each selected floorplan/dynamic-page source directly on the current official SAP site, then pin unique reference ID, exact page title, HTTPS URL, version/path marker, and checked date; do not use search snippets, blogs, local UI skills, or third-party summaries as oracle. Per D-02, SAP remains level-1 authority and local heuristics may only supplement non-conflicting accessibility/implementation notes. Keep all work in the two declared test files, use installed dependencies/Node APIs only, and do not install packages. Per D-06, do not edit production, package, lockfile, route, policy, API, cache, lifecycle, business action, rendered UI, runtime, browser evidence, or database state.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiFloorplanContracts.test.ts --maxWorkers=1</automated>
    <automated>git diff --check -- frontend/tests/uiFloorplanContracts.ts frontend/tests/uiFloorplanContracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every 26-01 canonical key has exactly one declaration, with no missing, duplicate, or stale contract key.
    - Every declaration is justified from state-specific task semantics and records reasoned exclusions; no label/name/appearance-only classification passes.
    - Every referenced source resolves to a unique, pinned, current official SAP Fiori for Web catalog entry checked directly during execution.
    - FLOOR-02 and D-02 are executable without any production, dependency, runtime, evidence, or database change.
  </acceptance_criteria>
  <done>Every canonical state has one falsifiable task-semantic floorplan/custom-composition expectation and current official SAP provenance, keyed only by the authoritative 26-01 scope contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce mandatory capabilities, strict ALP evidence, and complete table semantics</name>
  <files>frontend/tests/uiFloorplanContracts.ts, frontend/tests/uiFloorplanContracts.test.ts</files>
  <read_first>
    <file>frontend/tests/uiFloorplanContracts.ts</file>
    <file>frontend/tests/uiFloorplanContracts.test.ts</file>
    <file>frontend/tests/uiFloorplanScopeRegistry.ts</file>
    <file>frontend/tests/uiCanonSourceInventory.ts</file>
    <file>frontend/tests/uiStatePurityInventory.ts</file>
    <file>frontend/tests/ui-audit.spec.ts</file>
    <file>frontend/tests/visual-routes.spec.ts</file>
  </read_first>
  <behavior>
    - Test 1: every named floorplan resolves all mandatory capability IDs to observable rendered anchors and, for interactive capabilities, executable interactions with asserted postconditions.
    - Test 2: an Analytical List Page declaration fails separately when analytical drilldown is absent, when chart/table interaction is absent, when either rendered anchor is unresolved, or when either interaction is not actually exercised and asserted.
    - Test 3: every production-source-discovered table-bearing canonical state has a table contract, while every non-table state carries an explicit not-applicable reason.
    - Test 4: every table column has a semantic data type/role; an ID, code, or key classified as numeric/measure fails even when its runtime value contains only digits.
    - Test 5: every table contract declares stable row-key preservation, responsive intent, horizontal and vertical scroll intent, and one intended scroll-owner region; blank, ambiguous, or conflicting values fail by canonical key.
  </behavior>
  <action>Extend the tests before implementation with isolated mutants for each missing capability, false-ALP evidence case, missing table contract, numericized identifier, missing row key, and missing/conflicting scroll owner. Define mandatory capability sets by named floorplan in `uiFloorplanContracts.ts` and require each live declaration to satisfy its set. Per D-03, treat `analytical-drilldown` and `chart-table-interaction` as non-negotiable for an Analytical List Page: each must identify the actual rendered production anchor, the user interaction, the observable postcondition, and a resolvable executable test reference. For every live state classified as ALP, add a probe in `uiFloorplanContracts.test.ts` that renders the actual production screen with existing providers/fixtures, performs both interactions, and asserts the corresponding drilldown and chart/table state change; if the current UI cannot be rendered and exercised this way, the ALP classification must fail and be replaced by the task-correct declaration rather than waived. A synthetic malformed ALP contract must always prove this negative gate even when no live state is classified as ALP. Independently discover table-bearing states from current production screen sources using the repository's bounded source-aware conventions; do not derive table applicability from `uiFloorplanContracts` itself or maintain a second unguarded list. Per D-04, enumerate each rendered semantic column with stable column ID, user-facing semantic data type, semantic role, and identifier treatment; require row keys to preserve the actual source identifier through sorting/filtering/rendering and forbid numeric-measure semantics for IDs/codes regardless of lexical digits. Declare responsive behavior and both scroll axes explicitly, including the stable intended scroll-owner region and whether overflow is forbidden, reflowed, priority-hidden, or owned by the table region. Use `not-applicable` plus a reason for non-table states. Emit sorted diagnostics containing canonical key, diagnostic category, and offending capability/column/scroll field. Prove production files do not import either test-owned contract file. Run focused contract tests and the existing root verification/count-preservation gate. Finish the D-07 Lightweight lane with one GitNexus `detect_changes` call using `repo=IPCManagement` and the explicit current branch; reconcile all changed shared test-contract symbols and require zero undispositioned production process/edge. If detection exposes a production process/edge, public contract impact, unresolved mapping, partial/truncated evidence, or confidence below 0.8, stop and escalate to Full analysis rather than accepting reduced confidence. Compare final status to the captured baseline, preserve unrelated user changes, do not mutate `ipc_lane1`, and do not push.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiFloorplanContracts.test.ts tests/uiFloorplanScopeContract.test.ts --maxWorkers=1</automated>
    <automated>npm run verify</automated>
    <automated>git diff --check -- frontend/tests/uiFloorplanContracts.ts frontend/tests/uiFloorplanContracts.test.ts</automated>
    <automated>$matches = Select-String -Path 'frontend/tests/uiFloorplanContracts.ts','frontend/tests/uiFloorplanContracts.test.ts' -Pattern '(?i)(password\s*=|api[_-]?key\s*=|secret\s*=|TODO|FIXME|placeholder|hardcoded for now)' -AllMatches; if ($matches) { $matches | ForEach-Object { $_.ToString() }; exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - Each named floorplan satisfies its executable mandatory-capability set with key-specific diagnostics for omissions.
    - No ALP declaration passes without real rendered and exercised analytical drilldown plus chart/table interaction and asserted postconditions; negative mutants prove every failure branch.
    - Current source-derived table-bearing keys equal table-contract keys exactly, and every non-table state is explicitly dispositioned as not applicable.
    - All table columns, source row keys, identifier preservation, responsive behavior, scroll axes, and intended scroll owners are explicit; IDs/codes are never numeric measures.
    - Focused tests, prior scope closure, root verification/count preservation, whitespace, secret/stub checks, and final explicit-branch Lightweight detection pass with no undispositioned production process/edge.
    - Final attributable diff contains only the two declared test-owned files and preserves D-06, `ipc_lane1`, package manifests/lockfiles, runtime/evidence, business behavior, route access, and the five-phase roadmap.
  </acceptance_criteria>
  <done>FLOOR-03 and FLOOR-04 are executable: named floorplans are capability-backed, false ALP labels cannot pass, and every table-bearing state has complete identifier-safe semantic and scroll/responsive intent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 26-01 canonical scope registry → floorplan contracts | Downstream contracts must consume the exact authoritative keys without copying or relabeling scope. |
| Official SAP pages → pinned test-owned provenance | External design authority is recorded as immutable contract data and must not be spoofed by third-party or stale lookalike sources. |
| Production screens/tests → capability and table evidence | Source/render behavior is untrusted until independently resolved and exercised; contract declarations cannot prove themselves. |
| Test-owned contracts → later DOM/capture/judge plans | Later phases trust floorplan/capability/table semantics, so false classification would contaminate every downstream verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Spoofing | `sapFioriProvenance` | mitigate | Accept only directly checked HTTPS SAP Fiori for Web design-system pages; pin unique reference ID, title, URL, version/path, and checked date, and reject unresolved/duplicate catalog entries. |
| T-26-02-02 | Tampering | Canonical-key floorplan mapping | mitigate | Enforce bidirectional exact-key equality with the 26-01 registry and explicit missing/duplicate/stale mutants. |
| T-26-02-03 | Spoofing | ALP classification | mitigate | Require both `analytical-drilldown` and `chart-table-interaction` to resolve to real rendered anchors, executed interactions, and asserted postconditions; reject metadata-only evidence. |
| T-26-02-04 | Tampering | Table semantic declarations | mitigate | Independently discover table-bearing production states and reject missing contracts, numericized identifiers, unstable row keys, and ambiguous scroll ownership. |
| T-26-02-05 | Repudiation | Contract diagnostics | mitigate | Emit sorted canonical key plus exact provenance/capability/column/scroll diagnostic so no aggregate count can conceal the mismatch. |
| T-26-02-06 | Information disclosure | Test-owned contract boundary | mitigate | Assert production imports neither new test file; keep repository/source references in test scope and add no runtime DOM/bundle data in this plan. |
| T-26-02-07 | Denial of service | Source-aware table/evidence resolver | mitigate | Restrict reads to canonical scope-resolved frontend sources and declared test authorities; exclude `node_modules`, `dist`, artifacts, and workspace-wide recursion. |
| T-26-02-SC | Tampering | Package supply chain | accept | No package install or dependency change is authorized; package manifests and lockfiles remain outside the diff. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID / item | Plan | Status | Notes |
|--------|-----------|------|--------|-------|
| GOAL | Exact expectation for every existing screen | 26-02 | COVERED | Every authoritative canonical key gains one falsifiable floorplan/capability/table expectation. |
| REQ | FLOOR-02 | 26-02 | COVERED | Task-semantic named floorplan/custom composition, rationale, exclusions, and current official SAP provenance. |
| REQ | FLOOR-03 | 26-02 | COVERED | Executable mandatory capabilities and strict rendered/exercised ALP drilldown plus chart/table gate. |
| REQ | FLOOR-04 | 26-02 | COVERED | Semantic column types, preserved identifiers/row keys, and explicit responsive/scroll ownership. |
| RESEARCH | Task-semantic floorplan choice and ALP overclassification prevention from the approved outline | 26-02 | COVERED | Contract tests reject label/appearance inference and metadata-only ALP evidence; no new research is performed. |
| CONTEXT | D-02 | 26-02 | COVERED | SAP Fiori for Web remains level-1 oracle with pinned current official sources. |
| CONTEXT | D-03 | 26-02 | COVERED | Named floorplans have executable capabilities and ALP has two real interaction gates. |
| CONTEXT | D-04 | 26-02 | COVERED | Every table-bearing state has identifier-safe semantic and scroll/responsive contracts. |
| CONTEXT | D-06 | 26-02 | COVERED | Test-owned contracts only; no production/business/access/runtime/database/roadmap mutation. |
| CONTEXT | D-07 | 26-02 | COVERED | Shared behavior-bearing contract vocabulary uses the explicit-branch Lightweight graph gate and mandatory escalation conditions. |

D-01/FLOOR-01 is supplied by prerequisite Plan 26-01. D-05 and SOURCE-01..SOURCE-03 remain assigned exactly to Plans 26-03..26-05 by the authoritative outline. Phase 27 capture/geometry, Phase 28 judging, Phase 29 remediation, Phase 30 fresh rejudge/closeout, deferred VIEW-01, and deferred PROD-01 are deliberate exclusions rather than gaps.

<verification>
1. Run the focused Vitest command and confirm floorplan-contract keys equal the 26-01 canonical keys bidirectionally; isolated missing, duplicate, and stale mutations must fail with exact keys.
2. Confirm every declaration is based on task semantics, has reasoned exclusions, and resolves through the unique pinned catalog to a directly checked current official SAP Fiori for Web URL; third-party and malformed provenance mutants must fail.
3. For every live ALP declaration, render the actual production screen, exercise analytical drilldown and chart/table interaction, and assert both postconditions. The malformed synthetic ALP cases must fail even if no live ALP exists.
4. Confirm source-derived table-bearing states equal table contracts exactly and negative probes fail for missing semantics, numericized IDs/codes, unstable row keys, missing responsive intent, and ambiguous scroll ownership.
5. Run `npm run verify`; all existing application/API/frontend and UI-completeness gates must remain green and discovered test counts must not decrease.
6. Confirm the final attributable diff contains only `frontend/tests/uiFloorplanContracts.ts` and `frontend/tests/uiFloorplanContracts.test.ts`; production, package/lockfile, runtime/evidence, `.planning` outside the executor summary, route/access/business contracts, and database artifacts remain unchanged.
7. Run whitespace plus secret/stub scans and reconcile one final GitNexus `detect_changes` against `repo=IPCManagement` and the explicit current branch. Complete only with no undispositioned production process/edge; otherwise escalate and satisfy Full-analysis Done.
8. Do not install packages, start Phase 27 capture, mutate/reset/seed/import/restore `ipc_lane1`, push, add a route/panel/action, or change backend policy, lifecycle, API, cache, route access, business behavior, or rendered behavior.
</verification>

<success_criteria>
- FLOOR-02 is satisfied for every authoritative canonical state by one task-semantic floorplan/custom composition, reasoned alternatives, and pinned current official SAP provenance.
- FLOOR-03 is satisfied by executable named-floorplan capability sets; ALP cannot validate without real rendered and exercised analytical drilldown plus chart/table interaction.
- FLOOR-04 is satisfied by exact table-bearing-state coverage and complete semantic columns, identifier/row-key preservation, responsive intent, scroll axes, and intended scroll ownership.
- Focused tests, prerequisite scope closure, root/count-preservation gates, hygiene checks, and final explicit-branch Lightweight GitNexus detection pass with no undispositioned production process/edge.
- D-06 remains true: no business behavior, policy, API, cache, lifecycle, route access, rendered behavior, production, database, `ipc_lane1`, package, push, or phase-scope change occurs.
</success_criteria>

<output>
Create `.planning/phases/26-floorplan-scope-source-ownership/26-02-SUMMARY.md` when this plan is executed.
</output>
