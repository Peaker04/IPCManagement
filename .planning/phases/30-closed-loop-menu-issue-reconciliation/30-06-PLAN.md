---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 06
type: execute
wave: 4
depends_on: [30-05]
autonomous: true
status: pending
requirements: [MRX-03, MRX-04, MRX-06L]
files_modified:
  - backend/tests/IPCManagement.Api.Tests/Phase30InactiveDefaultLifecycleOwnerTests.cs
  - backend/src/IPCManagement.Api/Features/Inventory/Services/SupplementalMaterialRequestService.cs
  - backend/src/IPCManagement.Api/Features/Inventory/Services/LegacyLineageDispositionService.cs
  - backend/tests/IPCManagement.Api.Tests/SupplementalMaterialRequestServiceTests.cs
  - backend/tests/IPCManagement.Api.Tests/LegacyLineageDispositionServiceTests.cs
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "MRX-04: supplemental create, fulfill, route-to-purchasing and reject invoke the real DEFAULT-only service owner, persist exact DEFAULT ancestry fixtures, reject with complete zero-effect ledgers while inactive, then resume the same request identity/version/command once after switch-back."
    - "MRX-03: supplemental mutations consume current mode/version inside their owning transactions so a switch race cannot leave request, issue, purchasing, stock, lifecycle, audit or receipt residue."
    - "MRX-04: pending approved legacy disposition ApplyAsync rejects under reconciliation with zero provenance/status/version/receipt effects, then applies the same disposition ID/version/target/command after DEFAULT resumes and replay returns that canonical row."
  artifacts:
    - backend/tests/IPCManagement.Api.Tests/Phase30InactiveDefaultLifecycleOwnerTests.cs
  key_links:
    - "exact DEFAULT issue and MaterialRequestLine ancestry -> supplemental lifecycle service -> request/issue/purchase/stock ledgers"
    - "approved legacy disposition + distinct Manager review + Admin actor -> ApplyAsync -> exact source-line target + lifecycle receipt"
---

<objective>
Close the remaining DEFAULT-only supplemental lifecycle and pending legacy apply owner seams.

Purpose: Eager mode checks alone do not prove transaction-version safety or same-aggregate continuation.
Output: Persisted public-service freeze/resume matrices and source-map-proven transaction fencing without permission or ownership changes.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.artifacts/runtime/phase30-real-inactive-owner-matrix.map-real-inactive-owner-seams.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-SPEC.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-04-SUMMARY.md
@backend/src/IPCManagement.Api/Features/Inventory/Services/SupplementalMaterialRequestService.cs
@backend/src/IPCManagement.Api/Features/Inventory/Services/LegacyLineageDispositionService.cs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Freeze and resume every supplemental lifecycle mutation on the exact DEFAULT aggregate</name>
  <files>backend/tests/IPCManagement.Api.Tests/Phase30InactiveDefaultLifecycleOwnerTests.cs, backend/src/IPCManagement.Api/Features/Inventory/Services/SupplementalMaterialRequestService.cs, backend/tests/IPCManagement.Api.Tests/SupplementalMaterialRequestServiceTests.cs</files>
  <behavior>
    - "CreateAsync uses an exact received DEFAULT issue/header and MaterialRequestLine source; inactive invocation rejects and switch-back creates one pending supplemental request version 1 with the same source and command identity."
    - "FulfillAsync on a persisted actionable supplemental rejects inactive with no request/issue/stock effects; resumed invocation updates that same supplemental version, creates one exact DEFAULT issue/line and one ISSUE movement, and replay is canonical."
    - "RouteToPurchasingAsync rejects inactive with no request/purchase effects; resumed invocation routes the same supplemental ID/version once and preserves exact purchase header/line source identity with zero stock effect."
    - "RejectAsync rejects inactive with no state/reason effects; resumed invocation rejects the same supplemental ID/version once with no issue, purchase, stock or movement creation."
  </behavior>
  <action>Persist separate actionable fixtures where lifecycle branches would conflict, but every row must reuse its original aggregate ID, expected version and command identity across switch-away and switch-back. Snapshot IDs, parents, status, versions, requested/fulfilled quantities, exact source issue and line, request/issue/purchase child counts, Currentstocks, Stockmovements with references, Approvalhistories, Auditlogs, reconciliation actual/revision/disposition rows and all lifecycle/outbox/receipt rows. Invoke ISupplementalMaterialRequestService methods directly. Add protected transaction mode/version fencing to each mutation at the owning service as proven missing by the source map, retaining DEFAULT-only ancestry and existing permission checks.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~Phase30InactiveDefaultLifecycleOwnerTests&amp;FullyQualifiedName~Supplemental|FullyQualifiedName~SupplementalMaterialRequestServiceTests"</automated></verify>
  <done>All four supplemental mutations reject with complete zero-effect ledgers while inactive and continue only their original DEFAULT aggregates once after switch-back.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze and resume the same pending legacy disposition apply command</name>
  <files>backend/tests/IPCManagement.Api.Tests/Phase30InactiveDefaultLifecycleOwnerTests.cs, backend/src/IPCManagement.Api/Features/Inventory/Services/LegacyLineageDispositionService.cs, backend/tests/IPCManagement.Api.Tests/LegacyLineageDispositionServiceTests.cs</files>
  <behavior>
    - "Persist an APPROVED disposition for one legacy issue line, an exact target MaterialRequestLine, a distinct Manager review and an Admin actor/role; while reconciliation is active, ApplyAsync rejects and changes no disposition/source/target/lifecycle/audit/receipt fields."
    - "After DEFAULT resumes, ApplyAsync receives the same disposition ID, expected version, target ID, command ID and reason; that row alone becomes APPLIED version+1 and the original source line gains the exact target lineage."
    - "Replay returns the same disposition and creates no second transition, outbox message, command receipt, issue, return, stock movement or inferred candidate write."
  </behavior>
  <action>Invoke ILegacyLineageDispositionService.ApplyAsync rather than validating its operation key. Use the controller-consistent key selected by production and the current switch-away authority version. Preserve Manager/Admin separation, explicit target validation and DEFAULT-only ownership established by Plan 30-04; alter production only if the persisted test proves a missing mode/version check at the owner. Compare the complete common ledger before inactive rejection, immediately before resume and after canonical apply/replay.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~Phase30InactiveDefaultLifecycleOwnerTests&amp;FullyQualifiedName~Legacy|FullyQualifiedName~LegacyLineageDispositionServiceTests"</automated></verify>
  <done>The pending disposition itself freezes and resumes; no substitute guard row, replacement disposition or inferred lineage satisfies the test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| mode authority → supplemental transactions | DEFAULT-only eager checks must be backed by transaction-local current version checks. |
| reviewed disposition → exact provenance mutation | Manager review and Admin apply must target one approved row and one exact source/target pair. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-06-01 | Tampering | SupplementalMaterialRequestService | high | mitigate | Current mode/version protected transactions and full multi-ledger assertions. |
| T-30-06-02 | Elevation of Privilege | LegacyLineageDispositionService | high | mitigate | Preserve role separation, explicit target and DEFAULT-only protected apply. |
</threat_model>

<verification>Run both focused filters plus Plan 30-04 supplemental/legacy regressions. No protected DB, permission change, inferred matching or reconciliation supplemental ownership is allowed.</verification>
<success_criteria>Every supplemental lifecycle mutation and the pending legacy apply row are real executable-owner freeze/resume evidence with complete ledgers and current mode/version transaction safety.</success_criteria>
<output>Create `30-06-SUMMARY.md` with four supplemental ledgers and the legacy same-disposition apply/replay ledger.</output>
