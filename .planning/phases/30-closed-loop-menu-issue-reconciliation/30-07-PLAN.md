---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 07
type: execute
wave: 4
depends_on: [30-06]
autonomous: true
requirements: [MRX-03, MRX-04, MRX-06L]
files_modified:
  - backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs
  - backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "MRX-04: READY reconciliation facts remain byte/value-equivalent after authorized shared-master/BOM edits; only a newly materialized batch reflects changed authority."
    - "MRX-03: a command paused after validation and before first write loses to a mode/version change with zero workflow, issue, line, movement, stock, lifecycle, audit or idempotency residue."
    - "MRX-06L: response-loss replay and barrier-synchronized duplicate submission yield one issue, exact line set, movement chain, stock projection and family-specific net contribution."
    - "Canonical returns/corrections from Plan 30-04 remain in the original exact family net projection under retry and completion."
  artifacts:
    - backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs
  key_links:
    - "READY snapshot -> authorized shared-master edit -> unchanged persisted frozen fields"
    - "deterministic pre-write barrier -> mode/version transaction fence -> complete rollback"
    - "source-scoped command identity -> idempotency receipt + unique issue lineage -> exactly one stock projection"
---

<objective>
Execute backend Wave 4 slice D: prove immutable reconciliation facts, stale transaction rollback and exactly-once stock projection after all command and consumer owners are closed.

Purpose: The final backend slice isolates the high-contention transaction owners from command and reporting work while preserving exact return/correction net semantics.
Output: Deterministic no-sleep application-path tests and minimal transaction-owner repairs.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-SPEC.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-CONTEXT.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-03-SUMMARY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-04-SUMMARY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-06-SUMMARY.md
@backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs
@backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs
@backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Freeze one READY batch across authorized master edits</name>
  <files>backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs</files>
  <behavior>
    - "READY freezes source menu/version, serving contributors, BOM resolution, ingredient identity, canonical unit, required quantity and tolerance."
    - "Authorized dish/BOM/material/unit/customer edits leave every frozen field and child identity unchanged on reload."
    - "A newly materialized batch/version alone reflects the changed shared authority."
  </behavior>
  <action>Start red at the public materialize/read seam. Capture persisted bytes/values for every SPEC frozen field, mutate shared authorities through retained Admin Data service seams, clear tracking and reload, then compare exact values and child identities. Create a new batch/version and prove only it reflects changes. Repair only ReconciliationBatchService refresh/recalculation paths exposed by the test; never recompute an existing READY batch.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconciliationWarehouseIssueApplicationPathTests"</automated></verify>
  <done>Existing READY facts are immutable and a new batch/version is the sole path to changed shared authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lose a deterministic pre-write race with a complete zero-effect ledger</name>
  <files>backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs, backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs</files>
  <behavior>
    - "A test hook pauses after request/workflow validation and immediately before the first durable write."
    - "While paused, another transaction changes operation mode/version; resumed stale issue fails with the established conflict."
    - "Issue header/lines, stock movements, current stock, transfer/completion state, lifecycle, audit and idempotency receipt all equal their pre-command ledgers."
  </behavior>
  <action>Use a deterministic barrier or task-completion hook injected at the production transaction seam; do not use timing sleeps. Change mode/version through the real authority service in a separate context, release the command, and assert every named ledger plus the response contract. Keep the mode/version recheck inside the same issue transaction immediately before first persistence/stock mutation.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconciliationWarehouseIssueApplicationPathTests"</automated></verify>
  <done>The stale transaction deterministically fails with no durable or idempotency residue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Commit one net projection under replay, duplicate submit and correction</name>
  <files>backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs, backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs</files>
  <behavior>
    - "Response-loss replay returns the original command result with one issue, exact line set, movement chain and stock delta."
    - "Two barrier-synchronized submissions with the same source-scoped command identity produce one commit; the loser receives canonical replay or deterministic conflict."
    - "After an exact reconciliation correction created through Plan 30-04, completion sums persisted net issued quantity for that ReconciliationBatchLine only; DEFAULT total remains unchanged."
    - "The paired DEFAULT issue/correction contributes only to DEFAULT net semantics and zero to reconciliation."
  </behavior>
  <action>Drive sequential replay and parallel duplicate submission through the public application path and compare complete issue/idempotency/stock ledgers. Reuse canonical corrections produced through InventoryReturnService rather than constructing synthetic negative quantities. Ensure uniqueness, receipt persistence, issue/line creation, stock movement/current stock and lifecycle audit share one transaction. Completion must aggregate exact original source-line family and canonical net semantics only.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconciliationWarehouseIssueApplicationPathTests|FullyQualifiedName~Phase30WarehouseReturnFamilyTests" &amp;&amp; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore &amp;&amp; dotnet ef migrations has-pending-model-changes --project backend/src/IPCManagement.Api/IPCManagement.Api.csproj --startup-project backend/src/IPCManagement.Api/IPCManagement.Api.csproj</automated></verify>
  <done>Replay, concurrent duplicate and family-exact correction yield one canonical stock/net projection, and the complete local backend suite/model gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| shared-master edit → READY snapshot | Mutable master authority cannot rewrite historical transactional facts. |
| concurrent client → issue transaction | Stale or duplicate commands cannot create partial/second stock effects. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-07-01 | Tampering | frozen batch | high | mitigate | Persisted snapshot equivalence before/after authorized edits. |
| T-30-07-02 | Tampering | stale/duplicate issue | high | mitigate | Transaction-local fence, idempotency, uniqueness and deterministic barriers. |
</threat_model>

<verification>Run focused then full backend and EF parity locally. No protected target, reset, seed or browser execution belongs to this slice.</verification>
<success_criteria>Frozen facts, stale rollback and exactly-once family-specific stock/net projection are proven after bounded backend Wave 4 execution.</success_criteria>
<output>Create `30-07-SUMMARY.md` with frozen-field comparison and complete concurrency ledgers.</output>
