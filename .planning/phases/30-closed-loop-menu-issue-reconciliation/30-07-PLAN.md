---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 07
type: execute
wave: 4
depends_on: [30-06]
autonomous: true
status: pending
requirements: [MRX-03, MRX-04, MRX-06L]
files_modified:
  - backend/tests/IPCManagement.Api.Tests/Phase30InactiveApprovalOwnerTests.cs
  - backend/tests/IPCManagement.Api.Tests/Infrastructure/CustomWebApplicationFactory.cs
  - backend/src/IPCManagement.Api/Features/SystemOperation/Filters/SystemOperationModeFilter.cs
  - backend/src/IPCManagement.Api/Features/Approvals/Services/ApprovalWorkflowService.cs
  - backend/src/IPCManagement.Api/Features/Approvals/Services/ApprovalHandlers.cs
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "MRX-04: DEFAULT material-demand approval is tested through an in-process HTTP host because the MVC SystemOperationModeFilter is its actual public mode owner; inactive requests never enter handler persistence and leave the complete ledger unchanged."
    - "MRX-04: after a real switch back to DEFAULT, the same material request target, decision, expected state and actor continue the original approval history/aggregate without replacement or duplicate history."
    - "MRX-03: approval persistence is fenced against a mode/version change between MVC admission and handler write, while existing approval permissions, policies, route and handler ownership remain unchanged."
  artifacts:
    - backend/tests/IPCManagement.Api.Tests/Phase30InactiveApprovalOwnerTests.cs
  key_links:
    - "HTTP POST approvals route -> SystemOperationModeFilter excluded-controller disposition/current version -> ApprovalWorkflowService registered handler -> MaterialRequest/Approvalhistory transaction"
---

<objective>
Prove DEFAULT approval freeze/resume at the only actual public owner: the MVC filter plus registered approval handler.

Purpose: Direct service tests bypass mode ownership and cannot satisfy MRX-04 for approval.
Output: In-process HTTP executable-owner evidence and the smallest handler/owner version fence proven necessary by the source map.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.artifacts/runtime/phase30-real-inactive-owner-matrix.map-real-inactive-owner-seams.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-SPEC.md
@backend/src/IPCManagement.Api/Features/Approvals/Controllers/ApprovalsController.cs
@backend/src/IPCManagement.Api/Features/Approvals/Services/ApprovalWorkflowService.cs
@backend/src/IPCManagement.Api/Features/Approvals/Services/ApprovalHandlers.cs
@backend/tests/IPCManagement.Api.Tests/Infrastructure/CustomWebApplicationFactory.cs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject inactive approval through the real MVC filter and preserve the persisted request</name>
  <files>backend/tests/IPCManagement.Api.Tests/Phase30InactiveApprovalOwnerTests.cs, backend/tests/IPCManagement.Api.Tests/Infrastructure/CustomWebApplicationFactory.cs, backend/src/IPCManagement.Api/Features/SystemOperation/Filters/SystemOperationModeFilter.cs</files>
  <behavior>
    - "The in-process host uses production controller/filter/DI routing, authenticated claims satisfying the existing approval permission/policy, and a persisted MaterialRequest accepted by MaterialDemandApprovalHandler."
    - "After SystemOperationModeService switches to MATERIAL_RECONCILIATION, HTTP execution of the actual approval route is rejected by SystemOperationModeFilter using approvals.executeasync and ExcludedInMaterialReconciliation before ApprovalWorkflowService/handler persistence."
    - "MaterialRequest identity/status/version, approval-history IDs/count/decision/status/action/actor, audit, issue/stock/reconciliation and lifecycle/outbox/receipt ledgers are unchanged."
  </behavior>
  <action>Extend the existing custom application factory only with reusable local test overrides required to persist the fixture and authenticate the actor; retain production MVC filters, controller registration and approval handler DI. Do not simulate filter calls or invoke ApprovalWorkflowService as the inactive oracle. Capture the full common ledger plus approval routing step/cardinality before the HTTP request and compare it after the expected unavailable response.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~Phase30InactiveApprovalOwnerTests&amp;FullyQualifiedName~Reject"</automated></verify>
  <done>The actual HTTP public seam rejects before handler writes, and the original approval aggregate/history ledger is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resume the same approval and fence the filter-to-handler version window</name>
  <files>backend/tests/IPCManagement.Api.Tests/Phase30InactiveApprovalOwnerTests.cs, backend/src/IPCManagement.Api/Features/Approvals/Services/ApprovalWorkflowService.cs, backend/src/IPCManagement.Api/Features/Approvals/Services/ApprovalHandlers.cs</files>
  <behavior>
    - "After switching to DEFAULT through the real authority service, the identical HTTP target/decision/actor updates the same MaterialRequest and appends only the intended approval history; retry does not duplicate the canonical transition."
    - "For multi-step routing, an existing STEP_APPROVED history remains attached and the next required actor continues that same aggregate rather than restarting approval."
    - "A deterministic admission-to-write mode/version change causes the handler transaction to fail with zero MaterialRequest, history, audit, lifecycle, issue, stock or receipt residue."
  </behavior>
  <action>Use the production HTTP route for switch-back success as well. Add the smallest transaction-local current mode/version validation at ApprovalWorkflowService or the registered MaterialDemandApprovalHandler boundary where the source map proves ordinary unprotected transaction ownership; pass captured request context through existing scoped dependencies without changing controller route, handler selection, roles or policy. Use a deterministic test barrier rather than sleeps for the race. Assert same aggregate identity/version lineage and intended history delta after success, then canonical retry behavior.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~Phase30InactiveApprovalOwnerTests"</automated></verify>
  <done>Approval resumes on the original request/history chain and cannot write under a mode/version race after MVC admission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| HTTP client → MVC filter | Public mode admission and authenticated permission evaluation occur here. |
| MVC admission → approval handler transaction | Mode/version can change before the first handler write. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-07-01 | Elevation of Privilege | ApprovalsController/filter | high | mitigate | Production HTTP host retains auth policy and excluded-controller mode admission. |
| T-30-07-02 | Tampering | Approval handler transaction | high | mitigate | Deterministic race test and current mode/version validation before persistence. |
</threat_model>

<verification>Run the focused in-process HTTP suite and existing approval workflow tests. The test host must use only local ephemeral persistence and must not target protected databases.</verification>
<success_criteria>DEFAULT approval is rejected and resumed through the actual MVC owner, with same-aggregate ledgers and a protected filter-to-handler transaction window.</success_criteria>
<output>Create `30-07-SUMMARY.md` with HTTP request/response, filter ownership, race and approval ledger evidence.</output>
