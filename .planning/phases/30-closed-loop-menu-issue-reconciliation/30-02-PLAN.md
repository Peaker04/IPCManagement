---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 02
type: execute
wave: 2
depends_on: [30-01]
autonomous: true
requirements: [MRX-01, MRX-02, MRX-03, MRX-04, MRX-05]
files_modified:
  - frontend/src/lib/routeConfig.ts
  - frontend/src/app/layout/MainLayout.tsx
  - frontend/src/routes/AppRouter.tsx
  - frontend/src/routes/routeLoaders.ts
  - frontend/src/routes/routeDataPreloaders.ts
  - frontend/src/features/system-operation/systemOperationEligibility.ts
  - frontend/src/lib/navigationPreferences.ts
  - frontend/src/features/projects/pages/WeeklyMenuPage.tsx
  - frontend/src/features/warehouse/pages/WarehousePage.tsx
  - frontend/src/features/reconciliation/pages/ReconciliationPage.tsx
  - frontend/src/features/reconciliation/ReconciliationWorkspace.tsx
  - frontend/src/features/reconciliation/ReconciliationComparisonTable.tsx
  - frontend/src/features/reconciliation/reconciliationApi.ts
  - frontend/src/features/admin/components/AdvancedDisplaySettings.tsx
  - frontend/src/shared/api/contracts/openapi.json
  - frontend/src/shared/api/contracts/schema.ts
  - frontend/src/**/*.test.ts
  - frontend/src/**/*.test.tsx
must_haves:
  truths:
    - "The reconciliation-mode shell contains exactly Dashboard, Weekly Menu, Warehouse, Reconciliation and Admin Data; Purchasing and Reports are absent, blocked on direct access and never preloaded."
    - "Weekly Menu contains only Kế hoạch tuần and Định lượng xuất kho, and its retained content has no cost, production, supplier, purchase or excluded Coordination lifecycle requests."
    - "Warehouse contains only Danh sách cần xuất and Lịch sử xuất kho for the selected reconciliation source; unrelated purchase receipt, broad movement and exception content is not mounted in this mode."
    - "The new Reconciliation page has no tabs, one compact required-versus-issued table and one page-level detail/disposition drawer; issued quantity is read-only and source-linked."
    - "Admin Data exposes only proven BOM/material validation and audit owners; a stock-oriented tab is not relabeled as catalog without matching source ownership."
  artifacts:
    - frontend/src/features/reconciliation/pages/ReconciliationPage.tsx
    - frontend/src/features/projects/pages/WeeklyMenuPage.tsx
    - frontend/src/features/warehouse/pages/WarehousePage.tsx
  key_links:
    - "backend capabilities -> navigation/direct-route/preload/tab intersection"
    - "Weekly Menu customer/week -> material-demand transfer -> Warehouse source list"
    - "Warehouse issue success -> exact cache invalidation -> Reconciliation projected table"
---

<objective>
Deliver the minimal mode-specific work surfaces without cloning the DEFAULT application.

Purpose: Make the new closed loop understandable and efficient while proving removed UI owners also stop their queries and actions.
Output: New route, trimmed retained pages/tabs, source-linked Warehouse issue interaction, one reconciliation table and semantic/query-ownership tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-CONTEXT.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-RESEARCH.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-01-SUMMARY.md
@docs/DASHBOARD-UI-RULES.md
@docs/UI-UX-EXECUTION-HARNESS.md
@docs/FRONT-END-CHECKLIST-INTEGRATION.md
@frontend/src/features/projects/pages/WeeklyMenuPage.tsx
@frontend/src/features/warehouse/pages/WarehousePage.tsx
@frontend/src/features/reconciliation/ReconciliationWorkspace.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Connect one customer/week through Weekly Menu, Warehouse and the new Reconciliation route</name>
  <files>frontend/src/shared/api/contracts/openapi.json, frontend/src/shared/api/contracts/schema.ts, frontend/src/features/reconciliation/reconciliationApi.ts, frontend/src/lib/routeConfig.ts, frontend/src/routes/AppRouter.tsx, frontend/src/routes/routeLoaders.ts, frontend/src/routes/routeDataPreloaders.ts, frontend/src/app/layout/MainLayout.tsx, frontend/src/features/projects/pages/WeeklyMenuPage.tsx, frontend/src/features/warehouse/pages/WarehousePage.tsx, frontend/src/features/reconciliation/pages/ReconciliationPage.tsx</files>
  <behavior>
    - "An authorized user selects one customer/week, sees exact material demand, transfers once, opens Warehouse with the same scope, creates an issue and opens Reconciliation with the same batch identity."
    - "The Reconciliation table renders ingredient name/code/unit, required, warehouse-derived issued, signed user-language difference, verdict and one action; UUIDs and purchased fields are absent."
    - "Issue success invalidates only the source list, linked issue history and reconciliation batch projection; no document reload or broad domain invalidation occurs."
    - "Direct `/purchasing` and `/reports` access in this mode renders mode-unavailable, and route intent never preloads them."
  </behavior>
  <action>Regenerate contracts and add `ROUTES.RECONCILIATION`. Build a route-owned `ReconciliationPage` from existing OperationalFrame, QueryViewBoundary, Table/TableViewport, status, formatter and drawer primitives. Preserve customer/week/batch through stable URL/query state rather than adding a second storage authority. Add one Weekly Menu transfer CTA and one Warehouse issue flow that call Wave 1 APIs. Keep permissions distinct from mode. Remove page-bottom `ReconciliationWorkspace` integrations from Weekly Menu/Warehouse/Purchasing/Reports and eliminate the `owner` mutation switch.</action>
  <verify>
    <automated>npm exec --workspace frontend vitest run -- src/features/reconciliation src/features/projects/weekly-menu src/features/warehouse src/features/system-operation --maxWorkers=1 &amp;&amp; npm run check:api-contract</automated>
  </verify>
  <done>One semantic test tracer navigates the real three-page workflow and proves exact request ownership and readback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trim every retained surface to mode-relevant content and lock zero hidden requests</name>
  <files>frontend/src/features/projects/pages/WeeklyMenuPage.tsx, frontend/src/features/warehouse/pages/WarehousePage.tsx, frontend/src/features/reconciliation/ReconciliationComparisonTable.tsx, frontend/src/features/reconciliation/ReconciliationWorkspace.tsx, frontend/src/features/admin/components/AdvancedDisplaySettings.tsx, frontend/src/lib/navigationPreferences.ts, frontend/src/features/system-operation/systemOperationEligibility.ts, frontend/src/**/*.test.ts, frontend/src/**/*.test.tsx</files>
  <behavior>
    - "Weekly Menu mode view mounts only schedule and material-demand owners; production plan, price/cost, dish-material detail and purchase summary owners do not fetch."
    - "Warehouse mode view mounts only source-scoped demand and issue history; purchase receipt lifecycle, general stock/movement content and exceptions do not fetch or render."
    - "Dashboard provides one current-scope progress/next-action summary without broad KPIs or excluded route links."
    - "Admin Data shows only BOM/material validation and audit owners proven relevant by source inspection; contracts, cleanup, statistics and employees are absent and inactive."
    - "Advanced Settings shows backend-eligible preferences and cannot re-enable excluded navigation/tab identities; switching back to DEFAULT restores the user's DEFAULT preferences."
    - "If local preferences hide every eligible tab, the page shows a recovery empty state rather than an empty tablist or stale selected panel."
  </behavior>
  <action>Use backend capability intersection at one shared resolver seam, then trim page-local content at its actual owner. Do not merely apply CSS display rules; skip/unmount excluded queries and lazy owners. Reuse existing components and mode-aware labels (`Định lượng xuất kho`, `Danh sách cần xuất`, `Lịch sử xuất kho`). Limit main tables to 5–7 decision fields and move technical detail to the single existing drawer/detail surface. Run `frontend-checklist-global` discipline conservatively and address only evidenced critical/high findings within touched surfaces.</action>
  <verify>
    <automated>npm exec --workspace frontend vitest run -- src/features/projects src/features/warehouse src/features/reconciliation src/features/admin src/features/system-operation tests --maxWorkers=1 &amp;&amp; npm run lint -w frontend &amp;&amp; npm run build -w frontend &amp;&amp; npm run check:frontend-checklist</automated>
  </verify>
  <done>Every retained page is mode-specific in content as well as tabs, and automated contracts prove excluded owners generate zero controls, requests or preload.</done>
</task>

</tasks>

<verification>Focused semantic/query ownership first, then full frontend unit aggregate, ESLint, production build, dependency/architecture checks, API contract parity, `git diff --check` and secret/stub scan. Browser evidence is reserved for Wave 3.</verification>
<success_criteria>The user sees one compact closed workflow with no Purchasing concepts, redundant tabs, unrelated default content or hidden queries, while DEFAULT remains unchanged.</success_criteria>
<output>Create `30-02-SUMMARY.md` when done.</output>
