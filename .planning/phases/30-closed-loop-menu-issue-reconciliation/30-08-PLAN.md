---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 08
type: execute
wave: 5
depends_on: [30-07]
autonomous: true
requirements: [MRX-03, MRX-04, MRX-05, MRX-06L]
files_modified:
  - frontend/src/features/system-operation/systemOperationAuthorityChannel.ts
  - frontend/src/features/system-operation/systemOperationAuthorityChannel.test.ts
  - frontend/src/features/system-operation/SystemOperationProvider.tsx
  - frontend/src/features/system-operation/systemOperationApi.ts
  - frontend/src/features/system-operation/systemOperationContext.ts
  - frontend/src/features/reconciliation/reconciliationApi.ts
  - frontend/src/features/warehouse/pages/ReconciliationWarehousePage.tsx
  - frontend/src/features/reconciliation/pages/ReconciliationPage.tsx
  - frontend/src/routes/routeDataPreloaders.ts
  - frontend/src/lib/navigationPreferences.ts
  - frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "MRX-03/MRX-05: two independent tab stores converge monotonically on the newest server-confirmed mode/version through the production BroadcastChannel/storage adapter."
    - "MRX-05: authority loss cancels/removes only reconciliation-owned requests/cache/mutation residue and clears URL/view/persisted selection before inactive owners mount or preload."
    - "MRX-04: frontend switch-away/switch-back reveals the same backend workflow identities/versions after Plan 30-04's backend freeze oracle, without resurrecting stale client selection."
    - "DEFAULT navigation, tabs, caches and query ownership remain intact; no broad API reset or unrelated DEFAULT UI change is introduced."
  artifacts:
    - frontend/src/features/system-operation/systemOperationAuthorityChannel.ts
    - frontend/src/features/system-operation/systemOperationAuthorityChannel.test.ts
    - frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx
  key_links:
    - "server-confirmed mode/version -> production cross-tab channel -> monotonic provider refetch"
    - "provider authority transition -> targeted RTK cancellation/removal -> route/URL/storage cleanup"
---

<objective>
Execute frontend Wave 5 slice A: carry real cross-tab server authority and remove only inactive reconciliation owners before they can request or render stale data.

Purpose: Frontend evidence supplements, but never replaces, the backend inactive-workflow and stale-command fences.
Output: One production authority channel, targeted owner cleanup and independent two-tab tests with DEFAULT compatibility.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-SPEC.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-CONTEXT.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-07-SUMMARY.md
@frontend/src/features/system-operation/SystemOperationProvider.tsx
@frontend/src/features/reconciliation/reconciliationApi.ts
@frontend/src/routes/routeDataPreloaders.ts
@frontend/src/lib/navigationPreferences.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one server-confirmed change across independent tab stores</name>
  <files>frontend/src/features/system-operation/systemOperationAuthorityChannel.ts, frontend/src/features/system-operation/systemOperationAuthorityChannel.test.ts, frontend/src/features/system-operation/SystemOperationProvider.tsx, frontend/src/features/system-operation/systemOperationApi.ts, frontend/src/features/system-operation/systemOperationContext.ts</files>
  <behavior>
    - "Tabs A and B use independent Redux stores, RTK caches, routers/histories, providers and persisted-selection adapters."
    - "A server-confirmed change in B publishes stable mode/version through the runtime adapter; A receives the hint and refetches/accepts server authority."
    - "BroadcastChannel is preferred and storage events are fallback behind one injectable subscribe/publish/dispose contract."
    - "Newer versions win; duplicates are idempotent; same-mode higher versions invalidate; older messages and late queries cannot regress authority."
    - "Channel payload, URL and storage never grant capability independently of backend confirmation."
  </behavior>
  <action>Start red with adapter and independent-store tests. Implement the sole runtime cross-tab transport with injectable ports, lifecycle cleanup and stable mode/version messages. Publish only successful server mode query/mutation results. Subscribe once at the provider, use messages as refetch/invalidation hints, and gate accepted responses monotonically. Do not dispatch directly into another test store or share a singleton store.</action>
  <verify><automated>npm exec --workspace frontend vitest run -- src/features/system-operation/systemOperationAuthorityChannel.test.ts src/features/reconciliation/closedLoopWorkflow.test.tsx --maxWorkers=1</automated></verify>
  <done>Independent tabs converge monotonically through the same production-shaped adapter used at runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cancel and remove reconciliation RTK ownership on authority loss</name>
  <files>frontend/src/features/system-operation/SystemOperationProvider.tsx, frontend/src/features/reconciliation/reconciliationApi.ts, frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx</files>
  <behavior>
    - "Authority loss cancels in-flight reconciliation requests and removes batch/list/detail/history cache entries plus mutation residue before obsolete children can consume them."
    - "Deferred responses cannot repopulate inactive-family render state."
    - "Repeated invalidation is idempotent and preserves shared master/reference plus DEFAULT-owned caches."
  </behavior>
  <action>Add one provider-owned authority-transition coordinator using targeted reconciliation endpoint/tag cancellation and removal. Order transition handling before obsolete-mode child mounting. Assert API call history and cache state, not hidden DOM alone. Do not reset the complete RTK API slice and do not alter unrelated DEFAULT endpoints/components.</action>
  <verify><automated>npm exec --workspace frontend vitest run -- src/features/reconciliation/closedLoopWorkflow.test.tsx --maxWorkers=1</automated></verify>
  <done>Only reconciliation-owned RTK state is cancelled/removed and deferred data cannot return.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Clear URL, local view, preload and persisted selection before relocation</name>
  <files>frontend/src/features/warehouse/pages/ReconciliationWarehousePage.tsx, frontend/src/features/reconciliation/pages/ReconciliationPage.tsx, frontend/src/routes/routeDataPreloaders.ts, frontend/src/lib/navigationPreferences.ts, frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx</files>
  <behavior>
    - "Losing reconciliation clears batchId, selected warehouse view and persisted reconciliation navigation selection before route or intent preload runs."
    - "A DEFAULT tab opened with stale URL/storage state performs zero reconciliation requests and relocates to an eligible route."
    - "Returning to reconciliation does not resurrect stale selection; DEFAULT → reconciliation → DEFAULT restores existing DEFAULT tabs, queries and selections."
    - "Frontend round-trip displays the same backend identities/versions established by the backend freeze oracle, not a copied/converted client record."
  </behavior>
  <action>Inventory only the listed concrete owners and coordinate their cleanup through the provider transition. Add zero-request assertions for route loaders and intent preloads, and explicit URL/storage expectations. Preserve DEFAULT navigation preferences and caches. Keep this slice limited to authority cleanup and compatibility—no layout redesign, unrelated DEFAULT behavior change or broad cache reset.</action>
  <verify><automated>npm exec --workspace frontend vitest run -- src/features/reconciliation/closedLoopWorkflow.test.tsx src/routes/routeDataPreloaders.test.ts --maxWorkers=1</automated></verify>
  <done>Every known client owner is cleared before inactive use, stale openings produce zero requests, and DEFAULT returns intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| server authority → cross-tab transport | Browser messages are hints and cannot authorize. |
| authority transition → RTK/router/storage | Stale state must be removed before inactive owners act. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-08-01 | Spoofing | authority channel | high | mitigate | Server refetch and monotonic accepted-version gate. |
| T-30-08-02 | Information Disclosure | stale reconciliation state | high | mitigate | Targeted pre-mount cancellation/removal and zero-request assertions. |
</threat_model>

<verification>Run only bounded local Vitest targets. No headed browser, protected API/database or UI redesign is authorized.</verification>
<success_criteria>Real independent tabs follow server authority, every listed inactive owner produces zero stale request/render, and DEFAULT behavior remains sharply untouched.</success_criteria>
<output>Create `30-08-SUMMARY.md` with authority-channel and frontend owner matrices.</output>
