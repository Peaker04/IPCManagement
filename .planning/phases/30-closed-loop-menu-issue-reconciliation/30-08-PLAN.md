---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 08
type: execute
wave: 8
depends_on: [30-07]
autonomous: true
requirements: [MRX-03, MRX-04, MRX-06L]
files_modified:
  - backend/tests/IPCManagement.Api.Tests/Phase30InactiveReconciliationOwnerTests.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationActualService.cs
  - backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs
  - backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryReturnService.cs
  - backend/src/IPCManagement.Api/DependencyInjection.cs
  - backend/src/IPCManagement.Api/Features/SystemOperation/Services/SystemOperationEligibility.cs
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "MRX-04: reconciliation transfer and completion reject through their real public services while DEFAULT is active, then resume the same batch ID/version/frozen children and apply only their intended state/version delta."
    - "MRX-04: reconciliation issue and return create/confirm reject through canonical Inventory services while DEFAULT is active, then resume the same batch/line/issue/return lineage and mutate stock/net projection exactly once."
    - "MRX-04: registered reconciliation actual ISSUED and disposition mutations reject through ReconciliationActualService while inactive, then resume the same line and expected row versions without duplicate actual/revision/disposition effects."
    - "MRX-03: every reconciliation mutation uses the current switch-away mode version and protected transaction ownership; complete ledgers remain zero-effect on rejection and canonical under replay."
    - "MRX-04: truly absent DEFAULT/reconciliation cleanup and workflow-background owners are excluded only by an executable production controller/service/DI registration-set proof; lifecycle outbox delivery is classified as neutral infrastructure and no guard-validation row substitutes for an owner."
  artifacts:
    - backend/tests/IPCManagement.Api.Tests/Phase30InactiveReconciliationOwnerTests.cs
  key_links:
    - "real mode switch -> ReconciliationOnly request context/current version -> batch/actual/inventory public service -> protected transaction"
    - "ReconciliationBatchLine exact identity -> issue/return/actual/disposition -> family-specific quantities and complete effect ledgers"
---

<objective>
Close every remaining reconciliation mutation owner through persisted production service paths.

Purpose: Registered operation keys prove admission metadata only; MRX-04 requires the actual batch, inventory and actual/disposition owners to reject and later continue the original aggregates.
Output: One bounded persisted reconciliation matrix covering transfer, issue, return, actual/disposition and completion with full ledgers.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.artifacts/runtime/phase30-real-inactive-owner-matrix.map-real-inactive-owner-seams.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-SPEC.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-04-SUMMARY.md
@backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs
@backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationActualService.cs
@backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs
@backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs
@backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryReturnService.cs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Freeze and resume reconciliation transfer and completion on the original batch</name>
  <files>backend/tests/IPCManagement.Api.Tests/Phase30InactiveReconciliationOwnerTests.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs</files>
  <behavior>
    - "TransferToWarehouseAsync on a persisted READY batch with exact frozen lines/contributors and current DEFAULT authority rejects with SystemOperationUnavailableException and leaves batch/line versions, quantities, contributors and every effect ledger unchanged."
    - "After reconciliation resumes, the same batch ID and ExpectedVersion transitions READY to TRANSFERRED exactly once with version+1, zero stock effect and no duplicate batch; replay/concurrency behavior is canonical."
    - "CompleteAsync on a separately persisted IN_PROGRESS batch with exact linked issue quantities and required dispositions rejects inactive, then after switch-back transitions that same batch/version to COMPLETED once while preserving all frozen lines/children and zero stock effect."
  </behavior>
  <action>Persist production-valid transfer and completion fixtures and set request context to each explicit ReconciliationOnly operation key with the current version returned by the real switch to DEFAULT. Invoke ReconciliationBatchService.TransferToWarehouseAsync and ReconciliationCompletionService.CompleteAsync. Snapshot the complete common ledger: aggregate/parent IDs, state/version/quantity/lineage/cardinality; issues/returns and lines; stock/movements with references; supplemental/purchasing; approval/audit; reconciliation actual/revision/disposition; lifecycle/outbox/receipts. Verify the exact pre-resume snapshot again after switch-back, then execute the original IDs/versions and assert only intended deltas.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~Phase30InactiveReconciliationOwnerTests&amp;FullyQualifiedName~Batch"</automated></verify>
  <done>Transfer and completion are real service-owner freeze/resume evidence on the same persisted batches and frozen children.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze and resume reconciliation warehouse issue and return chains</name>
  <files>backend/tests/IPCManagement.Api.Tests/Phase30InactiveReconciliationOwnerTests.cs, backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs, backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryReturnService.cs</files>
  <behavior>
    - "InventoryIssueService.CreateAsync with ReconciliationBatchId and exact ReconciliationBatchLineId rejects while DEFAULT is current using production's current family-active behavior; no invented actual-row owner substitutes for warehouse issue."
    - "After reconciliation resumes, the same batch/line IDs, command and aggregate expected version create one exact issue/line/ISSUE movement chain; retry returns the canonical issue and DEFAULT aggregate contribution remains zero."
    - "InventoryReturnService CreateAsync and ConfirmReceiptAsync on that exact reconciliation issue line reject while DEFAULT is current with complete zero-effect ledgers; after switch-back they create/confirm the same return once, restore stock once and reduce reconciliation net only."
  </behavior>
  <action>Use the real inventory public services with reconciliation lineage and scoped current mode/version. Record the exact current inactive exception produced by production for issue continuation instead of assigning another operation's key. Preserve Plan 30-04 exact-family return behavior. Before and after every rejection and resume compare exact batch-line/issue-line/return-line identities, versions, quantities, stock, movement references, family net totals and all common lifecycle/audit/idempotency ledgers.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~Phase30InactiveReconciliationOwnerTests&amp;FullyQualifiedName~Inventory"</automated></verify>
  <done>Reconciliation warehouse issue and correction continue the original lineage once after inactive zero-effect rejection, without DEFAULT contribution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Freeze and resume registered actual ISSUED and disposition owners</name>
  <files>backend/tests/IPCManagement.Api.Tests/Phase30InactiveReconciliationOwnerTests.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationActualService.cs, backend/src/IPCManagement.Api/DependencyInjection.cs, backend/src/IPCManagement.Api/Features/SystemOperation/Services/SystemOperationEligibility.cs</files>
  <behavior>
    - "UpsertAsync for the registered reconciliation.actuals.issued owner rejects while DEFAULT is active; actual IDs/side/quantity/version, revisions, invalidated dispositions, batch state/version and all common ledgers remain unchanged."
    - "SetDispositionAsync rejects while DEFAULT is active on a production-valid READY/IN_PROGRESS line containing the actual rows required by production; disposition identity/category/reason/version/actor/time and batch/actual/revision ledgers remain unchanged."
    - "After reconciliation resumes, the same line IDs and expected actual/disposition versions update or insert each canonical row once; replay produces no duplicate revision/disposition and only the intended batch transition/version delta."
    - "`AbsentCleanupAndBackgroundMutationOwners_AreNotRegistered_AndLifecycleProcessorIsDeliveryOnly` enumerates production controllers, bounded mutation actions and DI registrations and fails if a new DEFAULT/reconciliation cleanup or background mutation owner appears, or if the registered lifecycle processor gains aggregate creation, continuation, return or completion capability; this is distinct from guard, capability and operation-key assertions."
  </behavior>
  <action>Invoke ReconciliationActualService.UpsertAsync for the registered manual ISSUED actual-row mutation in addition to the warehouse issue owner from Task 2, keeping the two authorities explicitly distinct. Invoke SetDispositionAsync on its real fixture. Use explicit ReconciliationOnly keys and current switch-away versions. Assert the complete common ledger and owner-specific actual/revision/disposition fields before rejection, before resume, after success and after replay. Repair transaction fencing only if this executable path demonstrates a missing current mode/version check. Implement the explicitly named test `AbsentCleanupAndBackgroundMutationOwners_AreNotRegistered_AndLifecycleProcessorIsDeliveryOnly` as a source-aware executable proof over production controllers, bounded mutation actions and DependencyInjection registrations. It must fail when a new controller/action/DI registration introduces a bounded DEFAULT or reconciliation cleanup/background mutation owner, and when the registered lifecycle processor contract or implementation gains MaterialRequest/ReconciliationBatch aggregate creation, continuation, return or completion mutation. Keep this oracle separate from SystemOperationModeGuard, capability classification and hand-authored operation-key assertions.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~Phase30InactiveReconciliationOwnerTests&amp;FullyQualifiedName~Actual" &amp;&amp; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~AbsentCleanupAndBackgroundMutationOwners_AreNotRegistered_AndLifecycleProcessorIsDeliveryOnly"</automated></verify>
  <done>Both registered actual-row owners are executable, persisted and replay-safe across inactive rejection and same-line resume; the exact absent-owner test executes and fails on any new bounded controller/action/DI cleanup/background mutation owner or lifecycle processor aggregate mutation capability, independently of guard/capability/operation-key assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| DEFAULT authority → reconciliation service | Reconciliation-only mutations must fail before any write. |
| exact batch line → inventory/actual/disposition child | Child ownership and family contribution must remain exact through retry. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-08-01 | Tampering | reconciliation batch/actual transactions | high | mitigate | Current mode/version protected execution with complete ledgers. |
| T-30-08-02 | Tampering | reconciliation inventory lineage | high | mitigate | Exact batch-line identity, source-family net assertions and canonical replay. |
</threat_model>

<verification>Run all three focused filters, then the complete Phase30InactiveReconciliationOwnerTests class and Plan 30-04 return regressions. No protected database, invented owner key, permission change or source reclassification is permitted.</verification>
<success_criteria>Every applicable reconciliation transfer, issue, return, actual/disposition and completion owner rejects with zero effects while inactive and resumes its exact original identity/version/state/quantity/lineage once after switch-back.</success_criteria>
<output>Create `30-08-SUMMARY.md` with the complete reconciliation owner ledger and explicit distinction between warehouse issue and manual ISSUED actual-row ownership.</output>
