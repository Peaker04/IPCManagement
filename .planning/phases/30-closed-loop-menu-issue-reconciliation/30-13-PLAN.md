---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 13
type: execute
wave: 13
depends_on: [30-12]
autonomous: true
requirements: [MRX-03, MRX-05, MRX-06L]
files_modified:
  - frontend/src/features/reconciliation/reconciliationApi.ts
  - frontend/src/features/reconciliation/reconciliationRequestContract.fixture.test.ts
  - frontend/package.json
  - contracts/phase30/reconciliation-stale-request.json
  - backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueRequestFixtureTests.cs
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "MRX-03/MRX-05: the production RTK endpoint serialization deterministically emits the exact stale mutation method, path, allowlisted headers and JSON body into one tracked credential-free fixture."
    - "The C# application-path test deserializes that exact tracked fixture, invokes the public backend seam after a mode/version change and proves the established conflict plus a complete zero-effect ledger."
    - "Generation parity fails when production RTK serialization changes method/path/headers/body without regenerating and reviewing the fixture; TS and C# do not maintain duplicate lookalike payloads."
    - "MRX-06L alone closes locally; MRX-06P remains separately authorization-blocked and no executable config or command targets protected ipc_lane7."
  artifacts:
    - contracts/phase30/reconciliation-stale-request.json
    - frontend/src/features/reconciliation/reconciliationRequestContract.fixture.test.ts
    - backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueRequestFixtureTests.cs
  key_links:
    - "production reconciliationApi endpoint serialization -> deterministic fixture generator -> tracked method/path/headers/body"
    - "tracked fixture -> C# fixture reader -> backend public application seam -> stale conflict + zero effects"
---

<objective>
Execute frontend Wave 5 slice B: add a real cross-language request-contract bridge and run bounded local closeout.

Purpose: Separate TS and C# tests must consume one production-generated artifact so independently constructed lookalike payloads cannot pass.
Output: A tracked credential-free request fixture, deterministic generator/parity gate, C# reader/application-path test and local-only MRX-06L closeout.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-SPEC.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-CONTEXT.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-11-SUMMARY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-12-SUMMARY.md
@frontend/src/features/reconciliation/reconciliationApi.ts
@backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Generate and pin the production RTK request contract</name>
  <files>frontend/src/features/reconciliation/reconciliationApi.ts, frontend/src/features/reconciliation/reconciliationRequestContract.fixture.test.ts, frontend/package.json, contracts/phase30/reconciliation-stale-request.json</files>
  <behavior>
    - "The generator invokes the production reconciliation issue endpoint definition/serialization path with fixed public GUIDs, command identity, expected mode and expected version."
    - "The emitted stable schema contains schemaVersion, method, path, sorted allowlisted headers and parsed JSON body; key order and trailing newline are deterministic."
    - "Headers exclude Authorization, Cookie, Set-Cookie, API keys, host/runtime target and every credential/session value."
    - "A parity test compares freshly serialized production output byte-for-byte with the tracked fixture and fails on divergence."
  </behavior>
  <action>Expose only the smallest production endpoint request-preparation seam needed to exercise the same RTK Query serialization used at runtime; do not create a second serializer. Add a fixture test/generator command in `frontend/package.json` that supplies fixed non-secret identities and captures the prepared request before transport. Canonicalize only representation (sorted allowlisted headers, parsed/stably serialized JSON, normalized relative path), never semantics. Write `contracts/phase30/reconciliation-stale-request.json` as tracked generated output. Add explicit secret/header denial assertions and byte parity against a fresh emission.</action>
  <verify><automated>npm exec --workspace frontend vitest run -- src/features/reconciliation/reconciliationRequestContract.fixture.test.ts --maxWorkers=1 &amp;&amp; npm run generate:phase30-request-contract -w frontend &amp;&amp; git diff --exit-code -- contracts/phase30/reconciliation-stale-request.json</automated></verify>
  <done>The tracked fixture is byte-identical to fresh production RTK serialization and contains only deterministic credential-free method/path/headers/body data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read that exact fixture in C# and close local gates</name>
  <files>contracts/phase30/reconciliation-stale-request.json, backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueRequestFixtureTests.cs</files>
  <behavior>
    - "The C# test locates the tracked repository fixture, deserializes its exact method/path/headers/body and rejects unsupported schema versions or forbidden headers."
    - "After changing server mode/version, the test submits the fixture-derived request through the public application/controller seam and receives the established stale authority conflict."
    - "Pre/post ledgers prove zero workflow, issue, line, movement, current-stock, lifecycle, audit and idempotency change."
    - "No C# constant duplicates the expected body/header fields; changing only the TS serializer or fixture makes parity/application tests fail until reconciled."
  </behavior>
  <action>Create a dedicated C# fixture consumer test rather than adding backend ownership to the broad frontend plan. Resolve the repository-relative tracked fixture deterministically, validate schema and credential-free header policy, map the exact serialized request into the real public endpoint/application request path, then apply the mode/version change and complete zero-effect assertions established in Plan 30-11. Run full bounded local gates and document only MRX-06L completion. Do not launch a browser, connect to protected API/MySQL, reset/seed data or claim MRX-06P/protected DEFAULT restoration.</action>
  <verify><automated>npm exec --workspace frontend vitest run -- src/features/reconciliation/reconciliationRequestContract.fixture.test.ts src/features/reconciliation/closedLoopWorkflow.test.tsx --maxWorkers=1 &amp;&amp; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconciliationWarehouseIssueRequestFixtureTests|FullyQualifiedName~ReconciliationWarehouseIssueApplicationPathTests|FullyQualifiedName~Phase30DiscoveredConsumerBijectionTests" &amp;&amp; npm exec --workspace frontend vitest run -- --maxWorkers=1 &amp;&amp; npm run lint -w frontend &amp;&amp; npm run build -w frontend &amp;&amp; npm run check:api-contract &amp;&amp; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore &amp;&amp; git diff --check</automated></verify>
  <done>The backend consumes the exact production-generated fixture, stale replay fails atomically, all local gates pass, MRX-06L is evidenced and MRX-06P remains blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| production serializer → tracked fixture | Generated artifacts must be deterministic and credential-free. |
| fixture → backend public seam | Backend evidence must consume exact frontend output rather than a duplicate contract. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-13-01 | Information Disclosure | request fixture | high | mitigate | Allowlisted headers, fixed public IDs and explicit credential denial test. |
| T-30-13-02 | Tampering | cross-language contract | high | mitigate | Production generation, byte parity and single tracked artifact consumed by C#. |
</threat_model>

<verification>Generate fixture and run TS parity before C# consumption, then execute local full gates. Protected execution remains excluded.</verification>
<success_criteria>One deterministic credential-free artifact bridges actual production RTK serialization to the backend public stale-replay test, and only local MRX-06L is closed.</success_criteria>
<output>Create `30-13-SUMMARY.md` with fixture schema/hash, generation command, C# consumption proof, local gates and explicit MRX-06P blocker.</output>
