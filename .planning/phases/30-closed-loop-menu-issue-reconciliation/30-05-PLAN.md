---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 05
type: execute
wave: 4
depends_on: [30-04]
autonomous: true
status: pending
requirements: [MRX-03, MRX-04, MRX-06L]
files_modified:
  - backend/tests/IPCManagement.Api.Tests/Phase30InactiveDefaultInventoryOwnerTests.cs
  - backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs
  - backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryReturnService.cs
  - backend/src/IPCManagement.Api/Data/Transactions/EfTransactionRunner.cs
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "MRX-04: persisted DEFAULT issue create, issue receipt continuation, return create and return receipt confirmation reject through their real public services while MATERIAL_RECONCILIATION is active, with the complete workflow/effect ledger unchanged."
    - "MRX-04: after a real switch back to DEFAULT, each command reuses the same aggregate/source IDs, aggregate expected version and command identity, mutates the original record once, and replay returns the canonical result without copied, re-parented or duplicate records."
    - "MRX-03: DEFAULT issue and return transactions consume the current scoped operation-mode version immediately before durable writes; a stale authority version fails with zero residue."
  artifacts:
    - backend/tests/IPCManagement.Api.Tests/Phase30InactiveDefaultInventoryOwnerTests.cs
  key_links:
    - "SystemOperationModeService.ChangeAsync -> scoped SystemOperationRequestContext -> InventoryIssueService/InventoryReturnService -> IEfTransactionRunner protected commit"
    - "exact MaterialRequest/MaterialRequestLine or InventoryIssue/InventoryIssueLine identity -> issue/return line -> CurrentStock/StockMovement/lifecycle receipt"
---

<objective>
Close the remaining MRX-04 DEFAULT inventory transaction owners without reopening the verified Plan 30-04 return-family truths.

Purpose: The real service transaction seams, rather than guard rows, must prove inactive zero-effect and same-record continuation.
Output: Persisted executable issue/create+confirm and return/create+confirm freeze/resume tests plus only source-map-proven transaction fencing.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.artifacts/runtime/phase30-real-inactive-owner-matrix.map-real-inactive-owner-seams.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-SPEC.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-04-SUMMARY.md
@backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs
@backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryReturnService.cs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Freeze and resume DEFAULT issue creation and receipt continuation through the production service</name>
  <files>backend/tests/IPCManagement.Api.Tests/Phase30InactiveDefaultInventoryOwnerTests.cs, backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs, backend/src/IPCManagement.Api/Data/Transactions/EfTransactionRunner.cs</files>
  <behavior>
    - "Persist an approved issuable MaterialRequest with one exact MaterialRequestLine, stock, warehouse, actor and current DEFAULT authority; switch through SystemOperationModeService.ChangeAsync to MATERIAL_RECONCILIATION and set request context key inventoryissues.createasync, Retained disposition and the returned current mode version."
    - "Calling IInventoryIssueService.CreateAsync with the original MaterialRequestId, exact MaterialRequestLineId, CommandId and aggregate ExpectedVersion rejects before issue, line, stock, ISSUE movement, lifecycle transition, outbox, receipt or audit changes."
    - "After switching to DEFAULT, the identical request creates one issue/movement chain from the original request and line; replay returns the same issue identity and leaves all cardinalities unchanged."
    - "Repeat the pattern for IInventoryIssueService.ConfirmReceiptAsync on one persisted unreceived DEFAULT issue: inactive rejection changes no received fields, supplemental consequence, lifecycle/effect count or audit; resumed call updates that same issue once and replay is canonical."
  </behavior>
  <action>Build real SQLite-backed service graphs and persist every fixture named by the source map. Snapshot owner/header/line IDs, parent IDs, status, aggregate and line versions, quantities, exact lineage and child counts plus Inventoryissues, Inventoryissuelines, Inventoryreturns, Inventoryreturnlines, Currentstocks, Stockmovements with movement/ref identity, Supplementalmaterialrequests, Approvalhistories, Auditlogs, Reconciliationactuals/revisions/dispositions and Lifecycletransitions/outbox/receipts. Invoke the real public service methods as the attempted mutations. Add or retain protected transaction dispatch for DEFAULT issue creation and receipt continuation so the scoped current mode/version is checked at commit; do not alter controller ownership, operation permissions or exact-lineage rules established by Plan 30-04.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~Phase30InactiveDefaultInventoryOwnerTests&amp;FullyQualifiedName~Issue"</automated></verify>
  <done>Both DEFAULT issue owners reject with a byte/value-equivalent full ledger while inactive, then mutate the original aggregate exactly once after DEFAULT resumes using the original versions and command identities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze and resume DEFAULT return creation and confirmation through the production service</name>
  <files>backend/tests/IPCManagement.Api.Tests/Phase30InactiveDefaultInventoryOwnerTests.cs, backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryReturnService.cs</files>
  <behavior>
    - "Persist an exact DEFAULT issue/header/line and stock; while MATERIAL_RECONCILIATION is current, InventoryReturnService.CreateAsync rejects the same IssueId, SourceIssueLineId, quantity, command and expected aggregate version with the complete ledger unchanged."
    - "After DEFAULT resumes, the same source issue line creates one pending return with exact lineage and zero stock movement; replay returns the same return."
    - "While inactive, ConfirmReceiptAsync on that pending return changes no return status/version/received fields, stock, RETURN movement, net-issued projection, lifecycle, audit or receipt."
    - "After DEFAULT resumes, confirmation changes the same return once, restores stock once, writes one RETURN movement tied to the original return/source issue line, reduces only DEFAULT net issued and is replay-stable."
  </behavior>
  <action>Reuse the persisted authority/snapshot helper from Task 1, but invoke InventoryReturnService.CreateAsync and ConfirmReceiptAsync directly with current switch-away versions. Preserve the already verified exact-family resolver and paired return semantics from Plan 30-04. Assert original aggregate identity/version/state/quantity/lineage before resume, then assert the intended resumed delta and no replacement aggregate, cross-family contribution or duplicate ledger row.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~Phase30InactiveDefaultInventoryOwnerTests&amp;FullyQualifiedName~Return"</automated></verify>
  <done>DEFAULT return create/confirm is executable-owner evidence with complete inactive and resumed ledgers, while all Plan 30-04 exact-family truths remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| mode authority → inventory transaction | Captured mode/version must still be current immediately before write. |
| request IDs → persisted DEFAULT lineage | Exact source IDs and versions must identify one original aggregate. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-05-01 | Tampering | InventoryIssueService transaction dispatch | high | mitigate | Protected current mode/version check plus full zero-effect ledger. |
| T-30-05-02 | Tampering | InventoryReturnService replay/lineage | high | mitigate | Exact source identity, canonical command replay and cardinality assertions. |
</threat_model>

<verification>Run the two focused filters and the existing Plan 30-04 return-family tests. No command may target a protected database or change permission/source ownership.</verification>
<success_criteria>All four DEFAULT inventory owners are exercised through real persisted service paths, reject without effects while inactive, and continue the exact original aggregates once after switch-back.</success_criteria>
<output>Create `30-05-SUMMARY.md` with per-owner inactive/resume ledgers and production-fence disposition.</output>
