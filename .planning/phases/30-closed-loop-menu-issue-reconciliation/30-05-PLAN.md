---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 05
type: execute
wave: 5
depends_on: [30-04]
autonomous: true
requirements: [MRX-01, MRX-02, MRX-03, MRX-04, MRX-05, MRX-06]
files_modified:
  - frontend/src/features/system-operation/SystemOperationProvider.tsx
  - frontend/src/features/system-operation/systemOperationApi.ts
  - frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A server mode-version change clears or partitions mode-owned RTK Query data, URL source scope and persisted selection before the inactive family can render (E3-1)."
    - "An invalid route relocates to a permitted retained surface and excluded navigation/tab/preload owners issue zero requests."
    - "A delayed mutation carries its expected version and surfaces the backend stale conflict without optimistic inactive-family residue."
    - "Switching back to DEFAULT restores its complete capability/navigation/query ownership without stale reconciliation state."
  artifacts:
    - frontend/src/features/system-operation/SystemOperationProvider.tsx
    - frontend/src/features/system-operation/systemOperationApi.ts
    - frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx
  key_links:
    - "server mode/version response -> provider transition -> RTK cache/selection reset -> route eligibility"
    - "route/tab eligibility -> preload/query skip ownership -> zero excluded network calls"
    - "mutation request expectedModeVersion -> backend conflict -> cache rollback/refetch"
---

<objective>
Make frontend cache, URL, preload and stale-session behavior subordinate to server mode/version while preserving DEFAULT (E3-1).

Purpose: The browser must not expose cached inactive-family records or keep issuing requests after a mode change, even though backend transaction fences remain the final authority.
Output: Red→green provider/query/router ownership tests and minimal production wiring for deterministic stale-session recovery.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-SPEC.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-CONTEXT.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-RESEARCH.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-04-SUMMARY.md
@frontend/src/features/system-operation/SystemOperationProvider.tsx
@frontend/src/features/system-operation/systemOperationApi.ts
@frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Switch one open reconciliation route to DEFAULT without stale render or request</name>
  <files>frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx, frontend/src/features/system-operation/SystemOperationProvider.tsx, frontend/src/features/system-operation/systemOperationApi.ts</files>
  <behavior>
    - "While a reconciliation detail is open and cached, a newer server response selecting DEFAULT removes the stale detail/selection, sanitizes source-specific URL state and relocates to an eligible DEFAULT route before another reconciliation preload/query fires."
    - "A same-mode higher version also invalidates mode-owned state so capability or authority changes cannot reuse a stale cache."
    - "Shared master/reference cache may remain only when it is not source-family owned."
  </behavior>
  <action>Start with a failing integration test around `SystemOperationProvider` and the existing closed-loop router/store harness. Treat the tuple of server mode and version as the cache/session authority: on tuple change, reset or partition only mode-owned RTK Query slices and local/persisted source selection, replace invalid source parameters, and relocate through the existing capability route owner. Order the transition so excluded components cannot mount and preload during cleanup. Do not rely on browser state to authorize commands.</action>
  <verify><automated>npm exec --workspace frontend vitest run -- src/features/reconciliation/closedLoopWorkflow.test.tsx --maxWorkers=1</automated></verify>
  <done>An open reconciliation route deterministically becomes a clean DEFAULT route on a newer authority tuple with zero stale render and zero excluded request.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close delayed mutation, preload ownership and DEFAULT round-trip cases</name>
  <files>frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx, frontend/src/features/system-operation/SystemOperationProvider.tsx, frontend/src/features/system-operation/systemOperationApi.ts</files>
  <behavior>
    - "Every mode-sensitive mutation sends the provider's expected server mode version; a stale conflict clears optimistic state, refreshes authority and presents the established recoverable error."
    - "Excluded navigation, tabs, route loaders and hover/intent preloads own zero reconciliation requests under DEFAULT and zero DEFAULT-family workflow requests under MATERIAL_RECONCILIATION."
    - "Rapid DEFAULT → MATERIAL_RECONCILIATION → DEFAULT changes cannot let an older response overwrite the newest authority tuple."
    - "The final DEFAULT state exposes its full existing navigation/tab/API capability and contains no reconciliation URL selection or cache entry."
  </behavior>
  <action>Add fake-timer/deferred-response tests for out-of-order authority responses, delayed mutations, route loaders and preload triggers. Wire expected mode version through existing mutation preparation, use the existing RTK error/refetch path for backend conflicts, and make authority updates monotonic by server version. Assert request ownership rather than only hidden DOM. Keep `DEFAULT` as the final test state and retain all existing DEFAULT capability expectations.</action>
  <verify><automated>npm exec --workspace frontend vitest run -- src/features/reconciliation/closedLoopWorkflow.test.tsx --maxWorkers=1 &amp;&amp; npm exec --workspace frontend vitest run -- --maxWorkers=1 &amp;&amp; npm run lint -w frontend &amp;&amp; npm run build -w frontend &amp;&amp; npm run check:api-contract &amp;&amp; git diff --check</automated></verify>
  <done>Stale clients recover through server authority, excluded owners make zero requests, response races are monotonic and DEFAULT round-trip compatibility passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| server authority → browser store/router | Responses may arrive late or out of order and cached data may belong to an inactive family. |
| browser mutation → API | Expected version is concurrency evidence, not authorization. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-05-01 | Information Disclosure | RTK cache/router | high | mitigate | Invalidate mode-owned cache/selection and relocate before inactive components mount. |
| T-30-05-02 | Tampering | out-of-order mode responses | high | mitigate | Accept authority updates monotonically by server version. |
| T-30-05-03 | Elevation of Privilege | client capability state | medium | mitigate | Backend remains authoritative; client sends expected version and handles conflict. |
</threat_model>

<verification>Run focused integration tests, full frontend Vitest, lint, build and contract parity locally. No headed browser, protected API or `ipc_lane7` mutation belongs to this wave.</verification>
<success_criteria>Mode/version changes cannot expose or request inactive-family data; stale mutations recover cleanly; DEFAULT retains its complete existing behavior.</success_criteria>
<output>Create `30-05-SUMMARY.md` when done.</output>
