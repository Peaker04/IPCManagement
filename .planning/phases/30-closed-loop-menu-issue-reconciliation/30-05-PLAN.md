---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 05
type: execute
wave: 5
depends_on: [30-04]
autonomous: true
requirements: [MRX-01, MRX-02, MRX-03, MRX-04, MRX-05, MRX-06L]
files_modified:
  - frontend/src/features/system-operation/SystemOperationProvider.tsx
  - frontend/src/features/system-operation/systemOperationApi.ts
  - frontend/src/features/system-operation/systemOperationAuthorityChannel.ts
  - frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx
  - backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Two independent tab stores use the real cross-tab authority channel; tab B's mode/version change invalidates tab A cache, URL and persisted selection and relocates tab A before inactive-family render/request (E3-1)."
    - "A mutation captured in tab A before tab B's change replays to the backend with the stale version and receives the established conflict with zero durable or optimistic residue."
    - "Excluded navigation/tab/preload owners issue zero requests and switching back to DEFAULT restores its complete capability/query ownership."
    - "Plans 30-03..05 can complete MRX-06L only; MRX-06P remains authorization-blocked and no protected ipc_lane7 access occurs."
  artifacts:
    - frontend/src/features/system-operation/systemOperationAuthorityChannel.ts
    - frontend/src/features/system-operation/SystemOperationProvider.tsx
    - frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx
    - backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs
  key_links:
    - "tab B server authority response -> BroadcastChannel/storage transport -> tab A provider/store cleanup -> route relocation"
    - "tab A captured request expectedModeVersion -> backend transaction fence -> conflict and atomic zero effects"
    - "route/tab eligibility -> preload/query skip ownership -> zero excluded network calls"
---

<objective>
Prove real cross-tab mode authority, captured stale replay and bounded local MRX-06L closeout while preserving DEFAULT (E3-1).

Purpose: A single-store sequence cannot prove that another open tab loses stale authority, and local closure must not be confused with separately authorized protected evidence.
Output: Red→green two-store/two-tab integration tests using the production cross-tab channel, backend captured-replay proof and explicit MRX-06L/MRX-06P disposition.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-SPEC.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-CONTEXT.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-RESEARCH.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-04-SUMMARY.md
@frontend/src/features/system-operation/SystemOperationProvider.tsx
@frontend/src/features/system-operation/systemOperationApi.ts
@frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Propagate one authority change across two independent tab stores</name>
  <files>frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx, frontend/src/features/system-operation/SystemOperationProvider.tsx, frontend/src/features/system-operation/systemOperationApi.ts, frontend/src/features/system-operation/systemOperationAuthorityChannel.ts</files>
  <behavior>
    - "Tab A and tab B have independent RTK stores, routers, histories, persisted selections and provider instances."
    - "After tab B receives and publishes a newer DEFAULT mode/version, the real production cross-tab channel delivers it to tab A; tab A clears reconciliation-owned cache/selection, removes source URL parameters and relocates to an eligible DEFAULT route before another excluded query/preload fires."
    - "Repeated and same-mode higher-version notifications are idempotent; older notifications/responses cannot overwrite the newest tuple."
  </behavior>
  <action>Start with a failing integration harness that creates two independent application stores/routers and a deterministic in-memory transport implementing the same interface as the production `BroadcastChannel` with a `storage`-event fallback. Add `systemOperationAuthorityChannel.ts` as the single production cross-tab authority transport, publish only server-confirmed tuples, subscribe in each `SystemOperationProvider`, and make cleanup/relocation monotonic by version. Do not share one store between tabs and do not simulate cross-tab behavior by directly dispatching into tab A. Preserve only genuinely shared master/reference cache; inactive-family state must be removed before route owners mount.</action>
  <verify><automated>npm exec --workspace frontend vitest run -- src/features/reconciliation/closedLoopWorkflow.test.tsx --maxWorkers=1</automated></verify>
  <done>A real two-store/two-router test proves tab B authority invalidates and relocates tab A with clean URL/cache/selection and zero excluded request.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replay tab A's captured stale command at the backend and close local-only verification</name>
  <files>frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx, frontend/src/features/system-operation/SystemOperationProvider.tsx, frontend/src/features/system-operation/systemOperationApi.ts, backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs</files>
  <behavior>
    - "Tab A captures a real mode-sensitive request before tab B changes authority; replaying that serialized request after the change reaches the backend with the old expected mode/version and returns the established stale conflict."
    - "Backend pre/post assertions show zero workflow, issue, line, stock movement, current-stock, lifecycle/audit and idempotency change; tab A removes optimistic residue and refreshes authority."
    - "Excluded route loaders/intent preloads own zero opposite-family requests, and DEFAULT → MATERIAL_RECONCILIATION → DEFAULT ends with complete DEFAULT capability and no reconciliation cache/URL selection."
    - "The local suite records MRX-06L as the only closable verification child; MRX-06P stays blocked pending fresh authorization and no command targets ipc_lane7."
  </behavior>
  <action>Capture the actual prepared request payload from tab A rather than constructing a second synthetic request. Feed the same serialized command/version into the existing backend application-path stale replay test after changing server authority, and assert the full atomic zero-effect ledger. On the frontend, exercise the normal RTK conflict/rollback/refetch path and assert no optimistic inactive-family state survives. Add deferred-response/preload tests and the complete DEFAULT round trip. Run only local test/build/static commands; scan commands/config for protected database names and fail if this plan attempts `ipc_lane7`. Do not create a headed-browser checkpoint or claim MRX-06/MRX-06P complete.</action>
  <verify><automated>npm exec --workspace frontend vitest run -- src/features/reconciliation/closedLoopWorkflow.test.tsx --maxWorkers=1 &amp;&amp; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconciliationWarehouseIssueApplicationPathTests" &amp;&amp; npm exec --workspace frontend vitest run -- --maxWorkers=1 &amp;&amp; npm run lint -w frontend &amp;&amp; npm run build -w frontend &amp;&amp; npm run check:api-contract &amp;&amp; ! (git diff --unified=0 -- backend frontend | grep '^+' | grep -v '^+++' | grep -Ev '^[+][[:space:]]*(//|/\*|\*|#)' | grep -E "(Database=ipc_lane7|--database[ =]+ipc_lane7|IPC_LANE7)") &amp;&amp; git diff --check</automated></verify>
  <done>The production cross-tab channel and backend transaction fence defeat a captured stale replay, local regressions/builds pass, DEFAULT is restored in the harness, MRX-06L is evidenced, and MRX-06P remains explicitly authorization-blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| server authority → browser store/router | Responses may arrive late or out of order and cached data may belong to an inactive family. |
| browser mutation → API | Expected version is concurrency evidence, not authorization. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-05-01 | Information Disclosure | RTK cache/router | high | mitigate | Invalidate mode-owned cache/selection and relocate before inactive components mount. |
| T-30-05-02 | Tampering | out-of-order mode responses | high | mitigate | Accept authority updates monotonically by server version. |
| T-30-05-03 | Elevation of Privilege | client capability state | medium | mitigate | Backend remains authoritative; client sends expected version and handles conflict. |
</threat_model>

<verification>Run focused integration tests, full frontend Vitest, lint, build and contract parity locally. No headed browser, protected API or `ipc_lane7` mutation belongs to this wave.</verification>
<success_criteria>Mode/version changes cannot expose or request inactive-family data; stale mutations recover cleanly; DEFAULT retains its complete existing behavior.</success_criteria>
<output>Create `30-05-SUMMARY.md` when done.</output>
