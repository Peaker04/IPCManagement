---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 05
type: execute
wave: 5
depends_on: [30-04]
autonomous: true
requirements: [MRX-01, MRX-02, MRX-03, MRX-04, MRX-05, MRX-06L]
files_modified:
  - frontend/src/features/system-operation/systemOperationAuthorityChannel.ts
  - frontend/src/features/system-operation/systemOperationAuthorityChannel.test.ts
  - frontend/src/features/system-operation/SystemOperationProvider.tsx
  - frontend/src/features/system-operation/systemOperationApi.ts
  - frontend/src/features/system-operation/systemOperationContext.ts
  - frontend/src/features/reconciliation/reconciliationApi.ts
  - frontend/src/features/warehouse/pages/ReconciliationWarehousePage.tsx
  - frontend/src/features/reconciliation/pages/ReconciliationPage.tsx
  - frontend/src/routes/routeDataPreloaders.ts
  - frontend/src/lib/navigationPreferences.ts
  - frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx
  - backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "MRX-03/MRX-05: two independent browser-tab stores converge monotonically on the newest server-confirmed mode/version through the production cross-tab channel."
    - "MRX-05: on DEFAULT ↔ MATERIAL_RECONCILIATION authority change, inactive-family RTK queries, URL batchId, local selected view and persisted navigation selections are cleared before an excluded route/query/preload can run."
    - "MRX-03: a real serialized reconciliation mutation captured before a cross-tab authority change reaches the backend with its old expected mode/version and fails with zero durable or optimistic residue."
    - "MRX-04/MRX-06L: switching away preserves unfinished workflow records and switching back restores the same identities/versions while the complete DEFAULT route/tab/query behavior remains available."
    - "MRX-06L closes only with local deterministic evidence; MRX-06P remains separately authorization-blocked and no command or configuration targets protected ipc_lane7."
  artifacts:
    - frontend/src/features/system-operation/systemOperationAuthorityChannel.ts
    - frontend/src/features/system-operation/systemOperationAuthorityChannel.test.ts
    - frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx
  key_links:
    - "server-confirmed mode mutation/query -> cross-tab transport -> monotonic provider authority"
    - "provider authority transition -> RTK invalidation/removal + URL/local-storage cleanup -> eligible route relocation"
    - "captured mutation expected mode/version -> backend transaction fence -> conflict + zero effects"
    - "DEFAULT round trip -> original navigation/tab/query ownership restored without reconciliation residue"
---

<objective>
Finish Phase 30's remaining local frontend boundary as one real two-tab authority flow and one captured stale-command flow, then record local-only MRX-06L closure.

Purpose: Single-store invalidation does not prove cross-tab authority. The production transport, every reconciliation state/request owner, backend transaction fence, and full DEFAULT round trip must be exercised together without protected execution.
Output: A monotonic BroadcastChannel/storage authority adapter, exhaustive frontend owner cleanup, two-store integration tests, backend captured-replay proof and explicit MRX-06L versus MRX-06P disposition.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-SPEC.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-CONTEXT.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-RESEARCH.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-03-SUMMARY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-04-SUMMARY.md
@frontend/src/features/system-operation/SystemOperationProvider.tsx
@frontend/src/features/system-operation/systemOperationApi.ts
@frontend/src/features/system-operation/systemOperationContext.ts
@frontend/src/features/reconciliation/reconciliationApi.ts
@frontend/src/features/warehouse/pages/ReconciliationWarehousePage.tsx
@frontend/src/features/reconciliation/pages/ReconciliationPage.tsx
@frontend/src/routes/routeDataPreloaders.ts
@frontend/src/lib/navigationPreferences.ts
@backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one server-confirmed authority change across two independent tab stores</name>
  <files>frontend/src/features/system-operation/systemOperationAuthorityChannel.ts, frontend/src/features/system-operation/systemOperationAuthorityChannel.test.ts, frontend/src/features/system-operation/SystemOperationProvider.tsx, frontend/src/features/system-operation/systemOperationApi.ts, frontend/src/features/system-operation/systemOperationContext.ts</files>
  <behavior>
    - "Tab A and tab B use independent Redux stores, RTK caches, routers/histories, provider instances and persisted-selection adapters."
    - "A mode mutation confirmed by the server in tab B publishes the exact stable mode plus version; tab A receives it through the same production adapter used at runtime and refetches/accepts server authority."
    - "BroadcastChannel is preferred when available and a storage-event transport is the browser fallback; both share one injectable adapter contract for deterministic tests."
    - "Newer versions win, duplicate tuples are idempotent, same-mode higher versions still invalidate stale state, and older channel messages or late query responses cannot replace the newest tuple."
    - "No browser preference, URL, local storage value or channel payload grants capability independently of the server-confirmed tuple."
  </behavior>
  <action>Start red with adapter tests and a two-store tracer in `closedLoopWorkflow.test.tsx`. Create `systemOperationAuthorityChannel.ts` as the only cross-tab transport, with injectable ports for tests, runtime BroadcastChannel and storage-event fallback, explicit subscribe/publish/dispose lifecycle, and stable mode/version messages. Publish only successful server mode query/mutation results from `systemOperationApi`; subscribe once in `SystemOperationProvider`. On a newer message invalidate/refetch operation authority and keep a monotonic last-accepted version so late RTK responses cannot regress context. Do not dispatch directly into the other store in the integration test, do not share a singleton test store, and do not treat channel data as backend authorization.</action>
  <verify><automated>npm exec --workspace frontend vitest run -- src/features/system-operation/systemOperationAuthorityChannel.test.ts src/features/reconciliation/closedLoopWorkflow.test.tsx --maxWorkers=1</automated></verify>
  <done>A production-shaped channel moves a server-confirmed tuple from tab B to independent tab A, and tab A monotonically converges without accepting stale or client-invented authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fence every reconciliation URL, cache, preload and persisted-selection owner before inactive requests</name>
  <files>frontend/src/features/system-operation/SystemOperationProvider.tsx, frontend/src/features/reconciliation/reconciliationApi.ts, frontend/src/features/warehouse/pages/ReconciliationWarehousePage.tsx, frontend/src/features/reconciliation/pages/ReconciliationPage.tsx, frontend/src/routes/routeDataPreloaders.ts, frontend/src/lib/navigationPreferences.ts, frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx</files>
  <behavior>
    - "When tab A loses MATERIAL_RECONCILIATION authority it removes reconciliation batch/list/detail/history cache entries, mutation residue, batchId query parameters, reconciliation selected view and persisted reconciliation navigation selections."
    - "Cleanup and relocation occur before ReconciliationWarehousePage, ReconciliationPage, routeDataPreloaders or intent preloads can issue another inactive-family request."
    - "Deferred responses from the prior authority cannot repopulate rendered inactive-family data after cleanup."
    - "Repeated invalidation is idempotent and a tab opened with stale batchId/local-storage state performs zero reconciliation requests under DEFAULT."
    - "Returning to MATERIAL_RECONCILIATION does not resurrect the old batch selection; returning to DEFAULT restores its existing navigation, tabs, queries and complete issue/return/supplemental behavior."
  </behavior>
  <action>Use the two-tab integration harness to inventory and exercise the concrete owners: `reconciliationApi` batch/list/detail/history tags and issue mutation, `ReconciliationWarehousePage` URL `batchId` plus local `selectedView`, `ReconciliationPage` URL-selected batch, `routeDataPreloaders`, and `navigationPreferences` local storage/event state. Add one provider-owned authority-transition coordinator that cancels/removes inactive-family RTK entries, clears owned URL/persisted selection, and relocates to a mode-eligible route before children for the obsolete mode can mount. Preserve shared master/reference caches and DEFAULT-owned caches; do not reset the entire API slice. Assert network calls, not only hidden DOM, so excluded owners produce zero requests. Include DEFAULT → MATERIAL_RECONCILIATION → DEFAULT and the reverse switch-away/resume identity checks required by MRX-04/MRX-05.</action>
  <verify><automated>npm exec --workspace frontend vitest run -- src/features/reconciliation/closedLoopWorkflow.test.tsx src/routes/routeDataPreloaders.test.ts --maxWorkers=1</automated></verify>
  <done>Every known reconciliation state/request owner is removed or skipped before inactive use, deferred data cannot return, and the DEFAULT workflow is fully restored rather than globally reset.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replay the captured stale command through the backend fence and close local MRX-06L</name>
  <files>frontend/src/features/reconciliation/reconciliationApi.ts, frontend/src/features/reconciliation/closedLoopWorkflow.test.tsx, backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs</files>
  <behavior>
    - "Tab A captures the actual serialized reconciliation issue request—including command identity and expected mode/version—before tab B changes server authority."
    - "Replaying that exact payload after the change reaches the backend public application seam and receives the established stale-mode/version conflict."
    - "Backend pre/post assertions show zero reconciliation workflow, issue, line, movement, current-stock, lifecycle/audit and idempotency change; frontend removes optimistic residue and refreshes current authority."
    - "Plan 30-04's exact-family backend matrix and the frontend round trip jointly keep the complete DEFAULT workflow green."
    - "Only local MRX-06L is claimed. MRX-06P stays blocked pending a new explicit authorization, with no protected database/API/browser action and final protected-mode restoration not claimed."
  </behavior>
  <action>Capture the prepared request generated by the production RTK mutation instead of constructing a lookalike payload. Feed that serialized body/header contract into the existing backend application-path stale-replay test after changing mode/version at its deterministic transaction barrier. Exercise RTK's real rejected-mutation rollback/refetch path in the two-tab test. Then run focused and full local backend/frontend gates plus a protected-target diff/config scan. Do not launch a headed browser, connect to a protected API/MySQL target, mention `ipc_lane7` in executable configuration, reset/seed data, or mark MRX-06P complete. Record in `30-05-SUMMARY.md` that local MRX-06L is complete and protected evidence remains separately authorization-gated.</action>
  <verify><automated>npm exec --workspace frontend vitest run -- src/features/reconciliation/closedLoopWorkflow.test.tsx --maxWorkers=1 &amp;&amp; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconciliationWarehouseIssueApplicationPathTests|FullyQualifiedName~Phase30IssueFamilyConsumerMatrixTests" &amp;&amp; npm exec --workspace frontend vitest run -- --maxWorkers=1 &amp;&amp; npm run lint -w frontend &amp;&amp; npm run build -w frontend &amp;&amp; npm run check:api-contract &amp;&amp; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore &amp;&amp; git diff --check</automated></verify>
  <done>The exact captured stale request fails atomically at the backend, browser residue is removed, all local regressions/build/contracts pass, DEFAULT is restored in local tests, MRX-06L is explicitly evidenced, and MRX-06P remains authorization-blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| server authority → cross-tab channel | Channel messages are invalidation hints, not independent authorization. |
| authority transition → router/cache/storage | Obsolete state must be removed before inactive owners issue requests or render data. |
| captured browser command → backend transaction | Expected mode/version must be revalidated immediately before durable writes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-05-01 | Spoofing | cross-tab authority message | high | mitigate | Refetch server authority; channel only announces a server-confirmed newer tuple. |
| T-30-05-02 | Information Disclosure | RTK/router/preload state | high | mitigate | Targeted cancel/remove, URL/storage cleanup and pre-mount relocation with zero-request assertions. |
| T-30-05-03 | Tampering | out-of-order responses | high | mitigate | Monotonic accepted-version gate across channel and RTK responses. |
| T-30-05-04 | Elevation of Privilege | stale mutation | high | mitigate | Backend transactional mode/version and permission checks with complete zero-effect ledger. |
</threat_model>

<verification>Execute only local deterministic Vitest, dotnet, lint, build and API-contract gates. No protected MySQL/API/browser execution or protected-data restoration belongs to this plan.</verification>
<success_criteria>Two real independent tab stores converge on server authority; every concrete inactive-family owner produces zero stale request/render; a captured stale mutation fails atomically; full DEFAULT behavior is restored locally; only MRX-06L is closed.</success_criteria>
<output>Create `30-05-SUMMARY.md` when done with the final frontend owner matrix, local gate results, DEFAULT round-trip evidence, and explicit MRX-06P authorization blocker.</output>
