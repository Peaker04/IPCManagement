---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 03
type: execute
wave: 3
depends_on: [30-02]
autonomous: true
requirements: [MRX-02, MRX-03, MRX-05, MRX-06L]
files_modified:
  - backend/src/IPCManagement.Api/Features/SystemOperation/SystemOperationRequestContext.cs
  - backend/src/IPCManagement.Api/Features/Inventory/Persistence/InventoryEntityConfigurations.cs
  - backend/src/IPCManagement.Api/Features/Inventory/Validators/InventoryValidators.cs
  - backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs
  - backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs
  - backend/tests/IPCManagement.Api.Tests/Phase30EndpointAuthorityMatrixTests.cs
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A mode-sensitive command commits only when its expected mode/version still matches server authority inside the write transaction (E2-1)."
    - "Both-family, neither-family, header/line mismatch, cross-batch and cross-line issue attempts are rejected by the real validator/database constraint owners with zero durable effect (E1-1)."
    - "Retained Admin Data master mutations and canonical Warehouse mutations keep their existing permissions in both modes; mode grants no permission and every non-Warehouse command has zero stock effect."
    - "Wrong-mode, stale-version and permission failures mutate neither active nor inactive workflow data, issue data, stock, lifecycle/audit nor replay state; DEFAULT remains compatible."
  artifacts:
    - backend/src/IPCManagement.Api/Features/Inventory/Persistence/InventoryEntityConfigurations.cs
    - backend/src/IPCManagement.Api/Features/Inventory/Validators/InventoryValidators.cs
    - backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs
    - backend/tests/IPCManagement.Api.Tests/Phase30EndpointAuthorityMatrixTests.cs
  key_links:
    - "request expected mode/version -> transaction-local server authority recheck -> first durable workflow/stock write"
    - "public issue command -> InventoryValidators + InventoryEntityConfigurations exact-one/header-line constraints -> atomic stock transaction"
    - "retained endpoint/command -> existing Admin Data or Warehouse permission -> both-mode permission and stock-delta oracle"
---

<objective>
Close the exact-one write boundary, transaction-local mode/version authority and retained endpoint permission/stock matrix for MRX-06L (E1-1, E2-1).

Purpose: A browser capability snapshot is advisory, source-family payloads are untrusted, and mode must never create a second master or inventory authority.
Output: Red→green public-seam tests, real validator/constraint enforcement, transaction fences and a source-enumerated Admin Data/Warehouse authority matrix while preserving DEFAULT.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-SPEC.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-CONTEXT.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-RESEARCH.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-01-SUMMARY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-02-SUMMARY.md
@backend/src/IPCManagement.Api/Features/SystemOperation/SystemOperationRequestContext.cs
@backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs
@backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs
@backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject raced and structurally invalid issue writes at the public seam</name>
  <files>backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs, backend/src/IPCManagement.Api/Features/SystemOperation/SystemOperationRequestContext.cs, backend/src/IPCManagement.Api/Features/Inventory/Persistence/InventoryEntityConfigurations.cs, backend/src/IPCManagement.Api/Features/Inventory/Validators/InventoryValidators.cs, backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs</files>
  <behavior>
    - "A valid reconciliation-origin issue under the current mode/version produces one exact source-linked issue and stock result."
    - "Both source families, neither source family, header/line family mismatch, a reconciliation line from another batch and a default line from another request are each rejected under otherwise valid mode/version/permission."
    - "Every invalid fixture leaves issue headers, issue lines, stock movements, current stock, lifecycle/audit rows and idempotency responses byte/count unchanged."
    - "A deterministic pre-write mode/version race fails with the established conflict and the same atomic zero-effect assertions."
  </behavior>
  <action>Start red at the controller/application seam. Exercise the request validator, `InventoryIssueService` invariant and EF/MySQL constraint definitions rather than test-only predicates. Make `InventoryValidators` reject invalid exact-one payload shapes, make `InventoryIssueService` load and compare exact header/line ownership and parent batch/request identity before stock mutation, and keep `InventoryEntityConfigurations` as the final database boundary for both/neither and header/line consistency that is expressible there. Re-read expected mode/version inside the same transaction immediately before the first durable write per E2-1. Never infer ownership from ingredient, code, customer, week or date and preserve the valid DEFAULT MaterialRequest path.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconciliationWarehouseIssueApplicationPathTests"</automated></verify>
  <done>All five invalid ownership/cross-parent cases and the mode race fail through actual production owners with atomic zero effects, while one valid issue in each family remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enumerate retained endpoint authority and fence every non-Warehouse command</name>
  <files>backend/tests/IPCManagement.Api.Tests/Phase30EndpointAuthorityMatrixTests.cs, backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs</files>
  <behavior>
    - "A source-aware matrix enumerates every retained Admin Data shared-master mutation and every retained Warehouse inventory mutation, naming route/command, production owner, required permission and applicability in DEFAULT and MATERIAL_RECONCILIATION."
    - "For both modes, existing Admin Data permission is required for material/unit/dish/BOM/customer mutations and canonical Warehouse permission is required for inventory mutations; changing mode grants no permission."
    - "Transfer, comparison, disposition, completion and every other retained non-Warehouse command produce zero stock movement and zero current-stock delta."
    - "Transfer, disposition and completion recheck mode/version in their own transaction and switch-away/switch-back preserves exact record IDs, versions, state and quantities."
  </behavior>
  <action>Create the executable endpoint/command authority matrix from controller route metadata and DI service owners so a retained mutation endpoint cannot be omitted silently. Give every row an Admin Data, Warehouse or explicit not-applicable disposition, then drive public-seam permission tests in both modes. Add exact pre/post stock movement and current-stock assertions for every non-Warehouse row, including transfer, comparison, disposition and completion. Apply transaction-local authority checks to `ReconciliationBatchService` and `ReconciliationCompletionService`; retain optimistic aggregate versions and existing permissions independently of mode. The matrix must prove there is no mode-local master copy, parallel stock ledger or duplicate mutation authority.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~Phase30EndpointAuthorityMatrixTests|FullyQualifiedName~ReconciliationWarehouseIssueApplicationPathTests" &amp;&amp; dotnet build backend/IPCManagement.sln --no-restore</automated></verify>
  <done>Every retained mutation endpoint has one production owner and both-mode permission oracle; all non-Warehouse commands prove zero stock effect and all Phase 30 writes use transaction-local mode/version authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser/API → command service | Expected mode/version and source IDs are untrusted, delayable inputs. |
| command service → database | Mode authority, workflow state and stock effects must commit atomically. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-03-01 | Tampering | mode-sensitive services | high | mitigate | Re-read mode/version inside the write transaction and reject before mutation. |
| T-30-03-02 | Elevation of Privilege | inventory/reconciliation commands | high | mitigate | Retain canonical permission checks independently of active mode. |
| T-30-03-03 | Repudiation | failed raced commands | medium | mitigate | Atomic rollback prevents misleading lifecycle/audit or replay records. |
</threat_model>

<verification>Run focused red→green tests first, then the bounded backend build. Verification is local only; do not connect to or mutate protected `ipc_lane7`.</verification>
<success_criteria>Matching commands commit atomically; stale, wrong-mode and unauthorized commands produce zero durable change; DEFAULT compatibility passes.</success_criteria>
<output>Create `30-03-SUMMARY.md` when done.</output>
