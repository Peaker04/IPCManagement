---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 03
type: execute
wave: 3
depends_on: [30-02]
autonomous: false
requirements: [MRX-01, MRX-02, MRX-03, MRX-04, MRX-05, MRX-06]
files_modified:
  - backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs
  - backend/tests/IPCManagement.Api.Tests/SystemOperationCapabilityProfileTests.cs
  - frontend/src/features/reconciliation/**/*.test.tsx
  - frontend/src/features/warehouse/**/*.test.tsx
  - frontend/src/features/projects/weekly-menu/**/*.test.tsx
  - .planning/phases/30-closed-loop-menu-issue-reconciliation/30-VERIFICATION.md
  - docs/EVIDENCE-INDEX.md
  - MEMORY.md
  - HISTORY.md
must_haves:
  truths:
    - "Exact, partial, excess, multiple-issue, unissued, stale, replay and unresolved-disposition paths are closed without duplicate stock or inferred lineage."
    - "A protected headed flow proves Weekly Menu import/materialization → transfer → real Warehouse issue/stock movement → required-versus-issued reconciliation → disposition/completion → reload."
    - "No Purchasing/Reports/Coordination-lifecycle request occurs in MATERIAL_RECONCILIATION, and DEFAULT still exposes its full navigation/tab/API path."
    - "All five desktop viewports render the compact retained surfaces without overflow, console/page/request errors or inaccessible controls."
    - "The final database operation mode is DEFAULT and all artifacts/documentation truthfully reconcile code, API, DB and browser state."
  artifacts:
    - .planning/phases/30-closed-loop-menu-issue-reconciliation/30-VERIFICATION.md
    - docs/EVIDENCE-INDEX.md
  key_links:
    - "visible control -> authenticated API response -> exact DB source-line/stock movement -> reload render"
    - "mode capability -> zero excluded navigation/tab/preload/request"
    - "required frozen line -> linked issue lines -> difference/disposition/completion"
---

<objective>
Expand the tracer, execute protected full-stack evidence and close Phase 30.

Purpose: Prove the simplified workflow is safe, exact and understandable under real MySQL/API/browser conditions rather than only component tests.
Output: Edge-case regression, protected migration/runtime evidence, five-viewport browser manifest, documentation and final GSD verification.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@LESSONS.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-CONTEXT.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-RESEARCH.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-01-SUMMARY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-02-SUMMARY.md
@docs/UI-UX-EXECUTION-HARNESS.md
@docs/DATA-GRAIN-MATRIX.md
@docs/EVIDENCE-INDEX.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close domain, authorization, compatibility and UI edge cases</name>
  <files>backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs, backend/tests/IPCManagement.Api.Tests/SystemOperationCapabilityProfileTests.cs, frontend/src/features/reconciliation/**/*.test.tsx, frontend/src/features/warehouse/**/*.test.tsx, frontend/src/features/projects/weekly-menu/**/*.test.tsx</files>
  <behavior>
    - "One batch supports multiple partial issue documents and sums only exact linked lines."
    - "Exact match, short, excess, no issue and within-tolerance cases produce stable signed differences and user labels."
    - "Completion rejects unissued lines, stale versions and unresolved differences, then makes the completed authority immutable."
    - "Issue correction/return behavior has an explicit disposition: included in or excluded from issued projection according to the canonical stock authority, with tests preventing silent double count."
    - "Unauthorized actors and wrong mode produce zero stock/reconciliation mutation."
    - "DEFAULT creation from MaterialRequest, Warehouse default tabs/content and full 10-navigation/32-tab capability remain green."
  </behavior>
  <action>Expand around the tracer without adding speculative features. Reconcile returns/corrections against existing inventory source-line semantics and document the chosen projection; do not simply sum every historical issue line if returns/allocations alter authoritative net issue. Add source-aware guards against duplicate/cross-batch linkage and source changes after transfer. Add semantic/accessibility tests for loading, empty, error, permission, stale and all-tabs-hidden recovery states.</action>
  <verify>
    <automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore &amp;&amp; npm exec --workspace frontend vitest run -- --maxWorkers=1 &amp;&amp; npm run lint -w frontend &amp;&amp; npm run build -w frontend &amp;&amp; npm run check:api-contract &amp;&amp; dotnet ef migrations has-pending-model-changes --project backend/src/IPCManagement.Api/IPCManagement.Api.csproj --startup-project backend/src/IPCManagement.Api/IPCManagement.Api.csproj</automated>
  </verify>
  <done>All focused and aggregate gates pass with explicit dispositions for every stock/reconciliation edge and no DEFAULT regression.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Authorize protected MySQL migration and headed closed-loop evidence</name>
  <action>Before mutation, inspect current `MEMORY.md`, git HEAD/status, database lane lineage, current migration count, operation mode, active listeners and credential source. Create a named rollback checkpoint/dump on the approved disposable/protected evidence lane. Review generated SQL for `USE`, create/drop database, drop table, business update/delete and guessed backfill. Present the exact target lane, rollback artifact, source customer/week and expected stock/procurement delta to the operator. Continue only after explicit authorization.</action>
  <verify>Operator approval is recorded without credential or connection-string disclosure.</verify>
  <done>Protected execution has an approved lane, clean source scope, rollback checkpoint and owned runtime ports.</done>
</task>

<task type="auto">
  <name>Task 3: Execute five-viewport browser/API/DB evidence and close documentation</name>
  <files>.planning/phases/30-closed-loop-menu-issue-reconciliation/30-VERIFICATION.md, docs/EVIDENCE-INDEX.md, MEMORY.md, HISTORY.md</files>
  <behavior>
    - "Chrome headed enters the real application at all five required viewports and shows exactly the five mode navigation items and explicit retained tabs."
    - "The controlled flow records source import/materialization, transfer response, real inventory issue response, linked DB issue/stock movement rows, reconciliation projection and reload."
    - "At least one exact match and one safely controlled discrepancy prove verdict/disposition/completion behavior without repairing the run in place."
    - "Network evidence records zero Purchasing, Reports and prohibited Coordination lifecycle requests."
    - "Screenshots, request records, console/page errors, overflow, focus, CLS and long tasks are stored; DOM/request/DB records decide PASS/FAIL."
    - "Final operation mode is DEFAULT; owned processes are torn down or intentionally handed off; unrelated user runtimes are untouched."
  </behavior>
  <action>Apply the official migration only to the approved lane, boot source-exact backend/frontend runtimes and run the project headed-browser harness. Use raw .NET GUID storage evidence rather than MySQL UUID conversion helpers. Preserve every failed attempt; do not edit data or source mid-run to rescue it. Register authoritative artifact hashes only in `docs/EVIDENCE-INDEX.md`. Write `30-VERIFICATION.md`, update `MEMORY.md`, move closed prior status to `HISTORY.md`, then run hygiene checks.</action>
  <verify>
    <automated>git diff --check &amp;&amp; npm run check:frontend-checklist &amp;&amp; git status --short --branch</automated>
  </verify>
  <done>Phase 30 has reproducible full-stack evidence, reconciled documentation, final DEFAULT mode and no unresolved scope item.</done>
</task>

</tasks>

<verification>Final gate requires full backend/frontend regression, Release builds, API parity, EF model parity, migration SQL inspection, exact database lineage, five headed viewports, zero excluded requests, zero undispositioned discrepancy and hygiene/secret checks.</verification>
<success_criteria>The application demonstrates one safe closed loop from menu-derived required material to real Warehouse issue and exact reconciliation, with a minimal mode-specific UI, no Purchasing behavior and complete DEFAULT compatibility.</success_criteria>
<output>Create `30-03-SUMMARY.md` and `30-VERIFICATION.md` when done.</output>
