---
phase: 30-closed-loop-menu-issue-reconciliation
plan: 03
type: execute
wave: 3
depends_on: [30-02]
autonomous: true
requirements: [MRX-02, MRX-03, MRX-05, MRX-06]
files_modified:
  - backend/src/IPCManagement.Api/Features/SystemOperation/SystemOperationRequestContext.cs
  - backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs
  - backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs
  - backend/tests/IPCManagement.Api.Tests/SystemOperationCapabilityProfileTests.cs
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A mode-sensitive command commits only when its expected mode/version still matches server authority inside the write transaction (E2-1)."
    - "Wrong-mode, stale-version and permission failures mutate neither active nor inactive workflow data, issue data, stock, lifecycle/audit nor replay state (E1-1, E2-1)."
    - "DEFAULT MaterialRequest issue behavior remains available and unchanged when DEFAULT is active."
  artifacts:
    - backend/src/IPCManagement.Api/Features/SystemOperation/SystemOperationRequestContext.cs
    - backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs
    - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs
    - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs
    - backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs
  key_links:
    - "request expected mode/version -> transaction-local server authority recheck -> first durable workflow/stock write"
    - "exact source family -> command owner -> issue/stock/lifecycle transaction"
---

<objective>
Make server mode/version the transactional authority for every remaining mode-sensitive write (E1-1, E2-1).

Purpose: A browser capability snapshot is advisory; stale or wrong-mode clients must lose before any workflow or stock mutation.
Output: Red→green backend service tests and transaction fences for transfer, inventory issue and completion/disposition commands while preserving DEFAULT.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-SPEC.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-CONTEXT.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-RESEARCH.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-01-SUMMARY.md
@.planning/phases/30-closed-loop-menu-issue-reconciliation/30-02-SUMMARY.md
@backend/src/IPCManagement.Api/Features/SystemOperation/SystemOperationRequestContext.cs
@backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs
@backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs
@backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject one raced reconciliation issue through the public application path</name>
  <files>backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs, backend/src/IPCManagement.Api/Features/SystemOperation/SystemOperationRequestContext.cs, backend/src/IPCManagement.Api/Features/Inventory/Services/InventoryIssueService.cs</files>
  <behavior>
    - "A reconciliation-origin issue carrying the current version succeeds and produces one exact source-linked issue/stock result."
    - "If server mode/version changes after request admission but before the first durable issue write, the command fails with the established stale/conflict response and rolls back issue header, lines, stock movement, current stock and replay state."
    - "A reconciliation source submitted under DEFAULT and a MaterialRequest source submitted under MATERIAL_RECONCILIATION both fail without cross-family mutation (E1-1)."
  </behavior>
  <action>Start with failing public application-path tests using the existing controller/service harness and a deterministic pre-write race hook or transaction-scoped setup. Extend the request context/service boundary so `InventoryIssueService` compares the caller's expected mode version and required family mode with freshly read server authority inside the same database transaction immediately before its first durable mutation, per E2-1. Keep Warehouse permission checks mandatory and keep the existing DEFAULT MaterialRequest path; do not convert, delete, infer or re-parent source records. Ensure failure covers all enlisted issue, stock and idempotency effects.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconciliationWarehouseIssueApplicationPathTests"</automated></verify>
  <done>The exact reconciliation issue path proves success under matching authority and all-or-nothing rejection under a deterministic mode/version race, while DEFAULT remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the same transactional authority fence to transfer, disposition and completion</name>
  <files>backend/tests/IPCManagement.Api.Tests/ReconciliationWarehouseIssueApplicationPathTests.cs, backend/tests/IPCManagement.Api.Tests/SystemOperationCapabilityProfileTests.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationCompletionService.cs</files>
  <behavior>
    - "Transfer, disposition and completion reject stale expected versions and wrong active modes before their first durable write."
    - "A race cannot partially change batch state, line disposition, completion metadata, audit/lifecycle state or issue/stock data."
    - "Permission failure remains independent of mode and creates no mutation."
    - "Switching away and back exposes the original unchanged records and versions."
  </behavior>
  <action>Add red tests at the existing public service/application seams, then centralize or consistently apply the transaction-local authority assertion to `ReconciliationBatchService` and `ReconciliationCompletionService`. Preserve optimistic aggregate versions in addition to operation-mode version. The check must execute in the same transaction as the command and after any potentially stale admission check. Keep inactive-family records frozen and do not broaden mode into authorization.</action>
  <verify><automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconciliationWarehouseIssueApplicationPathTests|FullyQualifiedName~SystemOperationCapabilityProfileTests" &amp;&amp; dotnet build backend/IPCManagement.sln --no-restore</automated></verify>
  <done>All mode-sensitive Phase 30 commands share server-authoritative transaction semantics and deterministic tests prove zero partial or inactive-family mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser/API → command service | Expected mode/version and source IDs are untrusted, delayable inputs. |
| command service → database | Mode authority, workflow state and stock effects must commit atomically. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-30-03-01 | Tampering | mode-sensitive services | high | mitigate | Re-read mode/version inside the write transaction and reject before mutation. |
| T-30-03-02 | Elevation of Privilege | inventory/reconciliation commands | high | mitigate | Retain canonical permission checks independently of active mode. |
| T-30-03-03 | Repudiation | failed raced commands | medium | mitigate | Atomic rollback prevents misleading lifecycle/audit or replay records. |
</threat_model>

<verification>Run focused red→green tests first, then the bounded backend build. Verification is local only; do not connect to or mutate protected `ipc_lane7`.</verification>
<success_criteria>Matching commands commit atomically; stale, wrong-mode and unauthorized commands produce zero durable change; DEFAULT compatibility passes.</success_criteria>
<output>Create `30-03-SUMMARY.md` when done.</output>
