---
phase: 27-warehouse-data-workspace-contract-pilot
plan: 03
type: execute
wave: 3
depends_on: [27-02]
files_modified:
  - frontend/src/components/common/SplitWorkbench.tsx
  - frontend/src/styles/components/operations.css
  - frontend/src/components/common/SplitWorkbench.test.tsx
  - frontend/src/features/warehouse/pages/WarehouseMovementPanel.tsx
  - frontend/src/features/warehouse/pages/WarehouseMovementPanel.test.tsx
  - frontend/src/features/warehouse/pages/WarehousePage.presentation.test.ts
autonomous: true
requirements: [UIC-01, UIC-03, UIC-05, WHP-02, WHP-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Only baseline deterministic FAILs or schema-valid AI FAILs authorize production edits, and each edit cites its finding ID per WHP-02."
    - "Warehouse wide desktop shows dataset workspace beside one document rail; narrow desktop stacks the same rail after the datasets without DOM/focus reorder per D-05..D-07."
    - "Current stock, movement history and document rail retain independent owners, search/query/pagination behavior and hierarchy per D-01, D-03 and D-04."
    - "Route/API/cache/permission/database behavior and every non-Warehouse SplitWorkbench consumer remain unchanged."
  artifacts:
    - path: frontend/src/components/common/SplitWorkbench.tsx
      provides: bounded opt-in layout seam only if authorized by the baseline owner finding
    - path: frontend/src/features/warehouse/pages/WarehouseMovementPanel.tsx
      provides: Warehouse-only consumption and semantic region corrections authorized by findings
    - path: frontend/src/components/common/SplitWorkbench.test.tsx
      provides: default-consumer and opt-in DOM/semantics regressions
    - path: frontend/src/features/warehouse/pages/WarehouseMovementPanel.test.tsx
      provides: detailed state/region/ownership/focus contract
  key_links:
    - from: frontend/src/features/warehouse/pages/WarehouseMovementPanel.tsx
      to: frontend/src/components/common/SplitWorkbench.tsx
      via: explicit bounded Warehouse mode; shared default remains unchanged
    - from: frontend/src/components/common/SplitWorkbench.tsx
      to: frontend/src/styles/components/operations.css
      via: one DOM order with measured side/stack styles
---

<objective>
Refactor only the lowest demonstrated Warehouse owner seams authorized by the baseline findings.

Purpose: Satisfy the proven contract failures while preserving behavior and avoiding a generic Data Workspace framework.
Output: Bounded Warehouse layout/semantic corrections and focused regressions for all shared consumers.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-warehouse-data-workspace-contract-pilot/27-02-SUMMARY.md
@.planning/phases/27-warehouse-data-workspace-contract-pilot/27-CONTEXT.md
@.planning/phases/27-warehouse-data-workspace-contract-pilot/27-UI-SPEC.md
@frontend/src/components/common/SplitWorkbench.tsx
@frontend/src/styles/components/operations.css
@frontend/src/features/warehouse/pages/WarehouseMovementPanel.tsx
@frontend/src/features/warehouse/pages/WarehousePage.tsx
@frontend/src/features/approvals/pages/ApprovalPage.tsx
@frontend/src/features/chef/receipts/KitchenReceiptSection.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Remediate the proven responsive owner without changing shared defaults</name>
  <files>frontend/src/components/common/SplitWorkbench.tsx, frontend/src/styles/components/operations.css, frontend/src/components/common/SplitWorkbench.test.tsx, frontend/src/features/warehouse/pages/WarehouseMovementPanel.tsx</files>
  <behavior>
    - The exact baseline responsive finding ID changes from failing evidence to a layout that is side-by-side at the measured wide canonical widths and stacked at the measured narrow widths.
    - Primary then detail remains the sole DOM order; no duplicate rail, CSS order, positive tabindex or hidden alternate copy exists per D-06.
    - Existing Approval and Chef consumers retain current default geometry and semantics.
    - Warehouse has one visible named `Phiếu kho` region independent of row selection per D-04.
  </behavior>
  <action>Read the baseline finding queue first and halt if no selector/metric/owner-backed responsive FAIL authorizes this change. Add the narrowest reversible opt-in prop/class at `SplitWorkbench` only if that shared component is the proven lowest owner; consume it only from `WarehouseMovementPanel`. Use the baseline-measured adjacent canonical transition and existing IPC spacing tokens, preserving the default column behavior for Approval/Chef. Do not add an archetype prop, generic renderer, registry, new component, package, route, API, cache or permission change. Cite the finding ID in the test name/commit and preserve one primary→aside DOM sequence per D-05..D-07 and UIC-05.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run src/components/common/SplitWorkbench.test.tsx src/features/warehouse/pages/WarehouseMovementPanel.test.tsx --maxWorkers=1 &amp;&amp; npm exec -w frontend playwright test tests/ui-audit.spec.ts --grep "Warehouse Data Workspace responsive contract" --workers=1</automated>
  </verify>
  <done>The authorized responsive finding is fixed at its lowest owner, default shared consumers pass focused regressions, and the same single rail transforms without semantic/focus reorder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply only authorized semantic and region corrections</name>
  <files>frontend/src/features/warehouse/pages/WarehouseMovementPanel.tsx, frontend/src/features/warehouse/pages/WarehouseMovementPanel.test.tsx, frontend/src/features/warehouse/pages/WarehousePage.presentation.test.ts</files>
  <behavior>
    - Exactly one shell H1 and valid declared heading order exist; each detailed region exists once with its accessible name per D-12.
    - Current stock stays primary; history owns separate search/query/table/pagination; document rail never becomes selection-dependent per D-03/D-04.
    - Loading, refreshing, error, forbidden, mixed-empty and all-empty remain distinct without altering query/cache behavior.
    - The three-tab shell keeps active semantics, keyboard/focus, preserve-visited lifecycle, zero overflow and zero clipping per D-02.
  </behavior>
  <action>For each remaining baseline FAIL, map its finding ID to the lowest existing owner and make only the markup/copy/class correction needed by its expected outcome. If a finding is NEEDS_EVIDENCE or UNRESOLVED, do not edit production. Preserve `WarehousePage` query arguments, RTK Query identities, permissions, route guard, document independence, pagination and business copy except where the accepted finding explicitly proves a contract violation. Add role/name/state and lifecycle regressions. Do not touch Admin Data, Purchasing, backend, generated API contracts, global tokens, component stack, or unrelated Warehouse panels.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run src/features/warehouse/pages/WarehouseMovementPanel.test.tsx src/features/warehouse/pages/WarehousePage.presentation.test.ts --maxWorkers=1 &amp;&amp; npm run lint:fe &amp;&amp; npm run depcruise:fe &amp;&amp; npm run build:fe</automated>
  </verify>
  <done>Every production diff line traces to an accepted finding ID; focused semantics/lifecycle tests and static/build gates pass with route/API/cache/permission behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Finding queue → production edits | Only fully evidenced FAIL findings authorize source changes. |
| Warehouse opt-in → shared SplitWorkbench | A shared primitive could alter Approval/Chef consumers if its default changes. |
| Browser UI → API/cache/auth | UI refactoring must not cross into business behavior or permissions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27-09 | Tampering | baseline/finding queue | high | mitigate | Freeze IDs/expected outcomes before edits and reject unresolved findings as authorization. |
| T-27-10 | Denial of service | shared layout | medium | mitigate | Preserve default mode and add focused regressions for every direct consumer. |
| T-27-11 | Elevation of privilege | Warehouse permission boundary | critical | mitigate | No role/guard/query change; retain route-forbidden browser regression. |
| T-27-12 | Information disclosure | UI states | medium | mitigate | Keep forbidden distinct and never expose data via fixture-driven permission weakening. |
</threat_model>

<verification>
Focused validation runs after each task. Wave validation: Warehouse unit tests → focused Warehouse Playwright → `npm run lint:fe` → `npm run depcruise:fe` → `npm run build:fe` → `git diff --check`. Compare `git diff --name-only` against this plan and verify no backend/API/generated/package/Admin/Purchasing path appears.
</verification>

<success_criteria>
- WHP-02: every change traces to an accepted baseline FAIL.
- WHP-03: behavior, API/cache, permission and visual foundation remain unchanged.
- D-05..D-07 responsive behavior is met with one DOM/rail and measured breakpoint.
- No broad framework or second consumer promotion is introduced.
</success_criteria>

<output>
Create `.planning/phases/27-warehouse-data-workspace-contract-pilot/27-03-SUMMARY.md` when done.
</output>
