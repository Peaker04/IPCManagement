---
phase: 27-warehouse-data-workspace-contract-pilot
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - frontend/tests/warehouseDataWorkspaceContract.ts
  - frontend/tests/warehouseDataWorkspaceFixture.ts
  - frontend/tests/warehouseEvidenceCollector.ts
  - frontend/tests/warehouseDeterministicRules.ts
  - frontend/tests/warehouseDataWorkspaceContract.test.ts
  - frontend/tests/ui-audit.spec.ts
  - frontend/test-results/warehouse-data-workspace/baseline/ai-review-input.json
  - frontend/test-results/warehouse-data-workspace/baseline/ai-findings.json
autonomous: true
requirements: [UIC-02, UIC-03, UIC-04, WHP-02]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Baseline evidence contains exactly 15 immutable browser captures: ready, mixed-empty and route-forbidden at the five canonical desktop viewports per D-08..D-11."
    - "Machine-decidable semantic, ownership, geometry, focus, responsive, action and runtime rules execute before AI and produce selector/box/metric/owner-backed findings per D-12..D-16."
    - "The unmodified baseline preserves the expected wide-layout failure instead of laundering its breakpoint or threshold."
    - "AI receives only a reasoned selection manifest and every accepted finding is schema-valid under D-17..D-21."
  artifacts:
    - path: frontend/tests/warehouseDeterministicRules.ts
      provides: pure capture-to-findings evaluator
    - path: frontend/test-results/warehouse-data-workspace/baseline/manifest.json
      provides: 15-record baseline and deterministic verdict
    - path: frontend/test-results/warehouse-data-workspace/baseline/selection-manifest.json
      provides: bounded AI review packet index with reasons
    - path: frontend/test-results/warehouse-data-workspace/baseline/ai-review-input.json
      provides: attested reviewer packet manifest with exact allowed inputs and exclusions
    - path: frontend/test-results/warehouse-data-workspace/baseline/ai-findings.json
      provides: schema-validated read-only reviewer output used as the authorization queue
  key_links:
    - from: frontend/tests/warehouseEvidenceCollector.ts
      to: frontend/tests/warehouseDeterministicRules.ts
      via: validated facts-only capture records
    - from: frontend/tests/warehouseDeterministicRules.ts
      to: frontend/test-results/warehouse-data-workspace/baseline/selection-manifest.json
      via: deterministic stage must finish before selection generation
---

<objective>
Expand the proven tracer into the complete Warehouse baseline, deterministic gate, and bounded read-only AI review input.

Purpose: Produce the finding evidence that alone can authorize the later lowest-owner refactor, satisfying UIC-02..04 and WHP-02 without production edits.
Output: Structural state contracts, 15 captures, deterministic findings, selection manifest, and schema-validated baseline AI findings.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-warehouse-data-workspace-contract-pilot/27-01-SUMMARY.md
@.planning/phases/27-warehouse-data-workspace-contract-pilot/27-CONTEXT.md
@.planning/phases/27-warehouse-data-workspace-contract-pilot/27-UI-SPEC.md
@.planning/phases/27-warehouse-data-workspace-contract-pilot/27-RESEARCH.md
@frontend/tests/ui-audit.spec.ts
@frontend/src/features/warehouse/pages/WarehouseMovementPanel.tsx
@frontend/src/components/common/SplitWorkbench.tsx
@frontend/src/styles/components/operations.css
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Extend the tracer through the complete evidence matrix</name>
  <files>frontend/tests/warehouseDataWorkspaceFixture.ts, frontend/tests/warehouseEvidenceCollector.ts, frontend/tests/warehouseDataWorkspaceContract.test.ts, frontend/tests/ui-audit.spec.ts</files>
  <behavior>
    - The manifest has exactly 15 unique composite identities and preserves the same ready fixture IDs at every viewport.
    - Mixed-empty empties only current stock; history and rail remain populated and independently named per D-11.
    - The no-read actor reaches the existing route-level forbidden boundary at every viewport; no permission behavior is weakened per D-08.
    - Loading, refreshing, error, all-empty and dataset-forbidden semantics are structural/component contracts unless measured geometry proves viewport dependence per D-09.
  </behavior>
  <action>Expand the existing focused Warehouse describe across only the five locked viewports and three browser scenarios. Capture screenshots, AI ARIA boxes, explicit region/control boxes, whitelisted contracted styles, active/focus/DOM order, console/page errors and non-GET requests for each record. Add structural tests for query states using the production phase union plus ready/isRefreshing rather than inventing a refreshing phase. Preserve all 15 records with stable run/fixture/contract identities; do not add Admin, receiving/mutation actors, mobile/tablet, or unrelated Warehouse tabs.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/warehouseDataWorkspaceContract.test.ts src/features/warehouse/pages/WarehouseMovementPanel.test.tsx --maxWorkers=1 &amp;&amp; npm exec -w frontend playwright test tests/ui-audit.spec.ts --grep "Warehouse Data Workspace contract baseline" --workers=1</automated>
  </verify>
  <done>The baseline manifest validates at exactly 15 captures, structural states are covered at component level, every capture is read-only, and screenshots are never used as the verdict oracle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evaluate the blocking contract before AI</name>
  <files>frontend/tests/warehouseDeterministicRules.ts, frontend/tests/warehouseDataWorkspaceContract.test.ts, frontend/tests/ui-audit.spec.ts</files>
  <behavior>
    - Known expected value + machine-readable evidence + explicit owner yields PASS or FAIL; any missing member yields NEEDS_EVIDENCE or UNRESOLVED per D-13.
    - Rules cover exactly one H1/heading order, required named regions, owner mapping, overflow/overlap/clipping, DOM/focus order, one rail and responsive relation, zero competing primary actions, contracted spacing ±0.5px, and runtime errors per D-12/D-15.
    - Every deterministic violation is FAIL regardless of severity per D-16.
    - The unmodified wide capture yields a responsive FAIL against D-05; expected values and thresholds remain frozen.
  </behavior>
  <action>Implement pure deterministic evaluators over validated records, with each finding carrying rule ID, selector, relevant boxes/metric, expected, actual, severity and resolved owner. Inventory all direct SplitWorkbench consumers in source-aware test data, but do not modify them. Classify rail relation from geometry and freeze the transition only from the five measured viewports; because the baseline cannot satisfy wide mode, retain a FAIL rather than selecting stacked-everywhere or guessing a breakpoint. Run this evaluator and write findings before any selection manifest or AI stage.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/warehouseDataWorkspaceContract.test.ts --maxWorkers=1 &amp;&amp; npm exec -w frontend playwright test tests/ui-audit.spec.ts --grep "Warehouse Data Workspace deterministic baseline" --workers=1</automated>
  </verify>
  <done>The deterministic report is reproducible, records the expected responsive FAIL plus any other proven failures, and has no finding lacking selector/metric/owner evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Execute, attest and validate the read-only AI baseline review</name>
  <files>frontend/tests/warehouseDataWorkspaceContract.ts, frontend/tests/warehouseDataWorkspaceContract.test.ts, frontend/tests/ui-audit.spec.ts, frontend/test-results/warehouse-data-workspace/baseline/ai-review-input.json, frontend/test-results/warehouse-data-workspace/baseline/ai-findings.json</files>
  <behavior>
    - Selection includes ready wide/transition/narrow, mixed-empty narrow, one representative forbidden capture and only reasoned near-threshold/context additions per D-17.
    - Hypotheses requiring an unselected state/viewport become NEEDS_EVIDENCE per D-18.
    - AI may assess hierarchy, grouping, balance and IA only; solution/component/token/CSS proposals are rejected per D-19.
    - FAIL requires selected evidence, concrete expected/actual, owner level, no unreviewed dependency and confidence >=0.8 per D-20.
  </behavior>
  <action>Generate a selection manifest only after the deterministic evaluator finishes. Preserve all 15 captures while exposing the reasoned subset to a fresh read-only Pi `reviewer` subagent (`context: fresh`, no production write tools) through an exact packet containing only `27-UI-SPEC.md`, the selection manifest, selected capture/ARIA/geometry evidence and the finding schema. Before launch, write `ai-review-input.json` with the reviewer run ID, hashes/paths of every supplied item, selection reasons, allowed dimensions and an explicit denied-fields list (`diff`, implementation rationale, auto-fix, unselected captures). Require the reviewer to write JSON only to `ai-findings.json`; retain its run/session identifier for audit. Validate output against the closed schema and reject PASS findings, auto-fix instructions, scope expansion and missing fields. Persist accepted baseline finding IDs/expected outcomes as the only authorization queue for Plan 27-03; AI FAIL turns the gate red but never writes production per D-21.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/warehouseDataWorkspaceContract.test.ts --maxWorkers=1 &amp;&amp; npm exec -w frontend playwright test tests/ui-audit.spec.ts --grep "Warehouse Data Workspace AI selection contract" --workers=1 &amp;&amp; node -e "const fs=require('fs');const p='frontend/test-results/warehouse-data-workspace/baseline/';const i=JSON.parse(fs.readFileSync(p+'ai-review-input.json'));const o=JSON.parse(fs.readFileSync(p+'ai-findings.json'));if(!i.reviewerRunId||!Array.isArray(i.selectedEvidence)||i.selectedEvidence.length===0||!Array.isArray(o.findings))process.exit(1);const forbidden=['implementationRationale','sourceDiff','autoFix'].some(k=>Object.hasOwn(i,k));if(forbidden||!Array.isArray(i.deniedFields))process.exit(1)"</automated>
  </verify>
  <done>The selection manifest is reasoned and bounded, the fresh reviewer invocation and exact input packet are attestable by run ID and file manifest, malformed/out-of-scope AI output is rejected, and accepted findings are traceable to selected evidence and owner level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Capture manifest → evaluator | Records may be incomplete/stale and cannot default to PASS. |
| Selection manifest → AI | Only selected evidence crosses to an untrusted qualitative reviewer. |
| AI output → authorization queue | Schema-valid non-PASS findings may authorize planning, never direct writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27-05 | Tampering | baseline thresholds | high | mitigate | Freeze contract/fixture/expected geometry before production edits and test versions. |
| T-27-06 | Elevation of privilege | AI review | high | mitigate | Read-only boundary, closed dimensions/schema, no tools or production write path. |
| T-27-07 | Repudiation | findings | medium | mitigate | Stable finding IDs link evidence, expected, actual, severity and owner. |
| T-27-08 | Information disclosure | reviewer packet | high | mitigate | Synthetic records only; selection manifest excludes credentials/storage/network bodies. |
</threat_model>

<verification>
Focused order is mandatory: unit/structural → 15-capture baseline → deterministic evaluation → selection/AI schema. Confirm the expected baseline FAIL is preserved and no production file appears in `git diff --name-only`. Run `git diff --check` and evidence secret/stub scans.
</verification>

<success_criteria>
- UIC-02: exactly 15 complete reproducible captures.
- UIC-03: deterministic stage precedes AI and fails closed.
- UIC-04: only schema-valid bounded findings enter the queue.
- WHP-02: immutable baseline and finding IDs exist before production refactoring.
</success_criteria>

<output>
Create `.planning/phases/27-warehouse-data-workspace-contract-pilot/27-02-SUMMARY.md` when done.
</output>
