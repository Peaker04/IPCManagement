---
phase: 27-warehouse-data-workspace-contract-pilot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/warehouseDataWorkspaceContract.ts
  - frontend/tests/warehouseDataWorkspaceFixture.ts
  - frontend/tests/warehouseEvidenceCollector.ts
  - frontend/tests/warehouseDataWorkspaceContract.test.ts
  - frontend/tests/ui-audit.spec.ts
autonomous: true
requirements: [UIC-01, UIC-02, UIC-03, UIC-04, UIC-05, WHP-01]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One Warehouse-only contract describes the Data Workspace archetype, stock-snapshot work object/grain, detailed Luân chuyển regions, minimal three-tab shell, owners, and responsive invariants per D-01..D-07."
    - "One real ready/1920x1080 capture flows through the existing audit runner into a schema-validated evidence record before any production edit."
    - "The contract can represent FAIL, NEEDS_EVIDENCE, UNRESOLVED and schema-valid non-PASS AI findings without changing historical schema-v1 audit records per D-13, D-16, and D-20."
  artifacts:
    - path: frontend/tests/warehouseDataWorkspaceContract.ts
      provides: Warehouse-local contract, capture, rule, finding, selection and reconciliation types/constants
    - path: frontend/tests/warehouseDataWorkspaceFixture.ts
      provides: versioned domain-valid ready fixture and actor/scenario identities
    - path: frontend/tests/warehouseEvidenceCollector.ts
      provides: atomic single-record browser evidence collector
    - path: frontend/tests/warehouseDataWorkspaceContract.test.ts
      provides: contract/schema/fixture fail-closed tests
  key_links:
    - from: frontend/tests/ui-audit.spec.ts
      to: frontend/tests/warehouseEvidenceCollector.ts
      via: focused Warehouse Data Workspace describe using the existing login, viewport and reporting seam
    - from: frontend/tests/warehouseEvidenceCollector.ts
      to: frontend/tests/warehouseDataWorkspaceContract.ts
      via: validated capture identity and required evidence fields
---

<objective>
Prove the smallest Warehouse contract architecture through one production-quality browser path before broadening the evidence matrix or touching production UI.

Purpose: Establish a trustworthy contract→fixture→real Warehouse route→capture seam, not a generic framework, per D-01, D-02, D-12 and WHP-01.
Output: Warehouse-local contract, fixture, collector, tests, and one complete ready/1920x1080 evidence record.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-warehouse-data-workspace-contract-pilot/27-CONTEXT.md
@.planning/phases/27-warehouse-data-workspace-contract-pilot/27-UI-SPEC.md
@.planning/phases/27-warehouse-data-workspace-contract-pilot/27-RESEARCH.md
@docs/UI-UX-MEASUREMENT-PROTOCOL.md
@frontend/tests/ui-audit.spec.ts
@frontend/tests/uiSourceOwnershipManifest.ts
@frontend/src/features/warehouse/pages/WarehousePage.tsx
@frontend/src/features/warehouse/pages/WarehouseMovementPanel.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Capture one Warehouse contract path end to end</name>
  <files>frontend/tests/warehouseDataWorkspaceContract.ts, frontend/tests/warehouseDataWorkspaceFixture.ts, frontend/tests/warehouseEvidenceCollector.ts, frontend/tests/warehouseDataWorkspaceContract.test.ts, frontend/tests/ui-audit.spec.ts</files>
  <behavior>
    - A contractVersion/fixtureVersion/warehouse-keeper/ready/1920x1080 identity validates only when route, active Luân chuyển tab, screenshot path, AI-mode ARIA snapshot with boxes, region geometry, approved computed styles, viewport, focus/DOM order, console/page errors, fixture IDs and non-GET requests are present.
    - The installed Playwright 1.60 API is compile-probed for ariaSnapshot({ mode: 'ai', boxes: true }); unsupported syntax fails closed rather than introducing a custom accessibility serializer.
    - Historical schema-v1 reports remain unchanged while the Warehouse schema supports FAIL, NEEDS_EVIDENCE and UNRESOLVED.
  </behavior>
  <action>Create Warehouse-local literal TypeScript contracts and a focused describe inside the existing `ui-audit.spec.ts`. Drive the real `/warehouse` route with one synthetic warehouse keeper and the frozen ready fixture, activate `Luân chuyển`, stabilize it, capture every required signal atomically, validate, attach, and write the record under a controlled run/scenario path. Reuse the current viewport/login/runtime-signal/report seams; do not create another Playwright config or runner. Keep screenshot as evidence only. Use semantic locators before test-owned source mappings, with production metadata unavailable unless the first three ownership steps fail, per D-12..D-16. This is the architecture proof for UIC-01..04 and WHP-01.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/warehouseDataWorkspaceContract.test.ts --maxWorkers=1 &amp;&amp; npm exec -w frontend playwright test tests/ui-audit.spec.ts --grep "Warehouse Data Workspace contract tracer" --workers=1</automated>
  </verify>
  <done>The focused run creates one complete schema-valid ready/1920x1080 record through the existing audit runner, records zero non-GET requests, and no production file or historical report schema changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze the bounded Warehouse contract and fixture invariants</name>
  <files>frontend/tests/warehouseDataWorkspaceContract.ts, frontend/tests/warehouseDataWorkspaceFixture.ts, frontend/tests/warehouseDataWorkspaceContract.test.ts</files>
  <behavior>
    - Detailed regions are exactly current stock, movement history and one tab-level document rail; current stock remains primary and the other regions remain independent per D-01, D-03 and D-04.
    - The three-tab shell contract contains semantics, names, keyboard/focus, preserve-visited lifecycle, geometry, overflow and clipping only per D-02.
    - The ready fixture has stable unique IDs, eight rows per paged dataset, valid short/long Vietnamese values, varied domain states and multiple documents; mixed-empty changes only current stock per D-10 and D-11.
    - AI finding validation rejects PASS, missing evidence/expected/actual/severity/owner/confidence, and confidence below 0.8 for FAIL per D-19 and D-20.
  </behavior>
  <action>Complete the literal Warehouse contract with Data Workspace archetype, stock snapshot work object/grain, required semantic regions, existing owner chain, five immutable desktop viewports, DOM/focus order, wide/stacked relation vocabulary, exact contracted spacing and ±0.5 CSS-px tolerance. Add finding and selection schemas without a generic DSL, registry, renderer, component abstraction, package, SAP/Carbon code, Admin Data, or Purchasing scope per UIC-05 and D-01..D-22. Encode route-forbidden as the browser actor boundary and dataset-forbidden as structural coverage unless an existing partial permission seam is proven per D-08.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/warehouseDataWorkspaceContract.test.ts --maxWorkers=1</automated>
  </verify>
  <done>Tests prove the exact Warehouse boundary, fixture invariants, owner order, verdict schemas and anti-framework exclusions; all Phase 27 vocabulary is local to tests/contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser fixture → production route | Synthetic actor/API responses enter the real Warehouse UI; all requests must remain read-only. |
| Rendered UI → evidence files | DOM text and artifact identifiers are untrusted; paths are generated only from controlled literals. |
| Evidence → later AI reviewer | UI copy/ARIA content is untrusted data and cannot grant tools or authorize edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27-01 | Tampering | Playwright interception | high | mitigate | Record and fail on every non-GET API request; use fixed GET-only handlers. |
| T-27-02 | Information disclosure | capture manifest | high | mitigate | Use synthetic actors/records, exclude credentials/storage state, and scan generated evidence. |
| T-27-03 | Tampering | evidence writer | medium | mitigate | Derive paths from controlled contract/scenario/viewport IDs and atomically validate before writing. |
| T-27-04 | Spoofing | ownership mapping | medium | mitigate | Resolve semantic locator → test manifest → source-aware mapping; fail closed when owner is absent. |
</threat_model>

<verification>
Focused: run the unit contract test, then only the tracer Playwright test in that order. Inspect the attached record after the command and confirm it contains the composite identity and all required fields. Hygiene: `git diff --check` and a scoped scan confirming no package/config/production file changed.
</verification>

<success_criteria>
- UIC-01/UIC-02 architecture is executable for one real path.
- UIC-03/UIC-04 verdict and finding boundaries are test-validated before any AI or production work.
- UIC-05/WHP-01 remain true: no dependency, stack, generic framework or non-Warehouse production change exists.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Express, collect and evaluate a bounded Warehouse UI contract | 27-01 | COVERED | One complete tracer path proves architecture. |
| REQ | UIC-01..05 | Contract/evidence/rules/AI/stack foundation | 27-01 | COVERED | Rules and AI are schema boundaries here, expanded in 27-02. |
| REQ | WHP-01 | Minimal Warehouse contract | 27-01 | COVERED | No broad framework. |
| CONTEXT | D-01..D-16 | Pilot, matrix and deterministic boundary | 27-01 | COVERED | Literal contract and tracer. |
| CONTEXT | D-17..D-22 | AI selection/re-review boundary | 27-01 | COVERED | Schemas established; execution follows in later plans. |
| RESEARCH | — | Extend ui-audit; compile-probe ARIA; route-forbidden boundary | 27-01 | COVERED | Explicit in tasks. |

<output>
Create `.planning/phases/27-warehouse-data-workspace-contract-pilot/27-01-SUMMARY.md` when done.
</output>
