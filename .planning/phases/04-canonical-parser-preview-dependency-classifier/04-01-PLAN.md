---
phase: 04-canonical-parser-preview-dependency-classifier
plan: 01
type: execute
wave: 1
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - backend/src/IPCManagement.Api/Services/Bom/Workbook/BomWorkbookSecurityLimits.cs
  - backend/src/IPCManagement.Api/Services/Bom/Workbook/IBoundedXlsxWorkbookReader.cs
  - backend/src/IPCManagement.Api/Services/Bom/Workbook/BoundedXlsxWorkbookReader.cs
  - backend/src/IPCManagement.Api/Services/Bom/IPresetBomWorkbookParser.cs
  - backend/src/IPCManagement.Api/Services/Bom/PresetBomWorkbookParser.cs
  - backend/tests/IPCManagement.Api.Tests/PresetBomWorkbookFixture.cs
  - backend/tests/IPCManagement.Api.Tests/PresetBomWorkbookSecurityTests.cs
  - backend/tests/IPCManagement.Api.Tests/PresetBomWorkbookParserTests.cs
  - .artifacts/bom-v1.1/gate-b/phase4-preedit/manifest.txt
  - .artifacts/bom-v1.1/gate-b/phase4-preedit/owned-paths.txt
  - .artifacts/bom-v1.1/gate-b/phase4-preedit/impact-report.md
autonomous: true
requirements: [CAN-02, SEC-02, SEC-03]
must_haves:
  truths:
    - "Phase 3 Gate A and immutable ownership baseline pass before any Phase 4 source edit."
    - "Canonical XLSX input is bounded by one versioned immutable security-limits contract with every numeric ceiling locked."
    - "Canonical parsing is deterministic, preserves sheet/row/column/cell trace, has no EF dependency, and blocks ambiguous data."
    - "No local filesystem path, apply mutation, frontend work, or compatibility-surface removal enters Phase 4."
  artifacts:
    - path: "backend/src/IPCManagement.Api/Services/Bom/Workbook/BomWorkbookSecurityLimits.cs"
      provides: "Versioned exact file, ZIP, XML, worksheet, row, cell, relationship, timeout and cancellation limits"
    - path: "backend/src/IPCManagement.Api/Services/Bom/Workbook/BoundedXlsxWorkbookReader.cs"
      provides: "Streaming bounded XLSX package validation, SHA-256 and physical cell records"
    - path: "backend/src/IPCManagement.Api/Services/Bom/PresetBomWorkbookParser.cs"
      provides: "Pure deterministic canonical normalization and source-trace issues"
    - path: "backend/tests/IPCManagement.Api.Tests/PresetBomWorkbookSecurityTests.cs"
      provides: "Exact boundary, ZIP/XML abuse, cancellation and path-leak fixtures"
    - path: ".artifacts/bom-v1.1/gate-b/phase4-preedit/owned-paths.txt"
      provides: "Phase 4-only ownership allowlist layered on immutable Phase 3 baseline"
  key_links:
    - from: "backend/src/IPCManagement.Api/Services/Bom/Workbook/BoundedXlsxWorkbookReader.cs"
      to: "backend/src/IPCManagement.Api/Services/Bom/Workbook/BomWorkbookSecurityLimits.cs"
      via: "all package and processing guards read the versioned limits"
      pattern: "BomWorkbookSecurityLimits\.(Max|PolicyVersion)"
    - from: "backend/src/IPCManagement.Api/Services/Bom/PresetBomWorkbookParser.cs"
      to: "backend/src/IPCManagement.Api/Services/Bom/CanonicalBomContract.cs"
      via: "contract sheets, headers, tiers and normalization policy"
      pattern: "CanonicalBomContract"
    - from: "backend/src/IPCManagement.Api/Services/Bom/PresetBomWorkbookParser.cs"
      to: "backend/src/IPCManagement.Api/Services/Bom/Workbook/IBoundedXlsxWorkbookReader.cs"
      via: "parser consumes only bounded physical workbook records"
      pattern: "IBoundedXlsxWorkbookReader|BoundedXlsxWorkbook"
    - from: ".artifacts/bom-v1.1/gate-b/phase4-preedit/owned-paths.txt"
      to: "scripts/Test-BomWorktreeOwnership.ps1"
      via: "Phase 4 allowlist is compared with immutable Phase 3 baseline before commit"
      pattern: "phase4-preedit/owned-paths\.txt"
---

# Phase 4 Plan 01: Bounded workbook boundary and pure canonical parser

<objective>
After Phase 3 Gate A is proven PASS, create a bounded XLSX package boundary and a deterministic `IPresetBomWorkbookParser` that consumes the delivered Phase 3 contract/policy, emits normalized rows plus physical source trace, and never depends on EF, a local path, or a mutation-capable service.
</objective>

<execution_context>
- Phase 3 is a hard completion dependency: `.artifacts/bom-v1.1/gate-a/GATE-A.md` must be PASS and the actual delivered `CanonicalBomContract`, `CanonicalBomModels`, `BomPolicy`, `IBomInvariantValidator`, provenance migration evidence, and `03-LIFECYCLE-POLICY.md` must exist before any source edit. These are planned artifacts at planning time, not current source.
- Read `04-CONTEXT.md`, `04-RESEARCH.md`, `04-PATTERNS.md`, `04-VALIDATION.md`, all Phase 3 plans/patterns, `ROADMAP.md`, `REQUIREMENTS.md`, and `AGENTS.md` first.
- Preserve the dirty worktree. Before every symbol edit run upstream GitNexus impact; an unapproved HIGH/CRITICAL result blocks. Before every commit compare immutable Phase 3 baseline `.artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt` with Phase 4 allowlist `.artifacts/bom-v1.1/gate-b/phase4-preedit/owned-paths.txt`, then run `node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all`.
- Keep existing `XlsxWorkbookReader` and old writer/template compatibility untouched. Do not add apply/commit endpoints, EF queries, cleanup DML, frontend/RTK/UI work, or old-surface removal.
</execution_context>

<must_haves>
- D-04-01: `IPresetBomWorkbookParser` accepts only a bounded stream/object and returns canonical rows plus structured issues; it has no EF or filesystem-path dependency.
- D-04-02: normalization and source trace are deterministic and consume the Phase 3 contract/unit policy rather than duplicating them.
- D-04-03: missing sheet/header, unsupported tier, invalid quantity, identity collision, missing serving basis and unknown/ambiguous unit are blocking issues; there is no silent fallback.
- D-04-09: compressed bytes, ZIP entries/expansion, XML, sheets, rows, cells, shared strings, relationships and cancellation are bounded before or during materialization.
- D-04-10: neither production contract nor issue/log output accepts or exposes a local path such as `D:\\...`; development orchestration may later pass a stream internally only.
- D-04-11: old writer/template/UI remains compatible and there is no destructive route in this phase.
</must_haves>

<tasks>

<task id="04-01-01" requirement="CAN-02,SEC-02,SEC-03">
  <name>[BLOCKING] Prove Phase 3 completion and capture Phase 4 ownership/impact</name>
  <files>.artifacts/bom-v1.1/gate-b/phase4-preedit/manifest.txt, .artifacts/bom-v1.1/gate-b/phase4-preedit/owned-paths.txt, .artifacts/bom-v1.1/gate-b/phase4-preedit/impact-report.md</files>
  <read_first>
    <file>.artifacts/bom-v1.1/gate-a/GATE-A.md</file>
    <file>.planning/phases/03-contract-provenance-safety-baseline/03-LIFECYCLE-POLICY.md</file>
    <file>backend/src/IPCManagement.Api/Services/Bom/CanonicalBomContract.cs</file>
    <file>backend/src/IPCManagement.Api/Services/Bom/CanonicalBomModels.cs</file>
    <file>backend/src/IPCManagement.Api/Services/Bom/BomPolicy.cs</file>
    <file>backend/src/IPCManagement.Api/Services/SampleData/XlsxWorkbookReader.cs</file>
    <file>AGENTS.md</file>
  </read_first>
  <action>
    Fail before source edits unless final Phase 3 Gate A is PASS, the backup/restore proof is linked, all old migration hashes remain unchanged, and the delivered contract/policy versions agree with `03-LIFECYCLE-POLICY.md`. Without editing any Phase 3 Gate A artifact, create the Phase 4-specific immutable snapshot `manifest.txt`, exact execution allowlist `owned-paths.txt`, and `impact-report.md` under `.artifacts/bom-v1.1/gate-b/phase4-preedit/`. The manifest records HEAD, current dirty/untracked paths and Phase 3 evidence hashes; the allowlist contains only every path declared across 04-01..04-03. Run upstream impact for `XlsxWorkbookReader`, its read/relationship/shared-string methods, `CanonicalBomContract`, `CanonicalBomModels`, `BomPolicy`, `IBomInvariantValidator`, `AddBackendServices`, and the future parser/reader consumers when they become indexable. Record UNKNOWN/not-found as an epistemic gap requiring direct consumer inspection, not LOW. Explicitly record D-04-01 through D-04-03 and D-04-09 through D-04-11 as the parser/security boundary. Do not edit production source in this task.
  </action>
  <acceptance_criteria>
    <criterion>Gate A, identified restore proof, lifecycle policy and actual Phase 3 contract/provenance artifacts all exist and agree.</criterion>
    <criterion>Ownership evidence distinguishes pre-existing dirty files, Phase 3-owned outputs and the Phase 4 allowlist.</criterion>
    <criterion>Impact report includes exact existing and planned symbols; UNKNOWN or HIGH/CRITICAL cannot be treated as approval.</criterion>
    <criterion>No backend source, database, frontend or legacy surface changes occur in this task.</criterion>
  </acceptance_criteria>
  <verify>
    <command>$ErrorActionPreference='Stop'; $gate=Get-Content -Raw .artifacts/bom-v1.1/gate-a/GATE-A.md; if($gate -notmatch '(?im)status\s*[:|]\s*PASS|Gate A\s+PASS'){throw 'Phase 3 Gate A is not PASS'}; $required=@('.planning/phases/03-contract-provenance-safety-baseline/03-LIFECYCLE-POLICY.md','backend/src/IPCManagement.Api/Services/Bom/CanonicalBomContract.cs','backend/src/IPCManagement.Api/Services/Bom/CanonicalBomModels.cs','backend/src/IPCManagement.Api/Services/Bom/BomPolicy.cs','.artifacts/bom-v1.1/gate-b/phase4-preedit/manifest.txt','.artifacts/bom-v1.1/gate-b/phase4-preedit/owned-paths.txt','.artifacts/bom-v1.1/gate-b/phase4-preedit/impact-report.md'); foreach($path in $required){if(-not (Test-Path $path)){throw "missing Phase 3/4 prerequisite: $path"}}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-b/phase4-preedit/owned-paths.txt; if($LASTEXITCODE -ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE -ne 0){throw 'GitNexus detect_changes failed'}</command>
    <behavior>Gate A and ownership/impact prerequisites pass before the first Phase 4 source edit; otherwise execution stops.</behavior>
  </verify>
  <done>Phase 4 begins only from a proven Phase 3 baseline with explicit ownership and blast-radius evidence.</done>
</task>

<task id="04-01-02" requirement="SEC-02,SEC-03">
  <name>Create a fail-closed bounded XLSX package reader</name>
  <files>backend/src/IPCManagement.Api/Services/Bom/Workbook/BomWorkbookSecurityLimits.cs, backend/src/IPCManagement.Api/Services/Bom/Workbook/IBoundedXlsxWorkbookReader.cs, backend/src/IPCManagement.Api/Services/Bom/Workbook/BoundedXlsxWorkbookReader.cs, backend/tests/IPCManagement.Api.Tests/PresetBomWorkbookFixture.cs, backend/tests/IPCManagement.Api.Tests/PresetBomWorkbookSecurityTests.cs</files>
  <read_first>
    <file>backend/src/IPCManagement.Api/Services/SampleData/XlsxWorkbookReader.cs</file>
    <file>backend/tests/IPCManagement.Api.Tests/XlsxWorkbookReaderTests.cs</file>
    <file>backend/src/IPCManagement.Api/Services/Bom/CanonicalBomContract.cs</file>
    <file>.planning/phases/04-canonical-parser-preview-dependency-classifier/04-PATTERNS.md</file>
  </read_first>
  <action>
    Run upstream impact for the existing reader and all new reader interfaces before edits. Implement D-04-09 through one immutable versioned `BomWorkbookSecurityLimits` artifact and `IBoundedXlsxWorkbookReader`; keep the compatibility reader unchanged. Lock these exact defaults: `MaxFileBytes=26214400`, `MaxZipEntries=256`, `MaxZipCompressionRatio=100`, `MaxExpandedBytes=134217728`, `MaxWorksheets=16`, `MaxRowsPerSheet=20000`, `MaxCellsPerSheet=250000`, `MaxSharedStrings=250000`, `MaxXmlCharactersPerEntry=67108864`, `MaxXmlDepth=64`, `MaxRelationships=512`, and `MaxProcessingSeconds=30` linked to cancellation. Read ZIP entries and XML incrementally with DTD prohibited and no resolver; reject encrypted entries, duplicate required package parts, path traversal, absolute targets, external relationships and unexpected workbook/worksheet targets before extraction. Enforce entry count, compression ratio, total expanded bytes, XML character/depth, relationship, worksheet/row/cell/shared-string and elapsed-time bounds before or during materialization, each with a stable 4xx-ready issue code. Hash exactly the accepted bounded bytes with SHA-256 while copying. Return immutable physical sheet/row/column/cell records; never write a temp file, accept a server path, include `file.FileName` as authority, or leak package/temp/local paths. Add exact limit and max+1 fixtures for every numeric ceiling plus empty, renamed non-XLSX, corrupt ZIP, traversal/absolute/external relationship, DTD/entity, compressed expansion and cancellation. Enforce D-04-10 by testing `D:\\` and temp-directory fragments are absent from returned errors. Keep D-04-11: no existing reader/writer/template route is removed.
  </action>
  <acceptance_criteria>
    <criterion>`BomWorkbookSecurityLimits` exposes the exact twelve locked numeric defaults and a version; every max+1 fixture fails before exceeding its bound with a stable typed 4xx issue code.</criterion>
    <criterion>External/traversal/absolute relationship targets, DTD/entity input, corrupt/renamed packages and cancellation fail safely without orphan temp files.</criterion>
    <criterion>Accepted content has one deterministic SHA-256 and physical source addresses; no local path enters the public/internal parser contract.</criterion>
    <criterion>Tests assert bounded 4xx-ready failures without exception stack or server/package path leakage.</criterion>
  </acceptance_criteria>
  <verify>
    <command>$ErrorActionPreference='Stop'; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter 'FullyQualifiedName~PresetBomWorkbookSecurityTests'; if($LASTEXITCODE -ne 0){throw 'bounded workbook security tests failed'}; dotnet build backend/src/IPCManagement.Api/IPCManagement.Api.csproj --no-restore; if($LASTEXITCODE -ne 0){throw 'backend build failed'}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-b/phase4-preedit/owned-paths.txt; if($LASTEXITCODE -ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE -ne 0){throw 'unexpected GitNexus flow'}</command>
    <behavior>Malicious and oversized fixtures terminate within configured limits, valid fixtures remain readable, build passes, and only reader/test flows are affected.</behavior>
  </verify>
  <done>A bounded stream/object is the only canonical workbook input boundary and is safe for later multipart use.</done>
</task>

<task id="04-01-03" requirement="CAN-02,SEC-03">
  <name>Implement deterministic pure parser with row/cell source trace</name>
  <files>backend/src/IPCManagement.Api/Services/Bom/IPresetBomWorkbookParser.cs, backend/src/IPCManagement.Api/Services/Bom/PresetBomWorkbookParser.cs, backend/tests/IPCManagement.Api.Tests/PresetBomWorkbookParserTests.cs</files>
  <read_first>
    <file>backend/src/IPCManagement.Api/Services/Bom/CanonicalBomContract.cs</file>
    <file>backend/src/IPCManagement.Api/Services/Bom/CanonicalBomModels.cs</file>
    <file>backend/src/IPCManagement.Api/Services/Bom/BomPolicy.cs</file>
    <file>backend/src/IPCManagement.Api/Services/Bom/IBomInvariantValidator.cs</file>
    <file>backend/src/IPCManagement.Api/Services/SampleData/SampleDataImportService.cs</file>
    <file>backend/tests/IPCManagement.Api.Tests/BomContractCharacterizationTests.cs</file>
  </read_first>
  <action>
    Run impact for the delivered Phase 3 contract/policy/validator and any existing dedupe/unit helpers that are intentionally reused. Implement D-04-01 and D-04-02 with `IPresetBomWorkbookParser` accepting only the bounded reader result and returning immutable normalized rows plus structured issues. Use canonical sheet order, then physical row, then stable reconciliation key; normalize with invariant culture and the Phase 3 `DecimalPolicy`, contract version, unit policy and shared invariant validator. Preserve source sheet, physical row, logical field, physical column/cell and deterministic source-trace ID on every row/issue. Implement D-04-03: required-sheet/header/tier/quantity/identity/unit failures and duplicate groups without positive serving basis are blockers; only Phase 3-authorized weighted dedupe may merge, and unknown/ambiguous unit never becomes KG. Assert parser assemblies/types have no `DbContext`, EF entity, `IFormFile`, local path, `SaveChanges`, existing import service or cleanup service dependency. Do not edit `SampleDataImportService` or old preview/template surfaces in this plan.
  </action>
  <acceptance_criteria>
    <criterion>Same accepted workbook and policy version yield byte-stable ordered normalized values, issue codes and source-trace IDs.</criterion>
    <criterion>All three tiers, Unicode/header normalization and physical sheet/row/column/cell trace are proven by deterministic fixtures.</criterion>
    <criterion>Every D-04-03 failure is represented as a blocker; unknown unit and missing weighted basis never silently pass.</criterion>
    <criterion>Parser and result models contain no EF entity/context, mutation service, `IFormFile` or filesystem path.</criterion>
  </acceptance_criteria>
  <verify>
    <command>$ErrorActionPreference='Stop'; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter 'FullyQualifiedName~PresetBomWorkbookParserTests|FullyQualifiedName~PresetBomWorkbookSecurityTests'; if($LASTEXITCODE -ne 0){throw 'canonical parser tests failed'}; $text=Get-Content -Raw backend/src/IPCManagement.Api/Services/Bom/PresetBomWorkbookParser.cs; if($text -match 'DbContext|IpcManagementContext|SaveChanges|IFormFile|ReplaceBomCatalog|CleanupDataQuality'){throw 'parser boundary violation'}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-b/phase4-preedit/owned-paths.txt; if($LASTEXITCODE -ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE -ne 0){throw 'unexpected GitNexus flow'}; git diff --check; if($LASTEXITCODE -ne 0){throw 'diff check failed'}</command>
    <behavior>Parser/security suites and boundary scan pass; GitNexus/ownership show only expected canonical parsing changes.</behavior>
  </verify>
  <done>CAN-02 is delivered as a deterministic, source-traceable, EF-free parser over a bounded non-path input.</done>
</task>

</tasks>

## Artifacts this phase produces

- Versioned `BomWorkbookSecurityLimits` with all twelve exact numeric defaults and linked cancellation.
- `IBoundedXlsxWorkbookReader`, bounded package records and exact max/max+1 malicious workbook fixtures.
- `IPresetBomWorkbookParser` / `PresetBomWorkbookParser` with deterministic canonical rows and structured physical source trace.
- Parser and security tests proving D-04-01..03 and D-04-09..11 without changing compatibility surfaces.
- Phase 4-specific immutable `manifest.txt`, `owned-paths.txt` and `impact-report.md` layered on Phase 3 evidence without modifying Gate A.

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-04-01">ZIP/XML amplification, external relationships or oversized workbooks exhaust resources; incremental hard limits, prohibited DTD/resolvers and cancellation fail closed.</threat>
  <threat id="T-04-02">Filesystem path or package target injection leaks infrastructure; stream-only contracts and relationship/path validation reject it.</threat>
  <threat id="T-04-03">Parser policy drift silently changes quantities/units; the parser consumes the Phase 3 versioned contract and deterministic blocker vocabulary.</threat>
  <verification>ASVS L1 parser/upload review, abuse fixtures, focused tests, per-symbol impact, ownership comparison and detect_changes; any HIGH/CRITICAL or unexpected flow blocks.</verification>
</threat_model>
