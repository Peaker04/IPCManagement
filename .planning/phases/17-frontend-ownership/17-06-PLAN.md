---
phase: 17-frontend-ownership
plan: "06"
type: execute
wave: 5
depends_on: ["17-05"]
files_modified:
  - frontend/src/app/pages/admin-data/useAdminDataPageModel.ts
  - frontend/src/app/pages/admin-data/**Model.ts
  - frontend/src/app/pages/admin-data/**/*.test.ts
  - frontend/src/app/pages/AdminDataPage.tsx
autonomous: true
requirements: [ARCH-17]
user_setup: []
must_haves:
  truths:
    - "D-08: Admin model splits by existing panel after endpoint ownership is stable"
    - "D-09: skip, args, mutation/toast timing, QueryView and UI behavior remain stable"
  artifacts:
    - frontend/src/app/pages/admin-data/useAdminDataPageModel.ts
  key_links:
    - AdminDataPage -> compatibility composition model -> seven panel models
---

# Phase 17 Plan 06: Split the Admin page model by panel use case

<objective>
Decompose `useAdminDataPageModel` into panel-owned hooks/models while keeping the composed return shape, queries, mutations, toasts, permissions, QueryView states, and UI identical.
</objective>

<threat_model>- **T-17-03 MEDIUM:** hook extraction can change unconditional hook order or `skip`/args and expose cached forbidden data. Preserve call order and state adapters with per-panel tests.</threat_model>

<tasks>
<task type="execute">
  <name>Task 1: Characterize and extract Admin read/query panel models</name>
  <read_first>frontend/src/app/pages/admin-data/useAdminDataPageModel.ts; adminDataPageTypes.ts; AdminQueryBoundary.tsx; seven Admin panel components; existing Admin state tests; Phase 17 patterns</read_first>
  <files>useAdminDataPageModel.ts; new BOM/contracts/cleanup/inventory/statistics/audit/employees model files; per-panel tests</files>
  <action>Run upstream impact for `useAdminDataPageModel`, `toAdminView`, and each helper moved; report risk. Add per-panel contract tests, then extract query state, pagination, derived data and handlers along existing seven panel seams. Keep hooks unconditional, preserve every `skip`, exact args/page sizes, permission gate, QueryView instruction/error/forbidden/retry text, stale-data behavior, mutation call/toast timing, and returned property names. The top-level model remains the compatibility composition API used by AdminDataPage.</action>
  <verify><automated>npm run test:unit -w frontend -- src/app/pages/admin-data && npm run lint:fe && npm run build:fe</automated></verify>
  <acceptance_criteria>Seven panel responsibilities have focused model modules/tests; `useAdminDataPageModel` composes the unchanged public model; Admin state/boundary/component tests, lint and build pass; no query/mutation/UI behavior changes.</acceptance_criteria>
  <done>Admin page model is panel-composed with unchanged public return and all Admin behavior/state tests green.</done>
</task>
</tasks>

<verification>Admin focused suite, full frontend suite at wave end, dependency gate and staged GitNexus audit.</verification>
<must_haves><truths>- D-08/D-09 split after ownership stability and keep query/mutation/UI behavior.
- No generic dumping-ground abstraction or speculative redesign.</truths></must_haves>

## Artifacts this phase produces
- Panel-focused Admin model hooks under `frontend/src/app/pages/admin-data/` for BOM, contracts/schedules, cleanup/data-quality, inventory, statistics, audit, and employees.
