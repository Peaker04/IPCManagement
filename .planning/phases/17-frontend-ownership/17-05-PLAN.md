---
phase: 17-frontend-ownership
plan: "05"
type: execute
wave: 4
depends_on: ["17-04"]
files_modified:
  - frontend/src/**
  - frontend/.dependency-cruiser-known-violations.json
  - frontend/.dependency-cruiser.cjs
  - frontend/eslint.config.js
autonomous: true
requirements: [ARCH-17]
user_setup: []
must_haves:
  truths:
    - "D-06: all 54 violations retire to an empty/absent baseline"
    - "D-07: R1/R2/R3 are resolved without weakening R1-R6 rules"
    - "D-10: each dependency category is independently tested and audited"
  artifacts:
    - frontend/.dependency-cruiser-known-violations.json
  key_links:
    - feature modules -> lower api/types/contracts, never app/routes or sibling feature internals
---

# Phase 17 Plan 05: Retire all remaining dependency violations

<objective>
Resolve the remaining R1/R2/R3/R6 edges to a zero known-violation baseline without weakening architecture rules or changing runtime behavior.
</objective>

<threat_model>- **T-17-02 HIGH:** careless import cleanup can move permission/session logic to an unowned shared module or bypass guards. Preserve behavior and keep presentation/state in feature owners.</threat_model>

<tasks>
<task type="execute">
  <name>Task 1: Resolve feature-to-app/router reversals</name>
  <read_first>frontend/.dependency-cruiser-known-violations.json; frontend/.dependency-cruiser.cjs; frontend/src/app/hooks.ts; frontend/src/routes/ActionGuard.tsx; every current R3 source file</read_first>
  <files>Current R3 source files and dependency-safe hooks/permission contracts</files>
  <action>Run impact for each edited hook/component. Replace 15 feature-to-app/router imports with dependency-safe typed Redux primitives and feature-owned selectors/permission presentation. Keep `ActionGuard` app/router composition or expose a lower domain-neutral permission primitive without importing routes/app. Preserve dispatch types, selector results, permission reasons, buttons, mutation timing and DOM behavior.</action>
  <verify><automated>npm run test:unit -w frontend -- src/features && npm run depcruise:fe && npm run build:fe</automated></verify>
  <acceptance_criteria>R3 count is zero, feature tests/build pass, and no replacement R1/R2/R6 edge appears.</acceptance_criteria>
  <done>All feature-to-app/router reverse imports are gone with unchanged permission/session behavior.</done>
</task>

<task type="execute">
  <name>Task 2: Clear remaining R1/R2/R6 and remove the baseline</name>
  <read_first>frontend/.dependency-cruiser-known-violations.json; frontend/.dependency-cruiser.cjs; output of dependency-cruiser without known violations; Phase 17 context/research</read_first>
  <files>Remaining violating source/test imports; frontend/.dependency-cruiser-known-violations.json</files>
  <action>Run impact for each exported symbol whose import path changes. Resolve every remaining reverse/cross-feature/deep-feature import through an existing owner, stable `api`/`types` contract, or app composition. Delete the 54-entry known-violations baseline (or replace with an empty array only if the npm script requires the file). Do not edit rule severity/path patterns or add a blanket exception. If a truly unavoidable exception remains, stop and request owner/reason/expiry approval rather than inventing a whitelist.</action>
  <verify><automated>npm run depcruise -w frontend && npm run test:fe:unit && npm run lint:fe && npm run build:fe</automated></verify>
  <acceptance_criteria>Dependency-cruiser passes without `--known-violations`; R1/R2/R3/R4/R6 unapproved violations are zero; baseline contains zero entries or is absent; rules retain severity and meaning; full frontend tests/lint/build pass.</acceptance_criteria>
  <done>The frontend graph passes strict dependency-cruiser with zero known violations and unchanged rule enforcement.</done>
</task>
</tasks>

<verification>Capture before/after counts; staged GitNexus detect_changes and whitespace check before each atomic dependency commit.</verification>
<must_haves><truths>- D-06/D-07 achieve a real zero baseline.
- No architecture rule is weakened and no unowned permanent whitelist is created.
- Public hooks/cache/UI remain stable under D-02/D-09.</truths></must_haves>

## Artifacts this phase produces
- Zero-entry or removed `frontend/.dependency-cruiser-known-violations.json`.
- Dependency-safe typed Redux and permission primitives documented by exact export in SUMMARY.md.
