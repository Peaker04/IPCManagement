---
phase: 27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c
plan: 01V
type: execute
wave: 5
depends_on: [27.1-01F]
files_modified:
  - frontend/tests/validatePhase271Reseal.ts
  - frontend/tests/validatePhase271Reseal.test.ts
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/attestations/27.1-01V-topology-validator-manifest.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/plan-results/27.1-01V-topology-validator.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/27.1-01V-SUMMARY.md
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/terminal-markers/27.1-01V-topology-validator.json
autonomous: true
requirements: [VREC-01, VREC-02, VREC-03]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Downstream validation receives explicit commits for original 01, invalid 01C, authority 01R, and focused 01F and validates each historical marker at that commit; HEAD is never substituted for a historical root."
    - "01V freezes immutable frozenOperationalBase `06f920e0` and dynamically captures planningHead from clean current HEAD at execution entry; 01F must be an ancestor and the exact `01F..planningHead` delta may contain only declared canonical planning documents."
    - "The 01V payload commit directly parents frozen planningHead, its marker directly parents the payload, and both seal planningHead plus the canonical planning-delta manifest/hash; downstream historical attestation validates these pins while each plan retains its own live predecessor HEAD."
    - "Every downstream topology command requires exact payload membership and 01V is authority only for topology-validator bytes; prior roots retain their provenance, source, reseal, and focused-adapter authority."
  artifacts:
    - path: frontend/tests/validatePhase271Reseal.ts
      provides: explicit-root, frozen operational base, dynamic planning-head delta, and stage-local live-predecessor validation
    - path: frontend/tests/validatePhase271Reseal.test.ts
      provides: later-planning-commit positive coverage plus source/evidence/unrelated/merge/dirty negative coverage
    - path: .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/attestations/27.1-01V-topology-validator-manifest.json
      provides: exact validator/test pins, planningHead and planningDelta manifest/hash, closed schema, and closed payload membership
  key_links:
    - from: frontend/tests/validatePhase271Reseal.ts
      to: git show and commitPaths
      via: each explicit historical root commit, never HEAD
    - from: terminal-markers/27.1-01V-topology-validator.json
      to: terminal-markers/27.1-01F-focused-launcher.json
      via: immediate marker-only 01V lineage after the 01F marker entry state
---
<objective>Correct the downstream reseal topology validator after 01F and seal the correction before Plan 02.

Purpose: Make lineage validation commit-addressed and fail closed without rewriting executed roots or performing ad hoc execution edits.
Output: Corrected validator/test, exact sealed payload, and immediate marker-only 01V commit.</objective>
<execution_context>@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/27.1-01-SUMMARY.md
@.planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/27.1-01C-SUMMARY.md
@.planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/27.1-01R-SUMMARY.md
@.planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/27.1-01F-SUMMARY.md
@frontend/tests/validatePhase271Reseal.ts
@frontend/tests/validatePhase271Reseal.test.ts
</context>
<precondition>Immutable frozenOperationalBase is the committed 01F marker `06f920e0`; current HEAD is a clean, non-merge descendant whose `06f920e0..HEAD` delta contains only declared canonical Phase-27.1 planning documents, and no Plan-02 payload or marker has begun.</precondition>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Freeze a clean planning head above the immutable operational base</name>
  <files>frontend/tests/validatePhase271Reseal.ts, frontend/tests/validatePhase271Reseal.test.ts</files>
  <behavior>
    - `--original-01-commit`, `--invalid-01c-commit`, `--authority-01r-commit`, `--focused-01f-commit`, `--frozen-operational-base`, `--planning-head`, `--planning-delta-manifest-sha256`, and `--expected-live-head` are mandatory downstream inputs; historical commits remain paired with exact marker paths.
    - The validator reads each marker and its commit path set from its explicit commit and checks marker-only membership, explicit parent/payload relationships, reachability, strict ancestry order `01 → 01C → 01R → 01F`, distinctness, and non-collapse.
    - At `preseal-01v`, dynamically freeze `planningHead` from current HEAD after proving a clean index/worktree/untracked set, `frozenOperationalBase=06f920e0`, and 01F ancestry. Enumerate exact commits and name-status paths in `01F..planningHead`; reject merges, deletions, and every path except `.planning/ROADMAP.md` and canonical `27.1-*-PLAN.md`, `27.1-RESEARCH.md`, or `27.1-VALIDATION.md` in the Phase-27.1 directory. Reject frontend, backend, evidence, runtime, snapshot, and other paths by closed allowlist, and reject non-planning content using parse/schema and planning-document shape checks where feasible.
    - Downstream historical attestation recomputes and validates sealed `frozenOperationalBase`, `planningHead`, the canonical ordered planning-delta manifest and its SHA-256. Only `--expected-live-head` is compared with current HEAD and remains plan-specific.
    - `--require-exact-payload-members` is mandatory and exact 01R/01F payload membership remains enforced.
  </behavior>
  <action>Write failing tests first, then change only `validatePhase271Reseal.ts` and its test. Replace downstream historical `git show` and `commitPaths` lookups that use HEAD with lookups at the matching explicit commit argument. Validate each marker at its own commit and prove every earlier root reaches immutable `frozenOperationalBase=06f920e0`. At execution entry, require clean unstaged/staged/untracked state, capture current HEAD dynamically as `planningHead`, require 01F ancestry, reject merge commits in `01F..planningHead`, and canonicalize the exact changed-commit and name-status path inventory. Permit only the declared ROADMAP and Phase-27.1 PLAN/RESEARCH/VALIDATION documents; reject deletions, source/evidence/runtime/snapshot or unrelated files and malformed/non-planning document content where feasible. Hash the canonical manifest. Downstream recomputes these historical pins and independently requires current HEAD to equal the caller-supplied plan-specific predecessor. Keep prior authority scopes unchanged. Do not hardcode the planning commit created by this correction.</action>
  <verify><automated>npm run test:unit -w frontend -- --run tests/validatePhase271Reseal.test.ts --maxWorkers=1 &amp;&amp; node frontend/tests/validatePhase271Reseal.ts --stage downstream --original-01-marker evidence/terminal-markers/27.1-01.json --original-01-commit &lt;commit-from-marker&gt; --invalid-01c-marker evidence/terminal-markers/27.1-01C-correction.json --invalid-01c-commit &lt;commit-from-marker&gt; --authority-marker evidence/terminal-markers/27.1-01R-reseal.json --authority-01r-commit &lt;commit-from-marker&gt; --focused-marker evidence/terminal-markers/27.1-01F-focused-launcher.json --focused-01f-commit 06f920e0 --frozen-operational-base 06f920e0 --planning-head &lt;captured-current-head&gt; --planning-delta-manifest-sha256 &lt;computed-sha256&gt; --expected-live-head &lt;captured-current-head&gt; --require-four-distinct-roots --reject-01C-authority --require-seven-sealed-artifacts --require-exact-payload-members &amp;&amp; git diff --check</automated></verify>
  <done>A clean later planning-only HEAD above 01F passes and is frozen with an exact canonical delta hash; dirty, merge, deletion, unrelated, source/evidence/runtime/snapshot, and malformed planning deltas reject.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Close every topology substitution and ordering escape</name>
  <files>frontend/tests/validatePhase271Reseal.test.ts</files>
  <behavior>
    - The former wrong-HEAD behavior and omission of the exact-members flag reject.
    - Detached, unreachable, reordered, collapsed, and substituted roots reject.
    - A marker commit with the wrong parent or any extra/missing member rejects.
    - A later planning-only commit above 01F is accepted and frozen dynamically; source, evidence, unrelated-path, deletion, merge-commit, staged, unstaged, and untracked cases reject independently.
    - Downstream frozen operational base, planningHead, delta manifest/hash, and plan-specific live predecessor mismatches each reject independently.
  </behavior>
  <action>Add isolated temporary-repository fixtures with immutable base `06f920e0`, a positive later commit changing only an allowed canonical planning document, positive Plan-02 `HEAD=01V`, and positive Plans-03-07 immediate-predecessor heads. Add independent negatives for frontend/source, evidence, runtime/snapshot and unrelated paths, deletion, merge commit, dirty unstaged, staged, and untracked state, plus malformed/non-planning content where the document parser can decide it. Add closed-schema tests requiring distinct `frozenOperationalBase`, `planningHead`, `planningDelta`, `planningDeltaSha256`, and `expectedLiveHead` fields/arguments and rejecting missing, conflated, unknown, or mismatched values. Include the former historical-HEAD regression and mutate one invariant per test.</action>
  <verify><automated>npm run test:unit -w frontend -- --run tests/validatePhase271Reseal.test.ts --maxWorkers=1 &amp;&amp; git diff --check</automated></verify>
  <done>Focused tests cover a later planning commit positive and all requested source/evidence/unrelated/merge/deletion/dirty negatives, historical topology failures, sealed planning pins, and plan-specific live-HEAD failures.</done>
</task>
<task type="auto">
  <name>Task 3: Seal exact 01V payload and immediate marker-only authority</name>
  <files>.planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/attestations/27.1-01V-topology-validator-manifest.json, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/plan-results/27.1-01V-topology-validator.json, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/27.1-01V-SUMMARY.md, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/terminal-markers/27.1-01V-topology-validator.json</files>
  <action>After validator/test bytes are final, recompute the entry-frozen planning delta and require it still matches the captured `planningHead`. Compute SHA-256 and Git blob IDs and write the closed manifest, READY_TO_SEAL result, and SUMMARY with exact five-member payload: validator, test, manifest, result, SUMMARY. Seal `frozenOperationalBase=06f920e0`, full `planningHead`, the exact ordered commit/path planning-delta manifest, and its SHA-256 in result and manifest. Stage and commit exactly those five members, requiring the payload commit's sole direct parent to equal frozen `planningHead`. Without intervening work, create and commit only `27.1-01V-topology-validator.json`; require its sole direct parent to be the payload commit and seal the same base/head/delta/hash pins in the marker. Preserve prior authority roles and do not amend, reset, rewrite, browse, or execute source/production/snapshot work.</action>
  <verify><automated>npm run test:unit -w frontend -- --run tests/validatePhase271Reseal.test.ts --maxWorkers=1 &amp;&amp; node frontend/tests/validatePhase271Reseal.ts --stage seal-01v --manifest evidence/attestations/27.1-01V-topology-validator-manifest.json --require-exact-payload-members --require-immediate-parent --require-marker-only-commit &amp;&amp; git diff --check &amp;&amp; git diff --cached --quiet</automated></verify>
  <done>The exact five-member validator payload directly parents frozen planningHead, the one-file COMPLETE marker directly parents the payload, all three artifacts seal identical planning pins, and no staged residue remains.</done>
</task>
</tasks>
<execution_contract>Chain is `01 → 01C → 01R → 01F → planningHead → 01V payload → 01V marker → 02`. Immutable `frozenOperationalBase` is `06f920e0`; `planningHead` is captured dynamically from clean current HEAD at execution entry and must descend from 01F through planning-only non-merge commits whose exact non-deleted paths are limited to ROADMAP and canonical Phase-27.1 PLAN/RESEARCH/VALIDATION documents. The payload directly parents planningHead and the marker directly parents payload. Result/manifest/marker seal base, planningHead, canonical delta manifest and hash. Plans 02-07 historically validate those pins; their live predecessor HEAD remains 01V then Plans 02-06 respectively. Every invocation requires exact payload membership. No browser/source/production/snapshot execution occurs.</execution_contract>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CLI root arguments → Git object reads | Every historical marker must be read from its explicit immutable commit. |
| historical topology → planning head → current HEAD | Frozen operational base/planning delta validity and stage-local live HEAD are separate checks. |
| 01V payload → marker | Validator authority must be exact, adjacent, and marker-only. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27.1-01V-01 | Spoofing | historical root lookup | critical | mitigate | Mandatory explicit commits and no HEAD substitution. |
| T-27.1-01V-02 | Tampering | topology order/membership | critical | mitigate | Reachability, strict ancestry, non-collapse, parent and exact-member tests. |
| T-27.1-01V-03 | Elevation of privilege | authority scope | high | mitigate | 01V grants topology-validator authority only. |
| T-27.1-01V-04 | Repudiation | planning-to-payload lineage | critical | mitigate | Dynamically freeze clean planningHead, exact planning-only delta/hash, direct payload parent and direct marker parent. |
| T-27.1-01V-05 | Tampering | planning delta | critical | mitigate | Reject merges, deletions, dirty state, non-planning content and every path outside the closed canonical planning-doc allowlist. |
</threat_model>
<verification>Focused validator unit tests including later-planning positive and source/evidence/unrelated/merge/deletion/dirty negatives, exact explicit-root invocation, planning-delta hash, direct-parent topology, Git hygiene, and no staged files.</verification>
<success_criteria>Downstream topology validation is explicit-commit-addressed and exact-member fail-closed; immutable 01F, dynamically frozen planningHead, its exact canonical planning delta/hash, direct payload/marker parentage, and each downstream plan-specific live predecessor are independently proved.</success_criteria>
<output>Create `27.1-01V-SUMMARY.md`, one attestation, one result, and one terminal marker during execution.</output>
