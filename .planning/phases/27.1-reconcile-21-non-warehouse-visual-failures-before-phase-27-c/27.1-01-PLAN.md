---
phase: 27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/visualReconciliationEvidence.ts
  - frontend/tests/visualReconciliationEvidence.test.ts
  - frontend/tests/runExpectedVisualMismatch.ts
  - frontend/tests/runExpectedVisualMismatch.test.ts
  - frontend/tests/validateVisualReconciliation.ts
  - frontend/tests/validateVisualReconciliation.test.ts
  - frontend/tests/createPhase271SourceRun.ts
  - frontend/tests/runPhase271Playwright.ts
  - frontend/tests/runPhase271Playwright.test.ts
  - frontend/tests/createPhase271SourceRun.test.ts
  - frontend/tests/fixtures/phase271-attempt-v2-sanitized-result.json
  - frontend/tests/fixtures/phase271-golden-21-identities.json
  - frontend/playwright.phase271-recovery.config.ts
  - frontend/tests/validatePhase271PlanResult.ts
  - frontend/tests/validatePhase271PlanResult.test.ts
  - frontend/tests/visual-routes.spec.ts
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/failure-inventory.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/source-run-manifest.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/phase-infrastructure-allowlist.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/packet-member-manifest.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/row-class-path-matrix.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/run-1/visual-run-manifest.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/run-1/playwright-results.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/run-1/stdout.log
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/run-1/stderr.log
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/run-2/visual-run-manifest.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/run-2/playwright-results.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/run-2/stdout.log
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/run-2/stderr.log
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/unit-rung-manifest.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/lint-rung-manifest.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/depcruise-rung-manifest.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/build-rung-manifest.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/final-gate/hygiene-rung-manifest.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/plan-results/27.1-01.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/terminal-markers/27.1-01.json
autonomous: true
requirements: [VREC-01, VREC-03]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All 21 failures are preserved and addressable by deterministic route, state, viewport, snapshot, and owner identity before any production or baseline edit (VREC-01)."
    - "Each evidence packet contains expected/actual/diff hashes, dimensions, semantic DOM, geometry, requests, and browser errors; screenshots alone never decide disposition."
    - "The existing threshold, viewport matrices, screenshot assertions, route fixtures, and comparison behavior remain byte-identical unless a later packet proves a stricter harness correction (VREC-03)."
  artifacts:
    - path: frontend/tests/visualReconciliationEvidence.ts
      provides: deterministic evidence-packet schema, hashing, inventory and invariant guards
    - path: frontend/tests/visualReconciliationEvidence.test.ts
      provides: 21-row completeness and fail-closed invariant tests
    - path: .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/failure-inventory.json
      provides: immutable 21-row source inventory
    - path: .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/phase-infrastructure-allowlist.json
      provides: immutable pre-remediation authorization and hash state for phase infrastructure plus all 105 packet members
    - path: .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/packet-member-manifest.json
      provides: exact expanded 21 × five-member destination manifest
    - path: .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/row-class-path-matrix.json
      provides: immutable row × disposition-class × permitted-path matrix
  key_links:
    - from: frontend/tests/visual-routes.spec.ts
      to: frontend/tests/visualReconciliationEvidence.ts
      via: per-test attachment/packet collection before screenshot comparison
    - from: failure-inventory.json
      to: source-run-manifest.json
      via: exact 21 identities and SHA-256 entries
---

<objective>
Recover the source evidence with one fresh, controlled, complete visual-suite run only after a non-consuming harness preflight and a pure classifier proof against the saved attempt-v2 result, atomically preserve its exact 21 known visual mismatches, then freeze infrastructure and row/class authorization.

Purpose: VREC-01 requires deterministic evidence and owner-level accountability. Preserve the consumed Windows path-parsing failure as a distinct `REJECTED_SETUP` ledger entry whose zero collected identities are never accepted. Preserve attempt v2 immutably as `REJECTED_CLASSIFIER` with its original session/run evidence, discovery SHA-256 `855caa1ec12583b2baaae7db7aedfabe45e735702f12b135168d7f504cdd39d1`, 76 discovered tests and 21 failed screenshot comparisons; never relabel it accepted. A cryptographically new v3 attempt ID may be authorized only after classifier-fixture, argv, discovery-preflight and path tests pass; rejected attempts are historical evidence, not retryable identities.
Output: A tested source-run/preservation harness, immutable 21-row/105-member inventory, and machine-readable Plan 01 result.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/27.1-RESEARCH.md
@frontend/tests/visual-routes.spec.ts
@frontend/playwright.config.ts
@frontend/package.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Create one controlled source run and atomically preserve the exact mismatch set</name>
  <files>frontend/tests/createPhase271SourceRun.ts, frontend/tests/createPhase271SourceRun.test.ts, frontend/tests/fixtures/phase271-attempt-v2-sanitized-result.json, frontend/tests/fixtures/phase271-golden-21-identities.json, frontend/tests/runPhase271Playwright.ts, frontend/tests/runPhase271Playwright.test.ts, frontend/playwright.phase271-recovery.config.ts, frontend/tests/visualReconciliationEvidence.ts, frontend/tests/visualReconciliationEvidence.test.ts, frontend/tests/validateVisualReconciliation.ts, frontend/tests/validateVisualReconciliation.test.ts, frontend/tests/validatePhase271PlanResult.ts, frontend/tests/validatePhase271PlanResult.test.ts, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/failure-inventory.json, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/source-run-manifest.json, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/phase-infrastructure-allowlist.json, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/packet-member-manifest.json, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/row-class-path-matrix.json</files>
  <behavior>
    - The attempt ledger preserves the consumed failed invocation under its original attempt ID with terminal status `REJECTED_SETUP`, the exact malformed-path diagnosis (`D:/Kì`), raw argv/cwd/output evidence and zero collected identities recorded only as rejected observations. It also preserves attempt v2 under its original attempt/session/run identities as terminal `REJECTED_CLASSIFIER`, pins discovery SHA-256 `855caa1ec12583b2baaae7db7aedfabe45e735702f12b135168d7f504cdd39d1`, records 76 discovered tests and 21 failed screenshot comparisons, and links the original structured result/stdout/stderr/argv evidence by path and SHA-256. Neither rejected entry may be relabeled, retried under the same identity, or admitted as an accepted source run.
    - `runPhase271Playwright.ts` is the single shared structured browser launcher used by source, focused, final, and Phase-27 rerun browser work. Its closed typed input accepts only `specs`, `project`, `workers`, `grep`, `reporter`, `outputRoot`, `config`, and `env`; reject unknown keys and any command string, `npm`, `npx`, `shell`, executable, argv, webServer, or package-script field. It resolves and SHA-256 hashes installed Playwright CLI JS and Vite CLI JS internally, invokes both through `process.execPath` with explicit argv/cwd/env and `shell:false`, requires the dedicated no-webServer config, owns/health-checks/tears down Vite, and emits an attestation of executable hashes and exact argv.
    - Resolve the Playwright CLI JS from the installed `@playwright/test` package and invoke it only as executable `process.execPath` plus argv. Freeze the resolved CLI path and SHA-256. The one canonical argv base is `[resolvedPlaywrightCli,"test","tests/visual-routes.spec.ts","tests/pagination-visual.spec.ts","--config=playwright.phase271-recovery.config.ts","--project=chromium","--workers=1"]`; no spec, config, project, or worker token may be reordered, omitted, duplicated, or added.
    - Before authorizing attempt v3, the harness appends only `--list` and `--reporter=json` to that canonical base. It parses every discovered test into canonical identity `{project,normalizedSpecPath,canonicalTitle}`, where `canonicalTitle` is the exact Playwright title emitted by list-only preflight, including the visual-routes suite segment for broad desktop/mobile cases and the explicit viewport suite segment for Phase-09 cases. Normalize only the repo-relative spec path; do not parse titles or infer viewport from title substrings. Reject duplicate/missing/extra identities and omitted pagination/extra specs, sort by project then normalized specPath then canonicalTitle, and freeze the SHA-256 of canonical JSON. Preflight is not a screenshot/source attempt and creates no accepted source identity.
    - Save a sanitized, byte-frozen structured fixture of attempt v2's complete 76-test result. A pure classifier maps expected failures only by exact `{project,normalizedSpecPath,canonicalTitle,snapshotName}` against a golden 21-identity manifest derived by joining list-only preflight identity with structured-result failure/attachment metadata. Viewport width/height and project metadata come from the Playwright project/result attachments, never title text. The manifest records a canonical JSON SHA-256 (`goldenIdentityManifestSha256`) and exact expected snapshot name for every row.
    - Pure classifier tests over the saved v2 fixture must accept exactly 21 screenshot mismatches, including Login mobile under its exact canonical title, with exact expected snapshot names and zero arbitrary/nonvisual errors. Mutation tests independently reject one changed title, snapshot name, project, normalized spec path, duplicate identity, missing failure, extra failure, and wrong viewport metadata. The golden 21 identity manifest hash is recomputed and must match before any browser process or v3 authorization.
    - After preflight passes, the harness generates a cryptographically new attempt ID and creates a source-run root under phase evidence. The source invocation uses the same base, appending only `--reporter=json` and `--output=<run-specific-output-directory>`, while `PLAYWRIGHT_JSON_OUTPUT_FILE` names the run-specific structured result file. Parse that result into the same complete canonical discovered set (all passed and failed tests) and require exact equality with the preflight set/hash before evaluating the exact 21 mismatches. Exactly-once is enforced per accepted attempt ID, and at most one source attempt can reach `ACCEPTED`.
    - Parse `frontend/package.json` separately and require `scripts["test:visual"]` to be token-equivalent to `playwright test tests/visual-routes.spec.ts tests/pagination-visual.spec.ts`; script drift blocks authorization, but npm is never invoked. Every source/preflight child uses Node `spawn` or `execFile` with executable `process.execPath`, `shell:false`, explicit cwd and argv; the manifest/ledger records executable path/hash, complete argv, cwd, environment-output path, and `shell:false`. Reject npm/npx/npm-run executables, command strings, shell intermediaries, and shell-enabled options.
    - Create `playwright.phase271-recovery.config.ts` as a test-owned import/derivation of production config with `webServer` disabled and all production-equivalent testDir, snapshot, timeout, expect threshold, viewport/project, use, and output semantics frozen and validated against `playwright.config.ts`; any weakening or drift fails. The harness starts Vite itself using `process.execPath` plus the resolved Vite CLI JS path and argv, `shell:false`, frontend cwd, explicit isolated port and environment. It health-checks that port before Playwright, records Vite executable path/hash/argv/cwd/env, and tears down only the exact child it owns. Vite and Playwright probes both prove exact argv/cwd round trips through space/Unicode repository and output paths.
    - When a provider or tool cannot safely consume the repository path, stage output in an ASCII-safe unique OS temp directory. Hash and fsync the closed staging tree, then atomically import it into the phase-owned evidence destination on a compatible filesystem (using a same-volume phase staging directory when required), verify imported hashes, fsync the parent where supported, and remove temp only after verification; partial/cross-device unsafe imports fail closed.
    - The only accepted non-zero outcome is exactly 21 distinct known screenshot-comparison failure identities: 17 broad non-Warehouse rows and four Purchasing Phase-09 rows. Fewer, more, different or duplicate identities; any Warehouse, timeout, navigation, setup, arbitrary assertion, discovery escape or other nonvisual failure is rejected.
    - Before any later Playwright invocation, each accepted failure's expected/actual/diff/error-context/trace is copied through a staging directory, fsynced/closed, hash-verified, and atomically renamed into durable `packets/row-NN`; interrupted or partial preservation leaves no accepted manifest.
    - The sealed inventory contains exactly 21 identities and the durable member manifest exactly 105 unique members; each durable member hash matches its source-run member.
    - A separate immutable phase-infrastructure/generated-artifact allowlist records every planned collector, validator, test, evidence, summary and verification path, with hash state for existing paths and an explicit absent-at-freeze marker for generated paths. It freezes `phaseBaseCommit` before any phase commit and separately freezes the complete pre-phase index/worktree/untracked path+hash state, so pre-existing dirt cannot be laundered into phase authorization. It explicitly preauthorizes `evidence/post-pass-planning-transition.json`, `frontend/test-results/warehouse-data-workspace/after/ai-rereview-attestation.json`, the fixed final-gate slots `evidence/final-gate/run-1` and `run-2`, each containing exactly `visual-run-manifest.json`, `playwright-results.json`, `stdout.log`, and `stderr.log`, and fixed root-level `unit-rung-manifest.json`, `lint-rung-manifest.json`, `depcruise-rung-manifest.json`, `build-rung-manifest.json`, and `hygiene-rung-manifest.json` as absent-at-freeze generated paths. The closed manifest schemas keep cryptographically fresh run IDs in content only; every concrete member path is frozen and the allowlist itself is never modified after Task 1.
    - Oracle/config/fixture/snapshot hashes are frozen before remediation.
    - `packet-member-manifest.json` expands exactly 105 paths before rerun: for every zero-padded row `01`..`21`, `evidence/packets/row-NN/expected.png`, `actual.png`, `diff.png`, `error-context.md`, and `trace.zip`; every entry carries source SHA-256 plus destination `absent-at-freeze` and post-copy current SHA-256.
    - `row-class-path-matrix.json` is immutable: each row maps `production-regression` only to its research-named page/component owner, `fixture-drift` only to its named fixture seam, `harness-nondeterminism` only to `frontend/tests/visual-routes.spec.ts` and/or `frontend/tests/visualReconciliationEvidence.ts`, and `stale-baseline` only to that row's exact snapshot path.
  </behavior>
  <action>Write tests first for the closed-input shared `runPhase271Playwright.ts` launcher, then make the controlled source-run orchestrator consume that launcher rather than owning a second process path. Add a closed attempt ledger and preserve the consumed failed Windows invocation under its original identity as `REJECTED_SETUP`, including the truncated `D:/Kì` argument observation and zero collected identities strictly as rejected evidence. Append attempt v2 immutably as `REJECTED_CLASSIFIER`, retaining its original session/run IDs and hashed structured result/stdout/stderr/argv evidence, discovery hash `855caa1ec12583b2baaae7db7aedfabe45e735702f12b135168d7f504cdd39d1`, 76 discovered tests, and 21 screenshot failures; tests must prove no transition can relabel it `ACCEPTED`. Sanitize the v2 structured JSON deterministically (remove secrets and machine-absolute roots while retaining identity, outcome, error category, attachment/snapshot and project viewport metadata), freeze it as a test fixture, and derive the golden 21 manifest by exact preflight/result join on project + normalized spec path + exact canonical Playwright title plus frozen snapshot name. Implement the classifier as a pure function with no title parsing and no viewport substring heuristic; obtain viewport only from project/result attachment metadata. Pin and verify the golden manifest's canonical JSON SHA-256. Add positive proof for exactly 21 accepted screenshot mismatches including Login mobile's exact canonical title and snapshot, plus mutation negatives for title/snapshot/project/spec mismatch, duplicate, missing/extra failure, wrong viewport metadata, and any arbitrary/nonvisual error. Generate a cryptographically new v3 attempt ID only after the pure classifier fixture/hash tests and all argv, discovery and path preflight gates pass; enforce one invocation per authorized attempt ID and no more than one `ACCEPTED` source attempt. Implement all process execution with Node `child_process.spawn` or `execFile`, `shell: false`, explicit executable/argv arrays and explicit cwd; reject command strings. Add a probe-child fixture whose received argv and cwd are emitted structurally, and unit/integration negative cases covering spaces, Unicode, embedded quotes, `&`, `|`, `;`, `$`, backticks, parentheses and traversal-like arguments; require exact round-trip values and prove none trigger shell interpretation. Resolve the installed Playwright CLI JS and Vite CLI JS, hash both, and launch each only as `process.execPath` plus explicit argv via `spawn`/`execFile`, `shell:false`, frontend cwd and no command string. Build the sole Playwright argv base exactly as `[cli,"test","tests/visual-routes.spec.ts","tests/pagination-visual.spec.ts","--config=playwright.phase271-recovery.config.ts","--project=chromium","--workers=1"]`. Separately tokenize and validate `package.json` `test:visual` as equivalent to the two-spec selection; fail on drift without invoking npm. Create the dedicated recovery config with `webServer` disabled, derive/freeze every production-equivalent setting, and reject timeout, assertion, snapshot, viewport/project, use, or comparison weakening. Start Vite directly on an isolated port with explicit env, health-check it, and terminate only that owned process. Preflight appends only `--list`,`--reporter=json`; source appends only `--reporter=json`,`--output=<run-root>` and sets the JSON reporter output file in explicit env. Canonically sort every exact `{project,normalizedSpecPath,canonicalTitle}` across all tests, reject duplicates/missing/extras, omitted pagination, added specs and config/project drift, hash the complete preflight set, and require the source set/hash to match before mismatch acceptance; never derive identity or viewport by parsing `canonicalTitle`. Record executable path/hash, argv, cwd, env output paths and `shell:false` in ledger/manifests. Add launcher API negatives for unknown fields, command/visual-command strings, npm/npx/npm-run, executable/argv injection, shell fields/options and webServer requests; exercise those rejections through source and representative focused launch adapters. Add source/preflight set mismatch, and path-space/Unicode argv for both CLIs. The preflight is explicitly not the controlled screenshot run. Allocate a unique evidence-owned directory that Playwright cleanup cannot reach, or an ASCII-safe unique OS temp staging root when provider/tool path handling requires it; invoke the complete visual suite serially exactly once and accept only the exact frozen 21-identity visual-mismatch set. For external staging, hash/fsync the complete tree and atomically import through a same-volume phase staging destination before exposing evidence; verify destination hashes and retain failed staging/import diagnostics. Copy the five required members per identity into a sibling staging tree; verify counts, identities, member names and SHA-256, durably close writes, then atomically rename the complete tree and hash-rename the source/member manifests. Do not expose an accepted manifest until the rename completes. Add negative tests for malformed Windows path arguments, preflight collection drift, Vitest discovery, discovery outside the intended visual spec set, Playwright cleanup of `frontend/test-results`, partial copy, interruption between staging and rename/import, cross-device rename, hash mismatch, arbitrary nonvisual failure, failure-count drift in both directions, duplicate/different identity, Warehouse failure, timeout and navigation failure. Only after durable preservation succeeds, freeze the infrastructure allowlist and row/class/path matrix. Before freezing, expand the exact 105-entry member manifest using the `row-NN/{expected.png,actual.png,diff.png,error-context.md,trace.zip}` convention; reject any extra or missing row/member. Put `frontend/tests/runPhase271Playwright.ts`, `frontend/tests/runPhase271Playwright.test.ts`, and `frontend/playwright.phase271-recovery.config.ts` into the immutable infrastructure allowlist with their freeze hashes (plus generated-state handling where applicable), and require every later browser attestation to match those hashes. Put every concrete durable destination, every `evidence/plan-results/27.1-01.json` through `27.1-07.json`, and every `evidence/terminal-markers/27.1-01.json` through `27.1-07.json` into the infrastructure allowlist with absent/current hash state; these plan-result and terminal-marker paths are frozen infrastructure and cannot be renamed, substituted, or dynamically generated. Also add the exact generated paths `evidence/post-pass-planning-transition.json`, `frontend/test-results/warehouse-data-workspace/after/ai-rereview-attestation.json`, fixed destination slots `evidence/final-gate/run-1` and `run-2`, each with exactly closed-schema `visual-run-manifest.json`, `playwright-results.json`, `stdout.log`, and `stderr.log`, plus fixed `unit-rung-manifest.json`, `lint-rung-manifest.json`, `depcruise-rung-manifest.json`, `build-rung-manifest.json`, and `hygiene-rung-manifest.json`. Cryptographically fresh run IDs are content fields and never path components. Test omission/addition, an extra file, path substitution, duplicate destination slot, and an unlisted rung output; each must fail reconciliation. The allowlist is immutable after this commit and no downstream plan may declare it writable. Freeze a separate row/class/path matrix: rows 1/2 DashboardPage, 3/4 WeeklyMenuPage, 5/6 CoordinationPage, 7/8 ChefDashboardPage, 9/10 ReportsPage, 11/12 ApprovalPage, 13/14 PurchasingPage, 15/16 AdminDataPage, 17 LoginPage, and 18..21 the four research-named Purchasing owner components for production class; each row's fixture class permits only its named fixture (`visual-routes.spec.ts`, except rows 18..21 `phase9-test-fixture.ts`), harness class permits only `visual-routes.spec.ts` and `visualReconciliationEvidence.ts`, and stale class permits only its exact inventory snapshot. Hash this matrix and forbid later mutation. Unit tests must reject 20/21, duplicate/Warehouse, one extra or missing packet member, packet hash drift, mutable allowlist/matrix, cross-row/class path laundering, production paths under fixture/harness classes, and snapshots under non-stale classes. For Plan 01 use frozen `phaseBaseCommit` as `waveBaseCommit`; every later plan derives its base only from the validated predecessor terminal-marker commit. Git fixtures must prove an authorized same-wave path committed before reconciliation remains in `waveBaseCommit..HEAD`, predecessor payload and marker commits are excluded wave-locally but retained by cumulative `phaseBaseCommit..HEAD` accounting, and wrong/mismatched `waveBaseCommit`, unrelated pre-existing dirt, extra same-wave tracked paths, and extra same-wave untracked paths fail. Wave-local accounting is `waveBaseCommit..HEAD` plus current index/worktree/untracked against that wave authorization; cumulative `phaseBaseCommit..HEAD` plus current state remains a separate gate.</action>
  <verify><automated>npm run test:unit -w frontend -- --run tests/createPhase271SourceRun.test.ts tests/runPhase271Playwright.test.ts tests/visualReconciliationEvidence.test.ts tests/validatePhase271PlanResult.test.ts --maxWorkers=1 &amp;&amp; node frontend/tests/createPhase271SourceRun.ts --preflight-only --require-exact-visual-identities --require-zero-vitest-discovery --probe-path-roundtrip &amp;&amp; node -e "const p='.planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/';const i=require('./'+p+'failure-inventory.json'),m=require('./'+p+'packet-member-manifest.json'),a=require('./'+p+'phase-infrastructure-allowlist.json'),x=require('./'+p+'row-class-path-matrix.json');if(i.failures?.length!==21||m.members?.length!==105||new Set(m.members.map(v=>v.path)).size!==105||a.paths?.filter(v=>v.authorizationClass==='preserved-packet').length!==105||x.rows?.length!==21)process.exit(1)" &amp;&amp; git diff --check</automated></verify>
  <done>The consumed malformed-path attempt remains terminal `REJECTED_SETUP`; attempt v2 remains immutable `REJECTED_CLASSIFIER` with original session/run evidence, discovery hash `855caa1ec12583b2baaae7db7aedfabe45e735702f12b135168d7f504cdd39d1`, 76 tests and 21 screenshot failures. Pure fixture tests prove the exact golden 21 identity/snapshot/viewport manifest (including Login mobile), reject every specified mutation/nonvisual case, and only then may a cryptographically fresh v3 ID be authorized after argv/discovery/path preflight. One controlled complete-suite v3 run yields exactly 21 accepted identities and 105 atomically preserved members. All argv, discovery, staging and recovery negatives fail closed, and only then are infrastructure, result paths and row/class scopes frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate the durable source and publish the Plan 01 execution result</name>
  <files>frontend/tests/visualReconciliationEvidence.ts, frontend/tests/visualReconciliationEvidence.test.ts, frontend/tests/runExpectedVisualMismatch.ts, frontend/tests/runExpectedVisualMismatch.test.ts, frontend/tests/validateVisualReconciliation.ts, frontend/tests/validateVisualReconciliation.test.ts, frontend/tests/visual-routes.spec.ts, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/source-run-manifest.json</files>
  <behavior>
    - The runner refuses to start unless the durable manifest proves all 21 source packets, 105 members and the frozen infrastructure allowlist.
    - `validatePhase271PlanResult.ts` enforces two closed schemas. The payload result is `{schemaVersion,phase,planId,status:"READY_TO_SEAL",summary:{path,sha256},dependencies:[{planId,terminalMarkerPath,terminalMarkerSha256,terminalMarkerCommit}],commits,blockers,validationHashes}`. The terminal marker is `{schemaVersion,phase,planId,status:"COMPLETE",summary:{path,sha256},payloadResult:{path,sha256},payloadCommit,dependencies:[{planId,terminalMarkerPath,terminalMarkerSha256,terminalMarkerCommit}],validationHashes}`. Unknown/missing fields are rejected; the marker never contains or claims its own commit hash.
    - Closeout first creates and validates the SUMMARY, then the READY_TO_SEAL payload result; it commits those two artifacts together in one payload commit. Only afterward it creates the terminal marker and commits that marker alone in a terminal-marker commit. The validator proves the current terminal-marker commit changes exactly the frozen marker path, the referenced SUMMARY and payload result hashes exist in the reachable payload commit, and every exact dependency marker appears once with matching path/hash/commit and validated COMPLETE status. HALTED/FAILED markers never advance.
    - The named Dashboard title maps to one stable family/route/state/viewport/snapshot identity and emits semantic DOM, geometry, request/error and invariant evidence.
    - Only the recognized screenshot-difference signature for that named comparison is an expected failure; timeout, navigation, infrastructure, assertion-shape, missing-artifact and arbitrary non-zero exits fail.
    - The Plan 01 result keeps explicit blockers until the attempt ledger proves the prior ID is `REJECTED_SETUP`, path-probe and list-only preflight attestations pass, a distinct cryptographically new ID has exactly one controlled invocation, exactly one source attempt is `ACCEPTED`, and the 21/105 atomic-import hashes validate. A blocked result cannot become READY_TO_SEAL or COMPLETE.
  </behavior>
  <action>After Task 1's durable 21/105 preservation gate succeeds, write failing tests and implement the reusable collector plus `runExpectedVisualMismatch.ts` and the phase plan-result validator. Validate result JSON structurally and cryptographically against the closed versioned contract. Write negative tests for unknown/missing fields, result created before SUMMARY, marker created before the payload commit, marker commit containing an extra path, unreachable payload commit, result or SUMMARY hash mismatch, duplicate/wrong dependency identity, non-COMPLETE dependency marker, predecessor artifacts leaking into a wave-local range, and payload/marker commits missing from cumulative accounting; do not scan summaries or logs with free-text regex to decide advancement. Plan-result tests must reject empty/omitted blockers while any path-preflight, discovery, attempt-identity, exactly-once, single-ACCEPTED or atomic-import condition is unresolved; they must also reject clearing blockers from the rejected attempt's zero identities. The executor must write `27.1-01-SUMMARY.md` first, then atomically write `evidence/plan-results/27.1-01.json` with READY_TO_SEAL and validate both before committing exactly those two artifacts as the payload commit. It must then atomically write the frozen `evidence/terminal-markers/27.1-01.json` COMPLETE marker and commit that marker alone. HALTED/FAILED terminal outcomes remain non-advancing and must never be represented by a READY_TO_SEAL payload plus COMPLETE marker. Then run the exact named tracer. The wrapper must first validate the preserved manifest and immutable infrastructure allowlist, then call only the shared typed `runPhase271Playwright.ts` launcher for the exact focused Dashboard selection; it must not construct or accept a command string or launch its own Playwright/Vite process path. After the named screenshot mismatch, independently validate the newly emitted packet, expected/actual/diff hashes, PNG dimensions, semantic/DOM/geometry evidence, request/errors, repeated identity and byte-identical oracle inputs. It must never treat an unnamed or differently shaped failure as success.</action>
  <verify><automated>npm run test:unit -w frontend -- --run tests/visualReconciliationEvidence.test.ts tests/runExpectedVisualMismatch.test.ts tests/validateVisualReconciliation.test.ts --maxWorkers=1 &amp;&amp; node frontend/tests/runExpectedVisualMismatch.ts --require-preserved-source-count 21 --require-frozen-infrastructure-allowlist --title "dashboard visual baseline" --project chromium --workers 1 --expect visual-mismatch --validate-named-mismatch --validate-packet &amp;&amp; node frontend/tests/validateVisualReconciliation.ts --rows none --phase-base-commit-from phase-infrastructure --cumulative-git-state --cumulative-authorization-union phase-infrastructure --wave-base-commit-from phase-infrastructure --wave-git-state --wave-authorization-union phase-infrastructure --pre-phase-dirty-from phase-infrastructure --verify-frozen-infrastructure-allowlist --verify-row-class-path-matrix --verify-before-after-sha256 &amp;&amp; git diff --check</automated></verify>
  <done>The first later Playwright invocation follows sealed preservation; Plan 01 emits a schema-valid result JSON and wave-1 reconciliation accepts only frozen infrastructure/result paths, with negative Git-state fixtures failing.</done>
</task>

</tasks>

<execution_contract>All browser execution in Plans 01-07 and the Phase-27 rerun goes through the tested closed-input `runPhase271Playwright.ts` API (`specs`, `project`, `workers`, `grep`, `reporter`, `outputRoot`, `config`, `env` only); it rejects command strings, npm/npx, shell/executable/argv/webServer and unknown inputs, directly owns the no-webServer Vite lifecycle, hashes both CLI JS executables and attests exact argv/cwd/env. The canonical direct CLI contract uses `process.execPath` with resolved-and-hashed Playwright CLI JS and base argv `[cli,"test","tests/visual-routes.spec.ts","tests/pagination-visual.spec.ts","--config=playwright.phase271-recovery.config.ts","--project=chromium","--workers=1"]`; preflight adds only `--list`,`--reporter=json`, while source adds only `--reporter=json`,`--output=<run-root>` plus explicit JSON-output env. The complete canonical `{project,normalizedSpecPath,canonicalTitle}` discovery hash must match across preflight/source. Classifier matching additionally requires exact frozen snapshot name, while viewport metadata comes only from project/result attachments; title parsing and viewport substring inference are forbidden. Attempt v2 remains immutable `REJECTED_CLASSIFIER` with its original session/run evidence, exact discovery hash, 76 tests and 21 screenshot failures. A cryptographically fresh v3 is authorized only after the sanitized-v2 pure classifier and golden-21 manifest hash gate plus argv/discovery/path gates pass. The package script is validated separately for token equivalence and never executed. A dedicated recovery config disables internal webServer; the harness directly starts resolved-and-hashed Vite CLI with `process.execPath`, shell false, isolated port, explicit cwd/env, health check, and owned-process teardown. The consumed Windows path-parsing attempt is immutable `REJECTED_SETUP`, is never called a retry, and contributes no accepted identities. A new cryptographic attempt ID is authorized only after exact argv/cwd round-trip probes and Playwright list-only collection prove the intended visual identities/spec paths with zero Vitest files and no discovery escape. All child execution uses spawn/execFile with shell false and explicit argv; ASCII-safe OS temp staging is permitted only with hash-verified atomic import into phase-owned evidence. Exactly-once is per accepted attempt ID and only one source attempt may reach ACCEPTED. Before the first phase commit, freeze `phaseBaseCommit`, use it as Plan 01's `waveBaseCommit`, and freeze the separate complete pre-phase index/worktree/untracked state plus all seven payload-result and terminal-marker paths in immutable infrastructure evidence. Reconcile wave-local `waveBaseCommit..HEAD` plus current index/worktree/untracked independently from cumulative `phaseBaseCommit..HEAD` plus current state. At closeout create SUMMARY then the READY_TO_SEAL payload result, validate their typed schemas/hashes/dependencies/validationHashes, and commit exactly those two artifacts in one payload commit. Then create the COMPLETE terminal marker and commit that marker path alone. The marker references the reachable payload commit and hashes but never claims its own commit. Only a validator-confirmed current terminal-marker commit advances; Plan 01 dependencies are empty. HALTED/FAILED never advance, and no prose or regex match determines status.</execution_contract>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| transient Playwright output → phase evidence | Test reruns can delete or replace source artifacts. |
| screenshot → disposition | Pixels are reviewer evidence, not semantic intent proof. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27.1-01 | Tampering | source artifacts | high | mitigate | Copy before rerun; SHA-256 every artifact and source/config input. |
| T-27.1-02 | Repudiation | 21-row inventory | high | mitigate | Exact IDs, source commit, command and fail-closed completeness test. |
| T-27.1-03 | Tampering | visual oracle | critical | mitigate | Hash thresholds, viewports, assertions, fixtures and comparison logic. |
</threat_model>

<verification>Inventory count is 21; all packets are deterministic and unresolved; no production or snapshot path is modified; `git diff --check` passes.</verification>
<success_criteria>VREC-01 has a complete tracer and source inventory; VREC-03 invariants are recorded before remediation.</success_criteria>
<output>Create `27.1-01-SUMMARY.md` when done.</output>
