---
phase: 27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c
plan: 03R
type: execute
wave: 9
depends_on: [27.1-02]
files_modified:
  - frontend/tests/validatePhase271PlanResult.ts
  - frontend/tests/validatePhase271PlanResult.test.ts
  - frontend/tests/validateVisualReconciliation.ts
  - frontend/tests/validateVisualReconciliation.test.ts
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/attestations/27.1-03R-general-validator-manifest.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/plan-results/27.1-03R.json
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/27.1-03R-SUMMARY.md
  - .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/terminal-markers/27.1-03R-general-validator.json
autonomous: true
requirements: [VREC-01, VREC-02, VREC-03]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Plans 03-07 can validate any allowlisted Phase-27.1 predecessor/result/marker schema without Plan-01C or Plan-02-specific logic."
    - "Downstream reconciliation derives exact identity sets, dispositions, class-specific paths, roots, pins, packets, invariants, locks and cumulative/wave Git accounting from sealed artifacts."
    - "Production-regression authorizes only the corrected-matrix row's exact frontend/src owner paths; stale-baseline authorizes only that row's exact snapshot; fixture and harness classes authorize only their named paths."
    - "03R changes no browser output, production source, route fixture, or snapshot."
  artifacts:
    - path: frontend/tests/validatePhase271PlanResult.ts
      provides: closed-table Phase-27.1 result and terminal-marker validation
    - path: frontend/tests/validateVisualReconciliation.ts
      provides: closed generic downstream-reconciliation validation
    - path: .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/terminal-markers/27.1-03R-general-validator.json
      provides: eighth typed authority root and exact validator/test pins
  key_links:
    - from: evidence/corrected-authorization-matrix.json
      to: frontend/tests/validateVisualReconciliation.ts
      via: exact identity-set, disposition-class and permitted-path resolution
    - from: evidence/terminal-markers/27.1-03R-general-validator.json
      to: Plans 03-07
      via: validator authority hash, marker-only commit and four validator/test pins
---

<objective>Generalize the fail-closed Phase-27.1 validators once after completed Plan 02 so Plans 03-07 can execute against their own typed identities and predecessor markers.

Purpose: Remove the discovered hardcoded Plan-01C/readiness assumptions without widening any correction path or running reconciliation work.
Output: Table-tested generic validators, sealed validator manifest/result/SUMMARY payload, and an immediate marker-only 03R authority.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/27.1-02-SUMMARY.md
@.planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/27.1-RESEARCH.md
@.planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/corrected-authorization-matrix.json
@frontend/tests/validatePhase271PlanResult.ts
@frontend/tests/validatePhase271PlanResult.test.ts
@frontend/tests/validateVisualReconciliation.ts
@frontend/tests/validateVisualReconciliation.test.ts
</context>

<precondition>Plan 02 is immutable and COMPLETE at its marker-only commit with committed reachable SUMMARY/payload, validation hashes, exact dependency chain and seven typed authority roots; the worktree has no staged files.</precondition>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Generalize Phase-27.1 plan-result and downstream reconciliation contracts</name>
  <files>frontend/tests/validatePhase271PlanResult.ts, frontend/tests/validatePhase271PlanResult.test.ts, frontend/tests/validateVisualReconciliation.ts, frontend/tests/validateVisualReconciliation.test.ts</files>
  <behavior>
    - `validatePhase271PlanResult` accepts only the explicit closed plan-ID/schema table containing `27.1-02`, `27.1-03R`, and `27.1-03` through `27.1-07`; the `27.1-03R` row names its general-validator manifest/result/SUMMARY/marker schema, while every other row names only its own closed schema. It verifies committed SUMMARY/payload, payload and marker commits, validation hashes, immediate marker-only topology, typed roots and exact dependencies.
    - Table tests prove the `27.1-03R` row passes with its exact schema, each `27.1-02` through `27.1-07` row passes only with its own schema, and arbitrary IDs (including same-prefix IDs), cross-row schemas, unknown flags/fields, missing/substituted SUMMARY, payload, marker, hash, commit, root, pin or dependency, duplicate accounting, non-immediate marker topology, and merge ancestry reject.
    - Existing source-inspection and downstream-readiness modes retain their tested behavior.
    - Closed `--mode pre-work-entry` is the only Plans-03..07 entry gate: it resolves the 03R marker SHA-256, marker-only commit and four validator/test blob pins internally from the sealed marker and emits a closed JSON context (or passes those values directly as quoted argv without a shell); validates COMPLETE, eight roots, exact predecessor chain, selected identity set and resolved classes; and rejects legacy reseal/downstream modes as entry authorization. Closed `--mode downstream-reconciliation` remains a post-work validator and consumes `--general-validator-values-from` rather than caller-written hash/commit tokens.
    - `--recovery-values-from evidence/terminal-markers/27.1-02R-validator-recovery.json` is the sole recovery authority input. The validator resolves the recovery marker SHA-256 and marker-only commit internally with Git plumbing, verifies the exact allowlisted marker path, content, blob identity, commit membership, and immediate marker-only topology, and rejects caller-supplied raw recovery hash/commit flags, substituted marker bytes, wrong paths or content, and commits that are not marker-only.
    - Table cases cover `core-login-dashboard`, `core-meal-weekly`, `secondary-reports-approvals-admin`, `purchasing-phase09`, every cumulative union used through `--all-21`, and Plan-07's final/post-pass authorization classes.
    - Production-regression permits only the selected matrix row's exact `frontend/src` owner path(s); stale-baseline permits only its exact snapshot; fixture-drift and harness-nondeterminism permit only their named fixture/harness paths. Cross-identity borrowing, broad prefixes/globs, production/snapshot class mismatch and unauthorized paths reject.
    - Reconciliation recomputes dispositions, two non-stale packet pairs, invariant/snapshot guards, Admin/Purchasing locks, eight distinct roots, predecessor and 03R pins, and cumulative plus wave committed/staged/unstaged/untracked accounting.
  </behavior>
  <action>Refactor both validators around explicit closed tables derived from the corrected matrix and allowlisted Plan-27.1 schemas. The generic plan-result table must contain exactly 27.1-02, 27.1-03R, and 27.1-03 through 27.1-07; add positive tests for the dedicated 03R general-validator schema and negative tests for arbitrary/same-prefix IDs and cross-row schema borrowing. Remove assumptions that authority is Plan 01C, that the only valid predecessor is Plan 02, or that identities are only Chef/Purchasing readiness. Preserve the existing source-inspection and downstream-readiness entry points. Add `pre-work-entry` as a separate closed mode whose first operation reads and validates the sealed 03R marker, resolves its hash/commit/four pins via Node APIs and Git plumbing, validates COMPLETE/eight roots/predecessor/selected identities and classes, and emits structured JSON; no raw angle-token or shell-evaluated value is accepted. Explicitly reject use of legacy reseal, downstream-readiness, or downstream-reconciliation success as entry authorization. Add downstream-reconciliation as a post-work closed mode whose inputs name one exact identity set or a declared exact union, one dispositions artifact, the validated predecessor, `--general-validator-values-from` the sealed 03R marker, and `--recovery-values-from` the exact sealed 02R recovery marker. Resolve recovery hash and marker-only commit internally through Node hashing and Git plumbing; validate exact path/content/blob/commit and immediate marker-only topology. Add negative tests for raw recovery hash/commit flags, substituted marker bytes, wrong marker path, wrong marker content, and a commit containing anything besides the marker. Resolve permitted paths row-by-row and class-by-class; never authorize a directory, wildcard, sibling identity, or class union. Derive Git membership from Git plumbing and reject omissions, duplicates and extras across commits, index, worktree and untracked state. Keep Plan 02 and its sealed artifacts byte-immutable.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/validatePhase271PlanResult.test.ts tests/validateVisualReconciliation.test.ts --maxWorkers=1</automated>
  </verify>
  <done>Table-driven tests prove exactly allowlisted Plans 02, 03R, and 03-07 (including the dedicated 03R schema) and every Plan-03..07 identity/class union pass only with exact roots, pins, paths, packets and Git accounting; arbitrary IDs and all requested substitution, mismatch, borrowing and accounting negatives fail. The structured pre-work gate and downstream recovery resolver derive sealed hashes and marker-only commits internally; caller raw recovery hash/commit flags, substituted markers, wrong path/content, and non-marker-only commits fail closed, and every legacy entry mode is rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seal generic validator authority without reconciliation side effects</name>
  <files>frontend/tests/validatePhase271PlanResult.ts, frontend/tests/validatePhase271PlanResult.test.ts, frontend/tests/validateVisualReconciliation.ts, frontend/tests/validateVisualReconciliation.test.ts, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/attestations/27.1-03R-general-validator-manifest.json, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/plan-results/27.1-03R.json, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/27.1-03R-SUMMARY.md, .planning/phases/27.1-reconcile-21-non-warehouse-visual-failures-before-phase-27-c/evidence/terminal-markers/27.1-03R-general-validator.json</files>
  <behavior>
    - Manifest pins SHA-256 and Git blob IDs for exactly both validators and both tests, the corrected matrix hash, Plan-02 terminal marker/hash/commit, and all seven inherited roots.
    - READY_TO_SEAL result and committed SUMMARY use the generalized result validator; the payload commit contains exactly four validator/test files plus manifest, result and SUMMARY.
    - The immediate child commit contains only the COMPLETE 03R marker, which records payload/SUMMARY hashes, payload commit, Plan-02 dependency, eight typed roots and validator pins but no self-commit claim.
    - Browser, production, snapshot, route fixture, disposition and Plan-02 bytes remain unchanged.
  </behavior>
  <action>After focused tests pass, run the generic validator against the completed Plan-02 result and marker, create the closed 03R manifest and SUMMARY, then create READY_TO_SEAL result. Commit exactly the four validator/test files, manifest, result and SUMMARY as one payload. Create the COMPLETE marker only afterward and commit it alone as the payload commit's immediate child. Validate its marker-only topology and clean staged state. Do not invoke Playwright or edit any production, snapshot, fixture, disposition or completed Plan-02 artifact.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/validatePhase271PlanResult.test.ts tests/validateVisualReconciliation.test.ts --maxWorkers=1 &amp;&amp; node frontend/tests/validatePhase271PlanResult.ts --plan 27.1-02 --require COMPLETE --require-committed-summary --verify-commits --verify-validation-hashes &amp;&amp; node frontend/tests/validatePhase271PlanResult.ts --plan 27.1-03R --require COMPLETE --require-committed-summary --verify-commits --verify-validation-hashes --require-immediate-marker-only &amp;&amp; git diff --check &amp;&amp; git diff --cached --quiet</automated>
  </verify>
  <done>The generalized validator payload is sealed and followed immediately by a marker-only COMPLETE 03R authority; no browser/production/snapshot/fixture work occurred.</done>
</task>
</tasks>

<execution_contract>Plan 02 remains immutable. 03R starts only from its validated marker-only commit. The payload has exactly seven members: two validators, two tests, manifest, result and SUMMARY; its immediate child has exactly the 03R marker. Plans 03-07 require the 03R marker path, SHA-256, marker-only commit and four validator/test pins as an eighth non-collapsible typed root in addition to the seven inherited roots. Generic validation never converts an identity set into a path prefix: authorization is the exact union of each selected row's exact path list for its resolved class.</execution_contract>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| plan/result arguments → Git objects | Caller-selected IDs and hashes are untrusted until resolved through an explicit allowlist and Git. |
| identity/class → changed path | A valid identity must not authorize another identity or disposition class. |
| evidence accounting → completion | Claimed completeness must match committed and dirty Git membership exactly once. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-27.1-03R-01 | Elevation of privilege | path authorization | critical | mitigate | Exact row/class path tables; no prefixes, globs or cross-identity unions. |
| T-27.1-03R-02 | Tampering | roots/pins/markers | critical | mitigate | Eight typed roots, exact hashes/commits/blob pins and immediate marker-only topology. |
| T-27.1-03R-03 | Repudiation | Git accounting | high | mitigate | Enumerate cumulative/wave committed, staged, unstaged and untracked paths with duplicate/missing rejection. |
</threat_model>

<verification>Focused table-driven validator suites pass; generic CLI validates Plan 02 and sealed 03R; marker-only topology and Git hygiene pass; no browser command runs.</verification>
<success_criteria>Plans 03-07 have one sealed generic fail-closed validator authority that supports every declared identity/class union without widening path authorization.</success_criteria>
<output>Create `27.1-03R-SUMMARY.md`, seal `evidence/plan-results/27.1-03R.json`, then finish with `evidence/terminal-markers/27.1-03R-general-validator.json` as the sole member of the immediate marker commit.</output>
