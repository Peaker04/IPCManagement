---
phase: 28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - frontend/src/styles/index.css
  - frontend/src/components/common/InlineAlert.tsx
  - frontend/src/features/auth/pages/LoginPage.tsx
  - frontend/src/features/dashboard/pages/DashboardPage.tsx
  - frontend/src/features/auth/pages/LoginPage.feedback.test.tsx
  - frontend/src/components/common/InlineAlert.test.tsx
  - frontend/tests/uiAuditRemediationAttribution.test.ts
  - frontend/tests/uiAuditBaselineDelta.test.ts
autonomous: true
requirements: [PUX-03, PUX-04, PUX-05, PUX-06]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Before findings are consumed, the ignored baseline manifest SHA-256 is literally a0dcb1d2ea24a6ff562510d6f1dc1af3204480f1607e870ea2fa3214ac648c51 and canonical-combined SHA-256 is literally b72c9a17e783c11ce49d6ec5e232afd5d6be5440abac95972fb6711c7ae05a5a; tracked 28-01 plan/summary remain byte-identical at eef897ba23cb5be8ac3ee019c11acd9ebf9ce8ad3e1850f5f7b769cdc426200d and fd7e45d65c1b503c121d0cba14bfee597181f4c7edd235c3b2341fa233a9d342 with zero Git diff."
    - "The sealed 1,453 FAIL findings are grouped by exact rule, route, state, viewport, actual value, and lowestOwner before a shared edit; NEEDS_EVIDENCE is never classified as a defect or remediation authority (PUX-05)."
    - "Login has one main landmark containing its single H1, and its measured foreground/background pair meets the UI-SPEC contrast minimum without changing its identity or flow (PUX-04, PUX-06)."
    - "A shared contrast/control seam changes only when selector and computed-token attribution prove that the same lowest rendered owner causes multiple sealed FAIL groups; otherwise the plan leaves that group for its route owner (PUX-03, PUX-05)."
    - "Only the existing shadcn/Base UI stack and spacing scale 4/8/16/24/32/48/64 are used; route, cache, API, permission, business, warehouse-ID, and database behavior remain unchanged (PUX-03, PUX-06)."
    - "All seven sealed DashboardPage A11Y-01 color-contrast FAIL identities have an exact disposition: remediate DashboardPage when its rendered node is route-owned, or attribute it to a lower shared owner only after identical selector/token provenance is captured (PUX-05)."
  artifacts:
    - path: frontend/tests/uiAuditRemediationAttribution.test.ts
      provides: sealed-FAIL-only owner grouping and selector/token attribution guard
    - path: frontend/tests/uiAuditBaselineDelta.test.ts
      provides: immutable baseline hash/count guard and remediation-delta schema
    - path: frontend/src/features/auth/pages/LoginPage.tsx
      provides: corrected login landmark and contrast presentation
    - path: frontend/src/components/common/InlineAlert.tsx
      provides: only a selector-proven shared contrast/control correction when attribution passes
    - path: frontend/src/features/dashboard/pages/DashboardPage.tsx
      provides: route-owned remediation for the seven Dashboard contrast identities when no lower shared seam is proven
  key_links:
    - from: frontend/test-results/ui-audit-phase28-baseline/canonical-combined.json
      to: frontend/tests/uiAuditRemediationAttribution.test.ts
      via: read-only extraction of verdict=FAIL with required lowestOwner; NEEDS_EVIDENCE rows are excluded and count-locked
    - from: frontend/tests/uiAuditRemediationAttribution.test.ts
      to: frontend/src/styles/index.css and frontend/src/components/common/InlineAlert.tsx
      via: exact selector/computed-token evidence required before shared ownership is accepted
    - from: frontend/src/features/auth/pages/LoginPage.tsx
      to: frontend/src/features/auth/pages/LoginPage.feedback.test.tsx
      via: landmark, accessible-name, submission, and invalid-credential regressions
---

<objective>
Create the remediation foundation from the sealed baseline, then ship the narrow login tracer and only selector-proven shared contrast/control corrections.

Purpose: Convert 1,453 repeated FAIL rows into auditable lowest-owner groups without laundering `NEEDS_EVIDENCE`, while proving the first production correction end to end.
Output: Immutable baseline/delta guards, exact owner attribution, corrected login landmark/contrast, and bounded shared semantic-token or InlineAlert fixes only where the rendered evidence proves shared ownership.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@LESSONS.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-CONTEXT.md
@.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-SPEC.md
@.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-UI-SPEC.md
@.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-01-SUMMARY.md
@frontend/test-results/ui-audit-phase28-baseline/manifest.json
@frontend/test-results/ui-audit-phase28-baseline/canonical-combined.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace sealed FAIL rows to owners and correct Login end to end</name>
  <read_first>frontend/test-results/ui-audit-phase28-baseline/manifest.json, frontend/test-results/ui-audit-phase28-baseline/canonical-combined.json, frontend/tests/uiAuditBaselineReconciliation.ts, frontend/src/features/auth/pages/LoginPage.tsx, frontend/src/features/auth/pages/LoginPage.feedback.test.tsx, frontend/src/styles/index.css</read_first>
  <files>frontend/tests/uiAuditRemediationAttribution.test.ts, frontend/tests/uiAuditBaselineDelta.test.ts, frontend/src/features/auth/pages/LoginPage.tsx, frontend/src/features/auth/pages/LoginPage.feedback.test.tsx</files>
  <behavior>
    - Baseline seal/counts are exactly 2,142 identities, 32 findings per identity, FAIL 1,453, PASS 4,763, NOT_APPLICABLE 15,120, NEEDS_EVIDENCE 47,208, UNRESOLVED 0, ownerless FAIL 0.
    - Grouping consumes only verdict FAIL and preserves each exact six-part identity, ruleId, expected, actual, severity, and lowestOwner; every NEEDS_EVIDENCE row remains excluded.
    - `/login|login-form|populated|anonymous|*|login-form` changes HIER-01 from mainCount 0 to one H1 inside one main and A11Y-01 color-contrast to zero serious/critical findings at D5+R2.
    - Existing submit, disabled/pending, and invalid-credential behavior is unchanged.
  </behavior>
  <action>Write failing tests first. Before parsing findings, require literal SHA-256 equality for `manifest.json` = `a0dcb1d2ea24a6ff562510d6f1dc1af3204480f1607e870ea2fa3214ac648c51` and `canonical-combined.json` = `b72c9a17e783c11ce49d6ec5e232afd5d6be5440abac95972fb6711c7ae05a5a`; require tracked 28-01 plan/summary SHA-256 = `eef897ba23cb5be8ac3ee019c11acd9ebf9ce8ad3e1850f5f7b769cdc426200d` / `fd7e45d65c1b503c121d0cba14bfee597181f4c7edd235c3b2341fa233a9d342` and `git diff --exit-code` for those two paths. Hash and read the sealed generated files but never write, rename, regenerate, or normalize them. Build a deterministic FAIL grouping keyed by `ruleId|route|region|state|viewport|lowestOwner|actual`, with an assertion that all 1,453 owner-bearing FAIL rows are represented and all 47,208 NEEDS_EVIDENCE findings remain honest terminal non-PASS dispositions outside remediation authority. Define the remediation-delta record now: baseline identity plus before/after verdict and production paths, rejecting missing owners, changed identity fields, snapshot/baseline paths, threshold edits, or a NEEDS_EVIDENCE source. Then make the production tracer in `LoginPage`: use the semantic `main` landmark as the existing login surface container and map the exact measured low-contrast text/token to an existing semantic IPC color that reaches at least 4.5:1. Preserve copy, form submission, focus order, visual identity, and all auth behavior. Do not add a wrapper that creates nested landmarks or change layout spacing outside 4/8/16/24/32/48/64.</action>
  <acceptance_criteria>The immutable baseline hash and totals pass; all 1,453 FAILs are grouped once; no NEEDS_EVIDENCE row is promotable; Login reports one main/one H1 and zero contrast failure across all seven baseline viewports while its existing behavior tests remain green.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiAuditRemediationAttribution.test.ts tests/uiAuditBaselineDelta.test.ts src/features/auth/pages/LoginPage.feedback.test.tsx --maxWorkers=1 &amp;&amp; NODE_OPTIONS=--max-old-space-size=4096 npm exec -w frontend -- playwright test tests/ui-audit.spec.ts --grep "login-form.*populated" --headed --workers=1</automated>
  </verify>
  <done>The sealed artifact is an immutable authorization source and the first corrected real route passes its exact deterministic findings end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct only selector-proven shared contrast and control seams</name>
  <read_first>frontend/tests/uiAuditRemediationAttribution.test.ts, frontend/src/styles/index.css, frontend/src/components/common/InlineAlert.tsx, frontend/src/components/common/index.ts, frontend/tests/ui-audit.spec.ts, frontend/test-results/ui-audit-phase28-baseline/canonical-combined.json</read_first>
  <files>frontend/tests/uiAuditRemediationAttribution.test.ts, frontend/src/styles/index.css, frontend/src/components/common/InlineAlert.tsx, frontend/src/components/common/InlineAlert.test.tsx, frontend/src/features/dashboard/pages/DashboardPage.tsx</files>
  <behavior>
    - Repeated `color-contrast` rows across Dashboard, Admin Data, Advanced Settings, Approval Rules, Approvals, Chef, Coordination, Purchasing, Reports, Warehouse, Weekly Menu, and Login are attributable to one shared selector/token only when captured node selector and computed token are identical.
    - Shared correction preserves semantic danger/warning/info/status meanings and text contrast >=4.5:1, non-text/focus contrast >=3:1.
    - If an owner group does not share that exact rendered seam, no shared production edit is authorized for it and its route owner remains in the attribution output.
    - The seven DashboardPage identities are exactly: dashboard-shift-status/error-no-data/320x900@200%, dashboard-shift-status/populated/1440x900, dashboard-shift-status/truly-empty/320x900@200%, dashboard-shift-status/truly-empty/320x900, dashboard-workflow-exceptions/no-results/320x900, dashboard-workflow-exceptions/populated/1440x900, and dashboard-workflow-exceptions/populated/1920x1080; each reaches zero color-contrast findings.
  </behavior>
  <action>Extend the attribution test with a focused headed capture that records axe target selectors, ancestor `data-ui-owner`, computed foreground/background, and resolving CSS custom properties for every distinct sealed `color-contrast`/`button-name` group. A count coincidence is insufficient. Reconcile coarse page owners dynamically: map the captured target to the deepest checked-in component path that renders it and authorize that lower path; retain the page owner only when no lower component boundary is proven. If two or more route groups resolve to the same shared semantic selector/token or `InlineAlert` control, adjust that lowest shared owner using existing semantic variables and the finite spacing/type contract. Explicitly disposition all seven DashboardPage rows listed in behavior: fix `DashboardPage.tsx` when route-owned, or cite the exact shared selector/token and shared file when lower-owner attribution is proven. Keep normal state neutral and color scarce; do not replace the palette, flatten stylesheet layers, add raw colors, change component dimensions, or modify a route-local selector through a global override. Groups not owned by this plan pass with their reconciled route/component path to Plan 28-03.</action>
  <acceptance_criteria>Every shared edit cites the exact baseline groups and matching selector/token evidence; all seven Dashboard rows have a route or proven lower-owner remediation and reach PASS; component regressions prove names and semantic variants; no unrelated route selector, raw color, unsupported spacing, or layout geometry changes.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiAuditRemediationAttribution.test.ts src/components/common/InlineAlert.test.tsx --maxWorkers=1 &amp;&amp; npm run lint -w frontend</automated>
  </verify>
  <done>Shared contrast/control defects proven to share one rendered owner are corrected once; all others retain exact route-owner assignments.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze the Wave 2 delta and prove architecture invariants</name>
  <read_first>frontend/tests/uiAuditBaselineDelta.test.ts, frontend/test-results/ui-audit-phase28-baseline/manifest.json, frontend/package.json, frontend/route-budgets.json, frontend/src/app/api/apiSlice.ts, frontend/src/shared/api/contracts/schema.ts</read_first>
  <files>frontend/tests/uiAuditBaselineDelta.test.ts</files>
  <action>Record the Wave 2 production paths and exact authorized baseline group keys in the delta test. Add negative cases proving changes under snapshots, visual baseline assets, thresholds, route budgets, backend, migrations, generated API contracts, API/cache-key owners, or warehouse identity contracts are rejected. Assert one RTK API slice remains the sole `createApi` owner, active-first plus preserve-visited tab contracts remain unchanged, and no `warehouseId`/`receivingWarehouseId` contract path is in the production diff. This is a regression guard, not a new renderer, checker framework, or broad baseline.</action>
  <acceptance_criteria>Wave 2 changed paths are a subset of its authorized UI/test paths; baseline hashes are unchanged; route budget values, visual thresholds, snapshots, API/cache/business contracts, backend/DB, and warehouse identity contracts are unchanged.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiAuditBaselineDelta.test.ts tests/uiAuditRemediationAttribution.test.ts --maxWorkers=1 &amp;&amp; npm run test:architecture-growth &amp;&amp; npm run check:architecture-growth &amp;&amp; npm run depcruise -w frontend &amp;&amp; npm run check:route-budgets -w frontend &amp;&amp; git diff --check</automated>
  </verify>
  <done>The foundation delta is bounded, owner-authorized, and safe for the feature-owner rollout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Sealed ignored artifact → tracked remediation | Generated evidence authorizes edits but must remain immutable and cannot be silently normalized. |
| Axe target → shared owner attribution | Repeated failures may come from different selectors; only exact selector/token provenance permits a shared edit. |
| UI presentation → API/cache/business contracts | Styling and semantics must not alter route, RTK Query, authorization, workflow, or warehouse identity behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-28-02-01 | Tampering | sealed baseline | high | mitigate | Hash/count lock and read-only path guard; remediation writes to a separate delta artifact only. |
| T-28-02-02 | Spoofing | shared owner attribution | high | mitigate | Require exact axe selector, `data-ui-owner`, computed colors, and resolving token before shared ownership. |
| T-28-02-03 | Tampering | NEEDS_EVIDENCE disposition | high | mitigate | Verdict filter and negative tests reject promotion or defect classification. |
| T-28-02-04 | Elevation of Privilege | warehouse/API contract | high | mitigate | Changed-path and literal contract guards; no generated schema, API slice, permission, or backend changes. |
| T-28-02-SC | Tampering | package installs | high | accept | No package-manager install task exists in this plan. |
</threat_model>

<verification>
Run the focused unit and headed Login tracer, shared seam tests, lint, architecture, unchanged route-budget gate, immutable baseline hash/count checks, scoped production diff review, secret/stub scan, and `git diff --check`. Generated reconciliation output must use a new remediation directory and must not replace `ui-audit-phase28-baseline`.
</verification>

<success_criteria>
- The 1,453 sealed FAIL rows are losslessly grouped to lowest owners; 47,208 NEEDS_EVIDENCE rows remain evidence-only.
- Login main/contrast failures are corrected at `LoginPage` with no auth behavior change.
- Shared seam edits exist only where selector/token attribution proves shared ownership.
- Existing identity, shadcn/Base UI, spacing/type/color contracts, one RTK API slice, active-first/preserve-visited behavior, route budgets, visual thresholds, and warehouse IDs are preserved.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 28 remediation | Lowest-owner production UI remediation after sealed baseline | Tasks 1-3 | COVERED |
| REQ | PUX-03 | Finite token/spacing/type/contrast convergence | Tasks 1-2 | COVERED |
| REQ | PUX-04 | Landmark, accessible control and contrast contracts | Tasks 1-2 | COVERED |
| REQ | PUX-05 | Shared root-cause grouping, first remediation wave | Tasks 1-3 | COVERED |
| REQ | PUX-06 | Regression/static/architecture gates | Tasks 1 and 3 | COVERED |
| RESEARCH | Findings 1,4-6,11 | accessibility, hierarchy, semantic tokens and async presentation | Tasks 1-2 | COVERED where sealed FAIL exists |
| CONTEXT | Locked UI/harness decisions | Existing identity/stack, exact FAIL owner, no screenshot oracle | Tasks 1-3 | COVERED |
| CONTEXT | Warehouse invariant implementation | canonical active warehouse and runtime enforcement | Plan 28-05 | SCOPED TO DEPENDENT PLAN |

<output>
Create `.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-02-SUMMARY.md` when done.
</output>
