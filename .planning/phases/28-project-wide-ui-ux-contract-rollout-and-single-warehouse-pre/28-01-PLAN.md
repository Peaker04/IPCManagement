---
phase: 28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - package-lock.json
  - frontend/tests/uiAuditInventory.ts
  - frontend/tests/uiAuditContract.ts
  - frontend/tests/uiAuditOracleRegistry.ts
  - frontend/tests/uiAuditFixtureRegistry.ts
  - frontend/tests/uiAuditEvidence.ts
  - frontend/tests/uiAuditInventory.test.ts
  - frontend/tests/uiAuditHierarchyTokenContainer.test.ts
  - frontend/tests/uiAuditTableQueryInteraction.test.ts
  - frontend/tests/uiAuditAccessibilityResponsive.test.ts
  - frontend/tests/uiAuditSourceLiterals.test.ts
  - frontend/tests/ui-audit.spec.ts
autonomous: true
requirements: [PUX-01, PUX-02, PUX-03, PUX-04, PUX-05, PUX-06]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 5
  confidence: low
must_haves:
  truths:
    - "One closed inventory expands all 13 routed pages and every declared route state, actor, viewport, region state, table/column contract, and lowest owner exactly once; missing, duplicate, extra, or silently omitted identities fail before capture (PUX-01)."
    - "All 32 approved deterministic rule IDs are implemented in bounded sequential cohorts through one oracle registry, one paired fixture registry, the existing ui-audit.spec.ts runner, and one evidence schema/report family (PUX-02..04)."
    - "A read-only source-aware Vitest checker reports production spacing/color literals without modifying production source, and A11Y-01 uses only @axe-core/playwright rather than a bespoke accessibility scanner."
    - "Two controlled headed whole-matrix runs preserve and compare runFingerprint and priorManifestHash; the second manifest seals only when identity, deterministic verdict, fixture, network, record, and artifact closure agree."
    - "No production source, snapshot, backend, database, migration, visual threshold, route budget, or warehouse singleton behavior changes in this plan."
  artifacts:
    - path: frontend/tests/uiAuditInventory.ts
      provides: exact route/state/actor/viewport/owner and region/table state registries
    - path: frontend/tests/uiAuditContract.ts
      provides: shared identities, verdicts, rule inputs/findings, network classification, evidence and manifest schemas
    - path: frontend/tests/uiAuditOracleRegistry.ts
      provides: the sole registry of exactly the 32 deterministic UI-SPEC rule IDs and explicit expected values
    - path: frontend/tests/uiAuditFixtureRegistry.ts
      provides: exact one-known-bad/one-known-clean fixture bijection for every rule and exact region-fixture bijection
    - path: frontend/tests/uiAuditEvidence.ts
      provides: controlled record writer, hashing, run fingerprinting, prior-manifest comparison and fail-closed sealing
    - path: frontend/tests/uiAuditSourceLiterals.test.ts
      provides: existing-style read-only production source checker for unsupported spacing/color literals
    - path: frontend/tests/ui-audit.spec.ts
      provides: existing-runner headed captures for the complete baseline matrix
  key_links:
    - from: frontend/tests/uiAuditInventory.ts
      to: frontend/src/lib/routeConfig.ts and frontend/src/routes/AppRouter.tsx
      via: source-aware INV-01 comparison to exactly 13 page routes; wildcard redirect is excluded
    - from: frontend/tests/uiAuditFixtureRegistry.ts
      to: frontend/tests/uiAuditOracleRegistry.ts
      via: ruleId bijection requiring exactly one bad and one clean fixture per registered deterministic rule
    - from: frontend/tests/uiAuditFixtureRegistry.ts
      to: frontend/tests/uiAuditInventory.ts
      via: route|regionId|state|actor|viewport|lowest-owner fixture bijection requiring one fixture or one explicit N/A(reason) for every expanded six-part identity
    - from: frontend/tests/ui-audit.spec.ts
      to: frontend/tests/uiAuditEvidence.ts
      via: existing Playwright runner and test-results/ui-audit-*.json report path
    - from: frontend/tests/uiAuditEvidence.ts
      to: test-results/ui-audit-phase28-baseline/manifest.json
      via: preserved prior manifest, priorManifestHash, stable runFingerprint and second-run seal comparison
---

<objective>
Extend the Phase 27 measurement harness in five bounded sequential cohorts, then run and seal the whole-web read-only baseline before any production UI edit.

Purpose: Make every approved machine-decidable Phase 28 rule executable and independently reviewable without turning the work into one monolithic implementation or a parallel audit framework.
Output: Shared inventory/schema/registries, four bounded oracle cohorts, source-literal checker, axe integration, existing-runner evidence output, and a repeatable two-run sealed baseline.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-SPEC.md
@.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-CONTEXT.md
@.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-UI-SPEC.md
@.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-STANDARDS-RESEARCH.md
@.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-SOURCE-INVENTORY.md
@.planning/phases/27-warehouse-data-workspace-contract-pilot/27-01-SUMMARY.md
@frontend/tests/ui-audit.spec.ts
@frontend/tests/warehouseDataWorkspaceContract.ts
@frontend/tests/warehouseEvidenceCollector.ts
@frontend/src/lib/routeConfig.ts
@frontend/src/routes/AppRouter.tsx
@frontend/package.json
@package-lock.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the inventory, shared schema, registries, and one end-to-end tracer</name>
  <files>frontend/tests/uiAuditInventory.ts, frontend/tests/uiAuditContract.ts, frontend/tests/uiAuditOracleRegistry.ts, frontend/tests/uiAuditFixtureRegistry.ts, frontend/tests/uiAuditInventory.test.ts</files>
  <behavior>
    - The clean `/login|ready|anonymous|1920x1080|LoginPage` identity and `login-form|populated` region fixture validate and execute INV-01 through the shared registries.
    - The route registry contains exactly 13 pages (12 protected and `/login`), D5 plus R2 viewports, declared actors/states/regions/tables/owners, and excludes only the wildcard redirect.
    - Every region/state expansion has exactly one executable fixture identity or one explicit `N/A(reason)`; missing, duplicate, extra, or undeclared entries reject before browser work.
    - Evidence schemas accept only PASS, FAIL, NOT_APPLICABLE, NEEDS_EVIDENCE, and UNRESOLVED; FAIL requires expected/actual/severity/lowestOwner and PASS requires complete measured fields.
  </behavior>
  <action>Start with failing inventory tests. Create literal, test-owned registries rather than a UI DSL or renderer. Parse `routeConfig.ts` and `AppRouter.tsx` as source data for route closure. Encode the UI-SPEC route inventory and independent canonical region-state registry, including table IDs, column priority/type/alignment, default sort, filters, pagination, actions and refresh dispositions needed by later cohorts. Define a sole oracle registry keyed by exactly the 32 UI-SPEC stable IDs and a sole fixture registry whose validation enforces: registered rule IDs equal the exact 32-ID UI-SPEC set and fixture rule IDs; each rule has exactly one known-bad and one known-clean key; every expanded `route|regionId|state|actor|viewport|lowest-owner` identity has exactly one fixture or explicit N/A reason; no orphan fixture exists. Define shared report/manifest/network types now so later tasks extend implementations without changing schemas. Implement only INV-01 and a clean login tracer input in this task. Do not touch production files or create a second runner/report format. Commit atomically as `test(28-01): prove UI inventory and evidence schema`.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiAuditInventory.test.ts --maxWorkers=1</automated>
  </verify>
  <done>The closed inventory and exact rule/fixture/region bijections are executable, one tracer passes end to end, and all tested omissions/duplicates/extras/schema gaps fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement hierarchy, token, container, and source-literal cohort</name>
  <files>frontend/tests/uiAuditOracleRegistry.ts, frontend/tests/uiAuditFixtureRegistry.ts, frontend/tests/uiAuditHierarchyTokenContainer.test.ts, frontend/tests/uiAuditSourceLiterals.test.ts, frontend/tests/ui-audit.spec.ts</files>
  <behavior>
    - HIER-01, HIER-02, TOK-SP-01, TOK-TY-01, TOK-CO-01, CONT-01, CONT-02 and ORDER-01 use the exact UI-SPEC counts, sets, contrast minima and geometry tolerances.
    - Each cohort rule's known-bad fixture yields exactly that rule ID and its known-clean fixture yields zero findings.
    - The source checker reads production CSS/TS/TSX without writing it, reports unsupported spacing/color literals with file, line, property/token and owner/disposition, and cannot manufacture rendered PASS.
  </behavior>
  <action>Implement this eight-rule cohort only, using the shared oracle and fixture registries. Extend the existing `ui-audit.spec.ts` collection helpers for headings, named regions, primary actions, computed typography/spacing/colors, semantic container depth, hover/focus geometry and visible order; screenshots remain attachments only. Add a Vitest source-aware checker following existing project checker style: scan tracked production CSS/TS/TSX, distinguish spacing properties from non-spacing geometry, recognize semantic CSS-variable use, and emit deterministic source findings for raw/unsupported spacing and color literals. It is read-only and complementary to rendered TOK evidence; source and rendered artifacts must both exist before PASS. Avoid blanket regex claims that cannot identify property context, comments, tests, generated contracts or approved explicit dispositions. Do not remediate reported production literals. Commit atomically as `test(28-01): add hierarchy token and container oracles`.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiAuditInventory.test.ts tests/uiAuditHierarchyTokenContainer.test.ts tests/uiAuditSourceLiterals.test.ts --maxWorkers=1 &amp;&amp; NODE_OPTIONS=--max-old-space-size=4096 npm exec -w frontend -- playwright test tests/ui-audit.spec.ts --grep "Phase 28 hierarchy token container cohort" --headed --workers=1</automated>
  </verify>
  <done>All eight rules have exact bad/clean proof, the headed cohort emits schema-valid records, and the source checker reports production facts without editing source or claiming rendered conformance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement table, query-state, and interaction cohort</name>
  <files>frontend/tests/uiAuditOracleRegistry.ts, frontend/tests/uiAuditFixtureRegistry.ts, frontend/tests/uiAuditTableQueryInteraction.test.ts, frontend/tests/uiAuditInventory.ts, frontend/tests/ui-audit.spec.ts</files>
  <behavior>
    - TABLE-01..03, QUERY-01..02, FILTER-01, SORT-01, COL-01, BADGE-01, PAGE-01, MUT-01 and REFRESH-01 execute with exact UI-SPEC expected fields and tolerances.
    - Every registered table/query region and applicable canonical state is driven by exactly one fixture identity; explicit N/A states retain their reason and never become PASS.
    - Mutation observations remain intercepted and non-persistent: duplicate-submit, rollback, pending copy, CLS, live region and focus are measured without an escaped business write.
  </behavior>
  <action>Implement this twelve-rule cohort only. Consume the region registry rather than inventing route-local state lists. Extend existing fixture API builders and `ui-audit.spec.ts` drivers for native table semantics/name/scope, alignment/tabular numerals, local focusable overflow/essential columns/row density, exclusive query markers, loading/refreshing/empty/no-results/error/stale transitions, persistent region identity/geometry, filters/focus/live counts, aria-sort/default sort, column priority/disclosure, badges, pagination, mutation pending/failure rollback, and paused/resumed bounded refresh. Every expanded `route|regionId|state|actor|viewport|lowest-owner` identity must reconcile to its fixture bijection before capture. Mutation-like requests must be intercepted, recorded, and prevented from reaching a backend. Missing transition facts produce NEEDS_EVIDENCE rather than PASS. Commit atomically as `test(28-01): add table query and interaction oracles`.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiAuditInventory.test.ts tests/uiAuditTableQueryInteraction.test.ts --maxWorkers=1 &amp;&amp; NODE_OPTIONS=--max-old-space-size=4096 npm exec -w frontend -- playwright test tests/ui-audit.spec.ts --grep "Phase 28 table query interaction cohort" --headed --workers=1</automated>
  </verify>
  <done>All twelve rules pass their bad/clean fixture contracts, every applicable region state has one fixture, and headed records contain no escaped mutation or guessed query-state PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add axe and implement accessibility, responsive, motion, performance, and warehouse-presentation cohort</name>
  <files>frontend/package.json, package-lock.json, frontend/tests/uiAuditOracleRegistry.ts, frontend/tests/uiAuditFixtureRegistry.ts, frontend/tests/uiAuditAccessibilityResponsive.test.ts, frontend/tests/ui-audit.spec.ts</files>
  <behavior>
    - A11Y-01 uses AxeBuilder from `@axe-core/playwright` and asserts serious/critical counts exactly zero; no bespoke accessibility scanner substitutes for axe.
    - A11Y-02..03, RESP-01..02, RESP-WH-01, MOTION-01, PERF-01 and presentation-only WH-01..03 use exact UI-SPEC values and bad/clean proofs.
    - Missing production perf evidence and server-side warehouse zero/one/multiple/tampered enforcement remain NEEDS_EVIDENCE; browser presentation evidence cannot upgrade them.
  </behavior>
  <action>Before install, run `npm view @axe-core/playwright name version repository.url dist.integrity --json` and require the official scoped package metadata; install exactly `@axe-core/playwright` as a frontend devDependency with the lockfile, adding no other direct dependency. Import `AxeBuilder` in the existing Playwright spec and execute WCAG 2.2 A/AA tags for A11Y-01; retain the UI-SPEC's separate accessible-name/visible-label evidence and do not build a substitute accessibility engine. Implement this remaining eleven-rule cohort: keyboard/focus/targets, modal behavior, document overflow/overlap/essential visibility, 320px and 200%-text reflow, warehouse 1366/1365 relation, reduced motion/duration/property limits, referenced perf-probe metrics, and presentation-only warehouse scope behavior. Use the shared fixture registry and exact bad/clean bijection. PERF-01 without a current referenced artifact is NEEDS_EVIDENCE. WH server enforcement remains NEEDS_EVIDENCE until a later backend plan; do not simulate it as proof or implement singleton behavior. Commit dependency/lockfile/integration and cohort tests atomically as `test(28-01): add axe and final deterministic oracle cohort`.</action>
  <verify>
    <automated>npm view @axe-core/playwright name version repository.url dist.integrity --json &amp;&amp; npm ls @axe-core/playwright -w frontend --depth=0 &amp;&amp; npm run test:unit -w frontend -- --run tests/uiAuditAccessibilityResponsive.test.ts --maxWorkers=1 &amp;&amp; NODE_OPTIONS=--max-old-space-size=4096 npm exec -w frontend -- playwright test tests/ui-audit.spec.ts --grep "Phase 28 accessibility responsive motion performance cohort" --headed --workers=1</automated>
  </verify>
  <done>The only new direct package is the official `@axe-core/playwright` frontend devDependency, all eleven cohort rules have paired proof, axe runs inside the existing runner, and absent perf/server evidence remains explicitly non-PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Execute twice and seal the complete read-only whole-web baseline</name>
  <files>frontend/tests/uiAuditEvidence.ts, frontend/tests/uiAuditContract.ts, frontend/tests/ui-audit.spec.ts, frontend/package.json, frontend/tests/uiAuditInventory.test.ts</files>
  <behavior>
    - The baseline covers every expanded route/state/actor/viewport and every applicable region fixture exactly once across D5 and R2, with exactly all 32 UI-SPEC deterministic rule IDs reconciled.
    - Network classification ignores only same-origin document/static assets (`document`, script, stylesheet, font, image, favicon/source-map assets); every observed API or other non-static request is recorded and must use GET.
    - Run 1 writes a stable runFingerprint and manifest hash; run 2 preserves run 1 as the prior manifest, records priorManifestHash, recomputes the same runFingerprint, and refuses SEALED unless identities, fixture keys and deterministic verdicts match.
  </behavior>
  <action>Add `test:ui-baseline` to the frontend scripts as the single headed one-worker entry point for the complete Phase 28 describe. Use controlled synthetic actors/data and existing runner helpers. Define GET-only precisely: observe every browser request; same-origin document and static asset requests may be excluded from the read-only request ledger, but all `/api/` requests and every non-static request regardless of origin are ledger entries and must be GET. Any POST/PUT/PATCH/DELETE/unknown method is an escaped mutation and blocks sealing even if intercepted. Write validated per-identity records beneath `test-results/ui-audit-phase28-baseline/records/`. Before run 2, preserve run 1's manifest as `manifest.prior.json`, compute its SHA-256, and pass that value as `priorManifestHash`; never overwrite it before comparison. Compute `runFingerprint` from contract/schema versions, source commit, sorted expanded identities, sorted fixture keys, sorted rule IDs/expected values, viewport definitions and deterministic fixture-data version—exclude timestamps, output paths and artifact hashes. Run 2 must match run 1 fingerprint, identity set, fixture bijection and deterministic verdicts; record hashes may differ only for explicitly nondeterministic evidence fields excluded from verdict inputs. Atomically write `manifest.json` with counts/hashes and `SEALED` only after schema/readback/hash, exact region-fixture bijection, all-rule coverage, network, owner, credential scan, and comparison gates pass. FAIL requires lowestOwner; absent measurements remain NEEDS_EVIDENCE/UNRESOLVED; qualitative review remains outside deterministic PASS. Do not commit generated test-results. Commit runner/schema/script changes atomically as `test(28-01): seal whole-web read-only baseline`.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiAuditInventory.test.ts tests/uiAuditHierarchyTokenContainer.test.ts tests/uiAuditSourceLiterals.test.ts tests/uiAuditTableQueryInteraction.test.ts tests/uiAuditAccessibilityResponsive.test.ts --maxWorkers=1 &amp;&amp; NODE_OPTIONS=--max-old-space-size=4096 npm run test:ui-baseline -w frontend &amp;&amp; NODE_OPTIONS=--max-old-space-size=4096 npm run test:ui-baseline -w frontend &amp;&amp; node --input-type=module -e "import fs from 'node:fs'; import crypto from 'node:crypto'; const d='frontend/test-results/ui-audit-phase28-baseline'; const m=JSON.parse(fs.readFileSync(d+'/manifest.json')); const p=fs.readFileSync(d+'/manifest.prior.json'); const h=crypto.createHash('sha256').update(p).digest('hex'); if(m.sealStatus!=='SEALED'||m.priorManifestHash!==h||!m.runFingerprint||m.runFingerprint!==JSON.parse(p).runFingerprint||m.missingIdentityCount||m.duplicateIdentityCount||m.extraIdentityCount||m.regionFixtureMismatchCount||m.invalidRecordCount||m.nonGetObservedRequestCount||m.ownerlessFailCount||m.guessedPassCount) process.exit(1); console.log(m.sealStatus,m.runFingerprint,m.identityCount,m.verdictTotals);" &amp;&amp; git diff --check</automated>
  </verify>
  <done>Two complete headed runs share one stable fingerprint, the second references the preserved first-manifest hash, and the SEALED manifest reports zero identity/fixture/schema/network/owner/guessed-PASS failures; only declared test/package files changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Synthetic fixtures → production routes | Test actors and payloads enter real frontend route code; no business mutation may escape. |
| Production source → literal checker | Source is untrusted input and is read only; parser uncertainty cannot become PASS. |
| Browser/network → evidence | Requests, DOM text, ARIA, console output and URLs may contain unsafe or sensitive data. |
| Run 1 → run 2 seal | The prior manifest must be immutable during comparison so a self-comparison cannot falsely prove repeatability. |
| Evidence → remediation authorization | Only schema-valid exact FAIL with a lowest owner may authorize later production work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-28-01 | Tampering | inventory/fixture registries | high | mitigate | Exact route, rule-fixture and six-part `route|regionId|state|actor|viewport|lowest-owner` fixture bijections; reject missing/extra/duplicate entries before capture. |
| T-28-02 | Tampering | two-run seal | high | mitigate | Preserve manifest.prior.json, hash it before run 2, compare priorManifestHash and stable runFingerprint. |
| T-28-03 | Spoofing | owner/actor | high | mitigate | Closed actor and lowest-owner registries; reject ownerless FAIL and undeclared actors. |
| T-28-04 | Information Disclosure | evidence writer | high | mitigate | Synthetic records, controlled relative paths, no storage/auth capture, credential-like content scan before sealing. |
| T-28-05 | Tampering | browser network | high | mitigate | Observe all requests; exclude only defined same-origin static/document assets; require GET for every API/non-static request. |
| T-28-06 | Supply Chain | @axe-core/playwright | high | mitigate | Allow exactly the official scoped frontend devDependency, verify npm metadata/integrity, lock it, and reject other direct additions. |
| T-28-07 | Denial of Service | complete matrix | medium | mitigate | One headed worker, 4096MB heap, deterministic fixtures and cohort-level focused commands before whole runs. |
</threat_model>

<verification>
Execute cohorts in order and commit after each green boundary. Then run all five unit files and the complete headed baseline twice. Independently read `manifest.prior.json` and `manifest.json`, recompute the prior hash, compare fingerprints, identity/fixture/verdict sets and network counts. Inspect one record for every verdict. Confirm axe is imported from `@axe-core/playwright`, package/lock diff contains no other direct dependency, source checker is read-only, screenshots are non-oracular, and absent perf/backend warehouse evidence is not PASS. Finish with `git diff --check`, scoped changed-path review, secret/stub scan, and `git diff --cached --name-only` after executor commits.
</verification>

<success_criteria>
- PUX-01: exact route inventory and six-part `route|regionId|state|actor|viewport|lowest-owner` inventory plus exact fixture bijections have zero gaps or duplicates.
- PUX-02..04: exactly all 32 UI-SPEC deterministic rule IDs execute in four bounded cohorts through shared registries, existing runner and one report schema.
- A11Y-01 is powered by the sole approved new dependency `@axe-core/playwright`; no bespoke accessibility scanner exists.
- PUX-05: only owned exact baseline FAIL findings can authorize later remediation.
- PUX-06: two headed runs prove stable fingerprint/manifest provenance and seal with zero escaped non-GET API/non-static requests.
- No production, snapshot, backend, database, migration, visual-threshold, route-budget, or warehouse-singleton change exists.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 28 first slice | Extend one harness and seal baseline before production edits | Tasks 1–5 | COVERED |
| REQ | PUX-01 | Exact route/state/region/owner inventory | Tasks 1 and 5 | COVERED |
| REQ | PUX-02 | Explicit deterministic rules in existing harness | Tasks 1–5 | COVERED |
| REQ | PUX-03 | Rendered plus source-aware spacing/type/color measurement | Task 2 | COVERED |
| REQ | PUX-04 | Container/table/query/accessibility/responsive coverage | Tasks 2–4 | COVERED |
| REQ | PUX-05 | Baseline authorization boundary | Task 5 | COVERED |
| REQ | PUX-06 | Repeatable headed/static evidence without weakened oracle | Task 5 | COVERED for baseline slice |
| RESEARCH | Findings 1–11 | WCAG/table/reflow/token/container/filter/query/passive warehouse measurement | Tasks 2–4 | COVERED |
| CONTEXT | Locked sequence/harness | Read-only baseline before edits; exact verdict/owner; existing runner | Tasks 1–5 | COVERED |
| CONTEXT | Deferred warehouse/data work | No singleton/backend/destructive consolidation implementation | All tasks | EXCLUDED (deferred) |

<output>
Create `.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-01-SUMMARY.md` when done.
</output>
