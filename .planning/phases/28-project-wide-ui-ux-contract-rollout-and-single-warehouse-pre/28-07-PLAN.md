---
phase: 28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre
plan: 07
type: execute
wave: 8
depends_on: [28-06]
files_modified:
  - frontend/tests/uiAuditBlindReviewInput.ts
  - frontend/tests/uiAuditBlindReviewValidator.ts
  - frontend/tests/uiAuditBlindReviewValidator.test.ts
autonomous: false
requirements: [PUX-06]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Blind input is pinned to selected run-2 SHA-256 and excludes prior verdicts, rationale, and source diffs."
    - "Fresh review has exact nonzero input/output identity bijection, UNRESOLVED=0, owner-bearing/actionable FAIL=0; honest identity-local NEEDS_EVIDENCE remains a terminal non-PASS disposition and cannot become PASS without separately authorized new production measurement provenance."
  artifacts:
    - {path: frontend/tests/uiAuditBlindReviewValidator.ts, provides: fail-closed qualitative review validator}
  key_links:
    - {from: selected run-2, to: blind review, via: hash-pinned qualitative-only input}
---
<objective>Run the independent blind-review gate after deterministic reconciliation. Purpose: keep qualitative review fresh and subordinate to deterministic oracles. Output: validated persisted review artifact.</objective>
<execution_context>@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@MEMORY.md
@LESSONS.md
@.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-06-SUMMARY.md</context>
<tasks>
<task type="tracer" tdd="true"><name>Task 1: Generate blind input and fail-closed validator</name>
<read_first>.artifacts/phase28-ui-audit/remediation/selected-attempt.json, frontend/tests/uiAuditBlindReviewInput.ts, frontend/tests/uiAuditBlindReviewValidator.ts, frontend/tests/uiAuditBlindReviewValidator.test.ts</read_first>
<files>frontend/tests/uiAuditBlindReviewInput.ts, frontend/tests/uiAuditBlindReviewValidator.ts, frontend/tests/uiAuditBlindReviewValidator.test.ts</files>
<behavior>- The entire blind-review attempt root is absent before atomic creation; its immutable append-only manifest pins input, review, schema, reviewer identity/tool provenance, selected remediation run, and member hashes.
- Input hash equals selected run-2 canonical hash.
- Input contains qualitative identity/DOM/ARIA/geometry evidence only.
- Validator rejects root reuse, manifest/member hash mismatch, zero counts, duplicates, missing/extra identity, hash/provenance mismatch, invalid schema, any UNRESOLVED, owner-bearing/actionable FAIL, identity/count drift, or NEEDS_EVIDENCE→PASS without separately authorized new production measurement provenance.
- Printed PASS/FAIL/NEEDS_EVIDENCE/UNRESOLVED/actionable totals are asserted.</behavior>
<action>Require the entire blind-review attempt root absent, atomically create it, and never reuse it. Generate exactly one input row per qualitative identity and strip baseline/remediation verdicts, prior conclusions, implementation rationale, and source diffs. Write an immutable append-only manifest pinning input/review/schema hashes, reviewer identity and tool provenance, selected remediation run hash, and all member hashes. Validate fresh reviewer identity, manifest and member hashes, source hashes, exact nonzero bijection, exact counts/identities, owner-bearing evidence, and verdict schema. Assert that no NEEDS_EVIDENCE becomes PASS without separately authorized new production measurement provenance. Deterministic rows remain authoritative and cannot be overridden by the reviewer.</action>
<acceptance_criteria>Known-clean review passes and every malformed/stale/incomplete/actionable fixture fails.</acceptance_criteria><verify><automated>set -o pipefail &amp;&amp; ATTEMPT="${PHASE28_BLIND_ATTEMPT:?new attempt-N}" &amp;&amp; PARENT=".artifacts/phase28-ui-audit/blind-review" &amp;&amp; mkdir -p "$PARENT" &amp;&amp; ROOT="$PARENT/$ATTEMPT" &amp;&amp; test ! -L "$PARENT" &amp;&amp; test ! -e "$ROOT" &amp;&amp; case "$(realpath "$PARENT")/" in "$(realpath frontend/test-results)/"*) exit 1;; esac &amp;&amp; mkdir "$ROOT" &amp;&amp; test ! -L "$ROOT" &amp;&amp; node frontend/tests/uiAuditBlindReviewInput.ts --selection .artifacts/phase28-ui-audit/remediation/selected-attempt.json --output "$ROOT/input.jsonl" --schema "$ROOT/schema.json" --manifest "$ROOT/manifest.json" --require-member-hashes --require-exact-nonzero-bijection &amp;&amp; node frontend/tests/uiAuditBlindReviewValidator.ts --selection .artifacts/phase28-ui-audit/remediation/selected-attempt.json --input "$ROOT/input.jsonl" --manifest "$ROOT/manifest.json" --validate-input-only --require-exact-nonzero-bijection --print-totals &amp;&amp; npm run test:unit -w frontend -- --run tests/uiAuditBlindReviewValidator.test.ts --maxWorkers=1</automated></verify><done>The blind-review interface and immutable hash-pinned input are generated and fail-closed (PUX-06).</done></task>
<task type="checkpoint:human-action" gate="blocking-human"><name>Task 2: Invoke a fresh independent reviewer</name>
<action>Invoke a reviewer identity that did not implement the changes, supplying only the attempt-scoped input and schema. Persist review.json, seal its hash and reviewer provenance in the append-only manifest, then run the validator. If any UNRESOLVED or owner-bearing/actionable FAIL exists, do not approve; return to the proven lowest owner, create a new immutable remediation attempt and a new absent blind-review attempt root, regenerate input, and use another fresh reviewer. NEEDS_EVIDENCE remains an honest terminal non-PASS disposition unless a separately authorized evidence-capture plan supplies new production measurement provenance.</action>
<instructions>1. After proving the new root is absent and canonically outside Playwright configured cleanup scope, invoke the fresh reviewer with only `.artifacts/phase28-ui-audit/blind-review/attempt-N/input.jsonl` and schema.
2. Run `node frontend/tests/uiAuditBlindReviewValidator.ts --selection .artifacts/phase28-ui-audit/remediation/selected-attempt.json --input .artifacts/phase28-ui-audit/blind-review/attempt-N/input.jsonl --review .artifacts/phase28-ui-audit/blind-review/attempt-N/review.json --require-fresh-provenance --require-exact-nonzero-bijection --reject-all-unresolved --reject-actionable-fail --print-totals`.
3. Run focused validator/reconciliation tests and git diff --check.</instructions>
<verification>Validator exits zero with exact nonzero bijection, fresh provenance, UNRESOLVED=0, and actionable FAIL=0.</verification><resume-signal>Type approved only after the persisted fresh review passes every validator gate.</resume-signal></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Deterministic evidence → external reviewer | Reviewer sees qualitative inputs only. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-28-07-01 | Spoofing | reviewer provenance | high | mitigate | Fresh identity and hash validation. |
| T-28-07-02 | Tampering | review bijection | high | mitigate | Exact nonzero identity gate. |
| T-28-07-SC | Tampering | package installs | high | accept | No install. |
</threat_model>
<verification>Prove the new blind-review root is absent and canonically outside Playwright configured cleanup scope; validate the tracked recovery authority and selected remediation hashes, validator fixtures, persisted review, exact totals, secret scan, and git diff --check.</verification>
<success_criteria>Independent qualitative review is fresh, complete, hash-pinned, and has no unresolved/actionable failure.</success_criteria>
## Source Coverage Audit
| SOURCE | ID | Feature / constraint | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Independent closeout | Blind review | Tasks 1-2 | COVERED |
| REQ | PUX-06 | Fresh review gate | Tasks 1-2 | COVERED |
| RESEARCH | Qualitative-only review | Input separation | Task 1 | COVERED |
| CONTEXT | AI limited to non-deterministic review | Deterministic precedence | Tasks 1-2 | COVERED |
<output>Create `.planning/phases/28-project-wide-ui-ux-contract-rollout-and-single-warehouse-pre/28-07-SUMMARY.md`.</output>
