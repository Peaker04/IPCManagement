---
phase: 29-system-operation-mode-and-material-reconciliation
plan: 24
type: execute
wave: 14
depends_on: [29-23]
files_modified:
  - frontend/src/shared/api/contracts/openapi.json
  - frontend/src/shared/api/contracts/schema.ts
  - frontend/src/features/reconciliation/reconciliationApi.ts
  - frontend/src/features/reconciliation/ReconciliationWorkspace.tsx
  - frontend/src/features/reconciliation/ReconciliationWorkspace.test.tsx
  - frontend/src/features/projects/weekly-menu/import/useWeeklyMenuImport.ts
  - frontend/src/features/projects/weekly-menu/import/WeeklyMenuImportJobs.tsx
  - frontend/src/features/projects/weekly-menu/import/WeeklyMenuImportJobs.confirmation.test.tsx
  - backend/tests/IPCManagement.Api.Tests/ReconciliationQuantityImportReachabilityTests.cs
  - frontend/tests/phase29EvidenceManifest.test.ts
autonomous: true
gap_closure: true
requirements: [OPM-03, MRC-01, MRC-02, MRC-03]
estimate:
  tokens: 33800
  raw_tokens: 33800
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-31/D-32/D-33, the retained Weekly Menu work area exposes one authorized preview/commit action using the existing RTK Query cache and query/dialog primitives; no parallel route or UI framework is added."
    - "Preview diagnostics distinguish empty, incomplete, mixed-version, stale and ready-to-commit states; preview/failure creates no durable authority."
    - "Successful commit shows the new import and exact-one DRAFT reconciliation batch identity/provenance, invalidates only reconciliation draft/batch sources, and enables the existing readiness workflow without document reload or a second create mutation."
    - "A production-boundary regression proves menu workbook commit → published menu → completed meal quantities → quantity preview/commit → exact-one DRAFT reconciliation batch readback, with exact zero deltas for procurement and stock authorities."
  artifacts:
    - frontend/src/features/reconciliation/ReconciliationWorkspace.tsx
    - backend/tests/IPCManagement.Api.Tests/ReconciliationQuantityImportReachabilityTests.cs
  key_links:
    - "Weekly Menu selected menu version -> quantity-import preview/commit mutation -> exact-one DRAFT batch readback and cache invalidation"
    - "generated OpenAPI schema -> one apiSlice endpoint family -> ReconciliationWorkspace control"
    - "application-path integration test -> protected retry handoff for ANV/AMANN 2026-09-07..2026-09-12"
---

<objective>
Expose and prove the quantity-import production path from the retained Weekly Menu reconciliation workspace.

Purpose: Make the new backend authority reachable by an authorized user and replace direct-fixture confidence with a complete application-boundary regression before protected evidence is retried.
Output: Generated contracts, RTK Query mutations, one bounded control, semantic tests and protected-retry handoff gates.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/phases/29-system-operation-mode-and-material-reconciliation/29-SPEC.md
@.planning/phases/29-system-operation-mode-and-material-reconciliation/29-CONTEXT.md
@.planning/phases/29-system-operation-mode-and-material-reconciliation/29-23-SUMMARY.md
@frontend/src/features/reconciliation/ReconciliationWorkspace.tsx
@frontend/src/features/projects/weekly-menu/import/useWeeklyMenuImport.ts
@frontend/src/api/coordinationApi.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Let Weekly Menu preview and commit the exact quantity source</name>
  <files>frontend/src/shared/api/contracts/openapi.json, frontend/src/shared/api/contracts/schema.ts, frontend/src/features/reconciliation/reconciliationApi.ts, frontend/src/features/reconciliation/ReconciliationWorkspace.tsx, frontend/src/features/reconciliation/ReconciliationWorkspace.test.tsx</files>
  <behavior>
    - "An authorized Weekly Menu user selects a published-compatible menu version, previews its canonical quantity source and sees exact plan/line count, source fingerprint and blocking diagnostics."
    - "The commit control is absent for actors without CoordinationAccess and disabled for empty/incomplete/mixed/stale sources; unavailable actors never receive an instruction to perform the action per D-37."
    - "Confirmation commits once, displays the returned import and DRAFT reconciliation batch identity/provenance, invalidates draft/batch sources only and makes the existing readiness control available without document reload or a second create-draft action."
    - "A replay renders the same idempotent authority; conflict renders current server state/retry guidance rather than success or an empty state."
  </behavior>
  <action>Regenerate OpenAPI/schema from the backend contract and add preview/commit endpoints to the existing `reconciliationApi` injected endpoint family. Extend the existing Weekly Menu-owned `ReconciliationWorkspace` rather than creating a route or generic component. Use one focused confirmation for commit, existing query boundaries and concise Vietnamese state copy. Preserve `apiSlice` reducer/middleware/cache identity per D-04/D-31/D-32/D-33. Invalidate only reconciliation draft-source/batch tags after successful commit. Do not add file parsing, legacy repair, stock/procurement actions or broad visual cleanup.</action>
  <verify>
    <automated>npm test -w frontend -- --run frontend/src/features/reconciliation/ReconciliationWorkspace.test.tsx &amp;&amp; npm run check:api-contract</automated>
  </verify>
  <done>A real authorized user can atomically create the committed quantity-import authority and exact-one DRAFT reconciliation batch from the retained Weekly Menu work area, then immediately continue to the existing readiness flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove production reachability and prepare the protected retry handoff</name>
  <files>backend/tests/IPCManagement.Api.Tests/ReconciliationQuantityImportReachabilityTests.cs, frontend/src/features/projects/weekly-menu/import/useWeeklyMenuImport.ts, frontend/src/features/projects/weekly-menu/import/WeeklyMenuImportJobs.tsx, frontend/src/features/projects/weekly-menu/import/WeeklyMenuImportJobs.confirmation.test.tsx, frontend/tests/phase29EvidenceManifest.test.ts</files>
  <behavior>
    - "The integration test begins at production service/controller seams: Weekly Menu preview/commit, publish-compatible menu, completed quick-servings plans, quantity-import preview/commit, then exact-one DRAFT reconciliation batch readback without invoking a second create mutation."
    - "The test never constructs QuantityImportBatch or assigns ImportBatchId directly; an explicit source-aware assertion fails if direct fixture construction returns to this reachability suite."
    - "Preview error, commit conflict and failed transaction leave QuantityImportBatch, MealQuantityPlan.ImportBatchId, ReconciliationBatch and audit authority unchanged."
    - "Two distinct menu imports/source fingerprints produce two distinct import-batch identities; repeating one exact commit remains idempotent."
    - "Before/after inventories prove zero PR, PO, receipt, inventory issue, movement, lot, snapshot and current-stock deltas."
    - "The protected evidence manifest requires the returned menuVersionId, quantityImportBatchId, content fingerprint and source-line IDs; historical workbook and Phase 5 fixture identifiers are rejected."
  </behavior>
  <action>Add a production-boundary integration suite that uses the same public services/controllers as runtime and independently inventories prohibited tables. Keep menu workbook commit feedback additive: surface that menu authority is saved, but do not falsely claim quantity authority until the new preview/commit action succeeds. Extend the Phase 29 evidence-manifest contract so the next protected retry must record newly generated workbook path/hash, menuVersionId, content fingerprint, quantityImportBatchId, linked plan/line IDs and draft reconciliation batch ID. Lock the candidate evidence scope as `ANV/AMANN`, `2026-09-07..2026-09-12` only as a read-only-preflight candidate; runtime must re-prove it clean before mutation per D-39/D-40/D-42. Explicitly reject the missing historical workbook hash/path and every Phase 5 fixture marker. Do not execute MySQL migrations or headed browser evidence in this plan.</action>
  <verify>
    <automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~ReconciliationQuantityImportApplicationPathTests|FullyQualifiedName~ReconciliationQuantityImportReachabilityTests" &amp;&amp; npm test -w frontend -- --run frontend/src/features/projects/weekly-menu/import/WeeklyMenuImportJobs.confirmation.test.tsx frontend/tests/phase29EvidenceManifest.test.ts &amp;&amp; npm run check:api-contract &amp;&amp; git diff --check</automated>
  </verify>
  <done>Production reachability is test-proven without direct fixtures, the controlled-source manifest cannot accept historical/Phase-5 authority, and Plan 29-22 has an executable fresh-preflight handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated API contract -> RTK Query/client | Contract drift could make the UI submit stale or incomplete source authority. |
| actor/route mode -> quantity-import control | A retained route must not expose unauthorized actions or let mode grant permission. |
| test fixture -> protected evidence handoff | Test-only/direct-fixture authority could be mistaken for production reachability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-24-01 | Elevation | Weekly Menu quantity-import control | high | mitigate | Server policy remains authoritative; semantic tests prove hidden/blocked control and zero request for unauthorized actors. |
| T-29-24-02 | Tampering | client fingerprint/token | high | mitigate | Client displays but never computes authority; server revalidates canonical content inside commit. |
| T-29-24-03 | Repudiation | commit confirmation/result | medium | mitigate | Confirmation names the source; response exposes actor-owned persisted batch identity and provenance. |
| T-29-24-04 | Information Disclosure | source IDs/fingerprint | low | mitigate | Full IDs are available only to authorized authenticated users and remain unchanged in API/audit; normal copy stays concise. |
| T-29-24-05 | Tampering | protected evidence manifest | high | mitigate | Reject missing historical source, Phase 5 fixture markers, direct fixture setup and absent source-line IDs. |
| T-29-SC | Tampering | package installs | high | accept | No package-manager install is planned. |
</threat_model>

<verification>Run focused backend reachability, frontend semantic/control, API contract parity, full frontend lint/build and the relevant Phase 29 evidence-manifest gate. No protected database/browser mutation occurs until the later Plan 29-22 retry starts from fresh lineage and checkpoint.</verification>
<success_criteria>The quantity-import authority and its exact-one DRAFT reconciliation batch are atomically reachable through the retained Weekly Menu UI and generated API contract; application-boundary tests prove the complete source chain without a second create mutation, prohibited business tables remain unchanged, and protected retry inputs are explicit and fail closed.</success_criteria>
<output>Create `.planning/phases/29-system-operation-mode-and-material-reconciliation/29-24-SUMMARY.md` when done.</output>
