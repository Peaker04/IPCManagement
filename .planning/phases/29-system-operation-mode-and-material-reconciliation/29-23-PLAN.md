---
phase: 29-system-operation-mode-and-material-reconciliation
plan: 23
type: execute
wave: 13
depends_on: [29-21]
files_modified:
  - backend/src/IPCManagement.Api/Models/Entities/QuantityImportBatch.cs
  - backend/src/IPCManagement.Api/Features/Coordination/Persistence/ProductionEntityConfigurations.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Contracts/ReconciliationContracts.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Controllers/ReconciliationBatchesController.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationQuantityImportService.cs
  - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs
  - backend/src/IPCManagement.Api/DependencyInjection.cs
  - backend/src/IPCManagement.Api/Migrations/20260826120000_AddQuantityImportCommitAuthority.cs
  - backend/src/IPCManagement.Api/Migrations/20260826120000_AddQuantityImportCommitAuthority.Designer.cs
  - backend/src/IPCManagement.Api/Migrations/IpcManagementContextModelSnapshot.cs
  - backend/tests/IPCManagement.Api.Tests/ReconciliationQuantityImportApplicationPathTests.cs
  - backend/tests/IPCManagement.Api.Tests/Phase29MigrationTests.cs
autonomous: true
gap_closure: true
requirements: [OPM-03, MRC-01, MRC-02, MRC-03]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per MRC-01/D-13/D-31, an authorized user can preview and commit a canonical quantity-import snapshot through a production API, and that same transaction creates exactly one DRAFT reconciliation batch for the committed import."
    - "Per D-14/D-15/D-16, preview or validation failure creates no authority; commit links only completed plans whose source lines belong to one exact published-compatible menu version and preserves every plan/line/source identity in the canonical fingerprint."
    - "Per D-23/D-24/D-39, retries are idempotent, concurrent duplicate commits produce one authority, stale source changes fail closed, and legacy rows receive no fabricated menu linkage or provenance."
    - "Per D-26/D-40/D-42, quantity-import commit changes no PR, PO, receipt, issue, movement, lot, snapshot or current-stock authority."
  artifacts:
    - backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationQuantityImportService.cs
    - backend/src/IPCManagement.Api/Migrations/20260826120000_AddQuantityImportCommitAuthority.cs
    - backend/tests/IPCManagement.Api.Tests/ReconciliationQuantityImportApplicationPathTests.cs
  key_links:
    - "quantity-import preview token/content fingerprint -> exact menu version + sorted meal plan/line IDs and quantities"
    - "quantity-import commit transaction -> QuantityImportBatch row + MealQuantityPlan.ImportBatchId links + audit"
    - "quantity-import commit transaction -> exact-one DRAFT ReconciliationBatch creation and readback"
---

<objective>
Close the backend production-reachability gap for committed quantity-import authority.

Purpose: Make the exact source pair required by reconciliation creatable through an authorized, audited, idempotent application command without direct fixtures or stock/procurement side effects.
Output: Protected preview/commit API, durable provenance schema, atomic linkage, migration policy and production-boundary tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@LESSONS.md
@.planning/phases/29-system-operation-mode-and-material-reconciliation/29-SPEC.md
@.planning/phases/29-system-operation-mode-and-material-reconciliation/29-CONTEXT.md
@.planning/phases/29-system-operation-mode-and-material-reconciliation/29-RESEARCH.md
@.planning/phases/29-system-operation-mode-and-material-reconciliation/29-NEW-CONTROLLED-SOURCE-EXECUTOR-OUTPUT.phase29-new-controlled-source-retry.md
@docs/DATA-GRAIN-MATRIX.md
@backend/src/IPCManagement.Api/Features/Coordination/Services/MealQuantityPlanService.cs
@backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Commit one exact quantity-import snapshot through the production API</name>
  <files>backend/src/IPCManagement.Api/Models/Entities/QuantityImportBatch.cs, backend/src/IPCManagement.Api/Features/Coordination/Persistence/ProductionEntityConfigurations.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Contracts/ReconciliationContracts.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Controllers/ReconciliationBatchesController.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationQuantityImportService.cs, backend/src/IPCManagement.Api/Features/Reconciliation/Services/ReconciliationBatchService.cs, backend/src/IPCManagement.Api/DependencyInjection.cs, backend/tests/IPCManagement.Api.Tests/ReconciliationQuantityImportApplicationPathTests.cs</files>
  <behavior>
    - "Preview for one published-compatible MenuVersion returns a transient token, canonical SHA-256 content fingerprint, exact sorted plan/line identity inventory and diagnostics, while QuantityImportBatch count and every ImportBatchId remain unchanged."
    - "Commit with the matching token/fingerprint and expected plan versions creates one CONFIRMED QuantityImportBatch, records actor/time/source kind/content version/file name or canonical-source label/fingerprint, links every and only previewed eligible MealQuantityPlan in one serializable protected transaction, appends audit, and returns the import batch ID."
    - "The same commit transaction creates exactly one DRAFT ReconciliationBatch for the exact menuVersionId/importBatchId pair; draft-source/readback endpoints expose that batch without a second user mutation or test-only entity construction."
    - "A replay of the same canonical fingerprint returns the same import and DRAFT reconciliation batch without a second authority or second audit; two concurrent first commits produce one winner/one idempotent readback rather than duplicate import or draft authority."
    - "A source plan/line/version/quantity change between preview and commit returns conflict and persists neither a batch nor any link."
    - "Non-Coordination actors, missing mode protection, wrong mode, unpublished menu versions, mixed menu versions, incomplete plans, empty source sets and mismatched tokens are rejected before persistence per D-06/D-07/D-16/D-24."
  </behavior>
  <action>Implement a dedicated `ReconciliationQuantityImportService` and preview/commit endpoints under the existing authenticated reconciliation controller family. Reuse the existing mode-protected transaction runner and `CoordinationAccess`; do not let mode substitute for permission per D-06/D-07/D-22. Canonicalize the fingerprint from a versioned serialization of menuVersionId plus sorted quantityPlanId, plan row-version/status/service date, quantityPlanLineId, menuScheduleId, customerId, menuId, shift and exact final servings. Preview uses the existing bounded preview-ticket pattern and writes nothing. Commit re-queries and re-hashes inside a serializable protected transaction, creates a distinct `QuantityImportBatch` for a distinct canonical source, assigns `MealQuantityPlan.ImportBatchId` only after all rows pass, creates exactly one `DRAFT ReconciliationBatch` for that exact menu/import pair, writes actor/time audit and verifies both authorities through `ReconciliationBatchService`. Use exact `CONFIRMED` as the committed import status rather than negative status matching; require durable provenance fields and exact committed status. Enforce an exact-pair duplicate fence so retry/concurrency returns the same import and draft batch. Readiness remains the existing explicit user transition; only draft creation is automatic as required by MRC-01/D-13.</action>
  <verify>
    <automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter FullyQualifiedName~ReconciliationQuantityImportApplicationPathTests</automated>
  </verify>
  <done>A public API test drives preview → commit → exact-one DRAFT reconciliation batch readback from real service/controller boundaries, with no direct QuantityImportBatch/ReconciliationBatch fixture setup and no second create mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist provenance and enforce duplicate/legacy policy in MySQL schema</name>
  <files>backend/src/IPCManagement.Api/Models/Entities/QuantityImportBatch.cs, backend/src/IPCManagement.Api/Features/Coordination/Persistence/ProductionEntityConfigurations.cs, backend/src/IPCManagement.Api/Migrations/20260826120000_AddQuantityImportCommitAuthority.cs, backend/src/IPCManagement.Api/Migrations/20260826120000_AddQuantityImportCommitAuthority.Designer.cs, backend/src/IPCManagement.Api/Migrations/IpcManagementContextModelSnapshot.cs, backend/tests/IPCManagement.Api.Tests/Phase29MigrationTests.cs</files>
  <behavior>
    - "New committed application rows persist menuVersionId, source/content fingerprint, fingerprint format version and immutable creation provenance with a unique duplicate fence."
    - "Existing legacy quantityimportbatches remain byte-preserved and ineligible when provenance/linkage fields are null; migration performs no guessed backfill and does not assign MealQuantityPlan.ImportBatchId."
    - "The unique fence permits distinct reimports when the canonical source fingerprint differs and prevents two rows for the same exact snapshot."
    - "Down migration removes only the new additive authority/index/FK after a precondition proves no new authority is referenced; operational rollback otherwise uses the named checkpoint per D-39."
  </behavior>
  <action>Create an additive EF migration with Designer and snapshot. Add nullable provenance columns for legacy compatibility, but make the application commit path require them for every new `CONFIRMED` row. Add an FK to `menuversions`, a fixed-length SHA-256 fingerprint, fingerprint-format version, source label/file metadata needed for audit, and a unique index on the exact canonical commit fingerprint. Do not backfill menuVersionId, source fingerprint or plan linkage from names, dates, BatchCode or nearby records. Keep old rows readable but fail closed in draft-source eligibility. Review generated SQL for `USE`, database create/drop, table drop, business updates/deletes and implicit legacy backfill before any protected application in a later plan.</action>
  <reversibility rating="costly">The additive migration is reversible before new committed authorities exist; after linkage is used, rollback requires the named database checkpoint rather than deleting provenance.</reversibility>
  <verify>
    <automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter "FullyQualifiedName~Phase29MigrationTests|FullyQualifiedName~ReconciliationQuantityImportApplicationPathTests" &amp;&amp; dotnet ef migrations has-pending-model-changes --project backend/src/IPCManagement.Api/IPCManagement.Api.csproj --startup-project backend/src/IPCManagement.Api/IPCManagement.Api.csproj</automated>
  </verify>
  <done>EF discovers the migration, model parity is clean, SQL/schema tests prove the unique/FK/provenance contract, and no legacy row is fabricated into an eligible source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| uploaded/menu source and persisted meal plans -> preview | Workbook-derived menu authority and database plans are untrusted until exact identity/status/version validation. |
| preview token/client fingerprint -> commit API | Client-provided token and fingerprint may be replayed, swapped or stale. |
| commit API -> MySQL | One transaction creates durable import authority and links multiple plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-29-23-01 | Spoofing/Elevation | quantity-import endpoints | high | mitigate | Existing authentication plus CoordinationAccess; mode eligibility is checked independently and cannot grant permission. |
| T-29-23-02 | Tampering | preview/commit content fingerprint | high | mitigate | Server-owned canonical serialization and SHA-256; re-query/re-hash inside protected serializable commit. |
| T-29-23-03 | Repudiation | committed import authority | high | mitigate | Persist actor/time/source metadata and append exact entity audit in the same transaction. |
| T-29-23-04 | Denial of Service | source inventory and preview token | medium | mitigate | Scope one menu version, enforce existing request/rate limits, bounded token lifetime and bounded source inventory. |
| T-29-23-05 | Tampering | concurrent duplicate commits | high | mitigate | Unique fingerprint fence plus idempotent reload after duplicate-key race; no last-write-wins linkage. |
| T-29-23-06 | Tampering | legacy migration compatibility | high | mitigate | Additive nullable fields, zero guessed backfill, fail-closed eligibility for incomplete provenance. |
| T-29-SC | Tampering | package installs | high | accept | No package-manager install is planned. |
</threat_model>

<verification>Run focused production-path and migration tests, generated SQL inspection, pending-model check, API contract generation/parity, then `git diff --check`. No protected database or browser run occurs in this plan.</verification>
<success_criteria>An authorized production request can atomically create exactly one durable committed quantity-import authority and exactly one DRAFT reconciliation batch for one canonical meal-plan snapshot; reachability is proven without direct fixtures or a second create mutation, stale/concurrent duplicates are closed, and legacy data is not fabricated.</success_criteria>
<output>Create `.planning/phases/29-system-operation-mode-and-material-reconciliation/29-23-SUMMARY.md` when done.</output>
