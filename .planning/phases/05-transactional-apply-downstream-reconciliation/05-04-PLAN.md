---
phase: 05-transactional-apply-downstream-reconciliation
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - backend/src/IPCManagement.Api/Services/Bom/Apply/ILegacyBomRetentionExecutor.cs
  - backend/src/IPCManagement.Api/Services/Bom/Apply/LegacyBomRetentionExecutor.cs
  - backend/src/IPCManagement.Api/Services/Bom/Apply/BomApplyTransactionCoordinator.cs
  - backend/tests/IPCManagement.Api.Tests/BomRetentionApplyTests.cs
  - scripts/Test-BomImmutableHistory.ps1
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04, DOWN-04]
must_haves:
  truths:
    - "D-05-05: approved/locked/completed, approval, audit and ledger IDs/counts/checksums remain bit-for-bit."
    - "D-05-06: delete requires fresh true-orphan proof, complete zero references and stock zero; uncertainty blocks."
    - "D-05-07: ordered/received/issued/returned evidence blocks and action IDs/counts must match."
    - "Legacy executor accepts ONLY keep/archive/deactivate/delete/block; regenerate is rejected and owned by 05-06."
    - "Retention executor is not composed ahead of downstream leaf handling and no production endpoint is enabled."
  artifacts:
    - path: "backend/src/IPCManagement.Api/Services/Bom/Apply/LegacyBomRetentionExecutor.cs"
      provides: "Legacy-only keep/archive/deactivate/delete/block executor"
    - path: "backend/tests/IPCManagement.Api.Tests/BomRetentionApplyTests.cs"
      provides: "Exact true-orphan/reference/stock/history proof"
    - path: "scripts/Test-BomImmutableHistory.ps1"
      provides: "Isolated ordered checksum/FK/count evidence"
  key_links:
    - from: "backend/src/IPCManagement.Api/Services/Bom/Apply/LegacyBomRetentionExecutor.cs"
      to: "backend/src/IPCManagement.Api/Services/Bom/Classification/IBomLegacyCleanupClassifier.cs"
      via: "fresh exact legacy action/reason/evidence and policy version"
      pattern: "keep|archive|deactivate|delete|block"
    - from: "scripts/Test-BomImmutableHistory.ps1"
      to: ".artifacts/bom-v1.1/gate-a/pre-edit"
      via: "protected family checksum comparison"
      pattern: "checksum|ledger|approval|audit"
---

# Phase 5 Plan 04: Legacy retention and true-orphan safety

<objective>
Implement and prove a vocabulary-isolated legacy retention executor that preserves history, archives/deactivates referenced catalog records and deletes only freshly proven zero-stock true orphans; defer all regenerate orchestration to Plan 05-06.
</objective>

<execution_context>
- Gate A/B, backup, Plan 05-03 atomic rollback and fresh classifier evidence must pass.
- Consume Phase 3 lifecycle policy and Phase 4 classifier/evidence; never infer deletion from workbook absence/name/code.
- No downstream regenerate, endpoint, UI, retirement or broad SQL runner.
</execution_context>

<tasks>

<task type="auto" id="05-04-01" requirement="DATA-02,DATA-03,DATA-04,DOWN-04">
  <name>Implement legacy-only true-orphan retention executor and immutable proof</name>
  <files>backend/src/IPCManagement.Api/Services/Bom/Apply/ILegacyBomRetentionExecutor.cs, backend/src/IPCManagement.Api/Services/Bom/Apply/LegacyBomRetentionExecutor.cs, backend/src/IPCManagement.Api/Services/Bom/Apply/BomApplyTransactionCoordinator.cs, backend/tests/IPCManagement.Api.Tests/BomRetentionApplyTests.cs, scripts/Test-BomImmutableHistory.ps1</files>
  <read_first>
    <file>.planning/phases/03-contract-provenance-safety-baseline/03-LIFECYCLE-POLICY.md</file>
    <file>backend/src/IPCManagement.Api/Services/Bom/Classification/IBomLegacyCleanupClassifier.cs</file>
    <file>backend/src/IPCManagement.Api/Services/Bom/Classification/IBomDependencyEvidenceReader.cs</file>
    <file>backend/src/IPCManagement.Api/Services/Bom/Apply/BomApplyTransactionCoordinator.cs</file>
    <file>backend/tests/IPCManagement.Api.Tests/BomRetentionApplyTests.cs</file>
  </read_first>
  <action>
    Preconditions: gates/backup/fresh blocker-zero evidence and Plan 05-03 pass; run impact for classifier/evidence/coordinator. Implement `ILegacyBomRetentionExecutor` with only `keep/archive/deactivate/delete/block`; explicitly reject `Unchanged/Create/Version/regenerate`. Enforce D-05-05 by never modifying approved/locked/completed, approval, audit, adjustment, ledger/movement/order/receipt/issue/return history. Enforce D-05-06: delete requires exact freshly previewed ID, complete zero reference families and aggregate stock exactly zero; history maps archive/deactivate, unknown/contradictory/truncated evidence blocks. Enforce D-05-07 with exact ID/count equality and ordered/received/issued/returned blockers. Do not yet wire retention before downstream: coordinator exposes a later composition seam and tests invoke the executor only on an isolated transaction harness. `Test-BomImmutableHistory.ps1` refuses default/equal/protected targets, uses explicit clone/backup marker and compares ordered IDs/counts/checksums plus FK and deleted-ID set. Stop/rollback: any drift throws and transaction rolls back; runtime script restores clone backup before nonzero exit. Before commit run ownership/detect_changes.
  </action>
  <acceptance_criteria>
    <criterion>Executor vocabulary is exactly keep/archive/deactivate/delete/block and contains no regenerate/canonical action.</criterion>
    <criterion>Deleted IDs equal exact fresh true-orphan IDs; stock/reference/history/unknown blocks.</criterion>
    <criterion>All protected checksums and FK invariants remain equal on isolated clone.</criterion>
    <criterion>No production apply route or premature retention composition exists.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; if(-not $env:IPC_TEST_CONNECTION_STRING){throw 'isolated MySQL required; BLOCKED'}; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter 'FullyQualifiedName~BomRetentionApplyTests|FullyQualifiedName~BomApplyTransactionTests'; if($LASTEXITCODE-ne 0){throw 'retention tests failed'}; $text=Get-Content -Raw backend/src/IPCManagement.Api/Services/Bom/Apply/LegacyBomRetentionExecutor.cs; if($text-match 'Unchanged|Create|Version|regenerate'){throw 'legacy vocabulary boundary violated'}; powershell -File scripts/Test-BomImmutableHistory.ps1 -ConnectionStringEnvironment IPC_TEST_CONNECTION_STRING -CloneId ipc_bom_v11_p5_retention -BackupMarker .artifacts/bom-v1.1/gate-a/pre-edit/recovery -EvidenceDirectory .artifacts/bom-v1.1/gate-c/retention -ConfirmIsolated; if($LASTEXITCODE-ne 0){throw 'immutable proof failed/restored'}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-c/phase5-preedit/owned-paths.txt; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}</automated>
    <behavior>Legacy vocabulary, true-orphan and immutable clone evidence pass; failures restore clone.</behavior>
  </verify>
  <done>Legacy retention is isolated and safe for later post-regenerate composition.</done>
</task>

</tasks>

## Artifacts this phase produces

- Legacy-only retention executor.
- Exact true-orphan, protected checksum and FK evidence.

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-05-03">Broad/cascade delete destroys history; exact IDs and checksum/FK proof block.</threat>
  <threat id="T-05-05">Regenerate/action vocabulary leaks into retention; static scan and interface enum reject it.</threat>
  <verification>ASVS L1 retention review, isolated MySQL proof, impact, ownership and detect_changes.</verification>
</threat_model>
