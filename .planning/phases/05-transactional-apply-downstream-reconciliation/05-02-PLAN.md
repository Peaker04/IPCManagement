---
phase: 05-transactional-apply-downstream-reconciliation
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - backend/src/IPCManagement.Api/Models/Entities/Bomdocumentreconciliation.cs
  - backend/src/IPCManagement.Api/Models/Entities/Bomreconciliationrun.cs
  - backend/src/IPCManagement.Api/Models/Entities/Materialrequest.cs
  - backend/src/IPCManagement.Api/Models/Entities/Productionplan.cs
  - backend/src/IPCManagement.Api/Data/IpcManagementContext.cs
  - backend/src/IPCManagement.Api/Migrations/*_AddBomApplyLineage.cs
  - backend/src/IPCManagement.Api/Migrations/*_AddBomApplyLineage.Designer.cs
  - backend/src/IPCManagement.Api/Migrations/IpcManagementContextModelSnapshot.cs
  - scripts/Test-BomPhase5Migration.ps1
autonomous: true
requirements: [SAFE-03, DOWN-01, DOWN-02, DOWN-04]
must_haves:
  truths:
    - "D-05-03: delivered run fingerprint remains the database idempotency authority; schema adds no competing identity."
    - "D-05-08/D-05-09: deterministic scope token and once-per-run document lineage are persisted."
    - "One forward-only migration uses restrictive/non-cascading links and preserves all applied migration hashes."
    - "No domain apply, cleanup, regenerate, API, UI or legacy retirement occurs."
  artifacts:
    - path: "backend/src/IPCManagement.Api/Models/Entities/Bomdocumentreconciliation.cs"
      provides: "Unique source-run/document/action old-new lineage"
    - path: "backend/src/IPCManagement.Api/Migrations/*_AddBomApplyLineage.cs"
      provides: "Forward-only scope-token and lineage schema"
    - path: "scripts/Test-BomPhase5Migration.ps1"
      provides: "Fresh and legacy isolated migration proof"
  key_links:
    - from: "backend/src/IPCManagement.Api/Models/Entities/Bomdocumentreconciliation.cs"
      to: "backend/src/IPCManagement.Api/Models/Entities/Bomreconciliationrun.cs"
      via: "non-cascading source-run FK and unique document guard"
      pattern: "SourceRunId|DocumentType|DocumentId"
    - from: "backend/src/IPCManagement.Api/Models/Entities/Materialrequest.cs"
      to: "backend/src/IPCManagement.Api/Models/Entities/Productionplan.cs"
      via: "bounded deterministic BomScopeToken fields"
      pattern: "BomScopeToken"
---

# Phase 5 Plan 02: Forward apply-lineage schema

<objective>
Add only the persisted scope-token and once-per-run document lineage required by later transaction/reconciliation plans, through one verified forward migration.
</objective>

<execution_context>
- Gate A/B, Plan 05-01 evidence and backup marker must remain PASS.
- Run upstream impact before every entity/context edit; applied migrations are immutable.
- Schema tests run only on explicit isolated MySQL targets; no domain apply endpoint is enabled.
</execution_context>

<tasks>

<task type="auto" id="05-02-01" requirement="SAFE-03,DOWN-01,DOWN-02,DOWN-04">
  <name>Create non-cascading scope-token and reconciliation-lineage schema</name>
  <files>backend/src/IPCManagement.Api/Models/Entities/Bomdocumentreconciliation.cs, backend/src/IPCManagement.Api/Models/Entities/Bomreconciliationrun.cs, backend/src/IPCManagement.Api/Models/Entities/Materialrequest.cs, backend/src/IPCManagement.Api/Models/Entities/Productionplan.cs, backend/src/IPCManagement.Api/Data/IpcManagementContext.cs, backend/src/IPCManagement.Api/Migrations/*_AddBomApplyLineage.cs, backend/src/IPCManagement.Api/Migrations/*_AddBomApplyLineage.Designer.cs, backend/src/IPCManagement.Api/Migrations/IpcManagementContextModelSnapshot.cs, scripts/Test-BomPhase5Migration.ps1</files>
  <read_first>
    <file>.artifacts/bom-v1.1/gate-c/phase5-preedit/impact-report.md</file>
    <file>backend/src/IPCManagement.Api/Models/Entities/Bomreconciliationrun.cs</file>
    <file>backend/src/IPCManagement.Api/Models/Entities/Materialrequest.cs</file>
    <file>backend/src/IPCManagement.Api/Models/Entities/Productionplan.cs</file>
    <file>backend/src/IPCManagement.Api/Data/IpcManagementContext.cs</file>
  </read_first>
  <action>
    Preconditions: Gate A/B and ownership hashes match; run impact for every target. Add bounded `BomScopeToken`/`BomSourceRunId` to draft snapshot owners and `Bomdocumentreconciliation` with unique `(SourceRunId,DocumentType,DocumentId)`, old/new IDs and action restricted to downstream `regenerate` lineage. Extend the delivered run only if a typed lineage navigation is absent; preserve D-05-03 fingerprint. Generate exactly one `*_AddBomApplyLineage` migration with restrictive/non-cascading FKs/indexes and no data cleanup. `Test-BomPhase5Migration.ps1` refuses missing/equal/protected/default targets, runs empty-to-latest and Gate-A legacy-to-latest on disposable MySQL databases, scans only the new delta for broad DML/cascade, and proves immutable checksums. Stop/rollback: on any migration/schema/checksum failure run generated Down only on the disposable target or restore its identified backup; never touch the source/default database. Before commit run ownership and detect_changes.
  </action>
  <acceptance_criteria>
    <criterion>Exactly one new forward migration; every older migration hash remains unchanged.</criterion>
    <criterion>Scope token and unique once-per-run lineage are database enforced with non-cascading FKs.</criterion>
    <criterion>New delta contains no DELETE/TRUNCATE/broad UPDATE or domain mutation.</criterion>
    <criterion>Fresh/legacy isolated paths retain all immutable IDs/counts/checksums.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; if(-not $env:IPC_TEST_CONNECTION_STRING){throw 'isolated MySQL required; BLOCKED'}; powershell -File scripts/Test-BomPhase5Migration.ps1 -ConnectionStringEnvironment IPC_TEST_CONNECTION_STRING -FreshDatabase ipc_bom_v11_p5_fresh -LegacyDatabase ipc_bom_v11_p5_legacy -EvidenceDirectory .artifacts/bom-v1.1/gate-c/migrations -ConfirmIsolated; if($LASTEXITCODE-ne 0){throw 'migration proof failed'}; dotnet build backend/src/IPCManagement.Api/IPCManagement.Api.csproj --no-restore; if($LASTEXITCODE-ne 0){throw 'build failed'}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-c/phase5-preedit/owned-paths.txt; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}</automated>
    <behavior>Provider migration proof and build pass without applied-history drift.</behavior>
  </verify>
  <done>Persisted lineage exists safely without enabling mutation.</done>
</task>

</tasks>

## Artifacts this phase produces

- Scope-token and regenerate-lineage model.
- One forward-only migration plus isolated fresh/legacy evidence.

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-05-03">Migration/cascade damages history; delta scan and immutable checksums block.</threat>
  <threat id="T-05-05">Missing uniqueness duplicates regeneration; DB unique lineage guard is mandatory.</threat>
  <verification>ASVS L1 schema review, MySQL path proof, impact, ownership and detect_changes.</verification>
</threat_model>
