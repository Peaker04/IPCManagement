---
phase: 05-transactional-apply-downstream-reconciliation
plan: 01
type: execute
wave: 1
depends_on: [03-01, 03-02, 03-03, 04-01, 04-02, 04-03]
files_modified:
  - .artifacts/bom-v1.1/gate-c/phase5-preedit/manifest.txt
  - .artifacts/bom-v1.1/gate-c/phase5-preedit/owned-paths.txt
  - .artifacts/bom-v1.1/gate-c/phase5-preedit/impact-report.md
  - backend/tests/IPCManagement.Api.Tests/BomApplyTransactionTests.cs
  - backend/tests/IPCManagement.Api.Tests/BomApplyIdempotencyTests.cs
  - backend/tests/IPCManagement.Api.Tests/BomRetentionApplyTests.cs
  - backend/tests/IPCManagement.Api.Tests/BomDownstreamReconciliationTests.cs
  - backend/tests/IPCManagement.Api.Tests/BomCacheReportConsistencyTests.cs
  - .artifacts/bom-v1.1/gate-c/wave0/
autonomous: true
requirements: [SAFE-03, DATA-02, DATA-03, DATA-04, DOWN-01, DOWN-02, DOWN-03, DOWN-04, DOWN-05, CRUD-03]
must_haves:
  truths:
    - "Phase 3/4 planned artifacts are not existing source until their execution and Gate A/B evidence are PASS."
    - "D-05-14: production/destructive apply remains absent or disabled until isolated Gate C PASS; only test fixtures are added here."
    - "D-05-15: prerequisite/hash/ownership/impact drift blocks before any Phase 5 source or schema edit."
    - "Wave-0 fixtures exist before implementation and prove rollback, retention, scope, lifecycle, cache/history and CRUD audit behavior."
  artifacts:
    - path: ".artifacts/bom-v1.1/gate-c/phase5-preedit/impact-report.md"
      provides: "Gate A/B, backup, freshness, ownership and blast-radius evidence"
    - path: "backend/tests/IPCManagement.Api.Tests/BomApplyTransactionTests.cs"
      provides: "Injected exception/cancellation rollback fixture"
    - path: "backend/tests/IPCManagement.Api.Tests/BomRetentionApplyTests.cs"
      provides: "True-orphan and immutable-history fixture"
    - path: "backend/tests/IPCManagement.Api.Tests/BomDownstreamReconciliationTests.cs"
      provides: "Tier/unit/overlay/staleness/lifecycle fixture"
  key_links:
    - from: ".artifacts/bom-v1.1/gate-c/phase5-preedit/owned-paths.txt"
      to: "scripts/Test-BomWorktreeOwnership.ps1"
      via: "Phase 5 allowlist checked before every commit"
      pattern: "gate-c/phase5-preedit/owned-paths\.txt"
    - from: "backend/tests/IPCManagement.Api.Tests/BomApplyTransactionTests.cs"
      to: "backend/tests/IPCManagement.Api.Tests/CoordinationTransactionTests.cs"
      via: "shared relational failure-interceptor pattern"
      pattern: "SaveChangesInterceptor|Rollback"
---

# Phase 5 Plan 01: Prerequisite boundary and Wave-0 fixtures

<objective>
Prove the delivered Gate A/B safety foundation, capture Phase 5 ownership/impact, keep destructive apply disabled, and add failing-first fixtures before any production or schema implementation.
</objective>

<execution_context>
- Read `05-CONTEXT.md`, `05-RESEARCH.md`, `05-PATTERNS.md`, `05-VALIDATION.md`, all Phase 3/4 plans/patterns, `ROADMAP.md`, `REQUIREMENTS.md`, and `AGENTS.md`.
- No `backend/src`, migration, runtime database, frontend, legacy surface or production feature flag changes are permitted.
- Before commit run ownership comparison and `node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all`; preserve unrelated user changes.
</execution_context>

<tasks>

<task type="auto" id="05-01-01" requirement="SAFE-03,DATA-02,DATA-03,DATA-04,DOWN-01,DOWN-02,DOWN-03,DOWN-04,DOWN-05,CRUD-03">
  <name>[BLOCKING] Prove Gate A/B and capture Phase 5 ownership/impact</name>
  <files>.artifacts/bom-v1.1/gate-c/phase5-preedit/manifest.txt, .artifacts/bom-v1.1/gate-c/phase5-preedit/owned-paths.txt, .artifacts/bom-v1.1/gate-c/phase5-preedit/impact-report.md</files>
  <read_first>
    <file>.artifacts/bom-v1.1/gate-a/GATE-A.md</file>
    <file>.artifacts/bom-v1.1/gate-b/GATE-B.md</file>
    <file>.planning/phases/03-contract-provenance-safety-baseline/03-LIFECYCLE-POLICY.md</file>
    <file>.planning/phases/05-transactional-apply-downstream-reconciliation/05-CONTEXT.md</file>
    <file>AGENTS.md</file>
  </read_first>
  <action>
    Fail before source/schema edits unless Gate A/B are PASS, delivered Phase 3/4 contract/policy/schema/parser/classifier/fingerprint hashes match their evidence, backup marker remains valid, and the approved preview is fresh with zero blockers. Capture HEAD, dirty/untracked paths, prerequisite hashes and exact seven-plan allowlist. Run upstream impact for every existing/future Phase 5 target named in 05-02..05-07; record DI/interface/not-found as lower-bound, and block unapproved HIGH/CRITICAL. Enforce D-05-14 by recording that no production/destructive apply route/feature is enabled before Gate C. Enforce D-05-15 by writing BLOCKED and stopping on any drift. No source/runtime mutation occurs. Before commit run ownership and detect_changes.
  </action>
  <acceptance_criteria>
    <criterion>Actual Gate A/B, restore, freshness, blocker-zero and delivered artifact hashes are verified.</criterion>
    <criterion>Ownership allowlist covers only Phase 5 paths and impact lists exact callers/processes/risk.</criterion>
    <criterion>No production/destructive apply surface is enabled and no source/schema/runtime data changed.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; foreach($g in @('.artifacts/bom-v1.1/gate-a/GATE-A.md','.artifacts/bom-v1.1/gate-b/GATE-B.md')){if(-not(Test-Path $g)){throw "missing $g"};if((Get-Content -Raw $g)-notmatch '(?im)status\s*[:|]\s*PASS|Gate [AB]\s+PASS'){throw "gate not PASS: $g"}}; foreach($p in @('.artifacts/bom-v1.1/gate-c/phase5-preedit/manifest.txt','.artifacts/bom-v1.1/gate-c/phase5-preedit/owned-paths.txt','.artifacts/bom-v1.1/gate-c/phase5-preedit/impact-report.md')){if(-not(Test-Path $p)){throw "missing $p"}}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-c/phase5-preedit/owned-paths.txt; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}</automated>
    <behavior>Prerequisites and ownership/impact pass or execution stops before implementation.</behavior>
  </verify>
  <done>Phase 5 begins only from actual PASS evidence with destructive apply disabled.</done>
</task>

<task type="auto" id="05-01-02" requirement="SAFE-03,DATA-02,DATA-03,DATA-04,DOWN-01,DOWN-02,DOWN-03,DOWN-04,DOWN-05,CRUD-03">
  <name>Create Wave-0 failure, retention, downstream and cache/audit fixtures</name>
  <files>backend/tests/IPCManagement.Api.Tests/BomApplyTransactionTests.cs, backend/tests/IPCManagement.Api.Tests/BomApplyIdempotencyTests.cs, backend/tests/IPCManagement.Api.Tests/BomRetentionApplyTests.cs, backend/tests/IPCManagement.Api.Tests/BomDownstreamReconciliationTests.cs, backend/tests/IPCManagement.Api.Tests/BomCacheReportConsistencyTests.cs</files>
  <read_first>
    <file>backend/tests/IPCManagement.Api.Tests/CoordinationTransactionTests.cs</file>
    <file>backend/tests/IPCManagement.Api.Tests/WorkflowGenerationTests.cs</file>
    <file>backend/tests/IPCManagement.Api.Tests/DishCatalogTests.cs</file>
    <file>.planning/phases/05-transactional-apply-downstream-reconciliation/05-PATTERNS.md</file>
  </read_first>
  <action>
    Preconditions are task 05-01-01 PASS and unchanged ownership. Add failing-first relational fixtures with these required named tests: `Apply_RollsBackDomainAndAudit_WhenInjectedFailure`, `DuplicateApply_IsPersistedNoOp`, `Retention_DeletesOnlyTrueOrphan`, `Demand_UsesPartialCustomerOverlayAndScopedToken`, `Downstream_RegeneratesEligibleDraftOnceBeforeRetention`, and `PostCommit_InvalidatesCurrentButPreservesHistory`. Cover cancellation, protected checksums, stock/reference blockers, 25k/30k/34k, eight technical-unit families, unrelated scope and locked states. Each intentionally red test must execute normally and fail only with assertion message prefix `PHASE5_MISSING_BEHAVIOR:`; compilation, discovery, fixture/dependency/connection/timeout or infrastructure errors are never an acceptable red state. Keep canonical actions `Unchanged/Create/Version`, legacy actions `keep/archive/deactivate/delete/block`, and downstream action `regenerate` as distinct fixture types. No implementation or runtime mutation. Stop if fixtures require broad deletion/default app DB. Before commit run ownership and detect_changes.
  </action>
  <acceptance_criteria>
    <criterion>Fixtures cover all ten requirements and D-05-01..15 without conflating action vocabularies.</criterion>
    <criterion>Tests use disposable relational contexts and never the default application database.</criterion>
    <criterion>All six required named tests are discovered and executed; every failure is an explicit `PHASE5_MISSING_BEHAVIOR:` assertion.</criterion>
    <criterion>Build, discovery, dependency, connection, fixture, timeout and infrastructure failures are rejected.</criterion>
    <criterion>Implementation is not added to make failing-first tests pass in this plan.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; $project='backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj'; $out='.artifacts/bom-v1.1/gate-c/wave0'; New-Item -ItemType Directory -Force $out|Out-Null; dotnet build $project --no-restore; if($LASTEXITCODE-ne 0){throw 'Wave-0 build failed; red state rejected'}; Remove-Item -Force "$out/phase5-wave0.trx" -ErrorAction SilentlyContinue; dotnet test $project --no-build --no-restore --filter 'FullyQualifiedName~BomApplyTransactionTests|FullyQualifiedName~BomApplyIdempotencyTests|FullyQualifiedName~BomRetentionApplyTests|FullyQualifiedName~BomDownstreamReconciliationTests|FullyQualifiedName~BomCacheReportConsistencyTests' --logger 'trx;LogFileName=phase5-wave0.trx' --results-directory $out; $testExit=$LASTEXITCODE; if($testExit-eq 0){throw 'Wave-0 tests unexpectedly green before implementation'}; $trx="$out/phase5-wave0.trx"; if(-not(Test-Path $trx)){throw 'TRX missing; discovery/infra failure rejected'}; [xml]$doc=Get-Content -Raw $trx; $ns=New-Object System.Xml.XmlNamespaceManager($doc.NameTable); $ns.AddNamespace('t','http://microsoft.com/schemas/VisualStudio/TeamTest/2010'); $results=@($doc.SelectNodes('//t:UnitTestResult',$ns)); if($results.Count-eq 0){throw 'zero tests discovered/executed'}; $required=@('Apply_RollsBackDomainAndAudit_WhenInjectedFailure','DuplicateApply_IsPersistedNoOp','Retention_DeletesOnlyTrueOrphan','Demand_UsesPartialCustomerOverlayAndScopedToken','Downstream_RegeneratesEligibleDraftOnceBeforeRetention','PostCommit_InvalidatesCurrentButPreservesHistory'); foreach($name in $required){$hit=@($results|Where-Object{$_.testName-like "*$name*"}); if($hit.Count-ne 1){throw "required Wave-0 test not executed exactly once: $name"}; if($hit[0].outcome-ne 'Failed'){throw "required Wave-0 test did not fail as expected: $name"}; $msg=[string]$hit[0].Output.ErrorInfo.Message; if($msg-notmatch '^PHASE5_MISSING_BEHAVIOR:'){throw "non-behavior failure rejected: $name :: $msg"}}; foreach($r in $results){if($r.outcome-ne 'Failed'){throw "unexpected non-failed Wave-0 result: $($r.testName)=$($r.outcome)"}; $msg=[string]$r.Output.ErrorInfo.Message; if($msg-notmatch '^PHASE5_MISSING_BEHAVIOR:' -or $msg-match '(?i)compile|discovery|dependency|connection|fixture|timeout|infrastructure|FileNotFound|TypeLoad'){throw "infra/non-contract red state rejected: $($r.testName) :: $msg"}}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-c/phase5-preedit/owned-paths.txt; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}</automated>
    <behavior>Build succeeds; TRX proves all required tests executed and failed only on explicit missing-behavior assertions.</behavior>
  </verify>
  <done>Wave-0 test contracts exist before implementation.</done>
</task>

</tasks>

## Artifacts this phase produces

- Gate A/B/backup/freshness ownership and impact evidence.
- Explicit destructive-apply-disabled evidence.
- Wave-0 transaction, idempotency, retention, downstream and cache/history fixtures.

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-05-01">Stale prerequisite permits unsafe work; hashes/freshness/blockers fail closed.</threat>
  <threat id="T-05-03">Tests accidentally mutate real data; disposable contexts and default-DB refusal are mandatory.</threat>
  <verification>ASVS L1 review, Gate A/B, impact, ownership and detect_changes; HIGH/CRITICAL blocks.</verification>
</threat_model>
