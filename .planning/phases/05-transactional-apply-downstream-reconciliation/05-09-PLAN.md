---
phase: 05-transactional-apply-downstream-reconciliation
plan: 09
type: execute
wave: 9
depends_on: [05-08]
files_modified:
  - backend/src/IPCManagement.Api/Controllers/AdminBomReconciliationController.cs
  - backend/src/IPCManagement.Api/Models/DTOs/Bom/BomCanonicalApplyDto.cs
  - backend/tests/IPCManagement.Api.Tests/BomCanonicalApplyApiTests.cs
  - scripts/Test-BomPhase5Gate.ps1
  - .artifacts/bom-v1.1/gate-c/GATE-C.md
autonomous: true
requirements: [SAFE-03, DATA-02, DATA-03, DATA-04, DOWN-01, DOWN-02, DOWN-03, DOWN-04, DOWN-05, CRUD-03]
must_haves:
  truths:
    - "D-05-14: production apply is disabled unless ApplyEnabled=true and a Gate C PASS evidence ID is configured."
    - "D-05-15: drift/new blocker/action mismatch/failed checksum/count/FK restores clone backup before BLOCKED."
    - "Gate C runs only against explicit distinct non-protected CloneId with ConfirmIsolated and RestoreOnAnyFailure."
    - "All ten requirements and D-05-01..15 map to non-skipped evidence; no UI/retirement/broad delete."
  artifacts:
    - path: "backend/src/IPCManagement.Api/Controllers/AdminBomReconciliationController.cs"
      provides: "Feature-disabled Admin bounded apply action"
    - path: "scripts/Test-BomPhase5Gate.ps1"
      provides: "Isolated clone apply/check/restore runner"
    - path: ".artifacts/bom-v1.1/gate-c/GATE-C.md"
      provides: "Ten-requirement, fifteen-decision PASS/BLOCKED evidence"
  key_links:
    - from: "backend/src/IPCManagement.Api/Controllers/AdminBomReconciliationController.cs"
      to: ".artifacts/bom-v1.1/gate-c/GATE-C.md"
      via: "deployment config requires ApplyEnabled and GateCEvidenceId from PASS"
      pattern: "ApplyEnabled|GateCEvidenceId"
    - from: "scripts/Test-BomPhase5Gate.ps1"
      to: ".artifacts/bom-v1.1/gate-a/pre-edit/recovery"
      via: "restore identified backup on every failed apply/assertion"
      pattern: "RestoreOnAnyFailure|BackupMarker|CloneId"
---

# Phase 5 Plan 09: Isolated Gate C and apply feature gating

<objective>
Expose the bounded Admin apply contract behind a default-disabled Gate C evidence configuration, rehearse the complete internal mutation only on an explicit restorable clone, and close or safely block Phase 5.
</objective>

<execution_context>
- Plans 05-01..08 and all focused tests/builds pass; Gate A/B and backup remain valid.
- Internal clone rehearsal may invoke coordinator; production/default DB and endpoint remain disabled until PASS configuration.
- No frontend/UI/RTK, legacy retirement or broad cleanup runner.
</execution_context>

<tasks>

<task type="auto" id="05-09-01" requirement="SAFE-03">
  <name>Add default-disabled Admin bounded apply contract</name>
  <files>backend/src/IPCManagement.Api/Controllers/AdminBomReconciliationController.cs, backend/src/IPCManagement.Api/Models/DTOs/Bom/BomCanonicalApplyDto.cs, backend/tests/IPCManagement.Api.Tests/BomCanonicalApplyApiTests.cs</files>
  <read_first>
    <file>backend/src/IPCManagement.Api/Controllers/AdminBomReconciliationController.cs</file>
    <file>backend/src/IPCManagement.Api/Services/Bom/Workbook/BomWorkbookSecurityLimits.cs</file>
    <file>backend/src/IPCManagement.Api/Security/ICurrentUserService.cs</file>
    <file>backend/tests/IPCManagement.Api.Tests/Infrastructure/CustomWebApplicationFactory.cs</file>
  </read_first>
  <action>
    Preconditions: Plans 05-01..08 pass and production apply is still absent/disabled; run controller/DTO/current-user impact. Add apply action with exact preview upload bounds and Admin policy, binding only preview ID, workbook, effective date and bounded reason; actor/hash/count/candidates remain server-owned. Enforce D-05-14 by returning 503 before coordinator unless both `BomReconciliation:ApplyEnabled=true` and a nonblank `GateCEvidenceId` are configured; defaults/missing evidence stay disabled. Hosted tests prove disabled by default/boolean-only/evidence-only, forged actor rejection, input bounds, stale conflict, and enabled invocation only under isolated test PASS evidence. Stop/rollback: request rejection invokes no coordinator/DML; failures remain safe typed errors. Before commit ownership/detect_changes.
  </action>
  <acceptance_criteria>
    <criterion>Endpoint is Admin-only, bounded and server-authoritative.</criterion>
    <criterion>Missing either enable flag or evidence ID returns 503 before coordinator.</criterion>
    <criterion>Hosted tests can enable only with explicit isolated PASS evidence.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; if(-not $env:IPC_TEST_CONNECTION_STRING){throw 'isolated hosted DB required; BLOCKED'}; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter 'FullyQualifiedName~BomCanonicalApplyApiTests'; if($LASTEXITCODE-ne 0){throw 'apply feature-gate/API tests failed'}; dotnet build backend/src/IPCManagement.Api/IPCManagement.Api.csproj --no-restore; if($LASTEXITCODE-ne 0){throw 'build failed'}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-c/phase5-preedit/owned-paths.txt; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}</automated>
    <behavior>Apply remains disabled without explicit Gate C evidence and input/auth tests pass.</behavior>
  </verify>
  <done>Production apply contract exists but is fail-closed until Gate C PASS configuration.</done>
</task>

<task type="auto" id="05-09-02" requirement="SAFE-03,DATA-02,DATA-03,DATA-04,DOWN-01,DOWN-02,DOWN-03,DOWN-04,DOWN-05,CRUD-03">
  <name>[BLOCKING] Rehearse isolated restore-safe Gate C and emit evidence ID</name>
  <files>scripts/Test-BomPhase5Gate.ps1, .artifacts/bom-v1.1/gate-c/GATE-C.md, backend/tests/IPCManagement.Api.Tests/BomCanonicalApplyApiTests.cs</files>
  <read_first>
    <file>.artifacts/bom-v1.1/gate-a/GATE-A.md</file>
    <file>.artifacts/bom-v1.1/gate-b/GATE-B.md</file>
    <file>.planning/phases/05-transactional-apply-downstream-reconciliation/05-VALIDATION.md</file>
    <file>scripts/Test-BomImmutableHistory.ps1</file>
    <file>backend/src/IPCManagement.Api/Controllers/AdminBomReconciliationController.cs</file>
  </read_first>
  <action>
    Preconditions: 05-09-01 and all prior tests pass; backup is readable. `Test-BomPhase5Gate.ps1` requires explicit SourceConnectionName, CloneConnectionName, unique CloneId, BackupMarker, `-ConfirmIsolated` and `-RestoreOnAnyFailure`; refuse default app connection, equal/protected names/hosts or missing clone identity. Rehearse internal coordinator only on clone and verify revalidation, three separate vocabularies, downstream-before-retention, transaction/idempotency, immutable checksums, exact IDs/counts/FKs/stock, scope math, cache/history and CRUD audit. D-05-15 requires a `try/finally` restore path: on EVERY failed apply, checksum, count, FK or assertion, restore identified backup to clone and verify restored checksums before writing BLOCKED/returning nonzero; never mutate source/default or compensate with broad DML. Map all ten requirements and D-05-01..15 with non-skipped MySQL evidence. PASS emits immutable `GateCEvidenceId` for deployment config but does not modify production config. Before commit ownership/detect_changes.
  </action>
  <acceptance_criteria>
    <criterion>Runner refuses absent/equal/protected/default targets and requires CloneId/ConfirmIsolated/RestoreOnAnyFailure.</criterion>
    <criterion>Every failure restores and verifies identified backup before BLOCKED.</criterion>
    <criterion>GATE-C maps 10 requirements and D-05-01..15 without skips and emits evidence ID only on PASS.</criterion>
    <criterion>Production configuration remains unchanged/disabled.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; if(-not $env:IPC_TEST_CONNECTION_STRING){throw 'isolated MySQL required; Gate C BLOCKED'}; powershell -File scripts/Test-BomPhase5Gate.ps1 -SourceConnectionName BomLegacyReadOnly -CloneConnectionName BomV11GateCClone -CloneId ipc-bom-v11-gate-c-20260716 -BackupMarker .artifacts/bom-v1.1/gate-a/pre-edit/recovery -EvidenceDirectory .artifacts/bom-v1.1/gate-c -ConfirmIsolated -RestoreOnAnyFailure; if($LASTEXITCODE-ne 0){throw 'Gate C BLOCKED after verified clone restore'}; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore; if($LASTEXITCODE-ne 0){throw 'full backend tests failed'}; dotnet build backend/src/IPCManagement.Api/IPCManagement.Api.csproj --no-restore; if($LASTEXITCODE-ne 0){throw 'build failed'}; $gate=Get-Content -Raw .artifacts/bom-v1.1/gate-c/GATE-C.md; if($gate-notmatch '(?im)status\s*[:|]\s*PASS|Gate C\s+PASS' -or $gate-notmatch 'GateCEvidenceId'){throw 'Gate C PASS/evidence ID missing'}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-c/phase5-preedit/owned-paths.txt; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}; git diff --check; if($LASTEXITCODE-ne 0){throw 'diff check failed'}</automated>
    <behavior>Gate C passes and emits evidence, or clone is restored and gate remains BLOCKED.</behavior>
  </verify>
  <done>Phase 5 is Gate C PASS and deployably enableable by evidence, or safely BLOCKED with clone restored.</done>
</task>

</tasks>

## Artifacts this phase produces

- Default-disabled Admin apply contract.
- Isolated clone Gate C runner, verified failure restore and PASS evidence ID.

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-05-01">Endpoint bypasses preview/actor scope; bounded Admin server-owned contract blocks.</threat>
  <threat id="T-05-03">Gate mutates source/default or fails without restore; explicit clone guards/finally restore block.</threat>
  <threat id="T-05-05">Feature enabled without evidence; dual configuration gate returns 503.</threat>
  <verification>ASVS L1 auth/restore review, full MySQL/hosted/backend suite, Gate C, impact, ownership and detect_changes.</verification>
</threat_model>
