---
phase: 05-transactional-apply-downstream-reconciliation
plan: 08
type: execute
wave: 8
depends_on: [05-07]
files_modified:
  - backend/src/IPCManagement.Api/Services/IDishService.cs
  - backend/src/IPCManagement.Api/Services/DishService.cs
  - backend/src/IPCManagement.Api/Controllers/DishesController.cs
  - backend/src/IPCManagement.Api/Models/DTOs/Dish/DishDto.cs
  - backend/tests/IPCManagement.Api.Tests/DishManualBomAuditTests.cs
  - backend/tests/IPCManagement.Api.Tests/DishCatalogTests.cs
autonomous: true
requirements: [CRUD-03]
must_haves:
  truths:
    - "D-05-13: manual published add/update/close requires server actor and bounded nonblank reason."
    - "Version/close, full adjustment and Auditlog share one transaction; failure/cancellation rolls back all."
    - "Add/close contracts gain reason and server actor; update keeps server actor and requires reason for published versioning."
    - "Production apply remains disabled; no cache/report/Gate C/UI/retirement work occurs."
  artifacts:
    - path: "backend/src/IPCManagement.Api/Services/IDishService.cs"
      provides: "Actual add/update/close actor and reason contracts"
    - path: "backend/src/IPCManagement.Api/Models/DTOs/Dish/DishDto.cs"
      provides: "Bounded required reason DTO validation"
    - path: "backend/tests/IPCManagement.Api.Tests/DishManualBomAuditTests.cs"
      provides: "Service and hosted rejection/audit/rollback proof"
  key_links:
    - from: "backend/src/IPCManagement.Api/Controllers/DishesController.cs"
      to: "backend/src/IPCManagement.Api/Services/IDishService.cs"
      via: "server-resolved actor and validated reason"
      pattern: "AddBomLineAsync|UpdateBomLineAsync|CloseBomLineAsync"
    - from: "backend/src/IPCManagement.Api/Services/DishService.cs"
      to: "backend/src/IPCManagement.Api/Models/Entities/Bomadjustment.cs"
      via: "version/close adjustment plus Auditlog in one transaction"
      pattern: "Bomadjustment|Auditlog|Reason"
---

# Phase 5 Plan 08: Manual BOM CRUD accountability

<objective>
Harden the actual dish-service/controller/DTO contracts so published manual add/update/close always carries server identity and bounded reason, with atomic version/adjustment/audit and rollback tests.
</objective>

<execution_context>
- Plan 05-07 and shared Phase 3 invariant tests pass.
- No canonical apply endpoint, Gate C, cache/report, UI or retirement changes.
- Run impact on exact interface/service/controller methods before edits and ownership/detect_changes before commit.
</execution_context>

<tasks>

<task type="auto" id="05-08-01" requirement="CRUD-03">
  <name>Require server actor and bounded reason in actual add/update/close contracts</name>
  <files>backend/src/IPCManagement.Api/Services/IDishService.cs, backend/src/IPCManagement.Api/Services/DishService.cs, backend/src/IPCManagement.Api/Controllers/DishesController.cs, backend/src/IPCManagement.Api/Models/DTOs/Dish/DishDto.cs, backend/tests/IPCManagement.Api.Tests/DishManualBomAuditTests.cs, backend/tests/IPCManagement.Api.Tests/DishCatalogTests.cs</files>
  <read_first>
    <file>backend/src/IPCManagement.Api/Services/IDishService.cs</file>
    <file>backend/src/IPCManagement.Api/Services/DishService.cs</file>
    <file>backend/src/IPCManagement.Api/Controllers/DishesController.cs</file>
    <file>backend/src/IPCManagement.Api/Models/DTOs/Dish/DishDto.cs</file>
    <file>backend/tests/IPCManagement.Api.Tests/DishCatalogTests.cs</file>
  </read_first>
  <action>
    Preconditions: 05-07 and shared invariant tests pass; run upstream impact for `IDishService`, `AddBomLineAsync`, `UpdateBomLineAsync`, `CloseBomLineAsync`, controller actions and DTOs. Implement D-05-13 in actual contracts: add/close accept bounded nonblank reason plus server-resolved actor; update retains server actor and requires bounded reason for published changes. `DishesController` resolves actor from authenticated claims and rejects/ignores forged client actor. `DishService` validates before DML and atomically performs create/version/close, complete field `Bomadjustment`, `Auditlog` and post-commit cache intent; unchanged no-op and close never hard-deletes. Add service and hosted tests for 401/403, missing/blank/oversize reason, forged/missing actor, valid adjustment/audit, audit-save failure and cancellation rollback. Stop/rollback on auth/validation/overlap/audit failure; verify fresh context. Before commit ownership/detect_changes.
  </action>
  <acceptance_criteria>
    <criterion>Actual IDishService add/update/close and Dish DTOs require bounded reason; actor is server-derived.</criterion>
    <criterion>Hosted tests reject missing/blank/oversize reason and forged/missing actor with zero mutation.</criterion>
    <criterion>Valid published change atomically creates version/adjustment/audit; close retains history.</criterion>
    <criterion>Audit/cancellation failure rolls back every manual change.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; if(-not $env:IPC_TEST_CONNECTION_STRING){throw 'isolated hosted DB required; BLOCKED'}; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter 'FullyQualifiedName~DishManualBomAuditTests|FullyQualifiedName~DishCatalogTests'; if($LASTEXITCODE-ne 0){throw 'manual CRUD/API tests failed'}; dotnet build backend/src/IPCManagement.Api/IPCManagement.Api.csproj --no-restore; if($LASTEXITCODE-ne 0){throw 'build failed'}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-c/phase5-preedit/owned-paths.txt; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}</automated>
    <behavior>Actual service/HTTP actor-reason and rollback/audit tests pass.</behavior>
  </verify>
  <done>CRUD-03 is enforced end-to-end for manual published BOM changes.</done>
</task>

</tasks>

## Artifacts this phase produces

- Actual IDishService, DishService, DishesController and Dish DTO actor/reason contracts.
- Hosted/manual version-adjustment-audit rollback evidence.

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-05-02">Manual mutation without attributable audit; server actor, bounded reason and one transaction block.</threat>
  <verification>ASVS L1 auth/audit review, hosted/relational tests, impact, ownership and detect_changes.</verification>
</threat_model>
