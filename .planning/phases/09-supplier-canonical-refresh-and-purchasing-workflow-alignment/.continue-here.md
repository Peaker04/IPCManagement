---
context: phase
phase: 09-supplier-canonical-refresh-and-purchasing-workflow-alignment
task: 0
total_tasks: 35
status: in_progress
last_updated: 2026-07-21T16:39:41.646Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Untracked SQL protection — plain `git diff` cannot prove an untracked file was not edited or staged — record and compare SHA-256, require exact `??` porcelain, and require `git ls-files --error-unmatch` to fail at every destructive/final gate.
- [ ] CONSTRAINT: PowerShell verification masking — semicolon-chained native commands may return success after an earlier failure — use `$ErrorActionPreference='Stop'`, assert `$LASTEXITCODE` after every native command, and make missing evidence an explicit throw/exit.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting `git diff` for untracked protected data tooling | The first checker proved the cleanup SQL could change or become staged without the planned assertion detecting it. | blocking | Hash the exact path, assert exact porcelain and absence from the index before and after every apply/E2E gate. |
| Non-fail-fast multi-command verification | Several initial plan commands used `;`, so a later successful command could hide a failed test. | blocking | Wrap commands in fail-fast PowerShell and assert every native exit code plus every expected evidence marker. |
| Treating interrupted revision as approved | Planner wrote revision files but was interrupted before returning or receiving a second checker pass. | advisory | Re-run structural checks and gsd-plan-checker; do not execute from the current drafts. |

<current_state>
Phase 09 planning is paused during revision iteration 1/3. Initial planning produced 14 plans and 35 tasks. The first plan-checker found 6 blockers and 2 warnings. The planner was asked to make targeted fixes and wrote substantial revisions, then was explicitly interrupted when the user requested a pause. The revised files have not been verified or committed. No Phase 09 implementation or database mutation has started.
</current_state>

<completed_work>

Completed planning work:
- Phase 09 context and 18 locked decisions committed at `806c09f`.
- UI-SPEC passed 6/6 after one copy revision and was committed at `8067e16`; session state at `ec02bec`.
- Research, 11 mapped requirements and initial validation strategy committed at `f7dc876`; research characterization reported 122 backend and 13 focused frontend tests passing.
- Pattern map covering 29/31 analogs committed at `c902ccc`.
- Initial planner created 14 plans, source audit and ROADMAP plan list.
- Initial checker completed and reported 6 blockers/2 warnings.
- Interrupted revision appears to have added resolved research decisions, approved/full task validation mapping, protected-SQL hash/index checks, fail-fast commands, required route/UI-audit/visual coverage, missing file declarations, and parallel Wave 1/2 branches.
</completed_work>

<remaining_work>

- Inspect all interrupted revision diffs for completeness and malformed/truncated content.
- Run `validate plan-structure`/frontmatter checks for all 14 plans and the decision-coverage gate.
- Re-run `gsd-plan-checker` against the revised plans.
- Fix remaining blockers through the normal maximum-three revision loop.
- Only after verification passes, commit Phase 09 plans, source audit, revised research/validation/roadmap and record planned state.
- Do not run `$gsd-execute-phase 09` until the checker passes.
</remaining_work>

<decisions_made>

- Canonical source: `.docs/IPC. Theo dõi đặt hàng ngày 20.7.2026.xlsx`; Development/sample path only in Phase 09.
- Normalization is deterministic and blocker-first; `BICH` conversion varies by ingredient, supplier and effective period.
- Reconciliation is preview-first, dependency-aware, immutable-history preserving and idempotent; hard delete only proven sample orphans.
- Workflow: finalized servings → date demand → Manager approval → week/date Purchasing → explicit supplier → price exception when `>15%` → PR approval → supplier-split PO → Warehouse receipt.
- Purchasing never records actual receipt; Warehouse is the sole atomic writer for receipt, stock and PO progress.
- Real/shared apply remains a non-autonomous operator checkpoint after clone restore and two clean E2E rounds.
</decisions_made>

<blockers>
- Verification blocker: revised plans have not received the second plan-checker pass.
- Scope protection: `backend/database/Clean_Legacy_Imported_Bom_Idempotent.sql` is user-owned, untracked and must remain untouched/untracked/unstaged.
- User-owned dirty paths `README.md`, `frontend/README.md`, and `.cursor/` remain outside Phase 09 planning and execution scope.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable exact pause state.
2. This `.continue-here.md` — constraints and next action.
3. `09-CONTEXT.md` — D-09-01 through D-09-18 locked user decisions.
4. `09-UI-SPEC.md` — approved 6/6 UI contract.
5. `09-RESEARCH.md` — architecture, requirement IDs and resolved questions.
6. `09-VALIDATION.md` — revised Nyquist map; still requires checker confirmation.
7. `09-PATTERNS.md` and `09-SOURCE-AUDIT.md` — direct-source analogs and source coverage.
8. `09-01-PLAN.md` through `09-14-PLAN.md` — interrupted revision output.
9. `AGENTS.md` — mandatory GitNexus impact and detect_changes rules.

## Critical Anti-Patterns (do NOT repeat these)

- Do not use plain `git diff` as proof for an untracked protected file; use hash + porcelain + index-absence checks.
- Do not chain verification commands without explicit native exit-code assertions.
- Do not stage with `git add .`; add only Phase 09 planning/handoff paths.
- Do not infer a successful revision from file timestamps; only a fresh checker pass can approve it.

## Infrastructure State

- Branch: `feature/production-plan`.
- Planner subagent was explicitly interrupted; no GSD planning subagent remains intentionally active for this task.
- No Phase 09 API, dashboard, Shipyard lane or database process was started by this planning session.
- GitNexus graph was previously observed stale; Plan 09-01 requires full refresh and commit equality proof before symbol impacts are trusted during execution.
- No database apply, cleanup, migration or E2E run occurred in this planning session.

## Pre-Execution Critique Required

- Design artifacts: `09-CONTEXT.md`, `09-UI-SPEC.md`, `09-RESEARCH.md`, `09-PATTERNS.md`, `09-VALIDATION.md`, and all 14 PLAN files.
- Critique focus: all six original checker blockers, both warnings, full requirement/decision coverage, fail-fast commands, protected SQL invariants and whether the Wave 0–10 DAG preserves safety.
- Gate: Do NOT begin execution until the revised plans pass `gsd-plan-checker`.

<context>
The plan intentionally separates pure reconciliation from the approval/workflow branch after Wave 0, then rejoins schema-sensitive work before supplier decision, price exception, PO, Warehouse, frontend and E2E gates. The interrupted revision looks substantially complete but must be treated as untrusted output until revalidated.
</context>

<next_action>
Start with: run `$gsd-resume-work`, acknowledge both blocking constraints, inspect the interrupted planning diff, then re-run the Phase 09 plan checker. Do not execute Phase 09.
</next_action>
