---
phase: 09-supplier-canonical-refresh-and-purchasing-workflow-alignment
plan: 05
type: execute
wave: 4
depends_on: [09-04]
files_modified:
  - backend/src/IPCManagement.Api/Services/SampleData/IPurchaseHistoryReconciliationService.cs
  - backend/src/IPCManagement.Api/Services/SampleData/PurchaseHistoryReconciliationService.cs
  - backend/src/IPCManagement.Api/Controllers/SampleDataController.cs
  - backend/tests/IPCManagement.Api.Tests/PurchaseHistoryReconciliationTests.cs
  - .planning/phases/09-supplier-canonical-refresh-and-purchasing-workflow-alignment/09-WAVE0-EVIDENCE.md
autonomous: true
requirements: [SUP-04]
must_haves:
  truths:
    - "An accepted manifest applies atomically only on a freshly fingerprinted disposable database with matching backup evidence."
    - "A changed source, policy, as-of date, database, action set, or count fails before mutation."
    - "A second apply and a post-apply preview are exact no-ops."
    - "Immutable or referenced history is versioned/remapped/deactivated, never destructively overwritten."
  artifacts:
    - path: "backend/src/IPCManagement.Api/Services/SampleData/PurchaseHistoryReconciliationService.cs"
      provides: "Atomic apply and no-op replay implementation"
    - path: "backend/src/IPCManagement.Api/Controllers/SampleDataController.cs"
      provides: "Development/disposable-only apply endpoint"
    - path: ".planning/phases/09-supplier-canonical-refresh-and-purchasing-workflow-alignment/09-WAVE0-EVIDENCE.md"
      provides: "Apply/replay/post-preview and protected-SQL invariant evidence"
  key_links:
    - from: "SampleDataController.cs"
      to: "PurchaseHistoryReconciliationService.ApplyAsync"
      via: "manifest ID/hash plus accepted action IDs"
      pattern: "ApplyPurchaseHistory"
    - from: "PurchaseHistoryReconciliationService.cs"
      to: "Purchasehistoryreconciliationrun"
      via: "single EF transaction writes business changes and evidence"
      pattern: "BeginTransactionAsync"
---

<objective>
Apply an accepted preview atomically and idempotently on a disposable lane per D-09-07 through D-09-10 and D-09-17.

Purpose: Reconciliation must be evidence-bound, recoverable, and unable to damage immutable or shared operational history.
Output: Apply service/API behavior, drift guards, transaction/audit writes, and disposal-lane proof.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/09-supplier-canonical-refresh-and-purchasing-workflow-alignment/09-CONTEXT.md
@.planning/phases/09-supplier-canonical-refresh-and-purchasing-workflow-alignment/09-RESEARCH.md
@.planning/phases/09-supplier-canonical-refresh-and-purchasing-workflow-alignment/09-WAVE0-EVIDENCE.md
@.planning/phases/09-supplier-canonical-refresh-and-purchasing-workflow-alignment/09-04-SUMMARY.md
@backend/src/IPCManagement.Api/Services/SampleData/PurchaseHistoryReconciliationService.cs
@backend/src/IPCManagement.Api/Controllers/SampleDataController.cs
@backend/src/IPCManagement.Api/Data/IpcManagementContext.cs
</context>

## Artifacts this phase produces

| Path | New symbols |
|---|---|
| `IPurchaseHistoryReconciliationService.cs` | `ApplyAsync` |
| `PurchaseHistoryReconciliationService.cs` | `ApplyAsync`, `ValidateAcceptedManifestAsync`, `AssertDisposableTarget`, `ApplyActionAsync` |
| `SampleDataController.cs` | `ApplyPurchaseHistory` (`POST api/sample-data/purchase-history/apply`) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject drift and non-disposable apply targets before mutation</name>
  <files>backend/src/IPCManagement.Api/Services/SampleData/IPurchaseHistoryReconciliationService.cs, backend/src/IPCManagement.Api/Services/SampleData/PurchaseHistoryReconciliationService.cs, backend/tests/IPCManagement.Api.Tests/PurchaseHistoryReconciliationTests.cs</files>
  <read_first>
    <file>backend/src/IPCManagement.Api/Services/SampleData/PurchaseHistoryReconciliationService.cs</file>
    <file>backend/src/IPCManagement.Api/Models/DTOs/SampleData/PurchaseHistoryReconciliationDto.cs</file>
    <file>backend/tools/IPCManagement.DatabaseTool/DatabaseClonePolicy.cs</file>
    <file>.planning/phases/09-supplier-canonical-refresh-and-purchasing-workflow-alignment/09-WAVE0-EVIDENCE.md</file>
    <file>backend/tests/IPCManagement.Api.Tests/PurchaseHistoryReconciliationTests.cs</file>
  </read_first>
  <behavior>
    - "Only `ipc_lane[1-9]`/approved disposable identities with matching backup/restore proof may apply."
    - "Any mismatch in source hash, policy, as-of date, database fingerprint, action IDs, or exact counts returns conflict with zero writes."
    - "A manifest containing blockers cannot apply."
  </behavior>
  <action>Run impact for the preview service and existing database clone policy before editing. Before any apply validation, load the protected SQL baseline from `09-WAVE0-EVIDENCE.md` and fail unless SHA-256 is identical, porcelain is exactly `?? backend/database/Clean_Legacy_Imported_Bom_Idempotent.sql`, and `git ls-files --error-unmatch` fails. Rebuild the preview from the server-owned workbook inside apply validation; compare every D-09-10 binding, exact action set/count, and named backup/restore evidence before starting a transaction. Resolve database identity from the active connection, never from the request, and allow only the tested disposable policy. Reject blockers, stale manifests, subset/superset actions, replay against another clone, and client actor/path fields. No automatic live/shared override or cleanup route is allowed per D-09-17.</action>
  <acceptance_criteria>
    <criterion>Every drift dimension and non-disposable target is a zero-write conflict.</criterion>
    <criterion>Only the exact accepted preview can reach the transaction.</criterion>
    <criterion>Server identity supplies actor and database identity.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj -c Release --no-restore --filter 'FullyQualifiedName~PurchaseHistoryReconciliationTests&amp;FullyQualifiedName~ApplyGuard'; if ($LASTEXITCODE -ne 0) { throw 'Apply guard tests failed' }; $p='backend/database/Clean_Legacy_Imported_Bom_Idempotent.sql'; $raw=Get-Content -LiteralPath '.planning/phases/09-supplier-canonical-refresh-and-purchasing-workflow-alignment/09-WAVE0-EVIDENCE.md' -Raw; $m=[regex]::Match($raw,'(?m)^ProtectedSqlSha256=([A-Fa-f0-9]{64})$'); if (-not $m.Success -or (Get-FileHash -Algorithm SHA256 -LiteralPath $p).Hash -ne $m.Groups[1].Value) { throw 'Protected SQL hash mismatch' }; $status=(git status --porcelain=v1 --untracked-files=all -- $p | Out-String).Trim(); if ($LASTEXITCODE -ne 0 -or $status -ne "?? $p") { throw 'Protected SQL porcelain mismatch' }; git ls-files --error-unmatch -- $p *> $null; if ($LASTEXITCODE -eq 0) { throw 'Protected SQL became tracked' }; $LASTEXITCODE=0"</automated>
  </verify>
  <done>Invalid/stale/live/shared applies are rejected before any EF mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply accepted actions atomically with immutable-history evidence</name>
  <files>backend/src/IPCManagement.Api/Services/SampleData/PurchaseHistoryReconciliationService.cs, backend/tests/IPCManagement.Api.Tests/PurchaseHistoryReconciliationTests.cs</files>
  <read_first>
    <file>backend/src/IPCManagement.Api/Services/SampleData/PurchaseHistoryReconciliationService.cs</file>
    <file>backend/src/IPCManagement.Api/Models/Entities/Purchasehistoryreconciliationrun.cs</file>
    <file>backend/src/IPCManagement.Api/Models/Entities/Purchasehistoryreconciliationaction.cs</file>
    <file>backend/src/IPCManagement.Api/Data/IpcManagementContext.cs</file>
    <file>backend/tests/IPCManagement.Api.Tests/PurchaseHistoryReconciliationTests.cs</file>
  </read_first>
  <behavior>
    - "All business and audit writes commit together or roll back together."
    - "Linked/approved/received snapshots remain byte-equivalent; corrections create versioned records."
    - "Only true sample-generated dependency-free orphans are deleted; referenced duplicates are remapped/deactivated."
    - "Second apply and post-apply preview report zero actions and preserve checksums."
  </behavior>
  <action>Within one explicit EF transaction, execute only actions produced by the accepted preview. Implement D-09-08 as versioned correction for immutable operational history and D-09-09 as delete-only-for-proven-orphan, with remap/deactivate for referenced duplicates. Persist run/action audit rows, before/after hashes, actor, backup identity, and exact counts in the same transaction. Roll back on any injected action failure. Mark the original manifest applied and make replay return the prior outcome without duplicate business/audit rows. After edits run GitNexus `detect_changes` against main and inspect changed execution flows.</action>
  <acceptance_criteria>
    <criterion>Fault injection at each action boundary proves all-or-nothing rollback.</criterion>
    <criterion>Immutable snapshots/checksums survive correction and replay.</criterion>
    <criterion>Second apply and post-preview are exact no-ops with stable audit counts.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~PurchaseHistoryReconciliationTests&amp;FullyQualifiedName~Apply"</automated>
  </verify>
  <done>The exact accepted reconciliation is atomic, auditable, immutable-safe, and idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose the guarded apply action and prove it on a restored lane</name>
  <files>backend/src/IPCManagement.Api/Controllers/SampleDataController.cs, backend/tests/IPCManagement.Api.Tests/PurchaseHistoryReconciliationTests.cs, .planning/phases/09-supplier-canonical-refresh-and-purchasing-workflow-alignment/09-WAVE0-EVIDENCE.md</files>
  <read_first>
    <file>backend/src/IPCManagement.Api/Controllers/SampleDataController.cs</file>
    <file>backend/src/IPCManagement.Api/Middlewares/SampleDataProductionGuardMiddleware.cs</file>
    <file>backend/tests/IPCManagement.Api.Tests/PurchaseHistoryReconciliationTests.cs</file>
    <file>shipyard/profiles/IPCManagement/hooks/reset.sh</file>
  </read_first>
  <behavior>
    - "Authorized Development call with exact manifest applies once."
    - "Production/unauthorized calls and changed manifests are rejected."
    - "Restored lane preview→apply→apply→preview ends with apply/no-op/no-op and stable protected checksums."
  </behavior>
  <action>Run impact for `SampleDataController` before adding the POST apply action. Reuse the same production guard and authorization as preview; accept only manifest ID/hash and exact accepted action IDs. Return counts, run ID, and audit reference without physical paths. On `ipc_lane1`, restore the Wave 0 template, preview, apply, apply again, preview again, and append command/output evidence to `09-WAVE0-EVIDENCE.md`; run each native command in fail-fast PowerShell. Before restore/apply and after post-preview, compare the protected SQL SHA-256/status/tracked invariants to the Wave 0 baseline. Do not use a command that resets the source worktree or points at a live/shared database.</action>
  <acceptance_criteria>
    <criterion>The endpoint returns one applied run and stable no-op replay.</criterion>
    <criterion>The lane evidence includes exact before/after protected checksums and action counts.</criterion>
    <criterion>Production and wrong-role access remain denied.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj -c Release --no-restore --filter 'FullyQualifiedName~PurchaseHistoryReconciliationTests'; if ($LASTEXITCODE -ne 0) { throw 'Reconciliation suite failed' }; $p='backend/database/Clean_Legacy_Imported_Bom_Idempotent.sql'; $raw=Get-Content -LiteralPath '.planning/phases/09-supplier-canonical-refresh-and-purchasing-workflow-alignment/09-WAVE0-EVIDENCE.md' -Raw; $m=[regex]::Match($raw,'(?m)^ProtectedSqlSha256=([A-Fa-f0-9]{64})$'); if (-not $m.Success -or (Get-FileHash -Algorithm SHA256 -LiteralPath $p).Hash -ne $m.Groups[1].Value) { throw 'Protected SQL hash mismatch' }; $status=(git status --porcelain=v1 --untracked-files=all -- $p | Out-String).Trim(); if ($LASTEXITCODE -ne 0 -or $status -ne "?? $p") { throw 'Protected SQL porcelain mismatch' }; git ls-files --error-unmatch -- $p *> $null; if ($LASTEXITCODE -eq 0) { throw 'Protected SQL became tracked' }; $LASTEXITCODE=0"</automated>
  </verify>
  <done>Reconciliation is proven end-to-end on a restored disposable lane and remains unavailable to live/shared targets.</done>
</task>

</tasks>

<threat_model>

| Threat ID | Source | Trust boundary | STRIDE | Threat | Disposition | Mitigation | Verification |
|---|---|---|---|---|---|---|---|
| T-09-13 | Client manifest | Client to apply API | Tampering | Action subset/count/hash is changed | mitigate | Server rebuild and exact set equality | Drift/zero-write tests |
| T-09-14 | Connection configuration | API to SQL Server | Elevation of privilege | Apply reaches live/shared database | mitigate | Server-resolved disposable policy; no override | Non-disposable rejection tests |
| T-09-15 | Multi-action transaction | Service to operational history | Tampering | Partial apply or destructive immutable update | mitigate | Explicit transaction and version/remap policy | Fault-injection/immutable checksum tests |
| T-09-16 | Retried request | Client to audit/business rows | Repudiation | Duplicate apply creates duplicate history | mitigate | Unique manifest identity and replayed prior result | Second-apply/no-op tests |
| T-09-SC | Package manifests | npm/NuGet supply chain | Tampering | Apply adds unaudited dependency | mitigate | Existing EF/framework only | Package manifest diff empty |

</threat_model>

<verification>
Before the plan commit, run GitNexus `detect_changes({scope:"compare",base_ref:"main"})`; inspect affected symbols/processes and stop if scope exceeds this plan.
All reconciliation tests pass on unit/integration and restored disposable lane; `detect_changes` stays inside reconciliation flows.
</verification>

<success_criteria>
The audited 20.7 reconciliation can be safely applied once on a disposable lane and replayed with no additional mutation.
</success_criteria>

<output>
Create `.planning/phases/09-supplier-canonical-refresh-and-purchasing-workflow-alignment/09-05-SUMMARY.md` when done.
</output>
