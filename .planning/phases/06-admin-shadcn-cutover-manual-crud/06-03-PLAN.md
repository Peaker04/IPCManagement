---
phase: 06-admin-shadcn-cutover-manual-crud
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx
  - frontend/src/features/workflow/components/bom/BomScopeSummary.tsx
  - frontend/src/features/workflow/components/bom/index.ts
  - frontend/src/features/workflow/components/bom/BomMigrationWorkbench.test.tsx
  - frontend/src/features/workflow/pages/AdminDataPage.tsx
autonomous: true
requirements: [CRUD-01, UI-01, UI-05]
must_haves:
  truths:
    - "D-06-01: /admin-data, tab order, labels and unrelated admin panels remain unchanged while BOM ownership is extracted."
    - "D-06-05: scope summary renders only server totals; search/display filters never change it."
    - "D-06-11: no-preview/loading/stale/error/success feedback is textual, contextual and accessible."
    - "Manual current-BOM mode remains reachable while canonical preview mode is introduced."
  artifacts:
    - path: "frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx"
      provides: "BOM-specific composition and state ownership"
    - path: "frontend/src/features/workflow/components/bom/BomScopeSummary.tsx"
      provides: "Immutable dataset-wide manifest summary"
    - path: "frontend/src/features/workflow/pages/AdminDataPage.tsx"
      provides: "Thin route/tab host for extracted BOM workbench"
  key_links:
    - from: "frontend/src/features/workflow/pages/AdminDataPage.tsx"
      to: "frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx"
      via: "bom-import tab composition only"
      pattern: "BomMigrationWorkbench"
    - from: "frontend/src/features/workflow/components/bom/BomScopeSummary.tsx"
      to: "frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts"
      via: "serverManifestTotals and serverCurrentTotal props"
      pattern: "manifest|serverCurrentTotal"
---

# Phase 6 Plan 03: Extract workbench and immutable dataset summary

<objective>
Surgically extract the dirty Admin page's BOM surface into a feature-owned workbench and render canonical server scope/safety state without changing the route or unrelated tabs.

Purpose: Reduce monolith risk while preserving the operational information architecture and client/server authority boundary.
Output: Workbench composition, dataset summary, barrel export and a thin AdminDataPage integration.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-CONTEXT.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-02-PLAN.md
@frontend/src/features/workflow/pages/AdminDataPage.tsx
@frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts
@frontend/src/components/common/ContextStrip.tsx
@frontend/src/components/common/InlineAlert.tsx
</context>

<tasks>

<task type="auto" id="06-03-01" tdd="true">
  <name>Build workbench composition and server-only summary states</name>
  <files>frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx, frontend/src/features/workflow/components/bom/BomScopeSummary.tsx, frontend/src/features/workflow/components/bom/index.ts, frontend/src/features/workflow/components/bom/BomMigrationWorkbench.test.tsx</files>
  <read_first>
    <file>frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts</file>
    <file>frontend/src/features/workflow/pages/AdminDataPage.tsx</file>
    <file>frontend/src/components/common/ContextStrip.tsx</file>
    <file>frontend/src/components/common/InlineAlert.tsx</file>
    <file>.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md</file>
  </read_first>
  <behavior>
    - Summary shows exact total/Unchanged/Create/Version/archive/deactivate/delete/regenerate/history-kept/blocker server fields.
    - Search/display projection changes visible rows only; summary values and apply eligibility remain equal.
    - No-preview/loading/stale/error/success states include exact visible recovery copy and appropriate alert/status semantics.
    - Current and Preview canonical modes remain reachable without exposing old Commit as canonical action.
  </behavior>
  <action>Confirm prerequisite hashes, then run upstream impact for future `BomMigrationWorkbench`, `BomScopeSummary` and their exported props plus existing `ContextStrip`, `InlineAlert`, `ViewSwitcher` and adapter selectors. Implement D-06-01, D-06-05 and D-06-11 using existing IPC/shadcn-style primitives and tokens only. The workbench owns canonical file/scope/effective-date/reason, manifest, display search and dialog-open state through the adapter; tier buttons remain current/manual display context and never narrow the canonical manifest. `BomScopeSummary` receives server values, keeps stable skeleton slots and preserves the three action vocabularies. Canonical copy uses `Xem trước thay đổi`, `Preview canonical` and exact UI-SPEC feedback; retain legacy helpers/hooks in code but do not advertise old commit as canonical. Turn only workbench Wave-0 assertions green. No table/dialog implementation, DataTableShell/global CSS/shared dialog edit or old hook removal. Run ownership and detect_changes before commit.</action>
  <acceptance_criteria>
    <criterion>Summary fields map one-to-one to manifest/server totals and never read filtered arrays.</criterion>
    <criterion>All required feedback states include visible text and live/alert semantics.</criterion>
    <criterion>Workbench uses existing tokens/primitives and exposes current/preview/manual entry points for later waves.</criterion>
    <criterion>Worktree ownership and GitNexus scope are expected.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; npm run test:unit --workspace frontend -- --run frontend/src/features/workflow/components/bom/BomMigrationWorkbench.test.tsx frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.test.ts; if($LASTEXITCODE-ne 0){throw 'workbench tests failed'}; npm run build --workspace frontend; if($LASTEXITCODE-ne 0){throw 'build failed'}; powershell -File scripts/Test-BomPhase6Prerequisites.ps1 -EvidenceDirectory .artifacts/bom-v1.1/gate-d/phase6-preedit -VerifyOwnership; if($LASTEXITCODE-ne 0){throw 'ownership drift'}</automated>
  </verify>
  <done>The feature workbench and immutable dataset summary are implemented and tested.</done>
</task>

<task type="auto" id="06-03-02">
  <name>Replace only the Admin BOM block with the extracted workbench</name>
  <files>frontend/src/features/workflow/pages/AdminDataPage.tsx</files>
  <read_first>
    <file>frontend/src/features/workflow/pages/AdminDataPage.tsx</file>
    <file>frontend/src/features/workflow/components/bom/index.ts</file>
    <file>frontend/src/routes/AppRouter.tsx</file>
  </read_first>
  <action>Re-run impact for `AdminDataPage` and record the stale indexed result as a lower bound; compare the exact dirty blob hash from PREREQUISITES before editing. Replace only BOM state/handlers/markup owned by the `bom-import` tab with `BomMigrationWorkbench`, preserving route `/admin-data`, admin tab order/query handling and every Contract, cleanup, inventory, statistics, audit and employees branch byte-for-byte where feasible. Pass only route context such as `dishId`; do not duplicate canonical totals or state in the page. Do not remove legacy imports/hooks until Phase 7 unless no longer called by the extracted block; if retained compatibility code is temporarily unreachable, keep it characterized rather than deleting. Run full diff review scoped to the BOM line ranges, ownership script and detect_changes. Any unrelated dirty hunk, HIGH/CRITICAL unexpected flow or route/tab regression blocks.</action>
  <acceptance_criteria>
    <criterion>/admin-data and BOM tab render the extracted workbench with unchanged navigation/labels.</criterion>
    <criterion>Unrelated AdminDataPage tab behavior and dirty changes are preserved.</criterion>
    <criterion>No old backend API/hook is removed and no Phase 7 retirement occurs.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; npm run lint --workspace frontend; if($LASTEXITCODE-ne 0){throw 'lint failed'}; npm run build --workspace frontend; if($LASTEXITCODE-ne 0){throw 'build failed'}; npm run test:smoke --workspace frontend -- --grep 'admin'; if($LASTEXITCODE-ne 0){throw 'admin smoke failed'}; powershell -File scripts/Test-BomPhase6Prerequisites.ps1 -EvidenceDirectory .artifacts/bom-v1.1/gate-d/phase6-preedit -VerifyOwnership; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'unexpected GitNexus flow'}</automated>
  </verify>
  <done>AdminDataPage is a thin, route-preserving BOM host with all unrelated work preserved.</done>
</task>

</tasks>

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-06-06" category="Tampering" disposition="mitigate">Filtered UI state could replace server authority; summary accepts only manifest/server props and equality tests block.</threat>
  <threat id="T-06-07" category="Denial of service" disposition="mitigate">Dirty monolith extraction could break other tabs; hash gating, surgical diff and route smoke block.</threat>
  <verification>Focused tests, full lint/build, admin smoke, dirty ownership and per-symbol impact/detect_changes.</verification>
</threat_model>

<verification>
Worktree ownership passes, workbench tests are green, admin smoke preserves route/tab behavior, and only BOM-owned AdminDataPage ranges plus new feature files changed.
</verification>

<success_criteria>
- Dataset totals remain server-authoritative under search.
- The extracted workbench is visible on the existing BOM tab.
- Unrelated Admin surfaces and legacy compatibility contracts remain intact.
</success_criteria>

<output>
Create `.planning/phases/06-admin-shadcn-cutover-manual-crud/06-03-SUMMARY.md` when done.
</output>
