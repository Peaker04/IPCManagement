---
phase: 06-admin-shadcn-cutover-manual-crud
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - frontend/src/features/workflow/components/bom/BomMigrationWorkbench.test.tsx
  - frontend/src/features/workflow/components/bom/BomTableViewport.test.tsx
  - frontend/src/features/workflow/components/bom/BomManualDialogs.test.tsx
  - frontend/src/features/workflow/components/bom/BomApplyDialog.test.tsx
  - .artifacts/bom-v1.1/gate-d/phase6-wave0/phase6-wave0.json
autonomous: true
requirements: [CRUD-01, UI-01, UI-02, UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "D-06-03..D-06-07 and D-06-14 are captured as executable component contracts before production UI extraction."
    - "D-06-08..D-06-11 manual and destructive dialog behaviors have failing-first keyboard, naming, focus, reason and gating tests."
    - "Every red result is a named missing-behavior assertion, never compile, discovery, environment or dependency failure."
  artifacts:
    - path: "frontend/src/features/workflow/components/bom/BomMigrationWorkbench.test.tsx"
      provides: "Dataset authority, stable summary and extraction contract tests"
    - path: "frontend/src/features/workflow/components/bom/BomTableViewport.test.tsx"
      provides: "Fixed table/search/action geometry component tests"
    - path: "frontend/src/features/workflow/components/bom/BomManualDialogs.test.tsx"
      provides: "Add/version/stop accessibility and payload behavior tests"
    - path: "frontend/src/features/workflow/components/bom/BomApplyDialog.test.tsx"
      provides: "Destructive prerequisite and focus/dismissal tests"
  key_links:
    - from: "frontend/src/features/workflow/components/bom/*.test.tsx"
      to: ".planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md"
      via: "exact UI-SPEC copy, dimensions, states and keyboard assertions"
      pattern: "520|440|1038|1120|PHASE6_MISSING_BEHAVIOR"
---

# Phase 6 Plan 02: Wave-0 executable UI contracts

<objective>
Create a compile-clean failing-first component contract suite for the workbench, table, manual dialogs and destructive apply before production components are extracted.

Purpose: Make the approved 6/6 UI-SPEC and locked decisions enforceable rather than relying on visual intent.
Output: Four focused Wave-0 test files and a machine-audited red-state report.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-CONTEXT.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-PATTERNS.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-01-PLAN.md
@frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts
@frontend/src/features/workflow/pages/AdminDataPage.tsx
@frontend/src/components/common/DataTableShell.tsx
</context>

<tasks>

<task type="auto" id="06-02-01" tdd="true">
  <name>Write failing-first workbench and stable-table contracts</name>
  <files>frontend/src/features/workflow/components/bom/BomMigrationWorkbench.test.tsx, frontend/src/features/workflow/components/bom/BomTableViewport.test.tsx</files>
  <read_first>
    <file>.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md</file>
    <file>frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts</file>
    <file>frontend/src/features/workflow/pages/AdminDataPage.tsx</file>
    <file>frontend/src/components/common/DataTableShell.tsx</file>
  </read_first>
  <behavior>
    - Dataset summary remains server-manifest-based when search changes visible rows.
    - Full/search/loading/error/dataset-empty/search-empty keep one toolbar/table shell and exact colgroup.
    - Desktop shell is 520px, narrow shell 440px; current min width 1038px and preview 1120px.
    - Search icon is inert with at least 36px input padding; action group never wraps or shrinks.
    - Feedback uses visible text plus roles and the table region remains focusable/named.
  </behavior>
  <action>Verify Plan 01 ownership PASS, then run upstream impact for every future symbol under test: `BomMigrationWorkbench`, `BomScopeSummary`, `BomTableToolbar`, `BomTableViewport`, `renderBomTableState` and `filterBomDisplayRows`; record not-found as expected future ownership. Write Testing Library tests with exact D-06-03..07, D-06-11, D-06-13 and D-06-14 copy/dimensions/semantics from UI-SPEC. Tests may use a test-local dynamic import adapter so TypeScript/build succeeds before component files exist; each absent behavior must fail only with assertion prefix `PHASE6_MISSING_BEHAVIOR:`. Do not edit production components, `DataTableShell`, `index.css` or AdminDataPage. Compare hashes and run detect_changes before commit.</action>
  <acceptance_criteria>
    <criterion>Tests enumerate all six table states, both colgroups, fixed viewports, local scroll, search clearance and same-row actions.</criterion>
    <criterion>Search tests prove manifest totals and apply state are unchanged while visible rows change.</criterion>
    <criterion>Red failures use only PHASE6_MISSING_BEHAVIOR and the test project compiles/discovers normally.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; npm run build --workspace frontend; if($LASTEXITCODE-ne 0){throw 'Wave-0 compile failed'}; npm run test:unit --workspace frontend -- --run frontend/src/features/workflow/components/bom/BomMigrationWorkbench.test.tsx frontend/src/features/workflow/components/bom/BomTableViewport.test.tsx --reporter=verbose; if($LASTEXITCODE-eq 0){throw 'Wave-0 workbench tests unexpectedly green'}; powershell -File scripts/Test-BomPhase6Prerequisites.ps1 -EvidenceDirectory .artifacts/bom-v1.1/gate-d/phase6-preedit -VerifyOwnership; if($LASTEXITCODE-ne 0){throw 'ownership drift'}</automated>
  </verify>
  <done>Workbench/table behaviors are represented by compile-clean, intentionally red assertions.</done>
</task>

<task type="auto" id="06-02-02" tdd="true">
  <name>Write failing-first manual and destructive dialog contracts</name>
  <files>frontend/src/features/workflow/components/bom/BomManualDialogs.test.tsx, frontend/src/features/workflow/components/bom/BomApplyDialog.test.tsx, .artifacts/bom-v1.1/gate-d/phase6-wave0/phase6-wave0.json</files>
  <read_first>
    <file>.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md</file>
    <file>frontend/src/components/ui/dialog.tsx</file>
    <file>frontend/src/features/projects/dishCatalogApi.ts</file>
    <file>frontend/src/features/projects/bomReconciliationApi.ts</file>
  </read_first>
  <behavior>
    - Add/version/stop support global and customer context; published edit versions and stop never hard-delete.
    - Reason is trimmed 1-500 where required and no actor control or payload field exists.
    - Dialog title/description are associated, Cancel receives initial focus and focus returns to the logical trigger.
    - Apply lists hash/date/counts/blockers/backup/Gate C/freshness and all false prerequisites disable CTA with exact copy.
    - Escape/backdrop are safe before mutation and cannot dismiss noncancellable apply progress.
  </behavior>
  <action>Run and record impact for future `BomDialogPrimitives`, `BomLineDialog`, `BomStopDialog`, `BomApplyDialog`, `validateBomReason`, `getApplyDisabledReasons` and existing Base UI package boundary; do not edit HIGH `DialogContent`. Add dynamic-import Wave-0 component tests for D-06-08..14 and exact UI-SPEC copy. Create Vitest JSON evidence and reject zero discovery, compile/type errors, missing dependency/jsdom errors or any failure not prefixed `PHASE6_MISSING_BEHAVIOR:`. No production UI or shared dialog modification is allowed. Verify ownership/detect_changes.</action>
  <acceptance_criteria>
    <criterion>All four dialogs have naming, description, keyboard, focus and loading/error tests.</criterion>
    <criterion>Every apply prerequisite is independently false-tested with exact visible reason.</criterion>
    <criterion>Manual payload tests enforce reason/no actor/version-or-close intent for both scopes.</criterion>
    <criterion>Vitest JSON contains only expected behavior-red results.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; $out='.artifacts/bom-v1.1/gate-d/phase6-wave0'; New-Item -ItemType Directory -Force $out|Out-Null; npm run test:unit --workspace frontend -- --run frontend/src/features/workflow/components/bom/BomManualDialogs.test.tsx frontend/src/features/workflow/components/bom/BomApplyDialog.test.tsx --reporter=json --outputFile="$out/phase6-wave0.json"; if($LASTEXITCODE-eq 0){throw 'Wave-0 dialog tests unexpectedly green'}; if(-not(Test-Path "$out/phase6-wave0.json")){throw 'Wave-0 JSON missing'}; $json=Get-Content -Raw "$out/phase6-wave0.json" | ConvertFrom-Json; if($json.numTotalTests-lt 1){throw 'zero tests discovered'}; $messages=($json.testResults.assertionResults.failureMessages -join "`n"); if($messages-notmatch 'PHASE6_MISSING_BEHAVIOR:' -or $messages-match '(?i)compile|discovery|dependency|jsdom|timeout|infrastructure'){throw 'invalid Wave-0 failure'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}</automated>
  </verify>
  <done>Dialog/manual/apply behaviors are compile-clean and intentionally red for implementation waves.</done>
</task>

</tasks>

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-06-03" category="Repudiation" disposition="mitigate">Manual changes could omit reason/actor guarantees; Wave-0 payload and history tests make the contract explicit.</threat>
  <threat id="T-06-04" category="Denial of service" disposition="mitigate">Unstable nested scroll/layout could make controls unreachable; fixed viewport and contained overflow tests block.</threat>
  <threat id="T-06-05" category="Elevation of privilege" disposition="mitigate">Destructive CTA could enable on partial evidence; independent false-prerequisite tests block.</threat>
  <verification>Compile-clean test discovery, marker-only red audit, ownership comparison and GitNexus impact/detect_changes.</verification>
</threat_model>

<verification>
Frontend compiles, every required test is discovered exactly once, red failures are behavior markers only, and no production source or protected dirty file changes.
</verification>

<success_criteria>
- All six Phase 6 requirements and D-06-03..14 are represented in executable tests.
- Later waves can turn each focused file green without weakening the assertions.
</success_criteria>

<output>
Create `.planning/phases/06-admin-shadcn-cutover-manual-crud/06-02-SUMMARY.md` when done.
</output>
