---
phase: 06-admin-shadcn-cutover-manual-crud
plan: 07
type: execute
wave: 7
depends_on: [06-06]
files_modified:
  - frontend/tests/control-surface.spec.ts
  - frontend/tests/ui-audit.spec.ts
  - frontend/tests/route-smoke.spec.ts
  - frontend/tests/visual-routes.spec.ts
  - frontend/tests/visual-routes.spec.ts-snapshots/admin-data-desktop-chromium-win32.png
  - frontend/tests/visual-routes.spec.ts-snapshots/admin-data-mobile-chromium-win32.png
  - frontend/tests/visual-routes.spec.ts-snapshots/admin-data-bom-1280-chromium-win32.png
  - scripts/Test-BomPhase6Compatibility.ps1
  - .artifacts/bom-v1.1/gate-d/GATE-D.md
autonomous: true
requirements: [CRUD-01, UI-01, UI-02, UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "D-06-13 and D-06-14: 1365x900, 1280x900 and 390x844 tests prove geometry, contained overflow, keyboard/focus and safety."
    - "Canonical preview/apply and manual global/customer CRUD pass integration/E2E against executed Phase 5 contracts."
    - "Old APIs/hooks remain for Phase 7, but canonical UI has zero runtime calls to the old writer and no old Commit control."
    - "Gate D fails closed on snapshot drift, blocker/backup/Gate C bypass, page overflow, a11y failure, unexpected dirty file or HIGH/CRITICAL GitNexus flow."
  artifacts:
    - path: "frontend/tests/ui-audit.spec.ts"
      provides: "Cross-state table geometry and page-overflow proof"
    - path: "frontend/tests/control-surface.spec.ts"
      provides: "Keyboard, focus, payload and manual/apply safety flow proof"
    - path: ".artifacts/bom-v1.1/gate-d/GATE-D.md"
      provides: "Phase 7 compatibility entry gate"
  key_links:
    - from: "scripts/Test-BomPhase6Compatibility.ps1"
      to: ".artifacts/bom-v1.1/gate-c/GATE-C.md"
      via: "requires Phase 5 integration evidence and safe apply configuration"
      pattern: "Gate C|GateCEvidenceId"
    - from: "frontend/tests/control-surface.spec.ts"
      to: "/api/admin/bom-reconciliation"
      via: "preview/status/apply fixtures and request inspection"
      pattern: "admin/bom-reconciliation"
---

# Phase 6 Plan 07: E2E, visual, accessibility and Gate D

<objective>
Prove the complete canonical Admin workbench and manual CRUD across keyboard, safety, data authority and exact geometry, then emit a fail-closed compatibility gate for Phase 7.

Purpose: Turn the approved UI contract into release evidence while preserving dirty snapshots and legacy-retirement boundaries.
Output: Extended Playwright suites, targeted snapshots, compatibility runner and GATE-D report.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-VALIDATION.md
@.planning/phases/05-transactional-apply-downstream-reconciliation/05-09-PLAN.md
@frontend/tests/control-surface.spec.ts
@frontend/tests/ui-audit.spec.ts
@frontend/tests/visual-routes.spec.ts
@frontend/tests/route-smoke.spec.ts
</context>

<tasks>

<task type="auto" id="06-07-01">
  <name>Extend browser fixtures for authority, keyboard and exact geometry</name>
  <files>frontend/tests/control-surface.spec.ts, frontend/tests/ui-audit.spec.ts, frontend/tests/route-smoke.spec.ts, frontend/tests/visual-routes.spec.ts, frontend/tests/visual-routes.spec.ts-snapshots/admin-data-desktop-chromium-win32.png, frontend/tests/visual-routes.spec.ts-snapshots/admin-data-mobile-chromium-win32.png, frontend/tests/visual-routes.spec.ts-snapshots/admin-data-bom-1280-chromium-win32.png</files>
  <read_first>
    <file>frontend/tests/control-surface.spec.ts</file>
    <file>frontend/tests/ui-audit.spec.ts</file>
    <file>frontend/tests/visual-routes.spec.ts</file>
    <file>frontend/tests/route-smoke.spec.ts</file>
    <file>.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md</file>
  </read_first>
  <action>Run upstream impact for existing `stubOperationalApis`, `stubAuditApi`, `stubVisualApi`, route test definitions and future BOM fixture helpers; record test symbols not indexed as lower bounds. Hash-gate all four test files and every snapshot; explicitly protect user-dirty dashboard desktop/mobile snapshots from overwrite/stage. Extend existing harnesses only. Control-surface must keyboard-open/cancel add/version/stop/apply, assert title/description, Cancel initial focus/return focus, reason validation, nondismissible apply progress, exact apply payload omissions, both global/customer manual flows, stale/blocker/backup/Gate C/server-disabled cases and search-before/after server total/payload equality. UI audit runs full/search/loading/error/dataset-empty/search-empty at 1365x900 and 1280x900, comparing toolbar/shell/header boxes within 1px, exact 520px/440px viewport, local scroll, document overflow <=2px, search clearance and Sửa/Ngừng same top/nowrap. Visual keeps existing desktop/mobile admin baselines and adds targeted 1280; inspect before accepting. Smoke preserves route/tab/handoff. Per D-06-13/14, do not claim a card-table mobile redesign. Run ownership/detect_changes before commit.</action>
  <acceptance_criteria>
    <criterion>All safety, keyboard, focus, both-scope manual and payload authority flows are browser-tested.</criterion>
    <criterion>All state/column/table boxes stay within 1px and page overflow is absent at target widths.</criterion>
    <criterion>Search clearance and same-row actions are computed-style/geometry assertions, not screenshot-only.</criterion>
    <criterion>No dashboard or unrelated snapshot changes.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; npm run test:controls --workspace frontend; if($LASTEXITCODE-ne 0){throw 'control surface failed'}; npm run test:smoke --workspace frontend; if($LASTEXITCODE-ne 0){throw 'smoke failed'}; npm run test:ui-audit --workspace frontend; if($LASTEXITCODE-ne 0){throw 'UI audit failed'}; npm run test:visual --workspace frontend; if($LASTEXITCODE-ne 0){throw 'visual failed'}; $dirty=git diff --name-only; if($dirty -match 'dashboard-(desktop|mobile)-chromium-win32.png'){throw 'protected dashboard snapshots changed'}</automated>
  </verify>
  <done>Browser evidence proves Phase 6 behavior, accessibility and geometry at all target widths.</done>
</task>

<task type="auto" id="06-07-02">
  <name>[BLOCKING] Run Phase 6 compatibility Gate D</name>
  <files>scripts/Test-BomPhase6Compatibility.ps1, .artifacts/bom-v1.1/gate-d/GATE-D.md</files>
  <read_first>
    <file>.artifacts/bom-v1.1/gate-c/GATE-C.md</file>
    <file>.artifacts/bom-v1.1/gate-d/phase6-preedit/PREREQUISITES.md</file>
    <file>.planning/phases/06-admin-shadcn-cutover-manual-crud/06-VALIDATION.md</file>
    <file>frontend/src/features/projects/dishCatalogApi.ts</file>
    <file>frontend/src/features/projects/bomReconciliationApi.ts</file>
    <file>AGENTS.md</file>
  </read_first>
  <action>Run impact for `BomMigrationWorkbench`, `bomReconciliationApi`, `dishCatalogApi`, `AdminDataPage`, all BOM components and test helpers; run final detect_changes against the Phase 6 ownership manifest. `Test-BomPhase6Compatibility.ps1` fails closed unless: Gate C PASS/evidence/backup remains valid; actual backend canonical preview/apply/manual integration tests pass; all Phase 6 unit/lint/build/control/smoke/UI-audit/visual suites pass without skips; canonical UI calls only `/api/admin/bom-reconciliation` and manual dish endpoints; old template/preview/commit hooks/endpoints remain inventoried for Phase 7 but have zero visible canonical control and zero runtime request in Phase 6 E2E; search leaves server totals/token/payload unchanged; every apply prerequisite is tested; global/customer add/version/stop preserve history; target geometry/a11y passes; protected dirty hashes and dashboard snapshots remain; no new package or Phase 7 deletion; detect_changes has only expected flows and no unresolved HIGH/CRITICAL. Emit requirement and D-06-01..14 traceability. If any condition fails, write BLOCKED and Phase 7 must not start; do not weaken snapshots, enable apply config or delete old surfaces to force PASS.</action>
  <acceptance_criteria>
    <criterion>GATE-D maps six requirements and D-06-01..14 to non-skipped evidence.</criterion>
    <criterion>Canonical/manual integration, frontend quality, a11y/geometry and ownership all pass.</criterion>
    <criterion>Old contract remains inventoried but is not a canonical runtime writer; retirement remains Phase 7.</criterion>
    <criterion>Any missing evidence/unexpected flow produces BLOCKED and nonzero exit.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; powershell -File scripts/Test-BomPhase6Compatibility.ps1 -EvidenceDirectory .artifacts/bom-v1.1/gate-d -RequireGateC -RequireBackendIntegration -RequireFrontendFull -RequireOldWriterRuntimeConsumersZero; if($LASTEXITCODE-ne 0){throw 'Gate D BLOCKED'}; $gate=Get-Content -Raw .artifacts/bom-v1.1/gate-d/GATE-D.md; if($gate-notmatch '(?im)status\s*[:|]\s*PASS|Gate D\s+PASS'){throw 'Gate D not PASS'}; npm run test:unit --workspace frontend; if($LASTEXITCODE-ne 0){throw 'full unit failed'}; npm run lint --workspace frontend; if($LASTEXITCODE-ne 0){throw 'lint failed'}; npm run build --workspace frontend; if($LASTEXITCODE-ne 0){throw 'build failed'}; powershell -File scripts/Test-BomPhase6Prerequisites.ps1 -EvidenceDirectory .artifacts/bom-v1.1/gate-d/phase6-preedit -VerifyOwnership; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}; git diff --check; if($LASTEXITCODE-ne 0){throw 'diff check failed'}</automated>
  </verify>
  <done>Phase 6 is Gate D PASS and Phase 7 may begin, or remains explicitly BLOCKED without retirement.</done>
</task>

</tasks>

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-06-16" category="Elevation of privilege" disposition="mitigate">UI-only mocks could mask server safety bypass; Gate D requires executed backend integration/Gate C evidence.</threat>
  <threat id="T-06-17" category="Tampering" disposition="mitigate">Snapshot/test weakening could hide regressions; protected hashes, computed geometry and full suite block.</threat>
  <threat id="T-06-18" category="Repudiation" disposition="mitigate">Legacy writer could remain a hidden runtime path; request inventory proves zero canonical runtime consumers while preserving Phase 7 removal scope.</threat>
  <verification>ASVS L1 integration/browser review, Gate C/D, all quality suites, per-symbol impact, ownership and final detect_changes.</verification>
</threat_model>

<verification>
Gate D PASS is the only successful terminal state; otherwise Phase 7 is blocked. No source retirement or production config change is permitted in this plan.
</verification>

<success_criteria>
- All six requirements and fourteen decisions have non-skipped evidence.
- Canonical preview/apply and manual CRUD work without filtered client authority.
- Geometry, accessibility and dirty-worktree preservation pass at 1365, 1280 and 390 widths.
- Phase 7 starts only after Gate D PASS.
</success_criteria>

<output>
Create `.planning/phases/06-admin-shadcn-cutover-manual-crud/06-07-SUMMARY.md` when done.
</output>
