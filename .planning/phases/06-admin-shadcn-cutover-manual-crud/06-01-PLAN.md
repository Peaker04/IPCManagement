---
phase: 06-admin-shadcn-cutover-manual-crud
plan: 01
type: execute
wave: 1
depends_on: [05-09]
files_modified:
  - scripts/Test-BomPhase6Prerequisites.ps1
  - .artifacts/bom-v1.1/gate-d/phase6-preedit/owned-paths.txt
  - .artifacts/bom-v1.1/gate-d/phase6-preedit/PREREQUISITES.md
  - frontend/src/features/projects/bomReconciliationApi.ts
  - frontend/src/features/projects/bomReconciliationApi.test.ts
  - frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts
  - frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.test.ts
  - frontend/src/features/projects/dishCatalogApi.ts
  - frontend/src/features/projects/dishCatalogApi.test.ts
autonomous: true
requirements: [CRUD-01, UI-01, UI-02]
must_haves:
  truths:
    - "D-06-05 and D-06-09: search can never alter manifest totals, apply payload or eligibility; apply fails closed without executed Phase 5 Gate C evidence."
    - "D-06-08: manual add/version/stop request shapes remain available and carry bounded reason without accepting client actor."
    - "Canonical types are copied from the executed Phase 4/5 DTO/OpenAPI surface, not invented from the current legacy import model."
    - "Old template/preview/commit hooks remain intact for Phase 7 compatibility retirement."
  artifacts:
    - path: "frontend/src/features/projects/bomReconciliationApi.ts"
      provides: "Server-authoritative canonical preview/status/apply RTK contract"
    - path: "frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts"
      provides: "Pure current/manifest/display separation and freshness invalidation"
    - path: ".artifacts/bom-v1.1/gate-d/phase6-preedit/PREREQUISITES.md"
      provides: "Gate A/B/C, DTO, ownership and GitNexus readiness evidence"
  key_links:
    - from: "frontend/src/features/projects/bomReconciliationApi.ts"
      to: "/api/admin/bom-reconciliation"
      via: "bounded multipart preview/status/apply requests using actual server DTO fields"
      pattern: "admin/bom-reconciliation"
    - from: "frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts"
      to: "frontend/src/features/projects/bomReconciliationApi.ts"
      via: "RTK hooks and immutable manifest-derived eligibility"
      pattern: "use.*Bom.*Query|use.*Bom.*Mutation"
---

# Phase 6 Plan 01: Readiness, canonical API and state authority

<objective>
Prove Phase 5/Gate C and dirty-worktree readiness, then establish the canonical RTK and pure state adapter that every Phase 6 view consumes without giving filtered UI state authority.

Purpose: Remove contract ambiguity and fail closed before the dirty Admin page or any destructive control is touched.
Output: Phase 6 ownership evidence, canonical API adapter, state adapter and manual reason-payload compatibility.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-CONTEXT.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-PATTERNS.md
@.planning/phases/04-canonical-parser-preview-dependency-classifier/04-03-PLAN.md
@.planning/phases/05-transactional-apply-downstream-reconciliation/05-08-PLAN.md
@.planning/phases/05-transactional-apply-downstream-reconciliation/05-09-PLAN.md
@frontend/src/features/projects/dishCatalogApi.ts

<interfaces>
Executed Phase 4/5 server files are authoritative: `BomCanonicalPreviewDto`, `BomCanonicalApplyDto`, and `AdminBomReconciliationController`. The browser may submit only workbook, effective date, preview ID and bounded reason exactly as delivered; actor, hash, counts, candidate IDs and filesystem path are forbidden.

Existing manual surface: `UpsertDishBomLineRequest` and `useAddDishBomLineMutation`, `useUpdateDishBomLineMutation`, `useCloseDishBomLineMutation`. Legacy `useDownloadBomTemplateMutation`, `usePreviewBomImportMutation` and `useCommitBomImportMutation` remain exported unchanged until Phase 7.
</interfaces>
</context>

<tasks>

<task type="auto" id="06-01-01">
  <name>Fail closed on Phase 5 evidence, ownership and blast radius</name>
  <files>scripts/Test-BomPhase6Prerequisites.ps1, .artifacts/bom-v1.1/gate-d/phase6-preedit/owned-paths.txt, .artifacts/bom-v1.1/gate-d/phase6-preedit/PREREQUISITES.md</files>
  <read_first>
    <file>.artifacts/bom-v1.1/gate-a/GATE-A.md</file>
    <file>.artifacts/bom-v1.1/gate-b/GATE-B.md</file>
    <file>.artifacts/bom-v1.1/gate-c/GATE-C.md</file>
    <file>backend/src/IPCManagement.Api/Models/DTOs/Bom/BomCanonicalPreviewDto.cs</file>
    <file>backend/src/IPCManagement.Api/Models/DTOs/Bom/BomCanonicalApplyDto.cs</file>
    <file>backend/src/IPCManagement.Api/Controllers/AdminBomReconciliationController.cs</file>
    <file>AGENTS.md</file>
  </read_first>
  <action>Before any frontend source edit, create an immutable Phase 6 baseline containing HEAD, status, every dirty/untracked path, SHA-256 for every owned and protected file, actual OpenAPI/controller route and DTO member inventory, Gate A/B/C status/evidence IDs, backup marker and apply feature-state. Run upstream GitNexus impact for every existing/future symbol named in this plan: `dishCatalogApi`, `buildBomImportFormData`, `UpsertDishBomLineRequest`, `bomReconciliationApi`, `buildBomCanonicalPreviewFormData`, `buildBomCanonicalApplyFormData`, `useBomMigrationWorkbench`, `deriveBomApplyEligibility`, and `invalidatePreview`; record future/not-found results as lower bounds, never as proof of safety. The script blocks on missing/non-PASS Gate C, missing verified backup, DTO/OpenAPI drift, unexpected old-writer removal, protected hash drift, or unapproved HIGH/CRITICAL. Record the exact nine-file allowlist and explicitly protect `DataTableShell`, `DialogContent`, `index.css`, coordination/dashboard files and existing dashboard snapshots. Per D-06-02 and D-06-09, no install, registry fetch or source mutation occurs until this report is PASS.</action>
  <acceptance_criteria>
    <criterion>PREREQUISITES.md contains PASS evidence for Gate A/B/C, backup, actual DTOs/routes, dirty hashes and all named impacts.</criterion>
    <criterion>The runner fails nonzero on any missing/drifted prerequisite and never edits production/default DB or source.</criterion>
    <criterion>Protected dirty files and the exact Phase 6 allowlist are machine-readable.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; powershell -File scripts/Test-BomPhase6Prerequisites.ps1 -EvidenceDirectory .artifacts/bom-v1.1/gate-d/phase6-preedit -RequireGateC -RequireBackup -RequireDtoInventory; if($LASTEXITCODE-ne 0){throw 'Phase 6 prerequisites BLOCKED'}; $r=Get-Content -Raw .artifacts/bom-v1.1/gate-d/phase6-preedit/PREREQUISITES.md; if($r-notmatch '(?im)status\s*[:|]\s*PASS'){throw 'readiness is not PASS'}</automated>
  </verify>
  <done>Phase 6 has a fail-closed, reproducible pre-edit ownership and contract baseline.</done>
</task>

<task type="auto" id="06-01-02" tdd="true">
  <name>Implement canonical RTK contract and pure workbench adapter</name>
  <files>frontend/src/features/projects/bomReconciliationApi.ts, frontend/src/features/projects/bomReconciliationApi.test.ts, frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts, frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.test.ts, frontend/src/features/projects/dishCatalogApi.ts, frontend/src/features/projects/dishCatalogApi.test.ts</files>
  <read_first>
    <file>backend/src/IPCManagement.Api/Models/DTOs/Bom/BomCanonicalPreviewDto.cs</file>
    <file>backend/src/IPCManagement.Api/Models/DTOs/Bom/BomCanonicalApplyDto.cs</file>
    <file>backend/src/IPCManagement.Api/Controllers/AdminBomReconciliationController.cs</file>
    <file>frontend/src/features/projects/dishCatalogApi.ts</file>
    <file>frontend/src/api/apiSlice.ts</file>
  </read_first>
  <behavior>
    - Search change filters only `displayRows`; server current total, manifest totals/token, apply payload and eligibility stay deep-equal.
    - File, customer scope or effective-date change invalidates the preview immediately.
    - Apply payload omits actor, path, candidate IDs and client-computed counts.
    - Missing blocker-zero, freshness, verified backup, Gate C or server-enabled evidence returns explicit disabled reasons.
    - Manual add/update/close payload includes trimmed reason where delivered by Phase 5 and never includes actor.
  </behavior>
  <action>Re-run the named impacts and compare every owned/protected hash immediately before edits. Derive TypeScript types field-for-field from the executed Phase 4/5 DTO/OpenAPI, preserving separate canonical `Unchanged/Create/Version`, legacy `keep/archive/deactivate/delete/block`, downstream `regenerate`, history-kept, blockers, source hash, scope, TTL/freshness, backup and Gate C evidence. Add canonical preview/status/apply RTK endpoints in a new `bomReconciliationApi.ts`; apply invalidates `DishCatalog` and `WorkflowReports` only on success. Implement a pure adapter with three separate values: current server dataset, immutable preview manifest and display-only filtered rows. Search must never feed totals/payload/eligibility per D-06-05. Input changes stale the manifest; D-06-09 eligibility is true only for matching fresh manifest, zero blockers, verified backup, Gate C PASS, server feature enabled and no running request. Surgically update only manual request/reason types in dirty `dishCatalogApi.ts` per D-06-08; preserve every old hook and characterization test. Never render or accept actor. Use no new Redux slice or dependency. Before commit, run the ownership script and `detect_changes`; unexpected or HIGH/CRITICAL scope blocks.</action>
  <acceptance_criteria>
    <criterion>Canonical request builders match actual server DTOs and omit all forbidden client-authority fields.</criterion>
    <criterion>Search-only adapter tests prove totals, manifest token and apply payload remain unchanged.</criterion>
    <criterion>Every stale/safety prerequisite produces a deterministic disabled reason.</criterion>
    <criterion>Legacy hooks remain exported and manual reason payload tests pass without unrelated dirty-file changes.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; npm run test:unit --workspace frontend -- --run frontend/src/features/projects/bomReconciliationApi.test.ts frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.test.ts frontend/src/features/projects/dishCatalogApi.test.ts; if($LASTEXITCODE-ne 0){throw 'API/adapter tests failed'}; npm run build --workspace frontend; if($LASTEXITCODE-ne 0){throw 'frontend build failed'}; powershell -File scripts/Test-BomPhase6Prerequisites.ps1 -EvidenceDirectory .artifacts/bom-v1.1/gate-d/phase6-preedit -VerifyOwnership; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}</automated>
  </verify>
  <done>The canonical frontend contract and state authority are safe, typed and independent of filtered rows.</done>
</task>

</tasks>

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-06-01" category="Tampering" disposition="mitigate">Client-forged actor/hash/count/candidate/path could expand apply; request builders omit them and server DTO/OpenAPI tests lock the boundary.</threat>
  <threat id="T-06-02" category="Elevation of privilege" disposition="mitigate">UI could advertise apply without Gate C; prerequisite and eligibility matrices fail closed.</threat>
  <threat id="T-06-SC" category="Supply chain" disposition="mitigate">No package or registry action is permitted; existing Base UI/shadcn-style stack only.</threat>
  <verification>ASVS L1 contract review, focused tests, prerequisite hashes, per-symbol impact and detect_changes; unresolved HIGH blocks.</verification>
</threat_model>

<verification>
Prerequisite report PASS, focused unit tests/build pass, legacy hook inventory unchanged, and ownership/GitNexus evidence contains only the planned adapter/manual-contract scope.
</verification>

<success_criteria>
- Canonical preview/apply state can be consumed without client-derived scope authority.
- Manual reason compatibility is present without deleting or renaming legacy APIs.
- Apply remains unavailable whenever Phase 5 evidence or freshness is missing.
</success_criteria>

<output>
Create `.planning/phases/06-admin-shadcn-cutover-manual-crud/06-01-SUMMARY.md` when done.
</output>
