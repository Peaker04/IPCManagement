---
phase: 06-admin-shadcn-cutover-manual-crud
plan: 06
type: execute
wave: 6
depends_on: [06-05]
files_modified:
  - frontend/src/features/workflow/components/bom/BomApplyDialog.tsx
  - frontend/src/features/workflow/components/bom/BomScopeSummary.tsx
  - frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx
  - frontend/src/features/workflow/components/bom/index.ts
  - frontend/src/features/workflow/components/bom/BomApplyDialog.test.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-05]
must_haves:
  truths:
    - "D-06-09: apply dialog shows full server manifest safety evidence and enables only with matching fresh preview, zero blockers, verified backup, Gate C PASS and server feature enabled."
    - "D-06-10: Cancel is initially focused; safe pre-apply dismissal works; in-flight apply cannot be dismissed accidentally."
    - "Search/display filters cannot change the dialog counts, source hash, manifest token, payload or eligibility."
    - "Success is announced only after the Phase 5 response completes downstream regeneration and post-commit refresh."
  artifacts:
    - path: "frontend/src/features/workflow/components/bom/BomApplyDialog.tsx"
      provides: "Fail-closed canonical destructive confirmation"
    - path: "frontend/src/features/workflow/components/bom/BomApplyDialog.test.tsx"
      provides: "Safety prerequisite, authority, focus and dismissal proof"
  key_links:
    - from: "frontend/src/features/workflow/components/bom/BomApplyDialog.tsx"
      to: "frontend/src/features/projects/bomReconciliationApi.ts"
      via: "manifest-token/workbook/effective-date/reason apply mutation only"
      pattern: "use.*Apply.*Mutation"
    - from: "frontend/src/features/workflow/components/bom/BomApplyDialog.tsx"
      to: "frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts"
      via: "server-derived applyDisabledReasons and immutable manifest"
      pattern: "applyDisabledReasons|manifest"
---

# Phase 6 Plan 06: Destructive apply dialog and fail-closed gating

<objective>
Implement the only canonical destructive confirmation with complete server evidence, visible disabled reasons, nondismissible progress and post-commit refresh/focus behavior.

Purpose: Make destructive apply inspectable and impossible to trigger from stale, filtered or incomplete client state.
Output: Canonical apply dialog, summary trigger/status integration and green safety/a11y tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-CONTEXT.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md
@.planning/phases/05-transactional-apply-downstream-reconciliation/05-09-PLAN.md
@frontend/src/features/projects/bomReconciliationApi.ts
@frontend/src/features/workflow/components/bom/BomDialogPrimitives.tsx
@frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts
</context>

<tasks>

<task type="auto" id="06-06-01" tdd="true">
  <name>Build complete manifest confirmation and prerequisite matrix</name>
  <files>frontend/src/features/workflow/components/bom/BomApplyDialog.tsx, frontend/src/features/workflow/components/bom/BomApplyDialog.test.tsx</files>
  <read_first>
    <file>frontend/src/features/workflow/components/bom/BomDialogPrimitives.tsx</file>
    <file>frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts</file>
    <file>frontend/src/features/projects/bomReconciliationApi.ts</file>
    <file>backend/src/IPCManagement.Api/Models/DTOs/Bom/BomCanonicalApplyDto.cs</file>
    <file>.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md</file>
  </read_first>
  <behavior>
    - Dialog visibly renders full selectable hash, local/ISO date, scope, total, archive/deactivate/delete, regenerate, history kept, blockers, backup ID/status, Gate C and freshness.
    - Each false prerequisite independently disables CTA and renders exact UI-SPEC reason.
    - Apply body includes only actual Phase 5 fields and is deep-equal before/after search.
    - Hủy initially focused; Escape/backdrop close before apply, never during apply; focus returns afterward.
    - Error preserves reason and retry; success uses exact copy only after successful server response.
  </behavior>
  <action>Run upstream impact for future `BomApplyDialog`, `getApplyDisabledReasons`, `formatManifestSummary`, existing `BomAlertDialogRoot`, API apply builder and adapter eligibility; compare hashes. Implement D-06-09 through D-06-11 with the BOM-specific Base UI AlertDialog. Render every required server field and all failing prerequisites above the disabled danger CTA; do not hide reasons in tooltip/color. Initial focus is Hủy. Before apply, Escape/backdrop are safe; after mutation begins, disable Escape/backdrop/close until a terminal response. Submit only actual Phase 5 preview ID/workbook/effective date/reason contract, never actor/path/hash/count/candidate IDs. Search/display/tier view state must not feed the dialog. Preserve reason on safe server error and show retry. Turn all BomApplyDialog Wave-0 tests green; do not edit shared DialogContent or old APIs. Ownership/detect_changes block unexpected flows.</action>
  <acceptance_criteria>
    <criterion>All manifest evidence and exact disabled messages are visible and accessible.</criterion>
    <criterion>CTA is enabled only when all seven UI-SPEC gating conditions are true.</criterion>
    <criterion>Payload authority and search invariance are test-proven.</criterion>
    <criterion>Focus/dismissal/progress/error behavior is test-proven.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; npm run test:unit --workspace frontend -- --run frontend/src/features/workflow/components/bom/BomApplyDialog.test.tsx frontend/src/features/projects/bomReconciliationApi.test.ts frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.test.ts; if($LASTEXITCODE-ne 0){throw 'apply dialog tests failed'}; npm run build --workspace frontend; if($LASTEXITCODE-ne 0){throw 'build failed'}</automated>
  </verify>
  <done>The canonical confirmation is complete, server-authoritative and fail-closed.</done>
</task>

<task type="auto" id="06-06-02">
  <name>Wire apply trigger, post-commit refresh and accessible outcome</name>
  <files>frontend/src/features/workflow/components/bom/BomScopeSummary.tsx, frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx, frontend/src/features/workflow/components/bom/index.ts, frontend/src/features/workflow/components/bom/BomApplyDialog.test.tsx</files>
  <read_first>
    <file>frontend/src/features/workflow/components/bom/BomScopeSummary.tsx</file>
    <file>frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx</file>
    <file>frontend/src/features/workflow/components/bom/BomApplyDialog.tsx</file>
    <file>frontend/src/features/projects/bomReconciliationApi.ts</file>
  </read_first>
  <action>Run impact for `BomScopeSummary`, `BomMigrationWorkbench`, `BomApplyDialog` and canonical RTK invalidation; verify ownership. Add one `Áp dụng BOM canonical` trigger associated with the current manifest; absent manifest/server-disabled state remains unavailable with reason. On success, wait for the Phase 5 apply response, RTK invalidation/refetch of current catalog and reports, then clear the consumed preview, return focus to trigger or stable summary anchor and announce the exact success copy. Never optimistically claim success or regenerate drafts client-side. On stale/conflict, keep inputs, invalidate preview, disable apply and make preview rerun primary recovery. Preserve old writer hooks/surface for Phase 7 inventory, but do not route this trigger to them. Run all focused/full frontend gates, ownership and detect_changes.</action>
  <acceptance_criteria>
    <criterion>Apply trigger is absent/disabled without a valid manifest and visible reason.</criterion>
    <criterion>Successful response refreshes current catalog/report, consumes preview and announces only after completion.</criterion>
    <criterion>Stale/conflict returns to fail-closed preview recovery with zero client-side mutation.</criterion>
    <criterion>Old import writer remains present but is never used by canonical apply.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; npm run test:unit --workspace frontend -- --run frontend/src/features/workflow/components/bom; if($LASTEXITCODE-ne 0){throw 'BOM component tests failed'}; npm run lint --workspace frontend; if($LASTEXITCODE-ne 0){throw 'lint failed'}; npm run build --workspace frontend; if($LASTEXITCODE-ne 0){throw 'build failed'}; powershell -File scripts/Test-BomPhase6Prerequisites.ps1 -EvidenceDirectory .artifacts/bom-v1.1/gate-d/phase6-preedit -VerifyOwnership; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}</automated>
  </verify>
  <done>Canonical preview-to-apply is wired with safe refresh, focus and recovery semantics.</done>
</task>

</tasks>

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-06-13" category="Elevation of privilege" disposition="mitigate">Partial/stale evidence could enable destructive apply; complete prerequisite matrix and server 503/conflict handling block.</threat>
  <threat id="T-06-14" category="Tampering" disposition="mitigate">Client counts/candidates could alter apply; payload allowlist and search equality tests block.</threat>
  <threat id="T-06-15" category="Repudiation" disposition="mitigate">Optimistic success could hide rollback/failure; success waits for terminal Phase 5 response and refresh.</threat>
  <verification>Focused payload/gating/a11y tests, lint/build, server contract diff, ownership and detect_changes; unresolved HIGH blocks.</verification>
</threat_model>

<verification>
All BOM unit/component tests pass, every prerequisite case is covered, old writer inventory remains, and build/lint plus ownership/GitNexus gates pass.
</verification>

<success_criteria>
- Destructive apply is inspectable, cancel-safe and disabled for every unsafe condition.
- Search never changes apply scope, totals, payload or eligibility.
- Success refreshes current data only after Phase 5 finishes and preserves history messaging.
</success_criteria>

<output>
Create `.planning/phases/06-admin-shadcn-cutover-manual-crud/06-06-SUMMARY.md` when done.
</output>
