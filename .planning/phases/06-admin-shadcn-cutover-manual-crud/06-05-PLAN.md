---
phase: 06-admin-shadcn-cutover-manual-crud
plan: 05
type: execute
wave: 5
depends_on: [06-04]
files_modified:
  - frontend/src/features/workflow/components/bom/BomDialogPrimitives.tsx
  - frontend/src/features/workflow/components/bom/BomLineDialog.tsx
  - frontend/src/features/workflow/components/bom/BomStopDialog.tsx
  - frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx
  - frontend/src/features/workflow/components/bom/index.ts
  - frontend/src/features/workflow/components/bom/BomManualDialogs.test.tsx
autonomous: true
requirements: [CRUD-01, UI-05]
must_haves:
  truths:
    - "D-06-08: Admin can add, create a version and stop a BOM line in global/customer scopes without hard-deleting published history."
    - "D-06-10: BOM dialogs are named/described, focus-trapped, cancel-safe and return focus without changing HIGH shared DialogContent."
    - "Reason is trimmed, nonblank and 1-500 for delivered Phase 5 published mutations; actor is never rendered/submitted."
    - "Saving preserves values on server error and prevents accidental Escape/backdrop dismissal."
  artifacts:
    - path: "frontend/src/features/workflow/components/bom/BomDialogPrimitives.tsx"
      provides: "BOM-specific Base UI Dialog/AlertDialog accessibility wrapper"
    - path: "frontend/src/features/workflow/components/bom/BomLineDialog.tsx"
      provides: "Manual add and create-version forms"
    - path: "frontend/src/features/workflow/components/bom/BomStopDialog.tsx"
      provides: "History-preserving stop confirmation"
  key_links:
    - from: "frontend/src/features/workflow/components/bom/BomLineDialog.tsx"
      to: "frontend/src/features/projects/dishCatalogApi.ts"
      via: "add/update mutations with Phase 5 reason contract"
      pattern: "use(Add|Update)DishBomLineMutation"
    - from: "frontend/src/features/workflow/components/bom/BomStopDialog.tsx"
      to: "frontend/src/features/projects/dishCatalogApi.ts"
      via: "close mutation with reason and no delete-history semantics"
      pattern: "useCloseDishBomLineMutation"
---

# Phase 6 Plan 05: Accessible manual add, version and stop

<objective>
Restore complete manual BOM CRUD through BOM-scoped accessible Base UI dialogs, including reason/accountability, focus recovery and history-preserving version/stop semantics.

Purpose: Keep quick one-line correction available after canonical cutover without risking shared dialogs or published history.
Output: BOM dialog primitives, add/version dialog, stop alert dialog and green manual-flow tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-CONTEXT.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md
@.planning/phases/05-transactional-apply-downstream-reconciliation/05-08-PLAN.md
@frontend/src/features/projects/dishCatalogApi.ts
@frontend/src/components/ui/dialog.tsx
@frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx
</context>

<tasks>

<task type="auto" id="06-05-01" tdd="true">
  <name>Create BOM-specific Base UI dialog primitives and manual forms</name>
  <files>frontend/src/features/workflow/components/bom/BomDialogPrimitives.tsx, frontend/src/features/workflow/components/bom/BomLineDialog.tsx, frontend/src/features/workflow/components/bom/BomStopDialog.tsx, frontend/src/features/workflow/components/bom/BomManualDialogs.test.tsx</files>
  <read_first>
    <file>frontend/node_modules/@base-ui/react/dialog/index.d.ts</file>
    <file>frontend/node_modules/@base-ui/react/alert-dialog/index.d.ts</file>
    <file>frontend/src/components/ui/dialog.tsx</file>
    <file>frontend/src/features/projects/dishCatalogApi.ts</file>
    <file>.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md</file>
  </read_first>
  <behavior>
    - Add preserves all current fields and scope/tier context; success announces and returns logical row focus.
    - Published edit uses exact create-version copy, requires reason and never overwrites history.
    - Stop uses danger AlertDialog, focuses Hủy first and describes retained history/audit.
    - All dialogs expose associated title/description, focus trap, field-describedby, first-invalid focus and trigger restoration.
    - Saving disables dismissal/buttons; server error keeps form/reason values and gives retryable visible feedback.
  </behavior>
  <action>Run upstream impact for future `BomDialogPrimitives`, `BomDialogRoot`, `BomAlertDialogRoot`, `BomLineDialog`, `BomStopDialog`, `validateBomReason`, `focusLogicalBomAnchor` and existing `DialogContent`. Record `DialogContent` HIGH and do not edit `frontend/src/components/ui/dialog.tsx` or any consumer; use the already installed Base UI Dialog/AlertDialog directly behind a BOM-specific wrapper per D-06-02 and D-06-10. Implement exact UI-SPEC titles/descriptions/CTA copy, initial Cancel focus, title/description association, focus trap/return, safe Escape/backdrop before save and disabled dismissal while saving. D-06-08 add/version/stop supports global/customer scope; published edit creates a version and stop closes only. Validate trimmed 1-500 reason where Phase 5 requires it, submit no actor, link errors and preserve values on server error. Use existing controls/tokens/Lucide only, no dependency. Turn manual Wave-0 tests green without touching shared dialog tests. Verify shared dialog hash, ownership and detect_changes.</action>
  <acceptance_criteria>
    <criterion>Add/version/stop payloads match executed Phase 5 and contain reason but no actor.</criterion>
    <criterion>Version/stop copy and behavior explicitly preserve published history.</criterion>
    <criterion>Dialog naming, first focus, trap, dismissal guard, invalid-field focus and focus return are test-proven.</criterion>
    <criterion>Shared DialogContent and all unrelated consumers remain unchanged.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; npm run test:unit --workspace frontend -- --run frontend/src/features/workflow/components/bom/BomManualDialogs.test.tsx frontend/src/features/projects/dishCatalogApi.test.ts; if($LASTEXITCODE-ne 0){throw 'manual dialog tests failed'}; $dirty=git diff --name-only; if($dirty -match 'frontend/src/components/ui/dialog.tsx'){throw 'HIGH shared DialogContent edited'}; npm run build --workspace frontend; if($LASTEXITCODE-ne 0){throw 'build failed'}</automated>
  </verify>
  <done>Manual add/version/stop dialogs are accessible, attributable and history-preserving.</done>
</task>

<task type="auto" id="06-05-02">
  <name>Wire manual dialogs to table triggers and resilient focus anchors</name>
  <files>frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx, frontend/src/features/workflow/components/bom/index.ts, frontend/src/features/workflow/components/bom/BomManualDialogs.test.tsx</files>
  <read_first>
    <file>frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx</file>
    <file>frontend/src/features/workflow/components/bom/BomTableViewport.tsx</file>
    <file>frontend/src/features/workflow/components/bom/BomLineDialog.tsx</file>
    <file>frontend/src/features/workflow/components/bom/BomStopDialog.tsx</file>
  </read_first>
  <action>Run impact for `BomMigrationWorkbench`, `BomTableViewport`, `BomLineDialog` and `BomStopDialog`; compare ownership hashes. Wire `Thêm dòng`, `Sửa` and `Ngừng` to the dialogs while retaining stable logical anchors across RTK invalidation. After add/version, refetch current data and focus the created/new-version edit trigger; after stop removes an active row, focus the named table region and announce dish/ingredient. Keep current view/search context unless the changed row no longer matches; never use filtered rows as mutation authority. Client validation is convenience only: surface safe server errors and keep dialogs open. Run both scope fixtures and full frontend quality commands; detect_changes/ownership block unexpected flows.</action>
  <acceptance_criteria>
    <criterion>All three manual triggers work in global and customer fixtures.</criterion>
    <criterion>Success refreshes current server data and returns focus to a stable logical target.</criterion>
    <criterion>Server error preserves operator input and gives a retry path.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; npm run test:unit --workspace frontend -- --run frontend/src/features/workflow/components/bom/BomManualDialogs.test.tsx frontend/src/features/workflow/components/bom/BomMigrationWorkbench.test.tsx; if($LASTEXITCODE-ne 0){throw 'manual wiring tests failed'}; npm run lint --workspace frontend; if($LASTEXITCODE-ne 0){throw 'lint failed'}; npm run build --workspace frontend; if($LASTEXITCODE-ne 0){throw 'build failed'}; powershell -File scripts/Test-BomPhase6Prerequisites.ps1 -EvidenceDirectory .artifacts/bom-v1.1/gate-d/phase6-preedit -VerifyOwnership; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}</automated>
  </verify>
  <done>Manual CRUD is fully wired with server refresh, resilient focus and accessible announcements.</done>
</task>

</tasks>

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-06-10" category="Repudiation" disposition="mitigate">Published change without reason/identity could be untraceable; bounded reason and server-derived actor contract block.</threat>
  <threat id="T-06-11" category="Tampering" disposition="mitigate">Stop could become hard delete; close-only endpoint/copy and history tests block.</threat>
  <threat id="T-06-12" category="Denial of service" disposition="mitigate">Modal focus/dismissal failure can trap or lose work; Base UI semantics and keyboard/error tests block.</threat>
  <verification>Manual unit/component tests, shared-dialog hash, lint/build, ownership and detect_changes; unresolved HIGH blocks.</verification>
</threat_model>

<verification>
Manual tests pass for both scopes and all dialog states; shared dialog consumers remain unchanged; lint/build and ownership/GitNexus gates pass.
</verification>

<success_criteria>
- Admin can add, version and stop individual BOM lines after canonical cutover.
- Published history is never hard-deleted or overwritten.
- Dialog keyboard, focus, validation and recovery meet UI-05.
</success_criteria>

<output>
Create `.planning/phases/06-admin-shadcn-cutover-manual-crud/06-05-SUMMARY.md` when done.
</output>
