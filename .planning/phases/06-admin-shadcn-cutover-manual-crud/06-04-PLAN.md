---
phase: 06-admin-shadcn-cutover-manual-crud
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - frontend/src/features/workflow/components/bom/BomTableToolbar.tsx
  - frontend/src/features/workflow/components/bom/BomTableViewport.tsx
  - frontend/src/features/workflow/components/bom/bomMigrationWorkbench.css
  - frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx
  - frontend/src/features/workflow/components/bom/BomTableViewport.test.tsx
autonomous: true
requirements: [UI-03, UI-04, UI-05]
must_haves:
  truths:
    - "D-06-03 and D-06-04: every table state keeps one fixed, focusable, independently scrolling viewport and exact colgroup geometry."
    - "D-06-06: the search icon never overlaps text and search never changes server totals/apply scope."
    - "D-06-07: Sửa and Ngừng remain one nonshrinking row at 1365px and 1280px."
    - "DataTableShell and global index.css remain untouched despite CRITICAL/shared blast radius."
  artifacts:
    - path: "frontend/src/features/workflow/components/bom/BomTableViewport.tsx"
      provides: "Native fixed current/preview tables and stable state body"
    - path: "frontend/src/features/workflow/components/bom/BomTableToolbar.tsx"
      provides: "Accessible display-only search and action toolbar"
    - path: "frontend/src/features/workflow/components/bom/bomMigrationWorkbench.css"
      provides: "BOM-local viewport, colgroup, search and nowrap geometry"
  key_links:
    - from: "frontend/src/features/workflow/components/bom/BomTableViewport.tsx"
      to: "frontend/src/components/common/DataTableShell.tsx"
      via: "BOM-specific class override without generic edit"
      pattern: "ipc-bom-table-viewport"
    - from: "frontend/src/features/workflow/components/bom/BomTableToolbar.tsx"
      to: "frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts"
      via: "searchText/display rows only"
      pattern: "searchText|setSearch"
---

# Phase 6 Plan 04: Fixed BOM table, search and same-row actions

<objective>
Implement the BOM-specific fixed table viewport and toolbar so full/search/loading/error/empty states share identical geometry, contained scroll, safe search padding and nonwrapping row actions.

Purpose: Close the user's reported search overlap, action wrapping and search-driven table resizing without risking the CRITICAL generic shell.
Output: BOM table/toolbar components, local CSS and green geometry component tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-PATTERNS.md
@.planning/phases/06-admin-shadcn-cutover-manual-crud/06-03-PLAN.md
@frontend/src/components/common/DataTableShell.tsx
@frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx
</context>

<tasks>

<task type="auto" id="06-04-01" tdd="true">
  <name>Implement stable native tables and BOM-local scrolling geometry</name>
  <files>frontend/src/features/workflow/components/bom/BomTableViewport.tsx, frontend/src/features/workflow/components/bom/bomMigrationWorkbench.css, frontend/src/features/workflow/components/bom/BomTableViewport.test.tsx</files>
  <read_first>
    <file>frontend/src/components/common/DataTableShell.tsx</file>
    <file>frontend/src/styles/index.css</file>
    <file>frontend/src/features/workflow/pages/AdminDataPage.tsx</file>
    <file>.planning/phases/06-admin-shadcn-cutover-manual-crud/06-UI-SPEC.md</file>
  </read_first>
  <behavior>
    - Current table has exact 215/190/80/85/70/115/115/168 columns and 1038px min width.
    - Preview has exact 96/205/190/80/90/80/120/259 columns and 1120px min width.
    - Desktop viewport is 520px and narrow is 440px with overflow auto, stable gutter and named focus region.
    - Headers/colgroups/shell stay mounted for full/search/loading/error/dataset-empty/search-empty.
    - Action group is nowrap with 68px Sửa and 76px Ngừng nonshrinking buttons.
  </behavior>
  <action>Run impact for future `BomTableViewport`, `CurrentBomTable`, `CanonicalPreviewTable`, `BomTableStateRow` and existing `DataTableShell`; explicitly record the existing CRITICAL DataTableShell blast radius and do not edit it. Verify protected hashes for `DataTableShell` and `index.css` before and after. Implement D-06-03, D-06-04, D-06-07 and D-06-11 with native tables, exact UI-SPEC colgroups/min-widths, sticky header, six skeleton rows and single full-span error/empty rows inside one `DataTableShell`. Add `.ipc-table-shell.ipc-bom-table-viewport` feature CSS strong enough to override global vertical overflow locally: 520px desktop, 440px narrow, overflow auto, stable gutter, contained overscroll, no page overflow. Use the exact action sizes/nowrap rules and Lucide Pencil/Power only. Turn table Wave-0 tests green without weakening tolerances. Ownership and detect_changes block on generic/shared changes.</action>
  <acceptance_criteria>
    <criterion>Both tables use exact documented fixed layouts and state geometry.</criterion>
    <criterion>Scroll is owned by the named table region and no generic/global CSS is edited.</criterion>
    <criterion>Sửa/Ngừng cannot wrap, shrink or switch rows.</criterion>
    <criterion>Loading/error/empty/search-empty preserve the same shell/header/colgroup.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; npm run test:unit --workspace frontend -- --run frontend/src/features/workflow/components/bom/BomTableViewport.test.tsx; if($LASTEXITCODE-ne 0){throw 'table component tests failed'}; $dirty=git diff --name-only; if($dirty -match 'frontend/src/components/common/DataTableShell.tsx|frontend/src/styles/index.css'){throw 'CRITICAL generic shell/global CSS was edited'}; npm run build --workspace frontend; if($LASTEXITCODE-ne 0){throw 'build failed'}</automated>
  </verify>
  <done>The BOM table owns stable fixed geometry and local scrolling in every state.</done>
</task>

<task type="auto" id="06-04-02" tdd="true">
  <name>Implement non-overlapping search toolbar and wire table states</name>
  <files>frontend/src/features/workflow/components/bom/BomTableToolbar.tsx, frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx, frontend/src/features/workflow/components/bom/BomTableViewport.test.tsx</files>
  <read_first>
    <file>frontend/src/features/workflow/components/bom/BomMigrationWorkbench.tsx</file>
    <file>frontend/src/features/workflow/components/bom/useBomMigrationWorkbench.ts</file>
    <file>frontend/src/components/ui/input.tsx</file>
    <file>frontend/src/components/common/ViewSwitcher.tsx</file>
  </read_first>
  <behavior>
    - Search label is accessible and placeholder is `Tìm món, nguyên liệu...`.
    - 16px inert icon is centered at left 8px; input left padding is at least 36px and desktop min width is 240px.
    - Toolbar remains mounted and stable across every state; mobile stacks in document order.
    - Search changes rendered rows only and search-empty shows scope-preservation copy.
  </behavior>
  <action>Run impact for `BomTableToolbar`, `BomMigrationWorkbench`, existing `Input` and `ViewSwitcher`, then verify ownership hashes. Implement D-06-05 and D-06-06 exactly: noninteractive `aria-hidden` Search icon, reserved padding, accessible label, min/preferred/max widths, stable 44px toolbar and current/preview/add order. Feed search only to adapter display projection; never use it in preview/status/apply endpoint args, summary or eligibility. Wire `BomTableViewport` for all current/preview states while keeping toolbar outside the state switch. Keep `Thêm dòng` available in current empty state. Re-run tests, lint/build, ownership and detect_changes.</action>
  <acceptance_criteria>
    <criterion>Icon/text clearance and exact accessible search copy are test-proven.</criterion>
    <criterion>Toolbar/table outer structure does not remount or resize between states.</criterion>
    <criterion>Search-empty affects visible rows only and explicitly states scope is unchanged.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>$ErrorActionPreference='Stop'; npm run test:unit --workspace frontend -- --run frontend/src/features/workflow/components/bom/BomTableViewport.test.tsx frontend/src/features/workflow/components/bom/BomMigrationWorkbench.test.tsx; if($LASTEXITCODE-ne 0){throw 'toolbar/table tests failed'}; npm run lint --workspace frontend; if($LASTEXITCODE-ne 0){throw 'lint failed'}; npm run build --workspace frontend; if($LASTEXITCODE-ne 0){throw 'build failed'}; powershell -File scripts/Test-BomPhase6Prerequisites.ps1 -EvidenceDirectory .artifacts/bom-v1.1/gate-d/phase6-preedit -VerifyOwnership; if($LASTEXITCODE-ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes -r IPCManagement --scope all; if($LASTEXITCODE-ne 0){throw 'GitNexus drift'}</automated>
  </verify>
  <done>Search is readable and display-only, and the complete table/toolbar geometry is stable.</done>
</task>

</tasks>

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-06-08" category="Denial of service" disposition="mitigate">Overflow/resize can make destructive controls unreachable; fixed local viewport and page-overflow tests block.</threat>
  <threat id="T-06-09" category="Tampering" disposition="mitigate">Search could narrow destructive scope; adapter-only display wiring and payload equality tests block.</threat>
  <verification>Exact colgroup/unit tests, protected generic hash checks, lint/build, ownership and detect_changes.</verification>
</threat_model>

<verification>
Focused tests prove fixed dimensions and search/action contracts; DataTableShell/index.css hashes remain unchanged; lint/build and GitNexus scope pass.
</verification>

<success_criteria>
- Search icon never overlaps input text.
- Sửa and Ngừng remain in one row at desktop targets.
- Full/search/loading/error/empty tables keep identical outer geometry and contained scrolling.
</success_criteria>

<output>
Create `.planning/phases/06-admin-shadcn-cutover-manual-crud/06-04-SUMMARY.md` when done.
</output>
