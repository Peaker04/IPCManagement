---
context: phase
phase: 16-persistence-and-reliability
task: 4
total_tasks: 5
status: in_progress
last_updated: 2026-07-28T16:05:41.000Z
---

<current_state>

Step 16 is paused after transaction-runner adoption across Coordination, Purchasing, Inventory,
SampleData, Catalog, Reports, Approvals and Admin. Source HEAD immediately before the handoff-doc commit
was `7e79106` on `feature/production-plan`; that source commit only makes two data migrations collation-safe
after the production incident. After the handoff-doc commit the branch is two commits ahead of origin. The
only remaining production `BeginTransactionAsync` outside the runner is
the now-unused legacy method on `IUnitOfWork`/`UnitOfWork`; `EnableRetryOnFailure` remains disabled.

The worktree contains two unrelated user-owned untracked files, `.dockerignore` and `Dockerfile`.
Do not stage, overwrite or remove them.

</current_state>

<completed_work>

- EF mappings completed through Coordination, Purchasing and Inventory; context is a thin registration root.
- `IEfTransactionRunner` foundation and duplicate-side-effect SQLite retry test committed at `94e3293`.
- Domain exception conversion completed through middleware at `ac11c6f`.
- Canonical migration lineage and restore rehearsal integrated through `b37606b`.
- Transaction adoption commits:
  - Coordination `49c678d`
  - Purchasing `1fe2eb8`
  - Inventory `a4e0368`
  - SampleData `f03947e`
  - Catalog `0ef15fe`
  - Reports `f41104e`
  - Approvals `2ef83a2`
  - Admin `f3e7bcd`
- Latest full backend gate after Admin: API 667 passed / 1 skipped; Application 47/47.
- No database seed/import/reset, SampleData import endpoint, admin seed endpoint or browser mutation was run.
- User-authorized production synchronization later completed at 61/61 tables and 53,404/53,404 rows with
  zero row/checksum mismatch; live/ready/database/migration checks are Healthy. Do not repeat the restore.
- Production ANV verification used preview only: 114/114 existing-dish rows, 0 new dishes and valid with
  0 errors/warnings. The committed menu renders **90/90 dishes with BOM**, 0 matched-without-BOM and
  0 unmatched. The earlier 0/90 display was stale 30-minute backend + 5-minute frontend cache after direct
  restore bypassed catalog invalidation, not missing BOM data.

</completed_work>

<remaining_work>

- Remove `BeginTransactionAsync` from `IUnitOfWork` and `UnitOfWork`; remove obsolete storage imports.
- Update the stale retry warning in `DependencyInjection.AddBackendServices`.
- Decide whether to enable `EnableRetryOnFailure` only after the source scan shows one transaction opener
  inside `EfTransactionRunner` and retry/idempotency regression remains green.
- Run final Step 16 gates: database script parser tests, `npm run verify`, Release build 0 warnings,
  EF pending-model, OpenAPI/TypeScript determinism, secret scan, `git diff --check`, and GitNexus
  staged/compare audits.
- Synchronize `docs/CURRENT-STATE.md`, architecture audit, `.planning/STATE.md`, `ROADMAP.md` and
  `REQUIREMENTS.md`. Do not use archived GSD v1.1.
- Mark ARCH-16E accurately: restore rehearsal passed, but real off-site physical storage remains an
  operational gap until NAS/cloud/external media is configured and verified.
- Add restart/catalog-cache invalidation to the production direct-restore runbook; direct restore bypasses
  `DishCatalogCache.Clear`.

</remaining_work>

<decisions_made>

- Mutable EF entities are loaded inside each runner operation because retry and commit verification clear
  the ChangeTracker.
- Every transaction supplies a database verifier based on stable document ID, fingerprint, status or audit
  identity; no unconditional `verifySucceeded` delegate was introduced.
- Non-relational EF providers execute once without a relational transaction; SQLite continues to prove real
  rollback/retry behavior.
- `EnableRetryOnFailure` remains off until legacy transaction API retirement and final gates.
- C:/D: matching backup hashes are not described as proven off-site storage.

</decisions_made>

<blockers>

- Non-blocking operational gap: C: and D: may be partitions on one physical disk. Production still needs a
  genuinely off-site backup destination and scheduled restore evidence.

</blockers>

## Required Reading (in order)

1. `AGENTS.md` — mandatory repository and GitNexus rules.
2. `docs/CURRENT-STATE.md` — live lane/evidence constraints; update its stale Step 16 section after gates.
3. `docs/ARCHITECTURE-AUDIT-2026-07-26.md` Part F, Step 16 — canonical acceptance criteria.
4. `.planning/phases/16-persistence-and-reliability/16-01-PLAN.md` — active plan.
5. `docs/DATABASE-RECOVERY-REHEARSAL-2026-07-28.md` — lineage/restore evidence and off-site limitation.
6. `.artifacts/shipyard-live/production-bom-debug.json` — preview/catalog/menu counts and browser evidence.

## Critical Anti-Patterns (do NOT repeat these)

- Do not load mutable EF entities before the runner delegate; a retry clears tracking and detaches them.
- Do not enable provider retry while any manual transaction remains outside `EfTransactionRunner`.
- Do not seed/import/reset a lane database merely to rerun a gate.
- Do not stage `.dockerignore` or `Dockerfile`; they are unrelated user changes.

## Infrastructure State

- No FE/API/Shipyard process was started during this transaction-adoption sequence.
- Database runtime was not accessed or mutated by this sequence.
- Backup/restore rehearsal uses a disposable clone that was dropped; `ipcmanagement` and `ipc_lane1` were not mutated.
- A later, separately authorized incident response synchronized production from local and removed the temporary
  User-scope `IPC_PROD_CONNECTION_STRING` afterward. Never print/reconstruct the secret; it must be configured
  again through environment/secret manager before any future direct production access.

<context>

Resume with the smallest final persistence slice: retire the unused UnitOfWork transaction API and update
the now-stale DI warning. Run GitNexus impact first; its historical index may still report CRITICAL callers
until re-analysis, so trust the source scan plus fresh index. Keep that commit separate from final docs/state.

</context>

<next_action>

Start with: refresh GitNexus on the merged branch, run upstream impact for `IUnitOfWork`, `UnitOfWork`,
`BeginTransactionAsync` and `DependencyInjection.AddBackendServices`, then remove only the unused
transaction members and update the retry comment.

</next_action>
