---
phase: 03-contract-provenance-safety-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-contract-provenance-safety-baseline/03-LIFECYCLE-POLICY.md
  - .docs/BOM_V11_RECOVERY_RUNBOOK.md
  - scripts/Get-BomSafetyBaseline.ps1
  - scripts/Invoke-BomBackupRestoreRehearsal.ps1
  - scripts/Test-BomWorktreeOwnership.ps1
  - backend/tests/IPCManagement.Api.Tests/BomContractCharacterizationTests.cs
  - .artifacts/bom-v1.1/gate-a/
autonomous: false
requirements: [SAFE-05, RETIRE-03]
---

# Phase 3 Plan 01: Pre-edit Gate A evidence and recovery proof

<objective>
Complete every pre-edit safety prerequisite before any production or schema symbol changes: preserve worktree ownership, characterize current behavior, hash every applied migration, lock the lifecycle/retention policy, capture a legacy-schema-compatible baseline, and prove an identified backup restores to an isolated clone.
</objective>

<execution_context>
- Read `03-CONTEXT.md` decisions D-03-03..D-03-10, `03-RESEARCH.md`, `03-PATTERNS.md`, `03-VALIDATION.md`, `ROADMAP.md`, `REQUIREMENTS.md`, and `AGENTS.md` first.
- This wave may add tests, scripts, docs and evidence only. It must not edit `backend/src`, EF model/snapshot/migrations, application data, or old import surfaces.
- Current dirty files are user-owned until proven otherwise. Never reset, checkout, stash, overwrite, bulk-format or stage them.
- Before every task/plan commit, run `scripts/Test-BomWorktreeOwnership.ps1` and `node .gitnexus/run.cjs detect_changes --scope all`; unexpected paths/flows or any unapproved HIGH/CRITICAL result blocks the commit.
</execution_context>

<must_haves>
- D-03-07: Chứng từ approved/locked/completed, audit, approval history và stock ledger là immutable boundary.
- D-03-10: Không edit code trước khi có dirty-worktree manifest, GitNexus impact, characterization tests và baseline report.
- Baseline runs against the pre-v1.1 legacy schema and never assumes `Dishbom.SourceKind`, `SourceRunId`, or reconciliation tables exist.
- Characterization and exact lifecycle policy exist before production edits.
- Backup ID/hash plus successful isolated restore and immutable checksum equality exist before Plan 03-02.
- Existing applied migration files have a stable SHA-256 manifest before later schema work.
</must_haves>

<tasks>

<task id="03-01-01" requirement="RETIRE-03">
  <name>[BLOCKING] Capture ownership, impacts, applied-migration hashes, and characterization</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>backend/src/IPCManagement.Api/Services/SampleData/SampleDataImportService.cs</file>
    <file>backend/src/IPCManagement.Api/Services/DishService.cs</file>
    <file>backend/tests/IPCManagement.Api.Tests/SampleDataImportServiceTests.cs</file>
    <file>backend/tests/IPCManagement.Api.Tests/DishCatalogTests.cs</file>
  </read_first>
  <action>
    Enforce D-03-10: Không edit code trước khi có dirty-worktree manifest, GitNexus impact, characterization tests và baseline report. Create `.artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt`, `owned-paths.txt`, `applied-migration-sha256.csv`, and `impact-report.md`. Record HEAD, `git status --short`, diff names, every existing migration path/hash, and upstream impact for `SampleDataImportService`, `ImportBomDataAsync`, `ValidateAndDeduplicatePresetBomRows`, `ResolvePresetBomUnit`, `DishService`, its constructor, `AddBomLineAsync`, `UpdateBomLineAsync`, `CloseBomLineAsync`, `Dishbom`, `IpcManagementContext`, and `OnModelCreating`; interface/DI results are lower bounds and direct consumers must also be listed. Add `Test-BomWorktreeOwnership.ps1` to compare current paths against the original manifest plus an explicit plan allowlist and fail on an unexpected path. Add `BomContractCharacterizationTests.cs` that freezes current three-sheet/header behavior, duplicate basis, technical-unit cases, manual interval/tier/global/customer behavior, and identifies current silent-KG/unweighted-average behavior as explicit replacement targets rather than silently changing it. No production edit is allowed in this task.
  </action>
  <acceptance_criteria>
    <criterion>Every dirty/untracked path, current SHA and every pre-existing migration hash is recorded before implementation.</criterion>
    <criterion>Impact report uses the exact existing BOM CRUD symbols and blocks unapproved HIGH/CRITICAL risk.</criterion>
    <criterion>Characterization is executable and labels unsafe current behavior with the target fail-closed assertion.</criterion>
    <criterion>Ownership script exits non-zero for a path outside the pre-existing manifest and current plan allowlist.</criterion>
  </acceptance_criteria>
  <verify>
    <command>$ErrorActionPreference='Stop'; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter 'FullyQualifiedName~BomContractCharacterizationTests'; if ($LASTEXITCODE -ne 0) { throw 'characterization failed' }; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-a/pre-edit/owned-paths.txt; if ($LASTEXITCODE -ne 0) { throw 'ownership drift' }; node .gitnexus/run.cjs detect_changes --scope all; if ($LASTEXITCODE -ne 0) { throw 'GitNexus detect_changes failed' }</command>
    <behavior>Tests pass, applied migration manifest is non-empty, and ownership/GitNexus report no unexpected change or unapproved HIGH/CRITICAL flow.</behavior>
  </verify>
  <done>Repository ownership, impact and immutable migration baseline are locked before source/schema edits.</done>
</task>

<task id="03-01-02" requirement="SAFE-05">
  <name>[BLOCKING] Lock exact lifecycle and retention policy v1.1</name>
  <read_first>
    <file>.planning/phases/03-contract-provenance-safety-baseline/03-CONTEXT.md</file>
    <file>backend/src/IPCManagement.Api/Services/CoordinationService.cs</file>
    <file>backend/src/IPCManagement.Api/Services/Workflow/MaterialDemandService.cs</file>
    <file>backend/src/IPCManagement.Api/Services/Workflow/PurchaseRequestWorkflowService.cs</file>
    <file>backend/src/IPCManagement.Api/Services/Workflow/PurchaseOrderService.cs</file>
    <file>backend/src/IPCManagement.Api/Services/InventoryReceiptService.cs</file>
    <file>backend/src/IPCManagement.Api/Services/InventoryIssueService.cs</file>
  </read_first>
  <action>
    Create `03-LIFECYCLE-POLICY.md` with `PolicyVersion`, exact observed status codes and a conservative action for each menu/menu-version, production schedule/plan, material demand/quantity plan, purchase request/order, receipt, inventory issue/return and stock-ledger state. Classify mutable draft/open states separately from immutable approved/locked/completed/ordered/partially-received/received/issued/returned; every unknown/unmapped state is `BLOCK`. Enforce D-03-07: Chứng từ approved/locked/completed, audit, approval history và stock ledger là immutable boundary. Record: effective date is chosen in Phase 4 preview and revalidated in Phase 5 apply, never inferred by the parser; exact draft regeneration scope is owned and locked in Phase 5 before mutation; rollback window is owned by Phase 5 and ends no later than the first new immutable downstream dependency; customer override outside canonical coverage is preserved and routed to Admin review in Phase 5/6; D-03-03 collision and D-03-07 history rules override cleanup convenience. Include source file/identifier evidence for each status and a version-change rule.
  </action>
  <acceptance_criteria>
    <criterion>Every required domain has exact code-to-policy rows backed by current identifiers; unknown always maps to BLOCK.</criterion>
    <criterion>Immutable statuses can never regenerate/delete; stock/ledger history is retain-only.</criterion>
    <criterion>Effective date, regeneration scope, rollback window and out-of-coverage override each name the owning later phase and conservative behavior until locked.</criterion>
    <criterion>The policy has one explicit version consumed by later contracts/manifests.</criterion>
  </acceptance_criteria>
  <verify>
    <command>$ErrorActionPreference='Stop'; $required=@('menu','production','material demand','purchase request','purchase order','receipt','inventory issue','inventory return','stock ledger','unknown','PolicyVersion','Phase 4','Phase 5'); $text=Get-Content -Raw .planning/phases/03-contract-provenance-safety-baseline/03-LIFECYCLE-POLICY.md; foreach($item in $required){if($text -notmatch [regex]::Escape($item)){throw "missing policy item: $item"}}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-a/pre-edit/owned-paths.txt; if($LASTEXITCODE -ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes --scope all; if($LASTEXITCODE -ne 0){throw 'unexpected GitNexus change'}</command>
    <behavior>Policy completeness check passes and no unexpected ownership/flow change exists before commit.</behavior>
  </verify>
  <done>Versioned lifecycle/retention decisions and later-phase ownership are exact and fail closed.</done>
</task>

<task id="03-01-03" requirement="SAFE-05,RETIRE-03">
  <name>[BLOCKING] Capture legacy baseline and prove backup/restore before edits</name>
  <read_first>
    <file>.planning/phases/03-contract-provenance-safety-baseline/03-LIFECYCLE-POLICY.md</file>
    <file>backend/src/IPCManagement.Api/Data/IpcManagementContext.cs</file>
    <file>backend/database/Clean_Legacy_Imported_Bom.sql</file>
    <file>scripts/Invoke-Iter1MigrationPlan.ps1</file>
  </read_first>
  <action>
    Add `Get-BomSafetyBaseline.ps1`, `Invoke-BomBackupRestoreRehearsal.ps1`, and `.docs/BOM_V11_RECOVERY_RUNBOOK.md`. Baseline must query only columns/tables available at migration `20260716113000_CorrectPresetBomTechnicalUnits`; feature-detect later provenance schema but never require it. Emit deterministic ordered JSON/CSV/SHA-256 for BOM by tier/customer/status, overlaps, unknown units, invalid quantities, TMP rows, catalog/reference/stock candidates, draft/open dependencies, and immutable approved/locked/completed, approval/audit and stock-ledger counts/checksums. Rehearsal requires explicit source/isolated target, refuses equal/protected targets, creates BackupId/hash/schema/migration metadata, restores to clone, reruns baseline and requires immutable equality. Create `.artifacts/bom-v1.1/gate-a/pre-edit/GATE-A-PREEDIT.md`; it passes only when characterization, lifecycle policy, repeatable baseline, applied migration hash manifest, identified backup and restored-clone equality all pass. Do not copy or execute broad-delete logic from the legacy SQL.
  </action>
  <acceptance_criteria>
    <criterion>Two unchanged legacy-schema baseline runs match exactly after excluding separately stored observation time.</criterion>
    <criterion>Backup has ID/hash/source schema and restores only to a distinct confirmed clone; immutable checksums match.</criterion>
    <criterion>Scripts contain no INSERT/UPDATE/DELETE/TRUNCATE/DDL, redact credentials/paths and exit non-zero on drift.</criterion>
    <criterion>GATE-A-PREEDIT PASS is required by Plan 03-02 and cannot pass with missing recovery or migration-hash evidence.</criterion>
  </acceptance_criteria>
  <verify>
    <command>$ErrorActionPreference='Stop'; powershell -File scripts/Get-BomSafetyBaseline.ps1 -ConnectionName BomLegacyReadOnly -OutputDirectory .artifacts/bom-v1.1/gate-a/pre-edit/baseline-1; if($LASTEXITCODE -ne 0){throw 'baseline 1 failed'}; powershell -File scripts/Get-BomSafetyBaseline.ps1 -ConnectionName BomLegacyReadOnly -OutputDirectory .artifacts/bom-v1.1/gate-a/pre-edit/baseline-2; if($LASTEXITCODE -ne 0){throw 'baseline 2 failed'}; $d=Compare-Object (Get-Content .artifacts/bom-v1.1/gate-a/pre-edit/baseline-1/checksums.csv) (Get-Content .artifacts/bom-v1.1/gate-a/pre-edit/baseline-2/checksums.csv); if($d){throw 'baseline drift'}; powershell -File scripts/Invoke-BomBackupRestoreRehearsal.ps1 -SourceConnectionName BomLegacyReadOnly -TargetConnectionName BomV11RestoreClone -OutputDirectory .artifacts/bom-v1.1/gate-a/pre-edit/recovery -ConfirmIsolatedTarget; if($LASTEXITCODE -ne 0){throw 'restore rehearsal failed'}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-a/pre-edit/owned-paths.txt; if($LASTEXITCODE -ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes --scope all; if($LASTEXITCODE -ne 0){throw 'unexpected GitNexus change'}</command>
    <behavior>GATE-A-PREEDIT is PASS with identical baseline and restored-clone immutable checksums; any mismatch blocks all production/schema work.</behavior>
  </verify>
  <done>Pre-edit Gate A is proven on the legacy schema, not merely documented.</done>
</task>

</tasks>

## Artifacts this phase produces

- `03-LIFECYCLE-POLICY.md` with exact versioned domain status/retention/ownership rules.
- `BomContractCharacterizationTests`, ownership guard, impact report and applied-migration SHA-256 manifest.
- Legacy-compatible baseline and identified backup/restore rehearsal scripts/runbook/evidence.
- `.artifacts/bom-v1.1/gate-a/pre-edit/GATE-A-PREEDIT.md`.

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-03-03">Applied migration drift or read-only scripts that mutate history; hash every old migration and scan scripts for DML/DDL.</threat>
  <threat id="T-03-04">Dirty-worktree overwrite or unexpected flow; ownership guard plus GitNexus detect_changes is mandatory before every commit.</threat>
  <threat id="T-03-05">Unrestored backup gives false safety; only identified hash-verified backup plus isolated restore/checksum equality passes.</threat>
  <verification>ASVS L1 review, ownership comparison, detect_changes, legacy baseline repeatability and isolated recovery proof; any HIGH/CRITICAL result blocks.</verification>
</threat_model>
