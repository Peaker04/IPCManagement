---
phase: 03-contract-provenance-safety-baseline
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - backend/src/IPCManagement.Api/Services/Bom/CanonicalBomContract.cs
  - backend/src/IPCManagement.Api/Services/Bom/CanonicalBomModels.cs
  - backend/src/IPCManagement.Api/Services/Bom/BomPolicy.cs
  - backend/src/IPCManagement.Api/Services/Bom/IBomInvariantValidator.cs
  - backend/src/IPCManagement.Api/Services/Bom/BomInvariantValidator.cs
  - backend/src/IPCManagement.Api/DependencyInjection.cs
  - backend/src/IPCManagement.Api/Services/SampleData/SampleDataImportService.cs
  - backend/src/IPCManagement.Api/Services/DishService.cs
  - backend/tests/IPCManagement.Api.Tests/BomContractCharacterizationTests.cs
  - backend/tests/IPCManagement.Api.Tests/BomInvariantValidatorTests.cs
autonomous: true
requirements: [CAN-03, CAN-04, CRUD-02]
---

# Phase 3 Plan 02: Versioned collision, unit, and shared CRUD invariants

<objective>
After pre-edit Gate A passes, extract versioned fail-closed duplicate/unit policy and make bulk normalization plus manual BOM CRUD share the same tier/scope/effective/overlay invariants. This plan creates reusable contract models but does not claim the Phase 4 parser/source-trace pipeline or Phase 7 single-surface retirement requirements.
</objective>

<execution_context>
- `GATE-A-PREEDIT.md` must be PASS before the first production edit.
- Honor D-03-03, D-03-04, D-03-05, D-03-06 and `03-LIFECYCLE-POLICY.md`; effective date remains caller-selected, not parser-inferred.
- Re-run upstream impact immediately before editing every named class/method. Unapproved HIGH/CRITICAL blocks execution.
- Existing modifications overlap both service/test files; edit narrow hunks and preserve every unrelated user change.
- Before every task/plan commit, ownership comparison and GitNexus `detect_changes --scope all` must pass with only expected flows.
</execution_context>

<must_haves>
- D-03-01: Bulk BOM canonical có đúng ba sheet `định lượng suất 25k`, `định lượng suất 30k`, `định lượng suất 34k`; nhận diện bằng cấu trúc, không bằng local filename/path. Phase 3 centralizes this reusable target contract while Phase 7 completes CAN-01 by retiring the old surface.
- D-03-02: Phase 3 định nghĩa target normalized model không chứa EF entity; Phase 4 triển khai parser và population source sheet/row/trace để hoàn thành CAN-02.
- D-03-03: Duplicate chỉ weighted-dedupe khi có serving basis; identity/unit ambiguous là blocker.
- D-03-04: Technical-unit mapping `CAI/HOP/QUA/O/MIENG/CAY/LAT/KG` phải versioned; unknown không fallback `KG`.
- D-03-05: Customer BOM overlay theo ingredient/unit; dòng không override fallback global.
- D-03-06: Bulk và manual CRUD dùng chung overlap/effective/tier/scope invariants; exact manual methods are `AddBomLineAsync`, `UpdateBomLineAsync`, and `CloseBomLineAsync`.
</must_haves>

<tasks>

<task id="03-02-01" requirement="CAN-03,CAN-04">
  <name>Extract deterministic duplicate and versioned technical-unit policy</name>
  <read_first>
    <file>.artifacts/bom-v1.1/gate-a/pre-edit/GATE-A-PREEDIT.md</file>
    <file>.planning/phases/03-contract-provenance-safety-baseline/03-LIFECYCLE-POLICY.md</file>
    <file>backend/src/IPCManagement.Api/Helpers/DecimalPolicy.cs</file>
    <file>backend/src/IPCManagement.Api/Services/SampleData/SampleDataImportService.cs</file>
    <file>backend/tests/IPCManagement.Api.Tests/BomContractCharacterizationTests.cs</file>
  </read_first>
  <action>
    Fail immediately unless pre-edit Gate A is PASS, then run impact for `SampleDataImportService`, `ValidateAndDeduplicatePresetBomRows`, and `ResolvePresetBomUnit`. Cover D-03-01: Bulk BOM canonical có đúng ba sheet `định lượng suất 25k`, `định lượng suất 30k`, `định lượng suất 34k`; nhận diện bằng cấu trúc, không bằng local filename/path. Cover D-03-02: Phase 3 định nghĩa target normalized model không chứa EF entity; Phase 4 triển khai parser và population source sheet/row/trace để hoàn thành CAN-02. Add reusable `CanonicalBomModels` results for normalized identity, duplicate decision and technical-unit resolution without EF entities or local paths; source-trace population remains Phase 4-owned. Enforce D-03-03: Duplicate chỉ weighted-dedupe khi có serving basis; identity/unit ambiguous là blocker. Enforce D-03-04: Technical-unit mapping `CAI/HOP/QUA/O/MIENG/CAY/LAT/KG` phải versioned; unknown không fallback `KG`. Add `BomPolicy` with `PolicyVersion`, supported tiers and exact unit codes/aliases. Change `ResolvePresetBomUnit` from returning an arbitrary Unit/KG fallback to Resolved/Ambiguous/Unknown, with Unknown/Ambiguous blocking. Change `ValidateAndDeduplicatePresetBomRows` so grouping/order is deterministic and weighted quantity uses `DecimalPolicy.RoundQuantity` only when all grouped rows have positive serving basis; otherwise block. `CanonicalBomContract` may centralize sheet/header constants for reuse but must coexist with the old surface until Phase 7 and must not claim CAN-01 completion.
  </action>
  <acceptance_criteria>
    <criterion>Valid duplicates produce the expected weighted rounded quantity; missing serving basis or identity/unit collision blocks.</criterion>
    <criterion>All eight supported unit codes resolve under a named policy version; unknown/conflicting aliases never return KG.</criterion>
    <criterion>Repeated identical inputs return stable ordering and decisions.</criterion>
    <criterion>No old endpoint/template/parser/UI is removed and no source-trace pipeline is claimed in this phase.</criterion>
  </acceptance_criteria>
  <verify>
    <command>$ErrorActionPreference='Stop'; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter 'FullyQualifiedName~BomContractCharacterizationTests|FullyQualifiedName~SampleDataImportServiceTests'; if($LASTEXITCODE -ne 0){throw 'contract/unit tests failed'}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-a/pre-edit/owned-paths.txt; if($LASTEXITCODE -ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes --scope all; if($LASTEXITCODE -ne 0){throw 'unexpected GitNexus flow'}</command>
    <behavior>Fail-closed collision/unit tests pass and ownership/detect_changes show only the approved sample-import contract flow.</behavior>
  </verify>
  <done>CAN-03 and CAN-04 policy is explicit, versioned, deterministic and fail closed.</done>
</task>

<task id="03-02-02" requirement="CRUD-02">
  <name>Use one invariant validator for bulk and manual BOM operations</name>
  <read_first>
    <file>backend/src/IPCManagement.Api/Services/IDishService.cs</file>
    <file>backend/src/IPCManagement.Api/Services/DishService.cs</file>
    <file>backend/src/IPCManagement.Api/Services/SampleData/SampleDataImportService.cs</file>
    <file>backend/src/IPCManagement.Api/DependencyInjection.cs</file>
    <file>.planning/phases/03-contract-provenance-safety-baseline/03-LIFECYCLE-POLICY.md</file>
  </read_first>
  <action>
    Run upstream impact for `DishService`, its constructor, `AddBomLineAsync`, `UpdateBomLineAsync`, `CloseBomLineAsync`, `SampleDataImportService`, `ImportBomDataAsync`, and `AddBackendServices`; stop on unapproved HIGH/CRITICAL. Enforce D-03-05: Customer BOM overlay theo ingredient/unit; dòng không override fallback global. Enforce D-03-06: Bulk và manual CRUD dùng chung overlap/effective/tier/scope invariants. Add `IBomInvariantValidator`/`BomInvariantValidator` for supported tier, positive rounded quantity, `EffectiveFrom &lt;= EffectiveTo`, published interval overlap, and scope key `(dish, ingredient, unit, tier, customer?)`. Implement ingredient+unit customer overlay with global fallback; never merge customer/global persisted versions. Register through `DependencyInjection.AddBackendServices` and inject the same validator into both services, replacing only duplicated validation branches. Preserve old bulk compatibility endpoints and manual version/close behavior. Add direct tests for adjacent/open-ended intervals, invalid tier/unit/quantity, global/customer isolation, per-ingredient override and global fallback.
  </action>
  <acceptance_criteria>
    <criterion>Bulk normalization and all three exact manual CRUD methods call the same validator implementation.</criterion>
    <criterion>Overlapping published versions in one scope fail; adjacent non-overlapping intervals pass.</criterion>
    <criterion>Customer row overrides only matching ingredient+unit and missing customer rows fall back global.</criterion>
    <criterion>Existing add/version/close characterization remains green; no published history is hard-deleted.</criterion>
  </acceptance_criteria>
  <verify>
    <command>$ErrorActionPreference='Stop'; dotnet test backend/tests/IPCManagement.Api.Tests/IPCManagement.Api.Tests.csproj --no-restore --filter 'FullyQualifiedName~BomInvariantValidatorTests|FullyQualifiedName~DishCatalogTests|FullyQualifiedName~SampleDataImportServiceTests'; if($LASTEXITCODE -ne 0){throw 'shared invariant tests failed'}; dotnet build backend/src/IPCManagement.Api/IPCManagement.Api.csproj --no-restore; if($LASTEXITCODE -ne 0){throw 'backend build failed'}; powershell -File scripts/Test-BomWorktreeOwnership.ps1 -Baseline .artifacts/bom-v1.1/gate-a/pre-edit/dirty-worktree.txt -Allowlist .artifacts/bom-v1.1/gate-a/pre-edit/owned-paths.txt; if($LASTEXITCODE -ne 0){throw 'ownership drift'}; node .gitnexus/run.cjs detect_changes --scope all; if($LASTEXITCODE -ne 0){throw 'unexpected GitNexus flow'}</command>
    <behavior>Focused tests/build pass and GitNexus reports only expected BOM import/manual CRUD/DI flows.</behavior>
  </verify>
  <done>CRUD-02 is shared by canonical bulk normalization and manual add/update/close without retiring compatibility.</done>
</task>

</tasks>

## Artifacts this phase produces

- `CanonicalBomModels`, `CanonicalBomContract`, and versioned `BomPolicy` boundaries (source-trace pipeline deferred to Phase 4).
- `IBomInvariantValidator` / `BomInvariantValidator` and DI wiring.
- Fail-closed duplicate/unit/invariant characterization and unit tests.

<threat_model standard="OWASP ASVS 5.0 L1" block_on="HIGH">
  <threat id="T-03-02">Silent KG fallback, ambiguous identity or invalid overlap corrupts demand; all such states block before persistence.</threat>
  <threat id="T-03-04">Overlapping dirty hunks or unexpected call flows overwrite user work; ownership comparison and detect_changes run before every commit.</threat>
  <verification>Focused fail-closed tests, backend build, per-symbol upstream impact and pre-commit detect_changes; any HIGH/CRITICAL or unexpected flow blocks.</verification>
</threat_model>
