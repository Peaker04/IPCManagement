---
quick_id: 260803-p7b
mode: quick-validate
status: complete
date: 2026-08-03
must_haves:
  truths:
    - A failed Dashboard query never renders zero or empty operational data as authoritative.
    - Ready Dashboard data remains visible while either query refreshes.
    - The standardization document distinguishes adopted rules from incremental rollout and open cross-stack work.
  artifacts:
    - docs/UI-UX-FE-BE-DATABASE-STANDARDIZATION.md
    - frontend/src/features/dashboard/pages/DashboardPage.tsx
    - frontend/src/features/dashboard/pages/DashboardPage.state.test.tsx
  key_links:
    - useWorkflowOverview exposes a QuerySnapshot-compatible aggregate consumed through toLabeledQueryView.
    - DashboardPage renders its data region through QueryViewBoundary.
---

# Quick 260803-p7b — Adopt the standardization contract and migrate Dashboard query state

## Scope

Adopt the user-provided FE–BE–Database standardization document after source validation, and implement one source-proven legacy boundary: Dashboard query state handling. Do not claim or attempt to close OPEN-02/05/06/07/08/09, change business rules, alter API contracts, mutate the database, or open Phase 27.

## Tasks

### 1. Lock regression behavior

- **Files:** `frontend/src/features/dashboard/pages/DashboardPage.state.test.tsx`
- **Action:** Add focused component tests for partial query failure, retry ownership, and stale-data refresh behavior.
- **Verify:** The new partial-error test fails against the current boolean-based Dashboard implementation for the expected false-data rendering reason.
- **Done:** Tests express the standardization contract without snapshot or source-text-only assertions.

### 2. Migrate the Dashboard boundary and adopt the document

- **Files:** `frontend/src/features/reports/reportsApi.ts`, `frontend/src/features/dashboard/pages/DashboardPage.tsx`, `docs/UI-UX-FE-BE-DATABASE-STANDARDIZATION.md`
- **Action:** Make the derived workflow overview QuerySnapshot-compatible, classify both Dashboard query owners through the existing `QueryView` algebra, block false content on error/forbidden, preserve skeletons during initial loading and stale data during refresh, and clarify the document's rollout status.
- **Verify:** Focused state tests and existing `queryView`/boundary tests pass.
- **Done:** No second state algebra, formatter, cache registry, API slice, or visual variant is introduced.

### 3. Verify and synchronize GSD state

- **Files:** `.planning/quick/260803-p7b-adopt-the-ui-ux-fe-be-database-standardi/260803-p7b-SUMMARY.md`, `.planning/quick/260803-p7b-adopt-the-ui-ux-fe-be-database-standardi/260803-p7b-VERIFICATION.md`, `.planning/STATE.md`, `MEMORY.md`, `HISTORY.md`
- **Action:** Run focused tests, frontend full gates, root verify where proportionate, hygiene/secret scans, then record only verified current facts. Keep Phase 27 unopened.
- **Verify:** Tests, lint, dependency-cruiser, build, `git diff --check`, and declared-scope inspection pass; any unavailable gate is recorded precisely.
- **Done:** Quick artifacts and living context agree; unrelated user changes remain untouched.
