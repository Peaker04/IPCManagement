# Plan 03-01 — Inventory and shared stability foundation

## Objective

Lock the full presentation inventory and lowest shared status/copy/refresh contracts before family rollout.

## Tasks

1. Count-lock every production table, dialog and tab switcher with a source-aware detector and negative test.
2. Remove raw unknown-status fallback and stabilize the shared badge DOM/geometry/transition contract.
3. Classify refresh signals: initial load, background fetch, mutation pending and polling; record owner-specific
   failures without treating all `isLoading` usages as defects.
4. Pilot directly evidenced refresh-replacement owners and add regression for keeping stale data/geometry.
5. Run focused/full frontend tests, lint/build, source inventory and headed read-only baseline on `3036/8036`.

## Done

Wave 1 closes only when inventory drift fails closed, shared contracts are green, pilot owners preserve usable
data during refetch, and browser evidence records DOM/request/CLS/long-task facts without DB mutation.
