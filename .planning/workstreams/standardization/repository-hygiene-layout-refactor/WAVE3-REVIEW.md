# Wave 3 deletion-ledger review

Status: reviewed; destructive execution not started
Branch: `refactor/large-refactor-20260924`
Baseline HEAD: `44124e091783765e3c52411d28cc5ee78c192986`

## Reviewed Tier A — generated output safe to reclaim

Exact ledger: [`wave-3-reviewed-delete-generated.txt`](wave-3-reviewed-delete-generated.txt)

- 270 existing roots
- approximately 20,472.7 MiB
- approximately 222,803 files
- zero path is referenced by `docs/EVIDENCE-INDEX.md`
- includes project-local `.artifacts`, `.artifactslk*`, `.tmp-*`, `.phase*test`, custom/default `bin`, `obj`, `TestResults`, recursive `backend/.../backend`, frontend `dist`/test output, logs, empty root `src`, Python bytecode caches, and Wave 1 `.artifacts/dotnet`
- excludes root `.artifacts` evidence outside `.artifacts/dotnet`
- excludes all 21 hash-mismatch and three missing evidence-index entries
- excludes tracked files and all source/config/migration/database paths

Tier A can be regenerated by restore/build/test commands. Wave 1 and Wave 2 now prevent the backend recursion from returning.

## Tier B — separate approval required

Machine-readable list: [`wave-3-needs-separate-approval.json`](wave-3-needs-separate-approval.json)

| Path | Approximate reason |
|---|---|
| `.gitnexus/` | optional graph index; project policy requires explicit GitNexus owner intent |
| `.dotnet-tools/` | local restored tool installation; reproducible but deleting forces restore |
| `node_modules/` | root npm install; reproducible but deleting interrupts current development |
| `frontend/node_modules/` | workspace link/cache; may be recreated by npm |

Tier B totals approximately 2,195.5 MiB and is not included in the proposed Wave 3 authorization below.

## Evidence exclusion

`docs/EVIDENCE-INDEX.md` reconciliation remains unresolved:

- 322 references checked
- 319 paths exist
- 297 hashes match
- 21 hashes mismatch
- three directory references are missing

No indexed `.artifacts` path is in Tier A. Root `.artifacts` must not be broadly deleted.

## Execution safeguards

If authorized, Wave 3 must:

1. Re-read the exact Tier A ledger and reject paths outside the repository.
2. Reject `.git`, root `.artifacts` and any tracked path.
3. Re-run evidence-reference overlap detection; expected overlap is zero.
4. Capture pre-delete byte/file counts.
5. Delete only Tier A paths; do not use `git clean`, wildcard root deletion, reset or restore.
6. Use Windows long-path-safe removal for recursive backend output.
7. Re-measure disk usage and prove every Tier A path absent.
8. Run `npm run test:ignore-policy`, `npm run build:be`, focused Phase 42 tests, and a clean output-tree assertion.
9. Leave Tier B, stale worktree metadata and evidence untouched.
10. Do not commit or push.

## Exact authorization text

To authorize only reviewed Tier A, use:

```text
Tôi cấp quyền thực thi Wave 3 Tier A theo
.planning/workstreams/standardization/repository-hygiene-layout-refactor/wave-3-reviewed-delete-generated.txt.

Được phép xóa đúng 270 generated roots trong ledger sau khi preflight xác nhận:
- zero tracked path;
- zero evidence-index overlap;
- không path nào thoát khỏi repository.

Không được xóa .gitnexus, .dotnet-tools, node_modules, frontend/node_modules,
root .artifacts evidence, stale worktree metadata hoặc bất kỳ file tracked/source/config/database nào.
Không dùng git clean/reset/restore, không commit hoặc push.
Sau xóa phải đo dung lượng, chạy ignore gate, backend build, focused Phase 42 tests và output-tree assertion.
```
