---
phase: 01-frontend-query-boundary-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/queryBoundaryInventory.ts
  - frontend/tests/queryBoundaryInventory.test.ts
  - frontend/src/features/projects/weekly-menu/purchasing/usePurchaseSummary.ts
  - frontend/src/features/projects/weekly-menu/purchasing/PurchaseSummarySection.tsx
  - frontend/src/features/projects/weekly-menu/purchasing/PurchaseSummarySection.feedback.test.tsx
autonomous: true
requirements: [FEQS-01, FEQS-02, FEQS-03, FEQS-04]
must_haves:
  truths:
    - Every production query-hook owner is source-discovered and classified by shared adapter or explicit exception.
    - Purchase summary never renders a false empty/local table when its authoritative aggregate query is loading, forbidden or failed.
    - Refreshing preserves current rows and exposes passive status; retry remains owned by the failed query.
  artifacts:
    - path: frontend/tests/queryBoundaryInventory.test.ts
      provides: AST-based exact-set closure and stale-exception failures
    - path: frontend/src/features/projects/weekly-menu/purchasing/PurchaseSummarySection.feedback.test.tsx
      provides: rendered query-state regressions
---

<objective>
Close FE query-state rollout with one permanent source-aware inventory and the remaining unsafe purchase-summary migration, while recording narrow specialized exceptions without changing business behavior.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install exact source-aware query-owner inventory</name>
  <files>frontend/tests/queryBoundaryInventory.ts, frontend/tests/queryBoundaryInventory.test.ts</files>
  <action>Use the installed TypeScript parser to discover production call expressions named like RTK query hooks. Exclude tests and generated API definitions. A discovered owner passes only if it invokes a recognized shared adapter or appears in the exact exception map with nonempty rationale and asserted source markers. Add negative tests for an uncovered owner and a stale exception. Do not use GitNexus.</action>
  <verify>npm run test:unit -w frontend -- --run tests/queryBoundaryInventory.test.ts --maxWorkers=1</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate weekly purchase summary to shared QueryView boundary</name>
  <files>frontend/src/features/projects/weekly-menu/purchasing/usePurchaseSummary.ts, frontend/src/features/projects/weekly-menu/purchasing/PurchaseSummarySection.tsx, frontend/src/features/projects/weekly-menu/purchasing/PurchaseSummarySection.feedback.test.tsx</files>
  <action>Write rendered failures first. Adapt the aggregate query with `toLabeledQueryView`; use ready data only, retain the local BOM fallback only for a successful authoritative empty response or an intentionally skipped query, and wrap the authoritative region with `QueryViewBoundary`. Remove parallel boolean query status from the component contract.</action>
  <verify>npm run test:unit -w frontend -- --run src/features/projects/weekly-menu/purchasing/PurchaseSummarySection.feedback.test.tsx tests/queryBoundaryInventory.test.ts --maxWorkers=1</verify>
</task>

<task type="auto">
  <name>Task 3: Run phase and root verification</name>
  <files>All declared phase files</files>
  <action>Run focused tests, frontend unit/lint/build gates, root verify, secret/stub scan and `git diff --check`. Reconcile test counts and update GSD summary/state/requirements/roadmap.</action>
  <verify>npm run verify</verify>
</task>

</tasks>

<success_criteria>
- FEQS-01..04 are executable and green.
- No false-empty/false-zero purchase summary remains.
- No production query-hook owner is undispositioned.
- Full repository verification and hygiene gates pass.
</success_criteria>
