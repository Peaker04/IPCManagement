---
context: phase
phase: 05-service-run-integration
plan: 05-04
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-08-14T02:06:00+07:00
checkpoint_commit: 5874bbfa
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Đây không phải gợi ý. Constraint dưới đây được phát hiện từ mutation thật.

- [ ] CONSTRAINT: Amendment decision over-fan — hồ sơ amendment duy nhất đã append 120 decision item bằng code grouping cũ; correction một item sẽ đóng sai toàn case — phải thiết kế và test remediation append-only trước mọi correction.

**Không tiếp tục mutation cho tới khi constraint trên được xác nhận và remediation được khóa bằng test.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Cleanup/recreate amendment | Xóa 120 item hoặc tạo amendment mới sẽ phá failure lineage và vi phạm append-only evidence. | blocking | Giữ nguyên amendment/case/decision hiện hữu; chỉ thêm remediation fact đã thiết kế và kiểm chứng. |
| Correct one over-fanned item | Service hiện đặt toàn case `RESOLVED` sau một correction dù còn các scope khác. | blocking | Không gọi correction endpoint cho tới khi remediation over-fan hoàn tất. |
| Re-run Task 0 | Reset/seed/import lại tuần 2026-08-10 sẽ phá Golden và exception lineage. | blocking | Chỉ readback exact lane7; không cleanup/seed/import. |

<current_state>

- Phase 5, plan 05-04, Task 3 đang chạy exception matrix theo thứ tự kitchen discrepancy → ambiguous lineage → shared shortage/blocked allocation → retry matrix.
- Checkpoint code gần nhất: `3a4da64a fix(phase05): isolate kitchen discrepancy lifecycle`; ambiguous lineage đang chờ commit checkpoint riêng.
- Task 0–2 PASS; Task 0 bị khóa vĩnh viễn cho lineage hiện tại.
- Golden authoritative: `.artifacts/shipyard-live/phase-05-multi-customer-lifecycle/golden/manifest.json`.
- Amendment authoritative readback: `.artifacts/shipyard-live/phase-05-multi-customer-lifecycle/exceptions/menu-amendment/readback.json`.
- Database exact `ipc_lane7`, migration count 69, head `20260812174836_AddInventoryAllocationDispositions`.
- `protectedLaneConnectionAttempts = 0`; không dùng GitNexus.

</current_state>

<completed_work>

- Task 1: OpenAPI/contract parity, physical headed preflight và reviewed migration receipts PASS.
- Task 2: ANV/DAV Golden lifecycle và năm viewport PASS.
- Task 3 phần đã xong: Service Run exceptions, RETURN, WASTE, EXCESS positive/negative, supplemental partial, fresh DAV sequence `0,1`, quality partial isolation và tab `Mua bổ sung` riêng.
- UI Bếp: checklist/pagination cùng owner, `Nhận`/`Đã nhận`, xem đầy đủ nguyên liệu.
- UI Thu mua: workflow theo ngày giữ vai trò chính; `Mua bổ sung` ở tab/URL riêng.
- UI chỉnh thực đơn: hiện món persist dù ngoài filter, báo số thay đổi chờ lưu, không mất draft khi gõ lý do.
- Một headed amendment đã tạo đúng scope ANV `2026-08-10 / AFTERNOON / savory-main`, đổi `Cá hố kho` → `Heo xào cải chua`; pointer/keyboard trusted, POST 200, Golden counts không đổi.
- Production grouping đã sửa theo customer string + service date + shift + price tier; focused backend `2/2`, frontend `7/7`, frontend/backend build và hygiene PASS.

</completed_work>

<remaining_work>

- Thiết kế và test remediation append-only cho 120 decision item của amendment hiện hữu.
- Chỉ sau remediation mới chứng minh reconciliation correction → receipt/transition/audit/outbox → reload.
- Kitchen discrepancy isolation và ambiguous lineage → `Cần quyết định` đã PASS.
- Shared shortage explicit decision và blocked allocation đã PASS.
- Còn retry/stale/concurrent/idempotency matrix và Task 3 closeout.
- Chạy full regression/OpenAPI/EF/lint/build/hygiene, tạo top-level manifest, cập nhật evidence/state và teardown chỉ process run-owned khi closeout.

</remaining_work>

<decisions_made>

- Không xóa/rewrite 120 decision item và không tạo lại amendment; failure lineage phải append-only.
- Không correction trong trạng thái hiện tại vì một item sẽ đóng toàn case sai.
- Dữ liệu `MENU CHAY` đang chứa món mặn là persisted Task 0 lineage; không import/reset để sửa.
- Hash artifact chỉ nằm trong `docs/EVIDENCE-INDEX.md`; state hiện hành nằm trong `MEMORY.md`.

</decisions_made>

<blockers>

- Amendment over-fan: amendment/case/audit `1/1/1`, decision item `120`, correction `0`.
- Remediation phải append-only và có regression trước live action.

</blockers>

## Required Reading (in order)

1. `AGENTS.md`
2. `MEMORY.md`
3. `.planning/HANDOFF.json`
4. File này
5. `.planning/workstreams/lifecycle-standardization/phases/05-service-run-integration/05-04-PLAN.md`
6. `docs/operations/phase-05-ipc-lane7-migration-runbook.md`
7. `docs/EVIDENCE-INDEX.md`

## Infrastructure State

- Frontend `http://127.0.0.1:3030`, PID 21360.
- Backend `http://127.0.0.1:8030`, PID 31400.
- Backend exact `ipc_lane7`; readiness Degraded chỉ vì outbox relay tắt, DB/migrations Healthy.
- Branch `feature/menu-amendment-reconciliation`, commit `c747189a`, working tree sạch.
- Không teardown khi resume; chỉ đóng process do Phase 5 sở hữu sau closeout.

<context>

Task 3 đã vượt qua nhiều exception branch. Điểm dừng là precondition fail có bằng chứng: create amendment
chạy trước grouping fix đã tạo 120 decision item. Source bug đã sửa cho create tương lai nhưng dữ liệu hiện hữu
không được cleanup. Cần một remediation model append-only có khả năng đánh dấu/supersede các item thừa mà không
rewrite history, đồng thời bảo đảm correction chỉ resolve đúng canonical scope/case semantics.

</context>

<next_action>

Bắt đầu retry matrix từ current source và exact lane7 hiện hữu: same-command replay, stale expected version và
hai command khác nhau cạnh tranh chỉ một winner; không duplicate document/allocation delta/movement/receipt/
transition/audit/outbox/close fact. Không tạo lại fixture shared shortage đã có issue receipt.

</next_action>
