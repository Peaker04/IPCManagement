---
phase: 26-floorplan-scope-source-ownership
plan: 04
type: execute
wave: 3
depends_on:
  - "26-02"
  - "26-03"
files_modified:
  - frontend/src/app/layout/MainLayout.tsx
  - frontend/src/components/common/OperationalFrame.tsx
  - frontend/src/components/common/ViewSwitcher.tsx
  - frontend/src/features/auth/pages/LoginPage.tsx
  - frontend/src/features/auth/pages/ForbiddenPage.tsx
  - frontend/tests/uiOwnershipInstrumentationContract.test.tsx
autonomous: true
requirements:
  - SOURCE-01
must_haves:
  truths:
    - "Per D-05, every canonical route and nested state resolves from rendered production DOM attributes to one approved opaque owner ID, opaque floorplan ID, and opaque region ID without importing test-owned source knowledge."
    - "Login and forbidden outliers, protected routes without OperationalFrame, pages with multiple ViewSwitcher instances, and every current tab/nested-view binding are covered rather than hidden by shared-boundary defaults."
    - "Ownership attributes contain only non-semantic uio-/uif-/uir- tokens; no route, scope key, repository-relative path, absolute path, source file, symbol, or fragment is rendered."
    - "Per D-06, instrumentation is inert: navigation, authorization, actions, keyboard behavior, layout, classes, text, API/cache/lifecycle behavior, route access, and rendered presentation remain unchanged."
    - "Per D-07, the five edited component symbols plus the modified/exported OperationalFrameProps and ViewSwitcherProps public contracts complete branch-aware two-way GitNexus impact before the first edit, risk-appropriate review/regression, and final explicit-branch change detection with no Deferred item; newly created production symbols are reconciled by final detection rather than assigned fictitious pre-edit impact."
  artifacts:
    - path: "frontend/src/app/layout/MainLayout.tsx"
      provides: "Full opaque route-root owner/floorplan/region tuple on the existing main boundary"
      contains: "data-ui-owner, data-ui-floorplan, data-ui-region"
    - path: "frontend/src/components/common/OperationalFrame.tsx"
      provides: "Typed optional ownership override plus a stable opaque operational-region boundary"
      exports:
        - "UiOwnershipMarker"
        - "OperationalFrameProps"
        - "OperationalFrame"
    - path: "frontend/src/components/common/ViewSwitcher.tsx"
      provides: "Opaque active nested-state and per-tab tuples keyed by the existing switcher identity"
      exports:
        - "ViewTab"
        - "ViewSwitcherProps"
        - "ViewSwitcher"
    - path: "frontend/src/features/auth/pages/LoginPage.tsx"
      provides: "Full opaque tuple on the public login root outside MainLayout"
      contains: "data-ui-owner"
    - path: "frontend/src/features/auth/pages/ForbiddenPage.tsx"
      provides: "Full opaque tuple on the native forbidden-page boundary"
      contains: "data-ui-owner"
    - path: "frontend/tests/uiOwnershipInstrumentationContract.test.tsx"
      provides: "Exact contract-to-production binding closure, opaque-only DOM assertions, outlier coverage, and inert shared-component regressions"
      contains: "MainLayout, OperationalFrame, ViewSwitcher, LoginPage, ForbiddenPage"
  key_links:
    - from: "frontend/tests/uiOwnershipInstrumentationContract.test.tsx"
      to: "frontend/tests/uiFloorplanScopeRegistry.ts, frontend/tests/uiFloorplanContracts.ts, and frontend/tests/uiSourceOwnershipManifest.ts"
      via: "test-only join of canonical scope, floorplan declaration, and owner/region targets; production imports none of these modules"
      pattern: "uiFloorplanScopeRegistry|uiFloorplanContracts|uiSourceOwnershipTargets"
    - from: "frontend/src/app/layout/MainLayout.tsx"
      to: "the existing ipc-main-content boundary"
      via: "current location selects one opaque full tuple and adds only three data attributes"
      pattern: "data-ui-owner.*data-ui-floorplan.*data-ui-region"
    - from: "frontend/src/components/common/ViewSwitcher.tsx"
      to: "every current tab and nested-view identity"
      via: "exact ariaLabel + tab.id binding emits the active tuple on the tablist and the target tuple on each tab"
      pattern: "ariaLabel|tab.id|data-ui-region"
    - from: "frontend/src/components/common/OperationalFrame.tsx"
      to: "the nearest full route or nested-state tuple"
      via: "region override with owner/floorplan inheritance under the documented nearest-ancestor resolution rule"
      pattern: "uiOwnership|data-ui-region"
---

<objective>
Instrument the stable shared production boundaries with opaque owner/floorplan/region identifiers that join every judged route and nested state to the frozen contracts, including login and forbidden outliers, per D-05, D-06, and D-07.

Purpose: Make SOURCE-01 observable in production DOM before Phase 27 capture, while keeping source paths test-owned and preserving every existing business, access, interaction, and rendered-presentation behavior.
Output: Attribute-only instrumentation in MainLayout, OperationalFrame, ViewSwitcher, LoginPage, and ForbiddenPage plus one focused exact-join/render contract test and complete Full-analysis evidence.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-floorplan-scope-source-ownership/CONTEXT.md
@.planning/phases/26-floorplan-scope-source-ownership/26-PLAN-OUTLINE.md
@.planning/phases/26-floorplan-scope-source-ownership/26-02-PLAN.md
@.planning/phases/26-floorplan-scope-source-ownership/26-02-SUMMARY.md
@.planning/phases/26-floorplan-scope-source-ownership/26-03-PLAN.md
@.planning/phases/26-floorplan-scope-source-ownership/26-03-SUMMARY.md
@frontend/tests/uiFloorplanScopeRegistry.ts
@frontend/tests/uiFloorplanContracts.ts
@frontend/tests/uiSourceOwnershipManifest.ts
@frontend/tests/uiSourceOwnershipContract.test.ts
@frontend/src/app/layout/MainLayout.tsx
@frontend/src/app/layout/MainLayout.ownership.test.tsx
@frontend/src/components/common/OperationalFrame.tsx
@frontend/src/components/common/ViewSwitcher.tsx
@frontend/src/components/common/ViewSwitcher.test.tsx
@frontend/src/features/auth/pages/LoginPage.tsx
@frontend/src/features/auth/pages/LoginPage.feedback.test.tsx
@frontend/src/features/auth/pages/ForbiddenPage.tsx

<interfaces>
The production DOM contract uses exactly `data-ui-owner`, `data-ui-floorplan`, and `data-ui-region`. Values are lower-case non-semantic opaque tokens matching `uio-[a-z0-9]+`, `uif-[a-z0-9]+`, and `uir-[a-z0-9]+` respectively. They must not contain a scope key, route, tab ID, label, slash, backslash, dot-extension, drive prefix, repository path, absolute path, source file, source symbol, or source fragment.

Create and export `UiOwnershipMarker` from `OperationalFrame.tsx` with `ownerId?: \`uio-${string}\``, `floorplanId?: \`uif-${string}\``, and required `regionId: \`uir-${string}\``. Export `OperationalFrameProps` and add optional `uiOwnership?: UiOwnershipMarker`. The existing `<section>` always emits an approved opaque region ID; explicit owner/floorplan values override the nearest ancestor when supplied, while omitted owner/floorplan values are resolved from the nearest ancestor carrying those attributes. No wrapper element is permitted.

Export `ViewSwitcherProps`. Add optional `uiOwnership?: UiOwnershipMarker` to `ViewSwitcherProps` and optional `uiOwnership?: UiOwnershipMarker` to `ViewTab`. Existing callers remain source-compatible. For every current caller without an explicit override, resolve a complete approved tuple from a bounded exact binding keyed by the already-existing `ariaLabel + tab.id`; this disambiguates multiple switchers on one route. Emit the active tab's full tuple on the existing `role=tablist` element and each tab's full tuple on its existing `role=tab` button. The lookup contains only existing UI identity strings and opaque tokens; it contains no test import, scope key, repository/source locator, policy, route-access rule, or new UI state.

`MainLayout` resolves every protected route, including `/403`, to a full approved tuple and emits it on the existing `<main id="ipc-main-content">`. Extend a dedicated ownership lookup rather than changing `pageContext`, menu filtering, preload, navigation, header state, Outlet placement, classes, or text. `LoginPage` emits its full tuple on the existing `.ipc-auth-shell`. `ForbiddenPage` emits its full tuple on the first existing native page-content `<div>` inside `SectionPanel`; do not change `SectionPanel` and do not add a wrapper.

A judged node resolves its ownership tuple component-wise from itself to its nearest ancestor: the nearest `data-ui-owner`, nearest `data-ui-floorplan`, and nearest `data-ui-region` form the join. A full tuple on MainLayout/Login/Forbidden starts every route; OperationalFrame may replace the region only; ViewSwitcher replaces the full tuple for active nested state and each meaningful tab control. The test-owned contract independently proves that this resolution reaches exactly one canonical scope/floorplan/owner/region row for every judged route and nested state.
</interfaces>
</context>

## Artifacts this phase produces

- `frontend/tests/uiOwnershipInstrumentationContract.test.tsx` — new focused Vitest contract for exact contract-to-production binding closure, opaque-only values, nearest-ancestor resolution, login/forbidden outliers, multiple-switcher disambiguation, and preserved common-component behavior.
- `UiOwnershipMarker` and exported `OperationalFrameProps` in `frontend/src/components/common/OperationalFrame.tsx` — new typed ownership override contract; no new wrapper or visual behavior.
- Exported `ViewSwitcherProps` plus `ViewTab.uiOwnership` and `ViewSwitcherProps.uiOwnership` in `frontend/src/components/common/ViewSwitcher.tsx` — new typed nested-state override contract with source-compatible existing callers.
- Bounded opaque binding constants local to `MainLayout` and `ViewSwitcher` — new production symbols containing current route/view identity inputs and opaque tokens only; source files, symbols, fragments, scope keys, permissions, policies, and API/cache/lifecycle values are forbidden.
- Three inert DOM attributes on existing native boundaries in `MainLayout`, `OperationalFrame`, `ViewSwitcher`, `LoginPage`, and `ForbiddenPage`; no route, panel, action, wrapper, style, package, database object, runtime artifact, or evidence artifact is created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete Full analysis and instrument protected/public route roots</name>
  <files>frontend/src/app/layout/MainLayout.tsx, frontend/src/features/auth/pages/LoginPage.tsx, frontend/src/features/auth/pages/ForbiddenPage.tsx, frontend/tests/uiOwnershipInstrumentationContract.test.tsx</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>MEMORY.md</file>
    <file>.planning/phases/26-floorplan-scope-source-ownership/CONTEXT.md</file>
    <file>.planning/phases/26-floorplan-scope-source-ownership/26-02-SUMMARY.md</file>
    <file>.planning/phases/26-floorplan-scope-source-ownership/26-03-SUMMARY.md</file>
    <file>frontend/tests/uiFloorplanScopeRegistry.ts</file>
    <file>frontend/tests/uiFloorplanContracts.ts</file>
    <file>frontend/tests/uiSourceOwnershipManifest.ts</file>
    <file>frontend/src/app/layout/MainLayout.tsx</file>
    <file>frontend/src/app/layout/MainLayout.ownership.test.tsx</file>
    <file>frontend/src/components/common/OperationalFrame.tsx</file>
    <file>frontend/src/components/common/ViewSwitcher.tsx</file>
    <file>frontend/src/components/common/ViewSwitcher.test.tsx</file>
    <file>frontend/src/features/auth/pages/LoginPage.tsx</file>
    <file>frontend/src/features/auth/pages/LoginPage.feedback.test.tsx</file>
    <file>frontend/src/features/auth/pages/ForbiddenPage.tsx</file>
  </read_first>
  <behavior>
    - Test 1: each protected canonical route, including forbidden, resolves to one full opaque tuple whose owner/region values belong to the 26-03 targets and whose floorplan value joins that same scope to its 26-02 declaration.
    - Test 2: the public login route exposes one full opaque tuple despite living outside MainLayout; forbidden exposes its own tuple on an existing native node rather than relying on an uninstrumented SectionPanel prop.
    - Test 3: every emitted owner/floorplan/region value satisfies its prefix/character contract and contains none of the route, scope, source-file, symbol, fragment, repository-relative, Windows-absolute, or POSIX-absolute strings derived by the test.
    - Test 4: adding the three data attributes does not change MainLayout Outlet/navigation/preload/permission behavior, LoginPage validation/submission behavior, or ForbiddenPage link target/text/classes.
  </behavior>
  <action>Before the first production edit, classify this implementation as the AGENTS.md Full-analysis lane and capture `git status --short --branch` plus `git branch --show-current`. Call GitNexus `list_repos` once and confirm the indexed repo is exactly `IPCManagement` on that explicit current branch. Resolve stable UIDs when available, otherwise use symbol name plus exact file path/kind, for the five component symbols `MainLayout`, `OperationalFrame`, `ViewSwitcher`, `LoginPage`, and `ForbiddenPage` and for the two modified/exported public contracts `OperationalFrameProps` and `ViewSwitcherProps`. For every one of these seven pre-existing production symbols/contracts, run branch-aware upstream and downstream impact with `repo=IPCManagement`, the explicit branch, and `includeTests=true`; start summary-first for hubs and page every depth until each direction reports `hasMore=false`, `partial=false`, and `truncated=false`. Record one distinct pre-edit evidence row per target as `Symbol | Upstream callers | Downstream dependencies | Affected processes | Confidence | Raw risk | Effective risk | Regression/disposition | Deferred + reason`; `OperationalFrameProps` and `ViewSwitcherProps` must each have their own row and must not be folded into their component's evidence. Use the props-contract closures to enumerate production and test consumers, disposition every caller/process, and lock source compatibility plus optional-prop render/type regressions before implementation. `UiOwnershipMarker` and any new bounded binding constant do not exist before the edit: record them as planned-new targets, do not claim pre-edit impact or zero risk/callers for them, and require their reconciliation in final `detect_changes`. MEDIUM requires `context` for every directly affected symbol and focused regression coverage. If raw or effective risk is HIGH or CRITICAL, issue a user-visible warning naming the symbol or props contract, callers, processes, and required rigor before any edit; HIGH requires complete cross-cluster call-chain/callsite handling and `gitnexus-pr-review`, while effective CRITICAL requires the branch PDG and anchored `explain`/`pdg_query` plus review under AGENTS.md. Unresolved nodes, incomplete pagination, confidence below 0.8, or any Deferred item blocks edits.

Write the failing cases in `uiOwnershipInstrumentationContract.test.tsx` first. Join the prerequisite scope, floorplan, and ownership contracts only in this test file; production must import none of them. Then instrument the three route roots using the exact attribute and resolution contracts from `<interfaces>`. In `MainLayout`, add a bounded current-route-to-opaque-tuple lookup using existing `ROUTES` values, cover every protected route explicitly including `ROUTES.FORBIDDEN`, and add only the three attributes to the existing `ipc-main-content` element. Keep the existing `pageContext`, header, visible-menu permission filtering, preloading, logout, route navigation, Outlet position, classes, and text byte-for-byte in behavior. In `LoginPage`, add the approved login tuple to the existing `ipc-auth-shell`; do not touch credentials, submit state, mutation, fallback, errors, navigation, controls, classes, or text. In `ForbiddenPage`, attach the approved tuple to the existing first native content div inside `SectionPanel`; do not modify SectionPanel, add a wrapper, or change the dashboard link, layout, classes, icons, or copy. Use exact opaque tokens approved by 26-02/26-03; never embed scope keys or source locators. Per D-06, make no policy, permission, lifecycle, API, cache, business-action, route-access, rendering, database, `ipc_lane1`, package, lockfile, runtime, evidence, roadmap, extra-phase, or push change.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiOwnershipInstrumentationContract.test.tsx src/app/layout/MainLayout.ownership.test.tsx src/features/auth/pages/LoginPage.feedback.test.tsx --maxWorkers=1</automated>
    <automated>git diff --check -- frontend/src/app/layout/MainLayout.tsx frontend/src/features/auth/pages/LoginPage.tsx frontend/src/features/auth/pages/ForbiddenPage.tsx frontend/tests/uiOwnershipInstrumentationContract.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Full branch-aware two-way analysis is complete before the first production edit for the five named component symbols and the two public props contracts, with `includeTests=true`, complete pagination, separate raw/effective risk, caller/dependency and affected-process disposition, props-consumer regression handling, and required HIGH/CRITICAL warning/review evidence.
    - MainLayout covers every protected canonical route, LoginPage covers the public outlier, and ForbiddenPage covers the SectionPanel outlier with exact approved full tuples.
    - Production imports no test-owned registry/contract/manifest and contains no source file, symbol, fragment, repository path, absolute path, or scope key.
    - Only three data attributes are added to existing native nodes; access, interactions, control flow, text, classes, layout, Outlet, and navigation remain unchanged.
    - No package, lockfile, database, `ipc_lane1`, runtime, evidence, roadmap, extra-phase, or push mutation occurs.
  </acceptance_criteria>
  <done>All route roots and both auth outliers expose exact opaque tuples after complete pre-edit Full analysis, with focused tests proving the instrumentation is inert and source-blind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument shared region/tab boundaries and reconcile regressions</name>
  <files>frontend/src/components/common/OperationalFrame.tsx, frontend/src/components/common/ViewSwitcher.tsx, frontend/tests/uiOwnershipInstrumentationContract.test.tsx</files>
  <read_first>
    <file>frontend/tests/uiOwnershipInstrumentationContract.test.tsx</file>
    <file>frontend/tests/uiFloorplanScopeRegistry.ts</file>
    <file>frontend/tests/uiFloorplanContracts.ts</file>
    <file>frontend/tests/uiSourceOwnershipManifest.ts</file>
    <file>frontend/src/components/common/OperationalFrame.tsx</file>
    <file>frontend/src/components/common/ViewSwitcher.tsx</file>
    <file>frontend/src/components/common/ViewSwitcher.test.tsx</file>
    <file>frontend/src/app/layout/MainLayout.tsx</file>
  </read_first>
  <behavior>
    - Test 1: every current ViewSwitcher `ariaLabel + tab.id` pair maps to exactly one approved full tuple; multiple switchers on one route and repeated tab labels cannot collide, and missing/duplicate/orphan bindings fail with exact keys.
    - Test 2: the tablist emits the active nested-state tuple and every tab button emits its own tuple while roles, aria attributes, roving tabindex, focus movement, click behavior, scroll adjustment, labels, and classes remain unchanged.
    - Test 3: OperationalFrame emits an approved region on its existing section, inherits owner/floorplan from the nearest route/nested boundary when not overridden, and emits typed opaque overrides without adding a wrapper or changing slot/layout behavior.
    - Test 4: nearest-ancestor resolution yields exactly one scope/floorplan/owner/region join for every canonical route, tab, and nested view, including pages without OperationalFrame and login/forbidden outliers; no count-only coverage can pass.
  </behavior>
  <action>Extend the failing contract tests before implementation. Export `UiOwnershipMarker` and `OperationalFrameProps`, add the optional `uiOwnership` prop, and apply its approved region (plus owner/floorplan only when supplied) directly to the existing OperationalFrame section. Use the exact stable operational-region target from 26-03 as the default so current callers remain source-compatible and inherit the full route tuple; do not alter the header/body/rail conditions, class composition, heading hierarchy, children, or DOM nesting. Add a props-contract regression that exercises the `OperationalFrameProps` consumers identified by the complete upstream/downstream evidence, proves existing prop objects/calls remain type-compatible, and proves omitted versus supplied optional ownership changes only the three inert attributes. In ViewSwitcher, export `ViewSwitcherProps`, add the optional component/tab `uiOwnership` overrides described in `<interfaces>`, and add one bounded binding keyed by exact existing `ariaLabel + tab.id` pairs for current callers. Resolve every binding to opaque tokens already represented by the prerequisite ownership targets and floorplan contracts; do not infer from labels, expose semantic IDs in data attributes, import a test file, or create a second route/state authority. Add the equivalent `ViewSwitcherProps` consumer/type/render regression from its separately recorded impact closure. Put the active binding on the existing tablist and each target binding on its existing button. Preserve click guards, arrow/Home/End handling, focus options, scroll adjustment, roles, aria-selected/controls/orientation, IDs, tabIndex, classes, labels, and caller signatures.

Complete `uiOwnershipInstrumentationContract.test.tsx` with source-aware exact-set closure rather than snapshots or counts: independently derive current MainLayout routes and all ViewSwitcher caller `ariaLabel + tab.id` identities from production source using the existing bounded TypeScript inventory, join them to 26-01/26-02/26-03 test-owned contracts, and compare both directions against production binding keys. Add isolated missing, duplicate, orphan, invalid-prefix, semantic-leak, path-leak, multiple-switcher-collision, login omission, and forbidden omission mutants with exact diagnostics. Render OperationalFrame and ViewSwitcher to prove attributes and original semantics, and retain the existing MainLayout/ViewSwitcher/Login focused regressions. Run the focused prerequisite and instrumentation suites, frontend lint/build, and the root count-preservation gate. Then run one final explicit-current-branch GitNexus `detect_changes` for `repo=IPCManagement` and reconcile every changed/new production symbol and affected process against the pre-edit evidence. The final table must use `Symbol | Pre-edit evidence status | Callers/dependencies found | Affected processes | Raw/effective risk | Regression/handling | Final detect disposition | Deferred + reason`; it must retain separate rows for the five component symbols, `OperationalFrameProps`, and `ViewSwitcherProps`, and add rows for newly created production symbols such as `UiOwnershipMarker` and the bounded ownership-binding constants returned by detection. For a newly created symbol, set pre-edit evidence to `N/A — symbol did not exist before edit` and disposition its actual post-edit callers/dependencies/processes from final detection; never present missing pre-edit impact as zero callers or low risk. Complete only when Deferred is empty and all changed/directed/low-confidence nodes are fixed or verified unnecessary. If final detection is partial/truncated, confidence is below 0.8, or exposes an undispositioned production edge/process, stop and complete the required Full-analysis/review closure rather than lowering effective risk. Preserve unrelated worktree changes and every D-05/D-06 fence.</action>
  <verify>
    <automated>npm run test:unit -w frontend -- --run tests/uiOwnershipInstrumentationContract.test.tsx tests/uiFloorplanScopeContract.test.ts tests/uiFloorplanContracts.test.ts tests/uiSourceOwnershipContract.test.ts src/app/layout/MainLayout.ownership.test.tsx src/components/common/ViewSwitcher.test.tsx src/features/auth/pages/LoginPage.feedback.test.tsx --maxWorkers=1</automated>
    <automated>npm run lint -w frontend</automated>
    <automated>npm run build -w frontend</automated>
    <automated>npm run verify</automated>
    <automated>git diff --check -- frontend/src/app/layout/MainLayout.tsx frontend/src/components/common/OperationalFrame.tsx frontend/src/components/common/ViewSwitcher.tsx frontend/src/features/auth/pages/LoginPage.tsx frontend/src/features/auth/pages/ForbiddenPage.tsx frontend/tests/uiOwnershipInstrumentationContract.test.tsx</automated>
    <automated>$paths = @('frontend/src/app/layout/MainLayout.tsx','frontend/src/components/common/OperationalFrame.tsx','frontend/src/components/common/ViewSwitcher.tsx','frontend/src/features/auth/pages/LoginPage.tsx','frontend/src/features/auth/pages/ForbiddenPage.tsx','frontend/tests/uiOwnershipInstrumentationContract.test.tsx'); $matches = Select-String -Path $paths -Pattern '(?i)(password\s*=|api[_-]?key\s*=|secret\s*=|TODO|FIXME|placeholder|hardcoded for now)' -AllMatches; if ($matches) { $matches | ForEach-Object { $_.ToString() }; exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - Every canonical route/tab/nested-view identity resolves bidirectionally to one approved full tuple; missing, duplicate, orphan, outlier, collision, prefix, and leakage mutants fail with exact identities.
    - OperationalFrame and ViewSwitcher implement the declared optional prop contracts while every existing caller remains source-compatible and fully instrumented through exact defaults/bindings.
    - ViewSwitcher and OperationalFrame rendered structure and interaction semantics are unchanged apart from the three inert data attributes.
    - Focused contracts, prerequisite closure tests, MainLayout/ViewSwitcher/Login regressions, frontend lint/build, root verification/count preservation, whitespace, and secret/stub scans pass.
    - Final explicit-branch `detect_changes` is fully reconciled with no undispositioned node/process and no Deferred item; the final report preserves distinct rows for the five component symbols and both props contracts, includes callers/dependencies, handling, raw/effective risk, `includeTests=true`, pagination, and affected-process evidence, and separately reconciles every newly created production symbol with pre-edit status recorded as not applicable rather than invented.
    - The attributable diff contains only the six declared files and changes no policy, permission, lifecycle, API, cache, route access, business action, presentation, package/lockfile, database/`ipc_lane1`, runtime/evidence, roadmap, phase, or push state.
  </acceptance_criteria>
  <done>SOURCE-01 is executable: every judged route, nested state, shared region, and meaningful tab control exposes an exact opaque join, behavior remains unchanged, and complete Full-analysis/regression evidence has no Deferred item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-owned scope/floorplan/source contracts -> production bindings | The executor may read approved IDs and relationships, but production may contain only opaque tokens and current UI identity inputs; test-owned modules and source locators cannot cross into the bundle. |
| Current route/nested state -> rendered ownership attributes | Existing navigation state selects evidence metadata; selection must not affect route access, control flow, layout, or presentation. |
| MainLayout/ViewSwitcher ancestry -> evidence collector | A collector resolves the nearest owner, floorplan, and region attributes; missing or ambiguous ancestry would misattribute later screenshots/findings. |
| Login/forbidden auth surfaces -> inert instrumentation | Auth-adjacent roots receive metadata only; credentials, session handling, guards, redirect logic, and error behavior remain untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Spoofing | Route/view-to-opaque bindings | mitigate | Exact bidirectional join to 26-01/26-02/26-03 contracts, key-specific missing/duplicate/orphan/collision mutants, and explicit login/forbidden coverage. |
| T-26-04-02 | Tampering | MainLayout, OperationalFrame/OperationalFrameProps, ViewSwitcher/ViewSwitcherProps, LoginPage, ForbiddenPage | mitigate | Attribute-only edits on existing native nodes, separate public-props consumer/type/render regressions, lint/build/root verification, and complete two-way impact plus final detection. |
| T-26-04-03 | Repudiation | GitNexus blast-radius disposition | mitigate | Record explicit repo/branch, includeTests, both directions, pagination flags, raw/effective risk, callers/dependencies, affected processes, and distinct evidence rows for the five components plus both props contracts; reconcile UiOwnershipMarker and every other new symbol through final detection with pre-edit status explicitly not applicable. |
| T-26-04-04 | Information disclosure | Production DOM/bundle boundary | mitigate | Allow only uio-/uif-/uir- values; forbid scope/source/path strings and production imports of tests. Complete emitted-bundle leakage closure remains assigned to SOURCE-03 in 26-05. |
| T-26-04-05 | Denial of service | Binding resolution | mitigate | Use bounded constant lookup on already-present route/view identities; add no I/O, effect, subscription, network call, recursive scan, or render-state update. |
| T-26-04-06 | Elevation of privilege | Login/forbidden/protected-route edits | mitigate | Keep guards, permissions, login mutation/session flow, navigation, and dashboard target unchanged; auth-focused regressions plus risk-appropriate PDG/review close any HIGH/CRITICAL evidence. |
| T-26-04-SC | Tampering | Package supply chain | accept | No package install or dependency change is authorized; package manifests and lockfiles remain outside the diff. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID / item | Plan | Status | Notes |
|--------|-----------|------|--------|-------|
| GOAL | Every future visual finding resolves from a judged screen to an editable source owner | 26-04 | COVERED | Opaque DOM tuples connect current routes/nested states to the frozen test-owned contracts and 26-03 manifest. |
| REQ | SOURCE-01 | 26-04 | COVERED | All judged roots/regions/meaningful tab controls expose stable opaque owner/floorplan/region joins. |
| RESEARCH | Opaque shared-boundary instrumentation before capture/remediation | 26-04 | COVERED | Uses the authoritative outline and existing shared roots; no new research, page-by-page remediation, or dependency is opened. |
| CONTEXT | D-05 | 26-04 | COVERED | Production exposes only opaque tokens; source file/symbol/fragment knowledge and exact joins remain test-controlled. |
| CONTEXT | D-06 | 26-04 | COVERED | Attribute-only instrumentation preserves behavior, policy, API/cache/lifecycle, access, rendering, package, database, and roadmap scope. |
| CONTEXT | D-07 | 26-04 | COVERED | Five component symbols and both modified/exported props contracts require complete explicit-branch two-way Full analysis before edit, risk warning/review, caller/process regressions, and final detection; newly created symbols are reconciled post-edit without fictitious pre-edit evidence. |

D-01/FLOOR-01 is supplied by prerequisite 26-01; D-02–D-04/FLOOR-02–FLOOR-04 by 26-02; SOURCE-02 by 26-03; and SOURCE-03 remains assigned exactly to 26-05. Phase 27 capture/geometry, Phase 28 judging, Phase 29 remediation, Phase 30 rejudge/closeout, deferred VIEW-01, and deferred PROD-01 are deliberate exclusions rather than gaps.

<verification>
1. Before any production edit, confirm explicit `repo=IPCManagement` and current branch, then retain complete upstream/downstream `includeTests=true` impact evidence for MainLayout, OperationalFrame, OperationalFrameProps, ViewSwitcher, ViewSwitcherProps, LoginPage, and ForbiddenPage. Every direction must end with `hasMore=false`, `partial=false`, `truncated=false`; each props contract must have its own callers/dependencies, affected-process disposition, raw/effective risk, and regression row; warn before editing on HIGH/CRITICAL and complete the required context/trace/PDG/review rigor. Do not claim pre-edit impact for UiOwnershipMarker or another symbol that does not yet exist.
2. Run the focused instrumentation/prerequisite/component command. Confirm exact bidirectional route/tab/nested-state closure, login/forbidden and multi-switcher coverage, opaque-only values, nearest-ancestor resolution, and all isolated failure mutants.
3. Run frontend lint, production build, and `npm run verify`; existing tests must remain green and discovered test counts must not decrease. No browser capture, runtime startup, or database operation is part of this plan.
4. Inspect the attributable diff and confirm it contains only the five named production files plus `frontend/tests/uiOwnershipInstrumentationContract.test.tsx`. Run `git diff --check` and the declared secret/stub scan.
5. Run final explicit-current-branch GitNexus `detect_changes` for `repo=IPCManagement`; reconcile all changed/new symbols and processes in distinct rows for the five component symbols, OperationalFrameProps, ViewSwitcherProps, UiOwnershipMarker, and every detected bounded binding/helper. Completion requires no partial/truncated/low-confidence evidence, no undispositioned process/edge, and Deferred empty; newly created symbols must say pre-edit evidence was not applicable, not silently inherit a component's risk.
6. Confirm no source/scope/path locator or test-owned import entered production, and no business/rendered behavior, authorization, action, API/cache/lifecycle, route access, package/lockfile, database/`ipc_lane1`, runtime/evidence, roadmap, extra-phase, or push state changed. SOURCE-03 bundle/DOM leakage closure remains for 26-05.
</verification>

<success_criteria>
- SOURCE-01 is satisfied by exact opaque owner/floorplan/region tuples for every canonical protected/public route, tab, nested view, shared operational region, and meaningful tab control, including login, forbidden, pages without OperationalFrame, and multiple-switcher outliers.
- Production exposes only uio-/uif-/uir- values and imports no test-owned registry, floorplan contract, source manifest, path, symbol, or fragment.
- MainLayout, OperationalFrame, ViewSwitcher, LoginPage, and ForbiddenPage retain current DOM structure, classes, text, interactions, navigation, access, API/cache/lifecycle, and business behavior apart from inert data attributes.
- Focused contracts/regressions, prerequisite closure, frontend lint/build, root verification/count preservation, whitespace, and secret/stub checks pass.
- Full GitNexus analysis is complete and reconciled for the five component symbols plus OperationalFrameProps and ViewSwitcherProps with explicit repo/branch, includeTests, complete pagination, separate caller/dependency and affected-process disposition, raw/effective risk, props regression handling, required warning/review, final detect, and no Deferred item; UiOwnershipMarker and every other new production symbol are explicitly reconciled through final detection with no invented pre-edit impact.
- The implementation touches only the six declared files; no package install, push, database or `ipc_lane1` mutation, runtime/evidence mutation, roadmap expansion, or extra phase occurs.
</success_criteria>

<output>
Create `.planning/phases/26-floorplan-scope-source-ownership/26-04-SUMMARY.md` when this plan is executed.
</output>
