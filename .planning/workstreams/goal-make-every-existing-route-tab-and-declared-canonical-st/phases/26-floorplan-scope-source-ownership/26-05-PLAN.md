---
phase: 26-floorplan-scope-source-ownership
plan: 05
type: execute
wave: 4
depends_on:
  - "26-04"
files_modified:
  - frontend/tests/uiSourceOwnershipLeakage.test.ts
  - frontend/tests/ui-source-ownership.spec.ts
  - scripts/check-frontend-test-count.mjs
  - frontend/package.json
  - package.json
autonomous: true
requirements:
  - SOURCE-03
must_haves:
  truths:
    - "Per D-05, no repository-relative, Windows, POSIX, or absolute source path derived from the test-owned manifest appears in any canonical rendered DOM state."
    - "Every emitted frontend/dist text asset is scanned, including every emitted JavaScript, CSS, HTML, JSON, and source-map asset; no manifest-derived source path or source-map source entry is leaked."
    - "Every canonical route, tab, nested view, and login/forbidden outlier renders an opaque ownership tuple that resolves to exactly one scope and manifest target; missing, duplicate, orphan, and path-leak cases fail by exact key or path."
    - "Build runs before the bundle scan, focused Vitest and headed Playwright ownership checks run through explicit scripts, UI-completeness remains green, and the frontend test-file/test-count gate is nondecreasing from the MEMORY.md baseline."
    - "Per D-06, the leakage proof changes only test/config wiring and makes no business, policy, API, cache, lifecycle, route-access, rendering, database, ipc_lane1, package-dependency, runtime, evidence, roadmap, phase, or push change."
  artifacts:
    - path: "frontend/tests/uiSourceOwnershipLeakage.test.ts"
      provides: "Manifest-derived Windows/POSIX/relative/absolute path variants, emitted dist/source-map scans, DOM-string leakage probes, and exact failure diagnostics"
      contains: "buildManifestPathVariants|scanDistTextAssets|source-map|sourcePathLeakage"
    - path: "frontend/tests/ui-source-ownership.spec.ts"
      provides: "Canonical rendered-state Playwright traversal, nearest-ancestor tuple resolution, opaque-only DOM assertions, and path-leak regression mutants"
      contains: "uiFloorplanScopeRegistry|uiSourceOwnershipManifest|data-ui-owner|data-ui-floorplan|data-ui-region"
    - path: "scripts/check-frontend-test-count.mjs"
      provides: "Temporary full-Vitest JSON reporter gate comparing discovered frontend test files and tests to MEMORY.md baseline without writing repository artifacts"
      contains: "numTotalTestSuites|numTotalTests|nondecreasing"
    - path: "frontend/package.json"
      provides: "Focused unit and Playwright ownership commands"
      contains: "test:source-ownership:unit|test:source-ownership:e2e"
    - path: "package.json"
      provides: "Build-first ownership orchestration, UI-completeness gate, and nondecreasing-count command"
      contains: "test:source-ownership|test:frontend-nondecreasing"
  key_links:
    - from: "frontend/tests/uiSourceOwnershipManifest.ts"
      to: "frontend/tests/uiSourceOwnershipLeakage.test.ts"
      via: "sourceFile values generate all normalized relative and absolute forbidden variants"
      pattern: "uiSourceOwnershipManifest|sourceFile|buildManifestPathVariants"
    - from: "frontend/dist/**/*"
      to: "frontend/tests/uiSourceOwnershipLeakage.test.ts"
      via: "bounded recursive text and source-map asset scan after production build"
      pattern: "scanDistTextAssets|sourceMappingURL|sourcesContent"
    - from: "frontend/tests/uiFloorplanScopeRegistry.ts"
      to: "frontend/tests/ui-source-ownership.spec.ts"
      via: "exact canonical route/tab/nested-state projection; no copied route vocabulary"
      pattern: "uiFloorplanScopeRegistry|UiFloorplanScopeEntry"
    - from: "frontend/tests/ui-source-ownership.spec.ts"
      to: "rendered ownership attributes"
      via: "nearest-ancestor data-ui-owner/data-ui-floorplan/data-ui-region tuple joins to one manifest target"
      pattern: "data-ui-owner|data-ui-floorplan|data-ui-region"
    - from: "package.json"
      to: "frontend/package.json"
      via: "test:source-ownership executes frontend build, focused unit scan, focused headed Playwright, UI-completeness, and count gate in order"
      pattern: "npm run build -w frontend.*test:source-ownership:unit.*test:source-ownership:e2e"
---

<objective>
Prove SOURCE-03 end to end: every canonical rendered state exposes only opaque ownership metadata, and no repository-relative or absolute source path from the test-owned manifest survives into the DOM or emitted frontend bundles.

Purpose: Close the DOM-to-source boundary after 26-04 instrumentation with deterministic, manifest-derived leakage detection while preserving all existing test counts, scope, and application behavior per D-05, D-06, and D-07.
Output: A Vitest dist/source-map and path-variant contract, a focused headed Playwright canonical-state DOM contract, a temporary nondecreasing frontend test-count gate, and narrow root/frontend scripts that enforce build-before-bundle scanning.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Administrator/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MEMORY.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-floorplan-scope-source-ownership/CONTEXT.md
@.planning/phases/26-floorplan-scope-source-ownership/26-PLAN-OUTLINE.md
@.planning/phases/26-floorplan-scope-source-ownership/26-01-PLAN.md
@.planning/phases/26-floorplan-scope-source-ownership/26-02-PLAN.md
@.planning/phases/26-floorplan-scope-source-ownership/26-03-PLAN.md
@.planning/phases/26-floorplan-scope-source-ownership/26-04-PLAN.md
@frontend/tests/uiFloorplanScopeRegistry.ts
@frontend/tests/uiFloorplanContracts.ts
@frontend/tests/uiSourceOwnershipManifest.ts
@frontend/tests/uiSourceOwnershipContract.test.ts
@frontend/tests/uiOwnershipInstrumentationContract.test.tsx
@frontend/tests/uiCanonSourceInventory.ts
@frontend/tests/support/route-smoke/auth.ts
@frontend/playwright.config.ts
@frontend/package.json
@package.json

<interfaces>
`uiSourceOwnershipManifest` entries carry opaque `ownerId`/`regionId` values and repository-relative `sourceFile` values stored as `src/...` relative to the frontend root. `uiFloorplanScopeRegistry` is the only canonical route/tab/nested-view/role/data-state projection. 26-04 emits exactly `data-ui-owner`, `data-ui-floorplan`, and `data-ui-region`, each using non-semantic lower-case `uio-`, `uif-`, or `uir-` tokens. A browser join resolves each attribute component from the node to its nearest ancestor and must match exactly one canonical scope/floorplan/manifest target.

The leakage checker must derive, never hand-write, forbidden variants from every manifest `sourceFile`: normalized `src/...`, `frontend/src/...`, slash and backslash forms, POSIX-normalized forms, native Windows absolute forms, forward-slash absolute forms, and POSIX-rooted absolute forms where applicable. It scans rendered HTML and all text files under `frontend/dist`, including emitted `.map` files and their `sources`/`sourcesContent`; a missing `dist` directory or an unreadable emitted text asset is a failure. A synthetic map/HTML/DOM mutant must prove each variant is rejected even when current Vite output does not emit a source map.

The count helper reads the current `frontend: N files / M tests` lower-bound from `MEMORY.md`, runs the full frontend Vitest suite with the JSON reporter in a temporary OS directory, and fails if discovered test files or tests decrease. It must not add a committed baseline or write under `frontend/dist`, `.artifacts`, or `.planning`.
</interfaces>
</context>

## Artifacts this phase produces

- `frontend/tests/uiSourceOwnershipLeakage.test.ts` — new test-owned source-path variant generator and bounded DOM/bundle/source-map scanner. Keep helpers pure and named `buildManifestPathVariants`, `scanTextForSourcePathLeaks`, and `scanDistTextAssets`; include exact offending manifest keys, asset paths, and matched variants in failures.
- `frontend/tests/ui-source-ownership.spec.ts` — new focused headed Playwright contract. Keep helpers named `buildCanonicalRenderedStates`, `activateCanonicalState`, and `resolveNearestOwnershipTuple`; import the 26-01/26-02/26-03 contracts only from this test and never from production.
- `scripts/check-frontend-test-count.mjs` — new temporary-output JSON reporter gate; no committed snapshot or generated evidence file.
- `frontend/package.json` — add only `test:source-ownership:unit` and `test:source-ownership:e2e` scripts; do not alter dependencies, lockfiles, dev server, or build options.
- `package.json` — add only `test:source-ownership` (build → unit leakage → headed Playwright → UI-completeness → count gate) and `test:frontend-nondecreasing`; preserve the existing `verify` command and all unrelated scripts.
- No production source, route, DOM owner token, package dependency, lockfile, database object, `ipc_lane1` state, runtime/evidence artifact, roadmap entry, additional phase, or push is produced or modified.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive manifest path variants and reject DOM, dist, and source-map leakage</name>
  <files>frontend/tests/uiSourceOwnershipLeakage.test.ts, frontend/package.json</files>
  <read_first>
    <file>AGENTS.md</file>
    <file>MEMORY.md</file>
    <file>.planning/phases/26-floorplan-scope-source-ownership/CONTEXT.md</file>
    <file>.planning/phases/26-floorplan-scope-source-ownership/26-03-PLAN.md</file>
    <file>.planning/phases/26-floorplan-scope-source-ownership/26-04-PLAN.md</file>
    <file>frontend/tests/uiSourceOwnershipManifest.ts</file>
    <file>frontend/tests/uiSourceOwnershipContract.test.ts</file>
    <file>frontend/tests/uiOwnershipInstrumentationContract.test.tsx</file>
    <file>frontend/vite.config.ts</file>
    <file>frontend/package.json</file>
  </read_first>
  <behavior>
    - Test 1: every manifest source file produces deterministic normalized `src/...`, `frontend/src/...`, slash, backslash, native-Windows-absolute, forward-slash-absolute, and POSIX-rooted variants; duplicate variants are de-duplicated without losing the manifest key.
    - Test 2: a synthetic HTML/DOM string containing each Windows or POSIX variant fails with the exact manifest key and matched variant, while opaque `uio-`, `uif-`, and `uir-` tokens pass.
    - Test 3: a synthetic JavaScript, CSS, HTML, JSON, and source-map asset containing a manifest-derived path fails; source-map `sources`, `sourcesContent`, and `sourceRoot` text are scanned rather than ignored.
    - Test 4: the live production build must exist before scanning; every emitted text asset under `frontend/dist` is visited, unreadable assets fail, and no repository or absolute source path is found.
  </behavior>
  <action>Classify this implementation as the outline's Lightweight graph lane because the checker and script are shared source-aware test/config vocabulary. Before editing, capture `git status --short --branch`, call GitNexus `list_repos` once, and confirm the indexed repo is `IPCManagement` on the explicit current branch. Per D-07, per-symbol impact is not required for test/config-only work, but record raw/effective Lightweight risk and the expected zero-production-process disposition. Write the failing tests first. Then implement `buildManifestPathVariants` from every live `uiSourceOwnershipManifest` `sourceFile` without hardcoded repository paths beyond the known frontend root; normalize both separators, include frontend-relative and repository-relative forms, native Windows and forward-slash absolute forms, and a POSIX-rooted absolute form where meaningful. Implement `scanTextForSourcePathLeaks` and bounded `scanDistTextAssets` over `frontend/dist` text assets, explicitly including `.js`, `.css`, `.html`, `.json`, `.txt`, and every `.map`; inspect source-map fields as ordinary text, reject missing dist directories and read failures, and report exact manifest key/asset/variant diagnostics. Add isolated synthetic DOM, bundle, CSS, HTML, and source-map mutants so a count-only or extension-only scan cannot pass. Keep all source paths and locators in tests; production remains opaque-token-only and imports none of these modules. Add only the frontend `test:source-ownership:unit` script invoking this file with Vitest and one worker. Do not install packages, alter Vite/build semantics, write committed output, start a database/runtime, mutate `ipc_lane1`, or change business/policy/API/cache/lifecycle/route-access/rendered behavior.</action>
  <verify>
    <automated>npm run test:source-ownership:unit -w frontend -- --maxWorkers=1</automated>
    <automated>git diff --check -- frontend/tests/uiSourceOwnershipLeakage.test.ts frontend/package.json</automated>
  </verify>
  <acceptance_criteria>
    - Every manifest row yields all required Windows/POSIX/relative/absolute variants deterministically, and synthetic mutants identify the exact leaking key and variant.
    - A real `frontend/dist` build is required and every emitted text/source-map asset is scanned; no bundle/source-map path leak is silently skipped.
    - The focused Vitest command passes after a build, and production source remains free of test-owned imports and source locators.
    - Only the declared test file and one frontend script entry change; no dependency, lockfile, runtime, evidence, database, `ipc_lane1`, route, policy, behavior, or phase change occurs.
  </acceptance_criteria>
  <done>The unit contract deterministically rejects every manifest-derived repository or absolute source path in synthetic DOM/assets and in the built dist/source-map tree, with build-before-scan enforced by the owning root script.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove canonical rendered-state joins and wire the final nondecreasing gates</name>
  <files>frontend/tests/ui-source-ownership.spec.ts, scripts/check-frontend-test-count.mjs, package.json</files>
  <read_first>
    <file>frontend/tests/uiSourceOwnershipLeakage.test.ts</file>
    <file>frontend/tests/uiOwnershipInstrumentationContract.test.tsx</file>
    <file>frontend/tests/uiFloorplanScopeRegistry.ts</file>
    <file>frontend/tests/uiFloorplanContracts.ts</file>
    <file>frontend/tests/uiSourceOwnershipManifest.ts</file>
    <file>frontend/tests/support/route-smoke/auth.ts</file>
    <file>frontend/tests/route-smoke.spec.ts</file>
    <file>frontend/playwright.config.ts</file>
    <file>MEMORY.md</file>
    <file>package.json</file>
  </read_first>
  <behavior>
    - Test 1: the exact 26-01 canonical route/tab/nested-view projection, plus login and forbidden outliers, renders at each approved desktop viewport; no hand-copied route list or representative-count shortcut can satisfy the test.
    - Test 2: each canonical state is activated through its existing route or exact `ariaLabel + tab.id` binding, and nearest-ancestor owner/floorplan/region resolution maps to exactly one scope/floorplan/manifest target; missing, duplicate, orphan, wrong-parent, and ambiguous-join mutants fail with exact identities.
    - Test 3: every visited document is scanned for every manifest-derived path variant and for source/symbol/fragment leakage; only `uio-`, `uif-`, and `uir-` values are accepted in the three ownership attributes, while existing text, roles, controls, navigation, and layout remain unchanged.
    - Test 4: the root ownership command builds before running the bundle scan, executes focused Vitest and headed Playwright, runs UI-completeness, and then the temporary JSON test-count helper proves frontend test files and tests are not below the MEMORY.md baseline.
  </behavior>
  <action>After Task 1, keep the same Lightweight classification for the shared Playwright/checker wiring. Before editing, confirm the explicit current branch and run the one required `list_repos` check if the earlier task did not leave a recorded branch slot. Write failing browser cases first. Implement `buildCanonicalRenderedStates` from `uiFloorplanScopeRegistry` and the existing 26-04 binding projection; derive route rows, tab rows, nested-view rows, role/data-state variants, and the login/forbidden outliers from those test-owned contracts rather than copying route strings or labels. Use the existing read-only Playwright auth/network helpers; do not add credentials, call mutating endpoints, seed/reset/import/restore `ipc_lane1`, or create new fixtures. Run every canonical state at all five MEMORY.md desktop viewports (`1920×1080`, `1440×900`, `1366×768`, `1365×900`, `1280×900`). `activateCanonicalState` must navigate the declared parent route and activate the existing control by its source-derived identity (`ariaLabel + tab.id` or existing route/query seam), then assert the state-specific ownership marker is present. `resolveNearestOwnershipTuple` must walk the rendered node and ancestors component-wise, reject absent or conflicting attributes, and join the full tuple bidirectionally to exactly one scope key, floorplan declaration, owner, and region manifest row; assert exact missing/duplicate/orphan/ambiguous diagnostics rather than aggregate counts. Serialize each canonical document's HTML and run the Task 1 path-variant scanner plus explicit checks that no source file, symbol, fragment, repository-relative path, Windows path, POSIX path, absolute path, test-module name, or scope key is present in production DOM; retain assertions for original roles, labels, tab selection, and route URLs to prove D-06 inertness. Add `scripts/check-frontend-test-count.mjs` to read the `frontend: N files / M tests` lower bounds from `MEMORY.md`, invoke the full frontend Vitest suite with the JSON reporter in an OS temporary directory, compare `numTotalTestSuites` and `numTotalTests` to those lower bounds, remove the temporary output, and fail on missing/invalid counts. Add only the root `test:source-ownership` and `test:frontend-nondecreasing` scripts: the former must run `npm run build -w frontend` first, then `test:source-ownership:unit`, headed `test:source-ownership:e2e`, `test:ui-completeness`, and `test:frontend-nondecreasing` in that order; do not rewrite `verify` or add dependencies. Finish with one explicit-current-branch GitNexus `detect_changes` for `repo=IPCManagement`; reconcile every changed test/config symbol and require no undispositioned production process/edge. If detection reveals a production flow, public contract, unresolved mapping, partial/truncated result, or confidence below 0.8, stop the Lightweight completion and escalate to Full analysis with complete two-way `includeTests=true` impact, affected-process disposition, and required review before declaring SOURCE-03 complete. Preserve unrelated worktree changes and all D-05/D-06 fences.</action>
  <verify>
    <automated>npm run test:source-ownership</automated>
    <automated>npm run test:source-ownership:e2e -w frontend -- --headed</automated>
    <automated>npm run test:ui-completeness</automated>
    <automated>npm run test:frontend-nondecreasing</automated>
    <automated>npm run verify</automated>
    <automated>git diff --check -- frontend/tests/uiSourceOwnershipLeakage.test.ts frontend/tests/ui-source-ownership.spec.ts scripts/check-frontend-test-count.mjs frontend/package.json package.json</automated>
    <automated>$paths = @('frontend/tests/uiSourceOwnershipLeakage.test.ts','frontend/tests/ui-source-ownership.spec.ts','scripts/check-frontend-test-count.mjs','frontend/package.json','package.json'); $matches = Select-String -Path $paths -Pattern '(?i)(password\s*=|api[_-]?key\s*=|secret\s*=|TODO|FIXME|placeholder|hardcoded for now)' -AllMatches; if ($matches) { $matches | ForEach-Object { $_.ToString() }; exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - All canonical route/tab/nested-view/role/data-state rows and login/forbidden outliers are rendered and joined by exact identity at all five approved desktop viewports; every row has one and only one resolved opaque tuple.
    - DOM scans reject all manifest-derived Windows/POSIX/relative/absolute paths and source metadata while preserving existing route URLs, controls, roles, labels, tab behavior, and layout; no production import or semantic scope key leaks.
    - The root script enforces build-before-bundle scan and runs focused Vitest, headed Playwright, UI-completeness, and nondecreasing-count gates without changing the existing `verify` pipeline or installing packages.
    - The count helper proves `numTotalTestSuites` and `numTotalTests` are not below the current MEMORY.md baseline and leaves no committed temp/evidence output.
    - Final explicit-branch `detect_changes` is complete and reconciled with no undispositioned production process/edge; any production-flow appearance triggers the required Full-analysis escalation rather than a downgraded result.
    - The attributable diff is limited to the five declared files and changes no business behavior, policy, API/cache/lifecycle/route access, package dependency, database/`ipc_lane1`, runtime/evidence, roadmap, extra phase, or push state.
  </acceptance_criteria>
  <done>SOURCE-03 is proven end to end: canonical rendered DOM is source-blind, every emitted bundle/source map is source-path clean, existing scope and test counts do not regress, and the final Lightweight graph evidence has no undispositioned production flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-owned source manifest → forbidden-variant generator | Manifest source locators are trusted only as test data; the generator must derive all path spellings without copying or exposing them to production. |
| Built frontend/dist assets → leakage scanner | Build output is untrusted text, including source-map `sources`, `sourcesContent`, and `sourceRoot`; every emitted text asset must be bounded, readable, and scanned. |
| Canonical scope contracts → headed browser DOM | Existing route/tab/nested state selects ownership metadata; the scanner must verify exact ancestry joins without changing navigation, access, controls, or rendering. |
| Root/frontend script wiring → quality gates | Script order controls whether a bundle is scanned before tests; no dependency installation, build-option change, or runtime/database mutation is allowed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-01 | Spoofing | Canonical DOM ownership join | mitigate | Derive every rendered state from `uiFloorplanScopeRegistry`, activate existing route/tab identities, resolve nearest ancestors component-wise, and reject zero/multiple manifest joins by exact canonical key. |
| T-26-05-02 | Tampering | Path-variant and dist/source-map scanner | mitigate | Generate slash, backslash, relative, Windows-absolute, forward-slash-absolute, and POSIX-rooted variants per manifest row; scan all emitted text/map assets and synthetic mutants with exact asset/variant diagnostics. |
| T-26-05-03 | Repudiation | Test-count and script-order evidence | mitigate | Build-first root command, UI-completeness invocation, JSON-reporter lower-bound comparison to MEMORY.md, temporary-output cleanup, and final explicit-branch detection record the gate outcome. |
| T-26-05-04 | Information disclosure | Production DOM and bundles | mitigate | Reject manifest-derived source paths, source file/symbol/fragment names, test-module names, and scope keys in every canonical DOM and every emitted dist/source-map text asset. |
| T-26-05-05 | Denial of service | Recursive dist/browser scans | mitigate | Traverse only the bounded `frontend/dist` tree and canonical state list, read known text extensions/maps, fail on unreadable files, and do not recurse into workspace, dependencies, artifacts, or database data. |
| T-26-05-06 | Elevation of privilege | Auth/login/forbidden browser states | mitigate | Reuse existing mock-login/read-only helpers, assert login and forbidden roots without changing credentials, guards, redirects, permissions, or API mutations, and keep source-aware joins inert. |
| T-26-05-SC | Tampering | npm/package supply chain | accept | No package install or dependency/lockfile change is authorized; only two script entries per package file are permitted. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID / item | Plan | Status | Notes |
|--------|-----------|------|--------|-------|
| GOAL | Every existing screen has an exact expectation and future visual findings resolve to editable source | 26-01–26-05 | COVERED | 26-05 closes the final rendered-DOM and emitted-bundle source-blindness proof after scope, floorplan, manifest, and instrumentation prerequisites. |
| REQ | FLOOR-01 | 26-01 | COVERED | Exact canonical scope registry is a prerequisite consumed here. |
| REQ | FLOOR-02 | 26-02 | COVERED | SAP-first floorplan/custom composition contract is a prerequisite consumed here. |
| REQ | FLOOR-03 | 26-02 | COVERED | Capability-backed floorplans and strict ALP interaction gate are prerequisites consumed here. |
| REQ | FLOOR-04 | 26-02 | COVERED | Table semantics/identifier/scroll contracts are prerequisites consumed here. |
| REQ | SOURCE-01 | 26-04 | COVERED | Opaque owner/floorplan/region markers are present before this plan scans DOM. |
| REQ | SOURCE-02 | 26-03 | COVERED | Test-owned exact source manifest is the only source-path input to this plan. |
| REQ | SOURCE-03 | 26-05 | COVERED | This plan rejects repository and absolute path leakage in canonical DOM and emitted bundles/maps. |
| RESEARCH | Contracts and observability precede presentation changes | 26-01–26-05 | COVERED | No styling remediation, capture, judging, new dependency, or production behavior is opened. |
| RESEARCH | Opaque DOM/source-path leakage requires both runtime and build-output checks | 26-04–26-05 | COVERED | Browser DOM scan and manifest-derived dist/source-map scan are separate gates. |
| CONTEXT | D-01 | 26-01 | COVERED | Browser states derive from the exact-set registry, not counts or samples. |
| CONTEXT | D-02 | 26-02 | COVERED | Browser joins use the frozen floorplan declarations without redefining them. |
| CONTEXT | D-03 | 26-02 | COVERED | Capability evidence remains prerequisite data; no ALP reclassification occurs here. |
| CONTEXT | D-04 | 26-02 | COVERED | Table semantics remain prerequisite data; no table behavior changes here. |
| CONTEXT | D-05 | 26-03–26-05 | COVERED | Source locators remain test-owned and all path variants are rejected in DOM/bundles. |
| CONTEXT | D-06 | 26-01–26-05 | COVERED | Only test/config checks run; no business, access, API/cache/lifecycle, database, or roadmap mutation. |
| CONTEXT | D-07 | 26-01–26-03, 26-05 | COVERED | Lightweight gate applies to checker/config work; final detect escalates to Full analysis if production flow appears. |

Deferred `VIEW-01`, deferred `PROD-01`, Phase 27 capture/geometry, Phase 28 judging, Phase 29 remediation, and Phase 30 rejudge/closeout remain deliberate exclusions, not gaps.

<verification>
1. Before implementation, capture the explicit branch and Lightweight graph evidence (`list_repos` once, `repo=IPCManagement`, expected zero production process/edge). If source-aware script wiring or the final detector reveals a production process, public contract, unresolved mapping, partial/truncated result, or confidence below 0.8, escalate to complete Full-analysis two-way impact/review before editing or closing.
2. Run `npm run test:source-ownership`: it must execute `npm run build -w frontend` before the unit bundle scan, then the focused headed Playwright canonical-state DOM scan, `npm run test:ui-completeness`, and the nondecreasing-count helper in that order. The unit and browser tests must report exact leaking asset/path or canonical key diagnostics, not aggregate counts.
3. Run the focused commands independently: `npm run test:source-ownership:unit -w frontend -- --maxWorkers=1`, `npm run test:source-ownership:e2e -w frontend -- --headed`, `npm run test:ui-completeness`, and `npm run test:frontend-nondecreasing`. Confirm all five MEMORY.md desktop viewports are exercised and every emitted `.map` file, when present, was scanned.
4. Run `npm run verify`, `git diff --check` on the five declared files, and the declared secret/stub scan. The count helper must prove `numTotalTestSuites` and `numTotalTests` are at least the MEMORY.md baseline, and UI-completeness must remain green.
5. Inspect the attributable diff: only `frontend/tests/uiSourceOwnershipLeakage.test.ts`, `frontend/tests/ui-source-ownership.spec.ts`, `scripts/check-frontend-test-count.mjs`, `frontend/package.json`, and `package.json` may change. No package install, dependency/lockfile edit, database or `ipc_lane1` mutation, runtime/evidence artifact, route/access/business/API/cache/lifecycle change, roadmap expansion, extra phase, or push is allowed.
6. Finish with one explicit-current-branch GitNexus `detect_changes` for `repo=IPCManagement`. Lightweight completion requires no undispositioned production process/edge; any production flow is an escalation blocker requiring the AGENTS.md Full-analysis evidence and disposition before SOURCE-03 can be marked complete.
</verification>

<success_criteria>
- SOURCE-03 is satisfied by a manifest-derived, exact-key path scanner that rejects all relative, Windows, POSIX, and absolute source-path spellings in canonical DOM and every emitted dist/source-map asset.
- Every canonical route/tab/nested-view/role/data-state row plus login/forbidden outliers renders and resolves to exactly one opaque owner/floorplan/region join at the five approved desktop viewports; no source path, symbol, fragment, test module, or scope key is exposed.
- Build-before-bundle ordering, focused Vitest/Playwright commands, UI-completeness, and the temporary nondecreasing frontend test-file/test-count gate are executable through explicit root/frontend scripts, without changing existing `verify` semantics or adding dependencies.
- Existing business/rendered behavior, route access, policy, API/cache/lifecycle, database/`ipc_lane1` lineage, package dependency state, runtime/evidence state, roadmap, and phase count remain unchanged.
- Final explicit-branch GitNexus detection is complete, reconciled, and has no undispositioned production flow; required Full-analysis escalation is performed if that condition is not met.
</success_criteria>

<output>
Create `.planning/phases/26-floorplan-scope-source-ownership/26-05-SUMMARY.md` when this plan is executed.
</output>
