---
phase: 17-frontend-ownership
plan: "08"
type: execute
wave: 6
depends_on: ["17-06", "17-07"]
files_modified:
  - docs/CURRENT-STATE.md
  - docs/ARCHITECTURE.md
  - docs/TESTING.md
  - frontend/README.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/17-frontend-ownership/17-VALIDATION.md
autonomous: true
requirements: [ARCH-17]
user_setup:
  - K6_PASSWORD must already exist in the environment for headed real-lane login; never print or persist it.
must_haves:
  truths:
    - "D-11: full unit/lint/dependency/build/contract/backend/browser gates pass"
    - "D-12: verification performs no push, reset, seed, import or preserved-lane mutation"
  artifacts:
    - docs/CURRENT-STATE.md
    - .planning/phases/17-frontend-ownership/17-VALIDATION.md
  key_links:
    - source ownership -> automated gates -> headed browser evidence -> CURRENT-STATE/GSD closeout
---

# Phase 17 Plan 08: Full gates, real-browser parity, and closeout memory

<objective>
Prove Gate 17 across source, contracts, dependency graph, tests, production build and three real-browser viewports; synchronize project and GSD memory without mutating the preserved database lane.
</objective>

<threat_model>- **T-17-01/T-17-02 HIGH:** a static-green refactor may still regress session refresh, permission navigation, warm cache behavior or runtime endpoint registration. Real headed-browser evidence is mandatory.</threat_model>

<tasks>
<task type="execute">
  <name>Task 1: Run the complete automated Gate 17 matrix</name>
  <read_first>package.json; frontend/package.json; docs/TESTING.md; 17-VALIDATION.md; AGENTS.md; current git status; GitNexus status</read_first>
  <files>Only test/implementation corrections required by verified failures; 17-VALIDATION.md</files>
  <action>Check worktree ownership and GitNexus freshness. Run public/cache characterization, full frontend tests, lint, dependency-cruiser without known violations, production build/typecheck, root `npm run verify`, `npm run check:api-contract`, backend regression included by verify, `git diff --check`, and repository secret scan using the existing project command/pattern. Do not hand-edit generated OpenAPI/schema files. Fix only regressions caused by Phase 17 and repeat the affected/full gate.</action>
  <verify><automated>npm run verify && npm run check:api-contract && git diff --check</automated></verify>
  <acceptance_criteria>All automated gates exit 0; frontend public surface remains 75 hooks with one apiSlice; dependency violations are zero without baseline; generated OpenAPI/TypeScript is deterministic; no secret finding or whitespace error.</acceptance_criteria>
  <done>Every automated Gate 17 command is green and the deterministic public/dependency/contract assertions match the target.</done>
</task>

<task type="execute">
  <name>Task 2: Run three-viewport headed Chrome parity</name>
  <read_first>AGENTS.md browser-use section; docs/CURRENT-STATE.md; docs/TESTING.md; .artifacts/shipyard-live/live-visual-audit.mjs and/or current runtime helper; Shipyard port/process status; K6_PASSWORD presence without printing it</read_first>
  <files>.artifacts/shipyard-live Phase 17 screenshots/performance/error evidence</files>
  <action>Verify FE 3001, API 8001, Shipyard 8090 and `/health/ready` against the current source/lane. Start only the required source-backed runtime without reset/seed/import. Use headed Google Chrome persistent helper from repo root and rotated credential from environment; never try admin/admin. Cover `1365×900`, `1280×900`, and `768×1024`, all affected navigation/routes, Admin panels, Reports views, warm revisits and request traces. Record screenshots, API responses, console/page/request errors, long tasks, CLS and overflow. Stop only processes created by this run.</action>
  <verify><manual>Evidence shows expected routes/panels rendered, API responses 2xx, warm revisits do not add requests for unchanged cache keys, zero console/page/request failures, no horizontal overflow, and stable CLS/long-task results at all three viewports.</manual></verify>
  <acceptance_criteria>Fresh timestamped Phase 17 evidence exists for all three viewports and proves navigation/cache/state/UI parity on the real app; database lineage is unchanged and no business mutation/import/reset/seed occurred.</acceptance_criteria>
  <done>Real headed Chrome evidence proves three-viewport navigation/cache/state/UI parity without data mutation.</done>
</task>

<task type="execute">
  <name>Task 3: Synchronize architecture docs and GSD state</name>
  <read_first>docs/CURRENT-STATE.md; docs/ARCHITECTURE.md; docs/TESTING.md; frontend/README.md; .planning/STATE.md; .planning/ROADMAP.md; all Phase 17 summaries/evidence</read_first>
  <files>docs/CURRENT-STATE.md; docs/ARCHITECTURE.md; docs/TESTING.md; frontend/README.md; .planning/STATE.md; .planning/ROADMAP.md; 17-VALIDATION.md</files>
  <action>Fact-check code/runtime evidence and update endpoint/layout/page-model ownership, zero dependency baseline, commands/results, evidence paths, verified/open items and resume checklist. Use gsd-tools state/roadmap handlers for registered mutations; do not directly rewrite STATE/ROADMAP structure. Mark validation Nyquist compliant only after every mapped automated/manual gate passes. Keep Step 18 pending and do not claim Phase 17 complete until verifier passes.</action>
  <verify><automated>git diff --check && node .gitnexus/run.cjs detect-changes --scope staged --repo IPCManagement</automated></verify>
  <acceptance_criteria>Docs match live code/gates/evidence; CURRENT-STATE contains verified/open/evidence/quality-gates/continue sections; GSD state points to Phase 17 verification; no secrets or stale branch/runtime claims; staged GitNexus scope is expected before commit.</acceptance_criteria>
  <done>Architecture/current-state/testing/frontend/GSD memory is factually synchronized and ready for independent verification.</done>
</task>
</tasks>

<verification>All Gate 17 criteria plus independent GSD verifier; no automatic push or transition to Step 18 until verification status is passed.</verification>
<must_haves><truths>- D-11 full structural/runtime gate passes.
- D-12 no push/reset/seed/import/database mutation.
- Gate 17 proves zero unapproved reverse/cycle import and stable navigation/cache/state/public-hook/generated-contract/browser behavior.</truths></must_haves>

## Artifacts this phase produces
- Fresh Phase 17 headed-browser screenshots and performance/request/error JSON.
- Updated architecture/testing/frontend/current-state/GSD memory.
- `17-VALIDATION.md` with completed sign-off.
