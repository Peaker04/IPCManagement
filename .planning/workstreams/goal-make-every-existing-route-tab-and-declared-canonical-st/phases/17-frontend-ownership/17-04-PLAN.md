---
phase: 17-frontend-ownership
plan: "04"
type: execute
wave: 3
depends_on: ["17-03"]
files_modified:
  - frontend/src/api/workflowApi.ts
  - frontend/src/api/workflowDocumentsApi.ts
  - frontend/src/features/dashboard/dashboardApi.ts
  - frontend/src/features/reports/reportsApi.ts
  - frontend/src/features/purchasing/purchasingApi.ts
  - frontend/src/features/warehouse/warehouseApi.ts
  - frontend/src/features/chef/chefApi.ts
  - frontend/src/features/approvals/approvalsApi.ts
  - frontend/src/features/admin/adminApi.ts
  - frontend/src/**
autonomous: true
requirements: [ARCH-17]
user_setup: []
must_haves:
  truths:
    - "D-01: feature injectors share one apiSlice/baseQuery/tag registry"
    - "D-02: 75 hooks, endpoint names, args, transforms and cache behavior remain stable"
    - "D-03: characterization and generated-contract gates run after every owner slice"
    - "D-10: each endpoint owner is an atomic audited commit"
  artifacts:
    - frontend/src/features/reports/reportsApi.ts
    - frontend/src/features/purchasing/purchasingApi.ts
    - frontend/src/features/warehouse/warehouseApi.ts
    - frontend/src/features/chef/chefApi.ts
    - frontend/src/features/approvals/approvalsApi.ts
  key_links:
    - workflowApi.ts compatibility barrel -> owner injectors -> apiSlice
---

# Phase 17 Plan 04: Split workflow endpoints by owner behind a stable barrel

<objective>
Replace the workflowApi endpoint monolith with owner injectors while preserving its exact 75-hook public surface, endpoint registration, generated types, request/response transforms, cache keys/tags, and invalidation behavior.
</objective>

<threat_model>- **T-17-01 CRITICAL:** changing endpoint registration/name/args or creating another `createApi` breaks authenticated caching across all operational flows. Every owner move is separately characterized and committed.</threat_model>

<tasks>
<task type="execute">
  <name>Task 1: Extract read/report/dashboard and neutral document endpoints</name>
  <read_first>frontend/src/api/workflowApi.ts; frontend/src/api/workflowCacheTags.ts; frontend/src/features/admin/adminApi.ts; frontend/src/features/coordination/coordinationApi.ts; frontend/src/api/workflowApi.publicSurface.test.ts; frontend/src/api/workflowApi.cacheContract.test.ts</read_first>
  <files>frontend/src/features/dashboard/dashboardApi.ts; frontend/src/features/reports/reportsApi.ts; frontend/src/api/workflowDocumentsApi.ts; frontend/src/api/workflowApi.ts; affected consumer imports</files>
  <action>Run impact on `workflowApi` plus every endpoint helper/export moved and report HIGH/CRITICAL before edits. Copy endpoint bodies and dependent pure transforms into their owners without renaming endpoint keys/hooks or changing generated types/tags. Give workflow documents one neutral lower-level API owner because dashboard/approvals/warehouse consume it. Update compatibility barrel registration/re-exports after each owner, ensuring no duplicate endpoint remains.</action>
  <verify><automated>npm run test:unit -w frontend -- src/api/workflowApi.publicSurface.test.ts src/api/workflowApi.cacheContract.test.ts src/api/workflowApi.cacheInvalidation.test.ts src/features/reports src/features/dashboard</automated></verify>
  <acceptance_criteria>Dashboard/reports/document endpoints have one implementation each; all original hooks and endpoint keys remain available from `@/api/workflowApi`; query args/tags/transforms and report UI tests pass.</acceptance_criteria>
  <done>Read/report/dashboard/document endpoints are singly owned and the full characterized surface remains green.</done>
</task>

<task type="execute">
  <name>Task 2: Extract purchasing and warehouse endpoints</name>
  <read_first>Current purchasing/warehouse blocks in frontend/src/api/workflowApi.ts; frontend/src/features/purchasing/**; frontend/src/features/warehouse/**; cache/public-surface tests</read_first>
  <files>frontend/src/features/purchasing/purchasingApi.ts; frontend/src/features/warehouse/warehouseApi.ts; frontend/src/api/workflowApi.ts; affected imports/tests</files>
  <action>Run impact for all moved endpoint/helper symbols. Move exact endpoint definitions and related pure types/transforms to the two feature injectors; preserve purchase/receipt/inventory invalidation fan-out, mutation bodies, status enum wire format and public hook names. Update consumers to owner modules only where dependency direction allows; keep the compatibility barrel stable.</action>
  <verify><automated>npm run test:unit -w frontend -- src/features/purchasing src/features/warehouse src/api/workflowApi.publicSurface.test.ts src/api/workflowApi.cacheContract.test.ts src/api/workflowApi.cacheInvalidation.test.ts src/api/workflowApi.approvalDecisionWire.test.ts</automated></verify>
  <acceptance_criteria>Purchasing and warehouse APIs have one definition per endpoint, all workflow hooks still export, cache invalidation and decision wire tests pass, and no UI behavior changes.</acceptance_criteria>
  <done>Purchasing/warehouse endpoint ownership is explicit with unchanged hooks, wire format and cache fan-out.</done>
</task>

<task type="execute">
  <name>Task 3: Extract chef, approvals, and admin endpoints</name>
  <read_first>Remaining chef/approval/admin blocks in frontend/src/api/workflowApi.ts; frontend/src/features/chef/**; frontend/src/features/approvals/**; frontend/src/features/admin/adminApi.ts; characterization tests</read_first>
  <files>frontend/src/features/chef/chefApi.ts; frontend/src/features/approvals/approvalsApi.ts; frontend/src/features/admin/adminApi.ts; frontend/src/api/workflowApi.ts; affected imports/tests</files>
  <action>Run impact for each endpoint/helper edited. Move exact definitions into owner injectors, merge admin endpoints into the existing adminApi without override, and preserve all query/mutation args, skip behavior at callers, workflow tags, 403/error behavior, request bodies, and hook names. Do not duplicate generated DTO types.</action>
  <verify><automated>npm run test:unit -w frontend -- src/features/chef src/features/approvals src/features/admin src/api/workflowApi.publicSurface.test.ts src/api/workflowApi.cacheContract.test.ts src/api/workflowApi.cacheInvalidation.test.ts</automated></verify>
  <acceptance_criteria>Chef/approvals/admin endpoints each have one feature owner; all relevant state/component tests and public/cache contracts pass.</acceptance_criteria>
  <done>Chef/approvals/admin endpoint ownership is explicit and focused/public/cache tests pass.</done>
</task>

<task type="execute">
  <name>Task 4: Collapse workflowApi.ts to the deterministic compatibility barrel</name>
  <read_first>frontend/src/api/workflowApi.ts; all owner API modules created in Tasks 1–3; frontend/src/api/apiSlice.ts; public/cache tests; all `@/api/workflowApi` imports</read_first>
  <files>frontend/src/api/workflowApi.ts; affected imports and mocks</files>
  <action>Run impact for `workflowApi`. Remove all endpoint implementations from the old file. Import every injector exactly once in deterministic order, export `workflowApi = apiSlice`, and re-export the same public hooks/types needed by existing consumers and mocks. Do not add `createApi`, `overrideExisting: true`, duplicated tags, or hand-maintained generated schemas.</action>
  <verify><automated>npm run test:fe:unit && npm run lint:fe && npm run depcruise:fe && npm run build:fe && npm run check:api-contract</automated></verify>
  <acceptance_criteria>`workflowApi.ts` is a compatibility barrel only; repository contains exactly one `createApi(` production call and one API reducerPath; 75-hook/endpoint/cache tests, full frontend suite, lint, dependency gate, build and contract determinism pass.</acceptance_criteria>
  <done>The old monolith contains no endpoint implementation and all 75 hooks resolve through one shared slice.</done>
</task>
</tasks>

<verification>Each owner task is an atomic commit with focused tests and staged GitNexus detect_changes; Task 4 runs the full frontend/contract gate.</verification>
<must_haves><truths>- D-01/D-02/D-03: one apiSlice and unchanged public/cache surface.
- D-10: owner groups commit independently; HIGH/CRITICAL blast radius is reported before edit.
- No endpoint or generated type is duplicated.</truths></must_haves>

## Artifacts this phase produces
- `dashboardApi`, `reportsApi`, `purchasingApi`, `warehouseApi`, `chefApi`, `approvalsApi`, expanded `adminApi`, and optional neutral `workflowDocumentsApi` injectors.
- Compatibility-only `frontend/src/api/workflowApi.ts`.
