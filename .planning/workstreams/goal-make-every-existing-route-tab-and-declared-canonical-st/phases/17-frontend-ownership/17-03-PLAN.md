---
phase: 17-frontend-ownership
plan: "03"
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - frontend/src/features/coordination/coordinationApi.ts
  - frontend/src/features/coordination/types.ts
  - frontend/src/api/coordinationApi.ts
  - frontend/src/types/coordination.ts
  - frontend/src/features/projects/weekly-menu/**
  - frontend/src/routes/routeDataPreloaders.ts
autonomous: true
requirements: [ARCH-17]
user_setup: []
must_haves:
  truths:
    - "D-01: coordination uses the one shared apiSlice"
    - "D-02: coordination endpoint keys, hooks, args and tags do not drift"
    - "D-05: projects consumes a lower owned API/type contract, not coordination feature internals"
    - "D-10: coordination and projects changes are separate audited commits"
  artifacts:
    - frontend/src/api/coordinationApi.ts
    - frontend/src/types/coordination.ts
  key_links:
    - projects/weekly-menu -> api/coordinationApi.ts
---

# Phase 17 Plan 03: Resolve projects to coordination through a lower owned API/contract

<objective>
Eliminate the dominant projects→coordination feature coupling while preserving the existing coordination endpoint keys, hooks, DTO transforms, query args, cache tags, Redux behavior, and weekly-menu UI.
</objective>

<threat_model>- **T-17-01 HIGH:** duplicated coordination injectors can register the same endpoint twice or fragment cache identity. The move must be single-source and public-surface characterized.</threat_model>

<tasks>
<task type="execute">
  <name>Task 1: Move the coordination transport boundary</name>
  <read_first>frontend/src/features/coordination/coordinationApi.ts; frontend/src/features/coordination/types.ts; frontend/src/api/apiSlice.ts; frontend/src/api/workflowCacheTags.ts; frontend/src/api/workflowApi.publicSurface.test.ts; .planning/phases/17-frontend-ownership/17-PATTERNS.md</read_first>
  <files>frontend/src/api/coordinationApi.ts; frontend/src/types/coordination.ts; frontend/src/features/coordination/coordinationApi.ts; frontend/src/features/coordination/types.ts; direct coordination consumers</files>
  <action>Run upstream impact for `coordinationApi` and every exported type/helper moved. Move the transport injector to `src/api/coordinationApi.ts` and stable cross-consumer DTO/query types to `src/types/coordination.ts`; keep feature presentation state private. Preserve every endpoint key, hook name, URL, params/body transform, FormData field, response transform, tag and `overrideExisting: false`. Use compatibility re-exports only if they do not leave duplicate definitions or forbidden edges.</action>
  <verify><automated>npm run test:unit -w frontend -- src/features/coordination src/api/workflowApi.publicSurface.test.ts src/api/workflowApi.cacheContract.test.ts</automated></verify>
  <acceptance_criteria>Exactly one coordination injector exists; `workflowApi` endpoint/hook/cache tests stay green; coordination feature behavior tests pass; moved types have one authoritative definition.</acceptance_criteria>
  <done>Coordination transport/types have a single lower owner with unchanged public/cache contracts.</done>
</task>

<task type="execute">
  <name>Task 2: Repoint projects weekly-menu consumers</name>
  <read_first>frontend/src/features/projects/weekly-menu/coordinationBoundary.test.ts; frontend/src/features/projects/weekly-menu/coordinationQueryOwnership.test.ts; all weekly-menu files named by the current 30-entry R2 baseline; frontend/src/api/coordinationApi.ts; frontend/src/types/coordination.ts</read_first>
  <files>frontend/src/features/projects/weekly-menu/**; frontend/src/routes/routeDataPreloaders.ts</files>
  <action>Run impact for each exported weekly-menu hook/model edited. Replace imports from `features/coordination` with the lower API/type contracts, preserving hook invocations, `skip`, args, transforms, import wizard behavior, query state, cache warming and UI. Do not move coordination presentation components or slice state into shared code.</action>
  <verify><automated>npm run test:unit -w frontend -- src/features/projects src/features/coordination && npm run depcruise:fe && npm run build:fe</automated></verify>
  <acceptance_criteria>No production or test file below `features/projects` imports `features/coordination`; coordination ownership/query-state tests, all weekly-menu tests, dependency gate and build pass; no endpoint/hook/cache drift.</acceptance_criteria>
  <done>Projects has zero imports from coordination feature internals and all weekly-menu behavior tests pass.</done>
</task>
</tasks>

<verification>Focused Coordination/Projects suite, public/cache contract, lint, dependency and build; detect staged changes before each atomic commit.</verification>
<must_haves><truths>- D-05 resolves ownership through a lower stable contract, not a renamed feature-to-feature edge.
- D-01/D-02 preserve one slice and endpoint identity.</truths></must_haves>

## Artifacts this phase produces
- `frontend/src/api/coordinationApi.ts`
- `frontend/src/types/coordination.ts`
