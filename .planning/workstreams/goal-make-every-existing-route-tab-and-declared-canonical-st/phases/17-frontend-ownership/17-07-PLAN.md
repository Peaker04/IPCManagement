---
phase: 17-frontend-ownership
plan: "07"
type: execute
wave: 5
depends_on: ["17-05"]
files_modified:
  - frontend/src/features/reports/pages/useReportsPageModel.ts
  - frontend/src/features/reports/pages/**Model.ts
  - frontend/src/features/reports/pages/**/*.test.ts
  - frontend/src/features/reports/pages/ReportsPage.tsx
autonomous: true
requirements: [ARCH-17]
user_setup: []
must_haves:
  truths:
    - "D-08: Reports model splits by existing report views after ownership is stable"
    - "D-09: skip, args, cursors, CSV, QueryView and UI behavior remain stable"
  artifacts:
    - frontend/src/features/reports/pages/useReportsPageModel.ts
  key_links:
    - ReportsPage -> compatibility composition model -> five report-view models
---

# Phase 17 Plan 07: Split the Reports page model by report use case

<objective>
Decompose `useReportsPageModel` into report-view models while preserving URL/view state, pagination/cursors, skip rules, transforms, permissions, CSV behavior, QueryView semantics, and UI.
</objective>

<threat_model>- **T-17-03 MEDIUM:** extracting conditional report queries can change hook order, warm cache reuse or forbidden/error presentation. Preserve exact skip/args and composition order.</threat_model>

<tasks>
<task type="execute">
  <name>Task 1: Characterize and extract Reports view models</name>
  <read_first>frontend/src/features/reports/pages/useReportsPageModel.ts; ReportsPage.tsx; ReportsNavigation.tsx; ReportsPricePanel.tsx; ReportQueryBoundary.tsx; reportPlanning.ts; reportCsv.ts; existing Reports tests; Phase 17 patterns</read_first>
  <files>useReportsPageModel.ts; new price/demand-purchase/stock-movement/kitchen-usage/audit-data-quality model files; focused tests</files>
  <action>Run upstream impact for `useReportsPageModel`, `toReportView`, and moved helpers; report risk. Add per-view tests, then extract models for price (four subviews), demand/purchase, stock/movement, kitchen/usage, and audit/data-quality. Preserve unconditional hook order; exact active-view/subview `skip`, args, cursor state, page sizes, date/sort filters, QueryView copy/retry/403 behavior, stale data, CSV values, URL/navigation state and returned property names. Keep a compatibility composition hook for ReportsPage.</action>
  <verify><automated>npm run test:unit -w frontend -- src/features/reports && npm run lint:fe && npm run build:fe</automated></verify>
  <acceptance_criteria>Five view responsibility groups have focused modules/tests; composed public model remains compatible; all Reports permission/state/planning/CSV tests, lint and build pass with no extra hidden-tab requests.</acceptance_criteria>
  <done>Reports page model is view-composed with unchanged public return/navigation/cache behavior and all Reports tests green.</done>
</task>
</tasks>

<verification>Reports focused suite, dependency gate and staged GitNexus audit; full frontend suite at wave end with Plan 06.</verification>
<must_haves><truths>- D-08/D-09 keep report navigation/cache/query/UI semantics.
- Hidden reports remain skipped and refreshing preserves stale authoritative data.</truths></must_haves>

## Artifacts this phase produces
- Reports model hooks for price, demand/purchase, stock/movement, kitchen/usage, and audit/data-quality under `frontend/src/features/reports/pages/`.
