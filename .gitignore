# =============================================
# .NET / C#
# =============================================
## Build results
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

## Visual Studio
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.rsuser

## NuGet
**/[Pp]ackages/*
!**/[Pp]ackages/build/
*.nupkg
**/packages/
project.lock.json
project.fragment.lock.json
artifacts/
**/.artifacts/

## User-specific files
*.suo
*.user
*.userosscache
*.sln.docstates

## Sensitive config (keep .example tracked, ignore actual secrets)
**/appsettings.json
!**/appsettings.json.example
**/appsettings.Development.json
**/appsettings.Production.json
**/appsettings.Staging.json

# =============================================
# Node.js / React / Vite
# =============================================
node_modules/
dist/

## Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

## Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

## Playwright artifacts
frontend/test-results/
frontend/playwright-report/
# Khớp mọi cấp: `dotnet test` ghi ra backend/tests/<Project>/TestResults/,
# còn npm run test:be:coverage ghi ra backend/TestResults/
TestResults/
frontend/coverage/

# =============================================
# IDE / Editor
# =============================================
.idea/
*.swp
*.swo
*~
.vscode/settings.json

# =============================================
# OS
# =============================================
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# =============================================
# Internal / AI tooling (not for repo)
# =============================================
.claude/
.planning/
AGENTS.md
CLAUDE.md
.vercel

# Dữ liệu vận hành thật của khách hàng (workbook đặt hàng, định lượng) — không đưa lên repo
.docs/
Project_Tracking*.xlsx

# P1.4: `docs/` và `scripts/` đã được gỡ khỏi danh sách ignore để tri thức vận hành
# và bộ script E2E nằm trong git — trước đó `git ls-files docs/` chỉ ra 1 file, và
# `shipyard/hooks/e2e.sh` phụ thuộc script không có trong repo nên lane E2E không
# tái lập được trên máy khác. Không đưa credential/connection string thật vào 2 thư mục này.
#
# Mục `scripts/` cũ khớp MỌI thư mục tên `scripts` ở mọi cấp, nên nó vô tình che cả
# `.codex/skills/*/scripts/`. Sau khi gỡ, chỉ còn rác build lộ ra — chặn đích danh bên dưới.

## Rác build Python của skill tooling
__pycache__/
*.pyc

## Artifact test của skill (sinh lại được, không commit)
.codex/skills/**/scripts/tests/coverage-*.json

## Lockfile của skill AI cài từ nguồn ngoài — trạng thái tooling của từng máy,
## không phải code dự án. Tự sinh lại khi cài skill, và đã suýt lọt vào một commit
## refactor ngày 27/07 vì `git add -A`.
skills-lock.json
