import { readFileSync } from 'node:fs'
import { resolve } from 'node:path'
import { describe, expect, it } from 'vitest'
const root=resolve(import.meta.dirname,'../..')
describe('Phase 29 evidence contract',()=>{it('requires exhaustive protected evidence before a headed run',()=>{const source=readFileSync(resolve(root,'.artifacts/shipyard-live/live-visual-audit.mjs'),'utf8');for(const field of ['sourceCommit','dirtySourceFingerprint','database','headed','viewports','apiResponses','consoleErrors','pageErrors','requestFailures','escapedMutations','phase29Contract'])expect(source).toContain(field);expect(source).toContain('1920x1080');expect(source).toContain('1365x900');expect(source).toContain('1280x900')});it('names every prohibited database authority independently',()=>{const sql=readFileSync(resolve(root,'tools/database/phase29-e2e-invariants.sql'),'utf8');for(const name of ['purchaseRequests','purchaseOrders','receipts','issues','movements','lots','snapshots','currentStock'])expect(sql).toContain(name)})})
