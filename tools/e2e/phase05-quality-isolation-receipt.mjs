import { chromium } from '@playwright/test'
import { execFileSync } from 'node:child_process'
import { mkdir, writeFile } from 'node:fs/promises'
import path from 'node:path'
const output=path.resolve('.artifacts/shipyard-live/phase-05-multi-customer-lifecycle/exceptions/quality-isolation/receipt');const mysql='C:/Program Files/MySQL/MySQL Server 9.5/bin/mysql.exe';const pr='PR-SUP-20260813-6F77';const mq=sql=>execFileSync(mysql,['--host=localhost','--port=3306','--user=root','--database=ipc_lane7','--batch','--raw',`--execute=${sql}`],{encoding:'utf8',env:{...process.env,MYSQL_PWD:process.env.IPC_LANE7_MYSQL_PASSWORD}}).trim();const result={verdict:'RUNNING',lane:'ipc_lane7',protectedLaneConnectionAttempts:0,requests:[],physicalInput:{pointerTrusted:false,keyboardTrusted:false,workaroundAccepted:false},consoleErrors:[],pageErrors:[],requestFailures:[]};let browser,context;await mkdir(output,{recursive:true});
const dateInput=async(page,input)=>{await input.click();await page.keyboard.press('Control+A');await page.keyboard.type('10/08/2026');await page.keyboard.press('Tab')}
try{result.dbPreflight=mq(`SELECT po.purchaseOrderCode,po.status,COUNT(ir.receiptId),(SELECT COUNT(*) FROM inventoryreceipts) FROM purchaseorders po JOIN purchaserequests pr ON pr.purchaseRequestId=po.purchaseRequestId LEFT JOIN inventoryreceipts ir ON ir.purchaseOrderId=po.purchaseOrderId WHERE pr.purchaseRequestCode='${pr}' GROUP BY po.purchaseOrderId;`);const parts=result.dbPreflight.replaceAll('\r','').split('\n').at(-1).split('\t');result.purchaseOrderCode=parts[0];if(parts[1]!=='ORDERED'||parts[2]!=='0'||parts[3]!=='6')throw new Error(`Receipt preflight drifted: ${result.dbPreflight}`);browser=await chromium.launch({channel:'chrome',headless:false,args:['--window-size=1365,900']});context=await browser.newContext({viewport:{width:1365,height:900}});await context.exposeBinding('__p05qualityReceipt',(_s,k)=>{result.physicalInput[`${k}Trusted`]=true});await context.addInitScript(()=>{addEventListener('pointerdown',e=>{if(e.isTrusted)void globalThis.__p05qualityReceipt('pointer')},true);addEventListener('keydown',e=>{if(e.isTrusted)void globalThis.__p05qualityReceipt('keyboard')},true)});const page=await context.newPage();page.on('console',m=>{if(m.type()==='error'&&!m.text().includes('status of 403'))result.consoleErrors.push(m.text())});page.on('pageerror',e=>result.pageErrors.push(e.message));page.on('requestfailed',r=>{if(r.failure()?.errorText!=='net::ERR_ABORTED')result.requestFailures.push({path:new URL(r.url()).pathname,failure:r.failure()?.errorText})});page.on('response',r=>{const p=new URL(r.url()).pathname;if(p.startsWith('/api/')&&r.request().method()!=='GET')result.requests.push({method:r.request().method(),path:p,status:r.status()})});await page.goto('http://127.0.0.1:3030/login',{waitUntil:'domcontentloaded'});await page.locator('#username').click();await page.keyboard.type('dieuphoi');await page.locator('#password').click();await page.keyboard.type(process.env.IPC_LANE7_COORDINATOR_PASSWORD);await Promise.all([page.waitForURL(u=>!u.pathname.endsWith('/login')),page.getByRole('button',{name:'Đăng nhập',exact:true}).click()]);await page.goto('http://127.0.0.1:3030/warehouse?week=2026-08-10',{waitUntil:'domcontentloaded'});await page.waitForLoadState('networkidle',{timeout:20000}).catch(()=>{});const row=page.getByRole('row').filter({hasText:result.purchaseOrderCode}).first();await row.getByRole('link',{name:'Xem dòng nhận'}).click();await page.getByRole('heading',{name:`Chi tiết ${result.purchaseOrderCode}`}).waitFor();await page.getByRole('button',{name:'Nhận toàn bộ dòng còn lại'}).click();const dialog=page.getByRole('dialog',{name:'Tạo phiếu nhập cho toàn bộ đơn mua'});const wh=dialog.getByRole('combobox',{name:'Kho nhận toàn bộ đơn mua'});await wh.click();await page.getByRole('option').first().click();await dateInput(page,dialog.getByLabel('Ngày nhận toàn bộ đơn mua'));const lot=dialog.getByLabel('Tiền tố số lô');await lot.click();await page.keyboard.type('LOT-DAV-QUALITY');const mf=dialog.getByLabel('Ngày sản xuất toàn bộ đơn mua');if(await mf.isVisible().catch(()=>false)){await dateInput(page,mf);const exp=dialog.getByLabel('Hạn sử dụng toàn bộ đơn mua');await exp.click();await page.keyboard.press('Control+A');await page.keyboard.type('17/08/2026');await page.keyboard.press('Tab')}await dialog.getByRole('button',{name:'Tiếp tục xác nhận'}).click();const rp=page.waitForResponse(r=>r.request().method()==='POST'&&new URL(r.url()).pathname.includes('/receipts'));await dialog.getByRole('button',{name:'Tạo phiếu nháp'}).click();const response=await rp;if(response.status()!==200)throw new Error(`Receipt ${response.status()}: ${JSON.stringify(await response.json().catch(()=>null))}`);await dialog.waitFor({state:'detached'});result.dbPostflight=mq(`SELECT ir.receiptCode,ir.status,ir.qualityStatus,irl.quantity,(SELECT COUNT(*) FROM inventoryreceipts) FROM inventoryreceipts ir JOIN inventoryreceiptlines irl ON irl.receiptId=ir.receiptId JOIN purchaseorders po ON po.purchaseOrderId=ir.purchaseOrderId JOIN purchaserequests pr ON pr.purchaseRequestId=po.purchaseRequestId WHERE pr.purchaseRequestCode='${pr}';`);result.verdict=/\tDRAFT\tPENDING_INSPECTION\t0\.001000\t7$/.test(result.dbPostflight.replaceAll('\r',''))&&result.physicalInput.pointerTrusted&&result.physicalInput.keyboardTrusted&&!result.consoleErrors.length&&!result.pageErrors.length&&!result.requestFailures.length?'PASS':'FAIL';await page.screenshot({path:path.join(output,'receipt-draft.png'),fullPage:true})}catch(e){result.verdict='FAIL';result.failure=String(e?.stack??e)}finally{if(context)await context.close().catch(()=>{});if(browser)await browser.close().catch(()=>{});result.finishedAtUtc=new Date().toISOString();const s=JSON.stringify(result);if(s.includes(process.env.IPC_LANE7_COORDINATOR_PASSWORD)||s.includes(process.env.IPC_LANE7_MYSQL_PASSWORD)||/Bearer\s+|"password"\s*:/i.test(s))throw new Error('Secret self-check failed');await writeFile(path.join(output,'result.json'),`${JSON.stringify(result,null,2)}\n`)}if(result.verdict!=='PASS')process.exitCode=1
