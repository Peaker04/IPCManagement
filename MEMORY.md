---
updated: 2026-09-15
branch: chore/systemic-ui-performance-artifact-retention-20260915
observed_base_head: 077779d848a02f7021faccca74904751a2428dc9
runtime_ports:
  task_frontend: 3001
  task_api: 8001
  user_frontend: 5173
  user_api: 5262
mysql: 3306
db_lane: ipc_lane9
e2e_lane: ipc_lane7
credentials_via: IPC_LANE7_<ROLE>_PASSWORD
---
# Working memory hiện hành

Working set này chỉ giữ pointer/runtime hiện hành. Luôn revalidate branch, HEAD, index, worktree, listeners, mode và database target trước hành động. Source/runtime thắng narrative stale.

## Current checkpoint

- **Systemic FE UI/UX audit/remediation COMPLETE / REVIEW_OK:** completed checklist `.planning/notes/SYSTEMIC-FE-UI-UX-REMEDIATION-PLAN.md`; canonical ledger `docs/ui-ux/SYSTEMIC_FE_UI_UX_AUDIT.md`; final evidence `.artifacts/goal-ui-ux/systemic-final-dual-mode-20260917/summary-final.json` indexed in `docs/EVIDENCE-INDEX.md`. Capability denominator reconciled DEFAULT 37/37 + MATERIAL_RECONCILIATION 9/9 under Admin read-only authority. Production-preview sequence exposed and fixed route-critical lazy first-paint CLS in Warehouse Exceptions and Reports Audit; focused reruns are 3/3 each at CLS about `0.00007`, MRX Warehouse demand/movement 2/2. Final root-owner review also closed Reports same-route Back/Forward query rehydration, multi-card Service Run ID/focus isolation, nested Dialog orphan-stack promotion, and remaining Purchasing/Chef/Weekly disabled-submit siblings. Focused changed tests 25 files/224 PASS; lint 0 errors/3 inherited warnings; production build PASS. Original mode restored `DEFAULT / version 65`; owned `3001/8001` clear. Explicit residuals: employee >200 needs natural data (lane has 7), non-Admin runtime needs credentials, and no all-role mutation/mobile/field-CWV/production-p75 claim. No push.
- **Dual-mode whole-project interaction-fluidity expansion COMPLETE / REVIEW_OK (`2fa17b0d-603a-4d57-9351-ac1ae70bd5a7`):** completed checklist `.planning/notes/DUAL-MODE-INTERACTION-FLUIDITY-PLAN.md`; ledger `docs/perf/DUAL_MODE_INTERACTION_FLUIDITY_RUNTIME_AUDIT.md`. Capability-derived Admin read-only denominator is 116/116 PASS: DEFAULT 79/79 and MATERIAL_RECONCILIATION 37/37 across every declared navigation route/retained tab, three canonical desktop route viewports, natural tab/search/filter/pagination/dialog actions, 12 continuous repeated-tab/history cells and seven excluded-route/API guards. DM-001 Reports Audit CLS persisted in DEV and preview (`0.159–0.383`) because ServiceRun first-page expansion mounted above the audit table; audit now renders first and ServiceRun initial feedback is geometry-free `sr-only`, reducing final matrix max DEFAULT load CLS to `0.04319`. Reports focused 71/71, performance/evidence contracts 8/8, lint/build/diff PASS. Public mode seam restored original DEFAULT family (authoritative run `51→53` after two authorized transitions); owned `3001/8001` clear. Evidence summary/teardown indexed and final independent review `REVIEW_OK`. Residuals: not all-role, no business mutation lifecycle, no field CWV/p75/mobile/browser-band claim. Next: independent review only.
- **Interaction fluidity optimization COMPLETE / REVIEW_OK (`4921a069-0684-4455-8b08-0407e8d30c42`):** completed checklist `.planning/notes/INTERACTION-FLUIDITY-OPTIMIZATION-PLAN.md`; ledger `docs/perf/INTERACTION_FLUIDITY_RUNTIME_AUDIT.md`. IF-A tooling is `PASS_FOCUSED/REVIEW_OK`. IF-B: Warehouse search is complete `DEVELOPMENT_DOMINANT / MEASURED_NOT_ACTIONABLE`; partial Reports Event Timing remains `NEEDS_EVIDENCE`. IF-C/IF-E: Approval History CLS root owner was a detail rail mounted before the eight-item collection stabilized (`0.1112163`, y=229→719). Shared `SplitWorkbench` now omits unavailable detail content and Approval defers the rail while loading; same-condition preview CLS is `0` at `1440×900`/`1366×768`, repeated-tab CLS `0`, and keyboard detail visibility/focus/restore passes. IF-D: repeated route-history LoAF had blocking `0`; current-source CDP longest Layout 11.692ms and FunctionCall 5.779ms, so no application owner. Warehouse rapid search and guidance dialog are bounded non-actionable/pass. Reports transition, Weekly Menu profiler, long-scroll, field CWV/RUM and all-role/all-mode remain explicit residuals. Canonical summaries/hashes are in `docs/EVIDENCE-INDEX.md`; owned `3001/8001` are clear. No blanket optimization is authorized.

- **Post-evidence publication remediation PAUSED at owner priority switch:** checklist `.planning/notes/POST-EVIDENCE-PUBLICATION-REMEDIATION-PLAN.md` remains the immutable resume owner for that campaign. Wave A is `PASS_FOCUSED/REVIEW_OK`. Wave B corrected production-preview matrix found/fixed Warehouse Exceptions loading-footprint CLS and the seven-route matrix passed, but reviewer correctly blocked closure because Approval query-string evidence was absent. The subsequent Approval queue/history run applied the normalized `/approvals` budget and failed history at `1440×900` CLS `0.11061393184894101 > 0.1`; no Approval production edit was made. Owned runtime/profile was torn down for the new owner request; `3001/8001` clear. Resume only if owner reprioritizes, starting from Approval history root-owner attribution and evidence publication/hash gate. No commit/push or runtime mutation authority is implied.

- **Prior systemic FE verification COMPLETED / read-only lineage:** checklist `.planning/notes/WHOLE-PAGE-BUSINESS-COMPOSITION-PLAN.md` §10 is no longer the active execution owner. Research/review receipts: `.artifacts/goal-ui-ux/pi-systemic-fe-01b77223/`; complete 385-rule disposition ledgers: `.artifacts/goal-ui-ux/frontend-checklist-385-20260915-sol/` (159 + 81 + 145, no missing/duplicate slug). G1–G4 focused owner packages are recorded in the checklist. G5 Weekly import setup accessibility is `PASS_FOCUSED/REVIEW_OK`: quick-customer/file labels are associated, exact quick-input names are locked, and customer/week/file validation uses alert semantics; focused 1 file/8 tests and changed-file ESLint passed. Review found no semantic regression and both test-oracle precision notes were closed. G6 dish-picker positioning is `PASS_FOCUSED/REVIEW_OK`, browser `NEEDS_EVIDENCE`: burst scroll/resize layout work is rAF-coalesced, scroll observation is passive, cleanup cancels pending work, and post-unmount events cannot reschedule it; focused 1 file/6 tests and changed-file ESLint passed. Review found no blocker and its cleanup-oracle note was closed. G7 app render/chunk recovery is `PASS_FOCUSED/REVIEW_OK`: `AppErrorBoundary` now owns an accessible full-reload recovery for unhandled render/lazy-route failures; focused rejected-`React.lazy` test verifies fallback and reload activation, and changed-file ESLint passed. Review found no blocker and its P2 oracle note was closed. No monitoring dependency was added; observability remains OPEN. G8 route-aware titles is `PASS_FOCUSED/REVIEW_OK`: one BrowserRouter-level owner covers all 14 configured routes, client navigation, trailing slashes and wildcard redirect; exact-title tests plus protected-route registry passed 2 files/10 tests, changed-file ESLint and production build passed. Review found no blocker and both P2 notes were closed. G9 Warehouse receipt quantity ARIA is `PASS_FOCUSED/REVIEW_OK`: the control retains its help ID and conditionally adds the rendered error ID; focused 1 file/18 tests and changed-file ESLint passed, no remaining static error-ID `aria-describedby` literal was found, and review found no issue. G10 table naming/header semantics is `PASS_FOCUSED/REVIEW_OK`: Approval Queue and Chef checklist use distinct region/table names while preserving table discoverability; Menu Amendment Inbox and Warehouse Receipt Lifecycle raw headers use `scope="col"`. Focused 3 files/13 tests and changed-file ESLint passed. Initial review blockers (unnamed tables, brittle detector) were fixed; final review found no issue. G11 Weekly quick-customer required semantics is `PASS_FOCUSED/REVIEW_OK`: both controlled fields now expose native required state and associated visible required markers; focused 1 file/8 tests and changed-file ESLint passed. Review found no implementation issue and its visible-marker oracle note was closed. The existing disabled-action policy remains separate. G12 production console enforcement is `PASS_SOURCE_GATE/REVIEW_OK`: source-only ESLint now errors on console usage while test diagnostics remain allowed. Full `frontend/src` lint had zero errors/three unrelated inherited hook warnings; production/test stdin probes proved the scope, and review found no issue. The single report `any` was inspected but deferred because it owns heterogeneous row/column pairing and requires deliberate genericization rather than a cosmetic cast. G13 navigation preference storage safety is `PASS_FOCUSED/REVIEW_OK`: navigation/reconciliation/admin/page-tab writes and removals, including browser `localStorage` getter access, are exception-safe; events dispatch only after successful browser-local persistence. Focused owner plus consumers passed 3 files/21 tests and changed-file ESLint. G14 Weekly Menu selection persistence is `PASS_FOCUSED/REVIEW_OK`: all customer/week memory callsites use one guarded model owner; focused 3 files/18 tests and changed-file ESLint passed, including blocked getter and normalization/write-back failure. This does not cover auth token storage. **External performance/visual-stability re-audit ACTIVE:** owner brief `C:/Users/Administrator/Downloads/AGENT_REAUDIT_AND_REMEDIATE.md` was read completely. Four read-only lanes completed under `d8af2668-f032-4d08-8902-f1e13f88d7db`; durable outputs and synthesis are in `.artifacts/goal-ui-ux/reaudit-async-performance-20260915-sol/`. The brief is normalized to 17 hypothesis families and 10 confirmed finding packages in sole checklist §10A. RA-05 Service Run Reports frontend N+1 is `PASS_FOCUSED/REVIEW_OK`: per-row full adjustment-history queries were removed in favor of embedded `lifecycle.correctionOverlay`; behavior/source gates passed 3 files/9 tests, changed-file ESLint and production build passed, final review OK. RA-04 Warehouse demand query ownership is `PASS_FOCUSED/REVIEW_OK`: the dashboard-wide four-query overview was removed; the existing workflow-documents query plus pure inbox mapping now owns receiving/movement/demand with 20/100 limits and exceptions skip. Focused 4 files/22 tests, changed-file ESLint and production build passed; final review OK after one no-change infrastructure timeout/retry. RA-03 Coordination cache invalidation is `PASS_FOCUSED/REVIEW_OK`: the broad tag was replaced by seven resource tags, explicit cross-resource dependencies preserve plans/orders/menu/schedule/audit coherence, and store fanout tests prove quick-servings/forecast avoid unrelated refetches. Focused 6 files/12 tests, changed-file ESLint and production build passed. Initial review stale-cache blockers were closed; final review OK after one no-change timeout retry. RA-02 Reports structural async state is `PASS_FOCUSED/REVIEW_OK; BROWSER_NEEDS_EVIDENCE`: all 11 consumers now retain one stable shell tree across loading/uninitialized/error/forbidden/ready/refresh. Blocking feedback and shell share one grid cell; the shell preserves footprint but is visually suppressed, inert and aria-hidden until ready. The behavior gate covers all blocking phases plus DOM identity/single mount through blocked→ready→refresh. Focused 4 files/44 tests, changed-file ESLint and production build passed. Initial review found stacked geometry and remount P1s; both closed, final review OK. RA-10 generic async geometry is `PASS_FOCUSED/REVIEW_OK; BROWSER_NEEDS_EVIDENCE`: source revalidation proved `RefreshStatus` was already clipped/absolute and geometry-neutral, so it was not falsely changed. QueryView's generic 180/380px reservations and Chef's initial-only 32rem reservation were removed. Chef now retains one content-shaped shell in the same grid cell during blocking states; hidden content is invisible/inert/aria-hidden, forbidden overrides fallback preservation, and ready refresh/truncation notices remain normal-flow decision feedback. Focused 4 files/35 tests, changed-file ESLint and production build passed. Initial review found two Chef P1 regressions and missing oracles; all closed, final review OK. RA-01 is now `PASS_FOCUSED/REVIEW_OK; BROWSER_NEEDS_EVIDENCE`: Report, Chef, QueryView and Admin boundary owners retain stable content-shaped child shells across hard blocking states. QueryView preserves only retryable error fallback; forbidden/loading/uninitialized remain invisible/inert/aria-hidden. Admin retains DOM identity through blocked→ready. Purchase Summary moved query-owned context inside the boundary and disables its external search while non-ready. Final affected suite passed 10 files/81 tests, changed-file ESLint and production build passed. Initial review found preserved-loading exposure and stale Purchase Summary controls; both closed, final review OK. Headed runtime geometry/CLS remains OPEN. Backend RA-06 is `PASS_FOCUSED`: ServiceRun projection/page reads now use a fixed-count batched loader plus one shared in-memory lifecycle mapper; relational SQLite proves 1-row and 20-row pages execute the same SELECT count under the <=18 ceiling, and focused ServiceRun tests/build pass. RA-07 is `PASS_FOCUSED/REVIEW_OK`; RA-08 was measured and closed `MEASURED_NOT_ACTIONABLE`. Bounded browser evidence exists, but no whole-browser/backend/DB production-performance PASS is claimed. Historical composition delivery and paused micro-retirement waves are not automatically resumed. Whole-FE acceptance remains unverified; corpus disposition is not compliance PASS. This explicit new owner request supersedes the historical execution-paused note below only for §10.

- **Mission System-wide Form Field Spacing, Vertical Rhythm & Internal Alignment Audit + Refactor (`PASS_FOCUSED`, build `PASS`):** Standardized `.ipc-search-field` gap (`var(--ipc-space-1, 4px)`) and typography in `tables.css`; restructured `PurchaseDecisionPanel.tsx` to canonical Gestalt rhythm (label → control 4px, control → guidance 4px, guidance → action 8px); untangled outer `TableViewport` wrappers in `PurchaseServiceDateWorkbench.tsx` and `WarehousePurchaseOrdersPanel.tsx`; added `hideLabel` to toolbar search fields in `PurchaseLineGroups.tsx` and `PurchaseOrderLineGroups.tsx`; eliminated double container padding in `SupplierQuotationSection.tsx`; re-synchronized AST pins in `formPrimitiveConvergence.test.ts`. 8/8 UI contracts (56/56 tests) passed; feature tests (18/18) passed; ESLint 0 errors; `npm run build` passed. Handover: `.planning/notes/goal-ui-ux-FORM-FIELD-SPACING-ALIGNMENT-HANDOVER.md`.

**Prior composition execution is COMPLETED/PAUSED as historical lineage.** Active execution moved to `.planning/notes/POST-EVIDENCE-PUBLICATION-REMEDIATION-PLAN.md`; do not resume older next-wave suggestions automatically.

- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — MRX Dashboard operation-mode pill retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** source tracing confirmed the static neutral `Chế độ đối chiếu` pill inside `Quy trình 4 bước` merely repeated the server-confirmed `MATERIAL_RECONCILIATION` identity already used by `DashboardPage` to select `ReconciliationDashboardPage` and already visible through the MainLayout `Chế độ vận hành` shell chip (`Đối chiếu nguyên liệu`). It owned no independent state, batch scope, lifecycle, permission, action, history or mutation. Only the static pill and its unused icon import were retired; the four named workflow links, command action, customer/time scope, KPI cards, lifecycle labels, batch routes and DEFAULT/MRX guards remain. Mounted red failed 1/1 before production edit; exact green passed 1/1; focused Dashboard/mode passed 3 files/10 tests. A wider MRX contract attempt remained 17/18 due solely to the inherited `9.95` MATCHED precision expectation in `closedLoopWorkflow.test.tsx`, unrelated to this wave. Browser remains `NEEDS_EVIDENCE` because mode switching is forbidden; no runtime, backend, database, schema, data, credential, user-port, destructive Git, stage, commit or push action occurred. Handover: `.planning/notes/goal-ui-ux-MRX-DASHBOARD-MODE-PILL-HANDOVER.md`.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — DEFAULT Purchasing supplier-evidence neutral-pill retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** the progressive supplier-price worklist → shared Drawer remains intact. Each evidence choice retains supplier name, effective quotation/latest valid receipt label and date, price/unit, backend identity/order, accessible selection name and `aria-pressed`; only the selected candidate now renders the warning `Đang chọn` status, while unselected choices no longer repeat the neutral `Bằng chứng` classification already owned by the region and evidence detail. Selection still updates the exact candidate and preserves dependent form reset, evidence/provenance, mutation payload, `expectedDecisionVersion`, superseded history, permissions and Manager approval handoff. Controlled mounted red failed 1/1 before production edit; exact green passed 1/1; focused Purchasing passed 14 files/83 tests. Browser remains `NEEDS_EVIDENCE` without a naturally populated owned supplier-evidence state; no backend, database, mode, credential, runtime/user-port, destructive Git, stage, commit or push action occurred. Handover: `.planning/notes/goal-ui-ux-PURCHASING-SUPPLIER-EVIDENCE-PILL-HANDOVER.md`.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — DEFAULT Admin generic CommandBar navigation retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** full Admin keeps the permission-filtered MainLayout `Tổng quan` and `Thực đơn tuần` destinations, while `/admin-data` keeps URL-selected panel ownership and BOM remediation identity (`dishId`, `customerId`, `tier`, `date`). Source tracing confirmed page shortcuts `Xem KHSX/BOM` and `Về bàn điều hành` were bare `/weekly-menu` and `/` routes with no customer/week/dish/tier/effective-date, selected-panel or recovery/handoff identity. Both links and the resulting redundant CommandBar were retired without a replacement menu/button; DEFAULT Admin panels, controls, queries/mutations and MRX Admin composition remain unchanged. The mounted real MainLayout + DEFAULT Admin route regression failed 1/1 before production edit, then passed 1/1; the final two-scenario file passed 2/2 and proves permission-visible shell destinations, keyboard activation, Back restoring exact `view=contracts`, retained selected panel/control and absent page shortcuts/CommandBar while preserving the BOM composition oracle. Focused Admin passed 18 files/119 tests; shell/view contracts passed 3 files/16 tests; changed-file ESLint and same-pattern scan passed. Browser remains `NEEDS_EVIDENCE`; no backend, database, mode, credential, runtime/user-port, destructive Git, stage, commit or push action occurred. Handover: `.planning/notes/goal-ui-ux-ADMIN-COMMANDBAR-NAVIGATION-HANDOVER.md`.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — Admin BOM import-preview valid-row status retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** preview preserves imported source-row grain, parsed dish/ingredient/unit/quantity/waste fields, effective scope, proposed action and warning/error reasons. Backend `DishBomImportService.BuildPreviewAsync` derives row status only from diagnostic presence; batch `CanCommit` separately requires a non-empty preview with zero error rows, so warnings remain commit-eligible and row `valid` owns no independent commit decision. The green `Hợp lệ` pill was retired only for warning/error-free rows, leaving their status cell empty while anomaly rows retain reason text. Mounted mixed-preview red failed 1/4 before the edit and exact green passed 4/4; backend DishBom import passed 12/12. Admin aggregate passed 16 files/116 tests with one unrelated inherited MRX source-change select-option failure. Browser remains `NEEDS_EVIDENCE`; no upload/mutation was manufactured and no backend, database, mode, credential, user-port, stage, commit or push action occurred. Handover: `.planning/notes/goal-ui-ux-ADMIN-BOM-VALID-ROW-STATUS-HANDOVER.md`.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — DEFAULT Purchasing price-exception duplicate-state retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** selected service-date `PriceExceptionStatus` keeps the backend-owned `blockingExceptionCount` sentence, blocked count, `Cần xử lý` warning and `Gửi duyệt ngoại lệ giá` action, but no longer repeats the zero-blocker fact with a green `Đủ căn cứ` badge. Source trace preserved service-date/FULLDAY workbench grain, selected-line supplier decision history and fingerprint/version provenance, exception approval reason/concurrency/history, `purchase.read` / `purchase.generate` / `purchase.price-exception.approve` boundaries and backend submit validation. A1, the progressive supplier-price worklist → shared Drawer and rejected A2 retirement remain unchanged. Mounted red failed 1/2 before the production edit; exact green passed 2/2 and focused Purchasing passed 13 files/82 tests. Browser remains `NEEDS_EVIDENCE` without a naturally populated owned exception state; no backend, database, mode, credential, user-port, stage, commit or push action occurred. Handover: `.planning/notes/goal-ui-ux-PURCHASING-PRICE-EXCEPTION-STATUS-HANDOVER.md`.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — Weekly Menu selected-day normal-status retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** customer/week scope, DEFAULT local persistence, MRX URL scope, schedule matrix, readiness blockers, editor/import/publish/export actions, KHSX/demand and purchasing handoff remain unchanged. Source trace confirmed `activeServiceDay` is only a local-calendar match against displayed week dates; the green `Đang thực hiện · <service label>` owned no backend lifecycle, blocker, permission, action or mutation and duplicated the matrix-owned active day/date plus `Hôm nay` marker. The green normal-success status was retired at `WeeklyScheduleSection`; the missing-day warning (`Tuần <date>` / `Chưa chọn tuần`) remains. Mounted red failed 1/3 before production edit; focused Weekly Menu passed 37 files/163 tests. Changed-file ESLint, same-pattern, scoped diff and empty-index gates passed. Browser remains `NEEDS_EVIDENCE` without naturally populated owned runtime data; no backend, database, mode, credential, user-port, stage, commit or push action occurred. Handover: `.planning/notes/goal-ui-ux-WEEKLY-SELECTED-DAY-STATUS-HANDOVER.md`.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — M7.1 Warehouse evidence-collector reviewer blocker closed (`PASS_FOCUSED`; prior browser promotion remains authoritative):** `frontend/tests/warehouseEvidenceCollector.ts` now inventories document-wide `h1…h6` records with explicit visibility and ownership (`shell-header`, `main`, `other-shell`, `overlay`, `hidden-keepalive`, `hidden-other`), excludes hidden headings, and emits logical shell-header → main → other-shell → overlay levels, names and selectors. The red-capable production-shaped DOM fixture begins with a visible sidebar `h2` before the shell-header `h1`, includes visible main `h2` siblings, hidden KeepAlive/other headings and optional overlay; raw DOM order is proven invalid while logical order passes. Warehouse contract 1 file / 20 passed / 2 evidence-dependent skipped; changed-file ESLint, focused Playwright discovery, diff check and empty-index gate passed. The exact deterministic browser baseline was not rerun because no production/runtime behavior changed and the configured canonical baseline owns `5173`, which this task must not use; the prior owned `3001/8001` six-cell headed promotion remains the runtime evidence. Historical 36-cell pre-fix manifest hash `26ddbe15…` is now registered in `docs/EVIDENCE-INDEX.md` as bounded failure lineage. Handover: `.planning/notes/goal-ui-ux-SECTION-PANEL-HEADING-HANDOVER.md`.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — M7.1 corrected Admin BOM + Warehouse headings promoted (`PASS_FOCUSED`, browser `PASS_COMPOSITION`):** immutable focused run `.artifacts/goal-ui-ux/admin-bom-warehouse-heading-browser-rerun-20260914T152705Z/manifest.json` observed all 6/6 required cells in real headed Google Chrome on owned current-source `3001/8001`, exact `ipc_lane7`, unchanged DEFAULT mode version 27, and `1366×768`, `1440×900`, `1920×1080`. Every cell had exactly one shell-header `h1`, document-wide ownership-aware logical ordering without skipped/orphan levels or duplicate visible names, non-positive document overflow, and zero console/page/request/API errors or unexpected write attempts. Admin main headings were exactly `h2 Import BOM theo đơn giá` then nested `h3 Chỉ cần nhập 3 thông tin`, with no same-name `h4`; Warehouse main headings began `h2 Không thể tạo phiếu xuất kho mới` followed by `h2 Tồn kho hiện tại` and `h2 Luân chuyển kho`. Database and migrations were Healthy; lifecycle outbox remained expected Degraded-disabled. Six screenshots, six DOM JSON files, request ledger, current source/served-module identity, hashes and exact teardown were captured. Owned listener/launcher PIDs only were stopped; `3001/8001` proved clear while user `5173/5262` remained observed-only and untouched. No mode switch, seed, business mutation or production edit occurred in the rerun. Handover: `.planning/notes/goal-ui-ux-SECTION-PANEL-HEADING-HANDOVER.md`.
- **Superseded browser finding state:** `.artifacts/goal-ui-ux/section-panel-heading-browser-promotion-retry-20260914T151139Z/manifest.json` remains preserved as the authoritative pre-fix browser failure lineage; its six Admin/Warehouse FAIL cells are superseded only by the focused current-source six-cell PASS above.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — Wave M7.1 SectionPanel semantic heading contract + headed promotion (`PASS_FOCUSED`, browser `FAIL_SCOPED`):** `MainLayout` remains the canonical route `h1`; titled `SectionPanel` defaults to route-primary `h2`, while five genuine nested/detail callsites explicitly retain `h3` and `ReconciliationSourceChangeLog` selects `h2` standalone / `h3` inside its disclosure. Reviewer fixes 1–2 remain green at source/unit level. The first headed artifact `.artifacts/goal-ui-ux/section-panel-heading-browser-promotion-20260914T092545Z/` is preserved but INVALID for promotion because its detector scoped headings to `main` and excluded the shell-header `h1`; the intermediate retry `...retry-20260914T150828Z/` is also non-authoritative because it applied raw DOM order instead of shell→main→overlay composition order. The authoritative immutable rerun is `.artifacts/goal-ui-ux/section-panel-heading-browser-promotion-retry-20260914T151139Z/manifest.json`: owned headed Chrome on `3001/8001`, `ipc_lane7`, DEFAULT version 27, three canonical viewports and 36 route/view cells. It proves exactly one document-visible shell-header `h1` in every cell, no duplicate visible heading names, no document overflow, no browser/API/request/page errors and no attempted business mutations. Result: 24 PASS, 6 FAIL and 6 natural-state `NEEDS_EVIDENCE`; Admin BOM fails `h2→h4` at `Chỉ cần nhập 3 thông tin`, and Warehouse movement fails because `Không thể tạo phiếu xuất kho mới` is the first main heading at `h4` before its `h2` work surfaces, consistently at all three viewports. Purchasing selected-date/drawer remained naturally unavailable and was not fabricated; MRX is N/A. No production edit was made. Exact owned listener/launcher PIDs were stopped and `3001/8001` proved clear; user ports were observed only and not used/stopped. Handover: `.planning/notes/goal-ui-ux-SECTION-PANEL-HEADING-HANDOVER.md`.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — systemic Reports supplier/period aggregate unit-grain discoverability + key wave (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** backend source reconfirmed supplier grain `ingredientId + supplierId + unitId` and period grain `ingredientId + unitId + calendar month`. The two aggregate tables now expose the row unit as compact ingredient metadata without adding decision columns, include `unitId` in React keys, and current-page supplier/period CSV exports include `ĐVT`. Mounted red-capable supplier/period fixtures use the same ingredient/supplier or ingredient/month across `kg` and `thùng`; pre-edit 4/4 focused assertions failed, then passed with both rows distinguishable and no duplicate-key console warning. Focused Reports passed 15 files/79 tests; changed-file ESLint, same-pattern scan, scoped diff check and empty-index gate passed. Warning/watch semantics, filters, pagination, permissions, calculations and all other columns remain unchanged. Browser remains `NEEDS_EVIDENCE` because no naturally populated owned runtime was established; no backend, database, mode, credential, user-port, stage, commit or push action occurred. Handover: `.planning/notes/goal-ui-ux-REPORTS-PRICE-AGGREGATE-UNIT-GRAIN-HANDOVER.md`.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — DEFAULT Reports period-aggregate price normal-status retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** populated `/reports?view=price&subview=period` retains the backend-owned `ingredientId + unitId + calendar month` grain, weighted monthly average/reference prices, both numeric variance columns, the 15% reference-price warning threshold, shared inclusive receipt-date filters, full-filter aggregate pagination, `PurchaseAccess` visibility and current-page CSV export. `PriceVarianceReportService` calculates `IsWarning` solely from `VariancePercentVsReference`; month-over-month variance remains numeric context and owns no separate status decision. The period table now retains `Vượt ngưỡng` for warning rows and `Theo dõi` for positive below-threshold reference variance, while zero/non-positive rows keep numeric variance without redundant green `Ổn định`. Mounted red failed 1/1 before production edit; exact green passed 1/1 and focused Reports passed 14 files/72 tests. Changed-file ESLint, same-pattern scan, scoped diff check and empty-index gate passed. Supplier and period tables/CSV still omit visible unit and their React keys omit `unitId`; this inherited grain-discoverability/key residual was recorded but not fixed because it does not invalidate the status-only wave. Browser evidence remains `NEEDS_EVIDENCE`; no backend, database, mode, credential, user-port, stage, commit or push action occurred. Handover: `.planning/notes/goal-ui-ux-REPORTS-PRICE-PERIOD-NORMAL-STATUS-HANDOVER.md`.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — DEFAULT Reports supplier-aggregate price normal-status retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** populated `/reports?view=price&subview=supplier` retains the backend-owned `ingredientId + supplierId + unitId` aggregate grain, weighted average/min/max/reference prices, numeric variance, 15% warning threshold, shared date filters, full-filter aggregate pagination, `PurchaseAccess` visibility and current-page CSV export. `PriceVarianceReportService` derives `IsWarning` solely from the already-rendered `VariancePercent`; it owns no separate normal decision. The supplier table now retains `Vượt ngưỡng` for warning rows and `Theo dõi` for positive below-threshold rows, while exact zero/non-positive rows keep their numeric variance without redundant green `Ổn định`. Mounted red failed 1/1 before production edit; exact green passed 1/1 and focused Reports passed 14 files/71 tests. Changed-file ESLint, supplier/period same-pattern scan, scoped diff check and empty-index gate passed. Period aggregate remains unchanged and deferred. Browser evidence remains `NEEDS_EVIDENCE`; no backend, database, mode, credential, user-port, stage, commit or push action occurred. Handover: `.planning/notes/goal-ui-ux-REPORTS-PRICE-SUPPLIER-NORMAL-STATUS-HANDOVER.md`.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — DEFAULT Reports receipt-line price normal-status retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** populated `/reports?view=price&subview=lines` retains the backend-owned inventory-receipt-line grain, date/ingredient/supplier filters, server search and pagination, PurchaseOrderReadAccess visibility, current-page CSV export, numeric variance, warning threshold and warning detail/actions. `PriceVarianceReportService` remains the fact owner: `IsWarning` is derived from `WorkflowReportCalculator.IsPriceIncreaseWarning(VariancePercent)`. The line table now leaves exact zero-variance rows with their existing `0%` value but no redundant green `Ổn định` badge; meaningful `Theo dõi` for positive below-threshold variance and `Vượt ngưỡng` plus detail/actions remain. Mounted red failed 1/1 before the edit on the redundant label; exact green passed 1/1 and focused Reports passed 14 files/70 tests. Changed-file ESLint, same-pattern scan, scoped diff check and empty-index gate passed. Supplier and period aggregate `Ổn định` labels remain separate deferred candidates because their grains and presentation were not edited in this one-candidate wave. Browser evidence remains `NEEDS_EVIDENCE`; no runtime, backend, database, mode, credential, user-port, stage, commit or push action occurred. Handover: `.planning/notes/goal-ui-ux-REPORTS-PRICE-LINES-NORMAL-STATUS-HANDOVER.md`.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — DEFAULT Reports purchase-plan warning vocabulary + normal-status retirement (`PASS_FOCUSED`, reviewer P1 corrected, browser `NEEDS_EVIDENCE`):** populated `/reports?view=purchase` retains day/week grain selection, date/search filters, server pagination/full-filter totals, supplier identity, quantities, permissions and page-current CSV columns/rows. Backend `PurchasingReportService` remains the owner of `(period day|week) + ingredientId + unitId` grouping, quotation-derived supplier identity, pending receipts, shortage and its three exact warning strings. The presenter now explicitly maps those exact strings to `Thiếu báo giá`, `Chờ nhập kho` and `Còn thiếu`; it no longer conflates the remaining-shortage warning with pending receipt through a generic `pending` substring. Unknown warnings retain bounded source copy, every rendered warning retains its complete first source string in `title`, and warning-free rows leave the cell empty with no `Sẵn sàng`. Reviewer-correction red failed on the exact pending-receipt label before the production edit; focused green passed 1/1 and full Reports passed 14 files/69 tests. Same-pattern scan confirmed the three service strings align with the mounted fixtures/presenter and found no Reports `Thiếu tồn kho` fallback; changed-file ESLint and scoped diff/index checks passed. Populated browser evidence remains `NEEDS_EVIDENCE`; no runtime, database, mode, credential or user-port action occurred. Handover: `.planning/notes/goal-ui-ux-REPORTS-PURCHASE-STATUS-HANDOVER.md`. Next ranked candidate: another populated report/table normal-success status only after fresh fact-owner tracing; otherwise use an owned read-only `3001/8001` Reports run when populated DEFAULT purchase data exists naturally.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — BC-01/BC-02 focused headed rerun (`PASS_COMPOSITION`, data `NEEDS_EVIDENCE`):** immutable current-source run `.artifacts/goal-ui-ux/bc01-bc02-browser-rerun-20260914T072838Z/manifest.json` used owned `3001/8001`, Google Chrome headed, `ipc_lane7`, unchanged `DEFAULT` mode version 27 and the canonical desktop matrix. All 6/6 composition cells passed: Coordination exposed exactly one visible `h2` named `Suất ăn theo ngày và ca`; `Suất dự kiến` / `Suất điều phối` stayed absent; Weekly schedule exposed exactly one horizontal owner, the canonical `[data-table-viewport="true"].ipc-weekly-menu-shell`, with computed `overflow-x:auto`; document overflow stayed absent. Console/page/request/API errors, blocked unexpected mutations and persisted credential values were zero. Both routes were naturally empty at all viewports, so populated data is separately `NEEDS_EVIDENCE`; do not seed. Six screenshots and six DOM JSON files were captured. Owned listener and launcher PIDs were stopped exactly and `3001/8001` proved clear; `5173/5262` were never used or stopped. Handover: `.planning/notes/goal-ui-ux-BC01-BC02-BROWSER-RERUN-HANDOVER.md`. Next wave: review this focused evidence, then continue another ranked mission cell; only revisit BC-01/BC-02 populated state when naturally available.
- **Superseded before evidence for BC-01/BC-02:** `.artifacts/goal-ui-ux/browser-composition-20260914T065247Z/manifest.json` remains the immutable pre-fix run (`missing-visible-heading` and `table-0-scroll-owner-count:0` at all three desktop viewports). Its other route findings and supplier-price Drawer `NEEDS_EVIDENCE` remain outside this focused rerun and are not promoted to PASS.

- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — DEFAULT Purchasing date-card normal-status retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** populated `/purchasing` keeps A1, the progressive supplier-price worklist → shared Drawer, all service-date blockers/counts, grouped rows/source-line actions, permissions, provenance and mutations. Source trace confirmed backend `PurchaseWorkbenchService` derives each date's canonical `currentStage` from pending approved demand or `PurchaseWorkbenchPolicy.ResolveStage`; that same stage drives the mounted six-stage guide and date-selection route. `approvedDemandCount` is only the per-date demand count. The date-card `Đã duyệt` badge therefore added no independent cross-date decision: it repeated approved-demand eligibility for demand/supplier/exception dates and reused the same ambiguous copy for purchase-request approval at `approved-order`, while the guide/current stage already owned lifecycle meaning. The normal label was retired at `PurchaseServiceDateWorkbench`; meaningful `Chưa tạo`, `Đã gửi duyệt`, `Đang nhập kho` and `Đã nhận đủ` states remain, with no speculative substitute. Mounted red failed 1/4 before production edit; exact green passed 4/4; focused Purchasing and changed-file ESLint/diff/index gates passed. Same-pattern production scan found no remaining date-card `Đã duyệt` rendering. Browser composition remains `NEEDS_EVIDENCE` because task-owned `3001/8001` was not established and user `5173/5262` stayed excluded. Next wave: owned `3001/8001` Purchasing browser composition, or audit another populated Purchasing normal-status badge only after a fresh owner trace.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — DEFAULT Purchasing workflow-help retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** populated `/purchasing` keeps the accepted A1 demand workspace and progressive supplier-price worklist → shared Drawer interaction. Source trace confirmed the workbench query owns week/date/stage/page scope; the six-stage guide owns stage meaning/navigation; date cards own service-date selection and per-date counts; grouped rows preserve `ingredientId + unitId` presentation with `purchaseRequestLineId` decisions; the Drawer owns supplier evidence, proposal fields and `expectedDecisionVersion`; request creation/submission/order creation retain their existing API permissions, provenance, validation, history and concurrency guards. The workflow heading help trigger `Một luồng sáu giai đoạn từ nhu cầu đã duyệt đến tiến độ nhập kho.` was retired because it merely restated the immediately mounted canonical six-stage guide and owned no scope, blocker, action, permission, history, export or deep link. Supplemental/quotation help remains because those views do not mount that guide. Mounted red failed 1/7 before production edit; focused Purchasing passed 12 files/79 tests. Same-pattern production scan found no retired workflow-help copy; changed-file ESLint and diff/index checks passed. Rejected A2 readiness summary, forced 280px table viewport, nested scrolling and permanently visible disabled submit remain absent. Browser composition remains `NEEDS_EVIDENCE` because task-owned `3001/8001` was not established and user `5173/5262` stayed excluded. Next wave: populated DEFAULT Purchasing date-card normal-status label review, or an owned `3001/8001` Purchasing browser composition pass.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — DEFAULT Weekly Menu normal-status retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** `/weekly-menu` keeps customer/week command scope, schedule editor/import/publish/export actions, URL-owned selected view, readiness blockers, demand/KHSX, purchasing handoff and all DEFAULT/MRX query/permission/mutation boundaries. The page-level pricing context still owns the applied BOM tier and source, but no longer adds the always-normal green `Đang dùng` badge when a tier exists; the blocking `Chưa cấu hình` warning remains. The duplicate unmounted `WeeklyMenuPricingContext` declaration in `WeeklyMenuCommandBar.tsx` was removed so the pricing surface has one source owner. Mounted red failed 1/1 before production edit; focused Weekly Menu passed 6 files/38 tests. Production same-pattern scan found no `Đang dùng` remnant and exactly one pricing-context export; changed-file ESLint and diff/index checks passed. Accepted IA-12 demand handoff, secondary analytical navigation and schedule/editor behavior remain unchanged. Browser composition remains `NEEDS_EVIDENCE` because task-owned `3001/8001` was not established and user `5173/5262` stayed excluded. Next wave: populated DEFAULT Purchasing composition, or an owned `3001/8001` Weekly Menu browser composition pass.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — DEFAULT Approval duplicate queue-summary retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** `/approvals` keeps `MainLayout` route identity and `purchase.request.approve` route guard; deep-link target/week/date/search filters and cursor pagination remain owned by the canonical approval inbox query; `ApprovalQueue` keeps populated record status, deadline, row detail and record-scoped decision actions; `WorkflowDocumentsState` keeps read-only purchase-document deep links; `ApprovalHistoryTab` keeps purchase-request selection and immutable decision history. `MenuAmendmentInbox` remains a separate Manager-review/Admin-execution worklist, while `MenuAmendmentReconciliation` remains a customer-scoped physical-evidence correction worklist; neither shares the canonical queue's actor/action/grain. The page-level `ContextStrip` was retired because `Trạng thái` and `Hạn gần nhất` repeated canonical queue facts without owning query scope, action, permission, deep link, history or export. Mounted red failed 2/12 before production edit; focused Approval passed 10 files/69 tests after it. Same-pattern scan found no Approval `ContextStrip`, `Hạn gần nhất` or `nearestDeadline` remnant; changed-file ESLint and diff/index checks passed. Decision mutations, receipt-detail retry, immutable history and generic CommandBar action retirement remain unchanged. Browser composition remains `NEEDS_EVIDENCE` because task-owned `3001/8001` was not established and user `5173/5262` stayed excluded. Next wave: populated DEFAULT Weekly Menu or Purchasing composition, or an owned `3001/8001` Approval browser composition pass.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — DEFAULT Coordination aggregate-summary retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** `/meal-orders` keeps `MainLayout` route identity and `HeaderInfo` date/shift scope; the order and meal-plan queries remain fail-closed through `QueryViewBoundary`; `OrderStatusBanner` keeps lifecycle/readiness/blocker state; `OrderTable` keeps customer + menu schedule + service date/day + shift rows, serving edits, signed variance and dish detail; `ActionToolbar` keeps permission/state-gated lock, selected date/shift send-to-kitchen, signoff, unlock and export commands. The page-level `ContextStrip` was retired because `Suất dự kiến`, `Suất điều phối` and `Chênh lệch` recomputed the same populated row facts without owning scope, permission, blocker, action, URL, history or export, and colored zero variance as normal success. Mounted red failed 2/6 before production edit; focused Coordination passed 11 files/75 tests after it. Same-pattern production scan found no Coordination summary remnant; changed-file ESLint and diff/index checks passed. Send-plan request scope and eligibility were unchanged. Browser composition remains `NEEDS_EVIDENCE` because owned `3001/8001` was not established and user `5173/5262` stayed excluded. Next wave: populated DEFAULT Approval composition, or an owned `3001/8001` Coordination browser composition pass.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — MRX Reconciliation comparison-question retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** the Reconciliation page keeps its customer/year/month/status/batch scope controls but no longer repeats the page question `Cần xuất và đã xuất kho` (or its presentation-only help trigger) above the canonical `Đối chiếu theo nguyên liệu` result owner. `MainLayout` remains route identity; the batch selector keeps batch/customer/week/status identity; `ReconciliationLifecycleStrip` keeps lifecycle/next-owner state; `ReconciliationComparisonTable` keeps frozen-required versus ledger-derived issued quantities; `ReconciliationDispositionDrawer` keeps permission-gated disposition; Warehouse `ReconciliationWarehousePage` + `ReconciliationIssueHistoryTable`/detail keep issue creation and append-only issue history; Admin Audit keeps immutable MRX event history. No scope, permission, query, mutation, deep link, stock snapshot/movement, batch identity or DEFAULT/MRX boundary changed. Mounted red failed 1/14 before the production edit; the exact page gate passed 14/14 and the focused Reconciliation/Warehouse/contracts gate passed 5 files/54 tests. A wider MRX attempt remained FAIL on two inherited stale expectations in `dataGrainUiContracts.test.ts` and `ClosedLoopTransferPanel.test.tsx`, unrelated to this presentation edit; do not attribute them to this wave. Same-pattern production scan found no retired label. Changed-file ESLint and diff/index checks passed. Browser composition remains `NEEDS_EVIDENCE` because task-owned `3001/8001` was not established and user `5173/5262` stayed excluded. Next wave: non-MRX retained Coordination/Approval composition, or an owned `3001/8001` MRX browser composition pass.
- **Mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5` — MRX Admin route-identity retirement (`PASS_FOCUSED`, browser `NEEDS_EVIDENCE`):** the MRX `/admin-data` page no longer repeats the shell-owned `Quản trị dữ liệu` title in a standalone card. `MainLayout` remains the route-identity owner; `ViewSwitcher` routes directly to the canonical BOM, immutable audit and batch-scoped source-change panels. The retired card owned no action, permission, query, batch scope, history, export or deep link. Mounted red failed 1/1 before the edit; the focused MRX Admin/history suite passed 19 files/117 tests after it. DEFAULT/MATERIAL_RECONCILIATION boundaries, Admin audit `sourceFamily`, batch selection, source-change history and all mutation owners remain unchanged. Browser composition is `NEEDS_EVIDENCE` because task-owned `3001/8001` was not established and user `5173/5262` remained excluded. Next ranked MRX wave: Weekly Menu, Warehouse or Reconciliation composition after a fresh owner/evidence trace.
- **Whole-project UI/UX goal active under direct Pi ownership (mission `7bbd1dc7-d315-4e42-b256-d27ded235dd5`):** DEFAULT Admin now retires the page-level per-tab `ContextStrip`; BOM scope/preview, contract records/schedules, data-quality issues/remediation, adjustment ledger, statistics/action handoffs, audit filters/export/history and employee management remain with their canonical panels and query/mutation owners. The strip owned no action, permission, audit/history or navigation and duplicated selected-panel facts; source-changes remains MRX-only and unchanged. Mounted red failed 1/1 before the edit; green Admin suite passed 17 files/115 tests. Production same-pattern scan found no Admin summary remnants. Changed-file ESLint and `git diff --check` passed; index remained empty. Browser composition is `NEEDS_EVIDENCE` because task-owned `3001/8001` was not established and user `5173/5262` remained excluded. Next ranked populated wave: MRX retained Weekly Menu/Warehouse/Reconciliation/Admin composition. Supplier-price progressive drawer remains implemented with browser acceptance pending. Systemic waves completed after cross-project research: Purchasing workflow guide retired ambiguous cross-grain counts/current badge; Approval CommandBar retired generic Duyệt/Từ chối actions that silently targeted the first record while row-owned decisions remain; table overflow ownership is now singular—`Table` is semantic markup only, `TableViewport` owns scrolling, and ReconciliationComparisonTable migrated to that boundary; DEFAULT Dashboard now retires the duplicate `Tổng quan ca hôm nay` aggregate strip while preserving risk signals, the real `roleInboxItems` queue and four-stage workflow owners. Dashboard red→green behavior gate: 1 expected failure before production edit, then 1 file/6 tests PASS; same-pattern scan found no production remnants; changed-file ESLint and diff checks PASS. DEFAULT Warehouse now also retires the page-header `Chưa xuất` / `Bếp nhận` context strip: those demand-handoff facts were repeated by the demand table/queue and rendered false-success defaults on receiving, exceptions and movement because their queries are view-skipped. Receipt lifecycle, purchase receipt commands, issue allocation/creation, current-stock snapshot, movement history, exceptions, permissions and links remain with their canonical owners. Warehouse red gate failed 1 mounted test before production edit; focused green gate passed 5 files/23 tests and same-pattern scan found no Warehouse header remnants. DEFAULT Chef now also retires the page-level three-fact `ContextStrip`: plan synchronization remains owned by `ChefProductionSection`, receipt state/action by `MaterialChecklist`, return creation/history by `OperationalActions` + `ShiftJournal`, and persisted return documents/movements by `ChefDocumentsSection`; the production-lock `ShiftAlert`, query boundaries, service-run blockers/permissions/history and all mutation owners remain unchanged. The mounted Chef composition oracle failed 1/2 before the edit; the full focused Chef suite then passed 12 files/71 tests. The obsolete source-string metric test was removed because its only subject was the retired summary, while the mounted regression now guards the page composition. Same-pattern scan found no production Chef `ContextStrip` or retired labels. Browser composition for these latest waves is `NEEDS_EVIDENCE` because task-owned `3001/8001` was not established; user `5173/5262` remained excluded. DEFAULT Reports now also retires the page-global cross-view `ContextStrip`: price warning, demand handoff, current-stock, audit and data-quality facts remain owned by their selected report surfaces and per-view queries/exports, while the retired strip showed unrelated `—`/normal values because inactive views are query-skipped. Report grain, filters, pagination, permission-gated tabs, per-view CSV exports, audit/source-line lineage and `LegacyLineageDispositionPanel` remain unchanged; legacy lineage relocation is `BLOCKED` pending complete destination/permission/history/DEFAULT-MRX evidence. Mounted red failed 1/24 before the edit; the focused Reports suite passed 11 files/63 tests after it. Same-pattern scan found no production remnants of the retired page-level labels. Browser Reports composition remains `NEEDS_EVIDENCE` because task-owned `3001/8001` was not established. Screenshot intake merge blocker fixed with 3/3 CLI regressions; handover `.planning/notes/goal-ui-ux-SCREENSHOT-INTAKE-FIX-HANDOVER.md`. Broad build remains BLOCKED by inherited TypeScript test errors. Next ranked populated wave: MRX retained Weekly Menu/Warehouse/Reconciliation/Admin composition; separately retain headed geometry/overflow evidence before considering removal of global `overflow-x: clip`.
- **Antigravity whole-project FE handoff:** `.planning/notes/ANTIGRAVITY-FRONTEND-WHOLE-PROJECT-HANDOVER.md` là packet authority cho quy trình/context FE toàn dự án và supersede `.planning/notes/project-information-simplification-FE-HANDOVER.md`. Bàn giao packet cho Antigravity; bắt đầu W0 inventory + before/after IA matrix, rồi từng visible SHORTEN/MERGE/MOVE_TO_DETAIL/RETIRE wave có red-capable oracle. Không chạy broad gate trước focused red→green. GSD vẫn là state owner duy nhất; một FE writer/cwd.
- **Information simplification mission `a30c4106-b4db-42c3-8580-0c86c8fa8f37`:** `COMPLETE_WITH_DECLARED_RESIDUALS`. Owner `.planning/notes/project-information-simplification-PLAN.md`; acceptance `.artifacts/information-simplification/ia-visible-wave-20260913/v10-owner-acceptance.json`. V9 final sau tooling follow-up: 40/40 = 32 PASS, 8 NEEDS_EVIDENCE, 0 OPEN/BLOCKED/FAIL. Approval keyboard automation là `NEEDS_EVIDENCE_TOOLING`, không phải app defect đã xác nhận; experimental production edits đã revert. Không tự reopen mission.
- **Current-tree mounted inventory:** 15 route nodes, 69 presentation owners, 62 tables, 42 dialogs, 9 switchers, 78 action owners/296 actions, 1 drawer. Inventory/route/mode/preload focused suite: 6 files/50 tests PASS. Counts là current-tree inventory, không phải mục tiêu ép xóa.

## Verified lifecycle anchors

- **DEFAULT disposable headed lifecycle PASS:** `.artifacts/shipyard-live/default-full-e2e/runs/20260913-170647/`; 25 steps, zero console/page/request failures, disposable DB dropped, protected mutation false, owned 3052/8052 clear.
- **MRX disposable headed lifecycle PASS:** `.artifacts/shipyard-live/material-reconciliation-full-e2e/runs/20260913-085032/`; 49/49 steps, disposable DB dropped, protected lanes untouched, owned 3042/8042 clear.
- These runs prove bounded lifecycle behavior only; they do not automatically certify later UI composition, usability or performance.

## Active safety boundaries

- Worktree intentionally dirty with inherited FE/BE work. Never reset/restore/stage/commit/push unless Kỳ grants separate authority.
- No schema migration, seed, mode switch, credential creation, protected business mutation or destructive cleanup by default.
- Never assume a lane is empty. Preserve DEFAULT/MATERIAL_RECONCILIATION separation and `docs/domain/material-reconciliation.md`.
- Do not use or stop user runtime `5173/5262`. Task-owned `3001/8001` must have owned PID manifest and exact teardown.
- Presence of `IPC_LANE7_*` variables is not permission to print/persist values. Login/read-only browser runs must redact auth and fail on unexpected business writes.
- No GitNexus unless Kỳ explicitly requests impact/blast-radius/context/detect_changes; then read `docs/GITNEXUS-POLICY.md` first.
- No commit, push, deploy, restore or database cleanup inherited from a skill instruction.

## Current runtime status

- Last task-owned `3001/8001` runtime was stopped and ports verified clear.
- User `5173/5262` remained running and untouched at last observation; revalidate rather than relying on this pointer.
- `ipc_lane7` readiness in the latest bounded read-only run: database/migrations Healthy, lifecycle outbox Degraded-disabled. This is historical evidence, not current readiness proof.

## Authority map

- Startup/permissions: `AGENTS.md`.
- Documentation map: `docs/README.md`.
- Delivery/verdicts: `docs/harness/DELIVERY.md`.
- Runtime/skills/subagents: `docs/harness/RUNTIMES.md`.
- Harness governance: `docs/harness/README.md`, `docs/harness/GOVERNANCE.md`.
- UI execution: `docs/UI-UX-EXECUTION-HARNESS.md`.
- UI architecture/readiness: `docs/DESIGN.md`.
- UI normative rules: `docs/DASHBOARD-UI-RULES.md`.
- UI measurements: `docs/UI-UX-MEASUREMENT-PROTOCOL.md`.
- Data grain: `docs/DATA-GRAIN-MATRIX.md`.
- Material reconciliation: `docs/domain/material-reconciliation.md`.
- Evidence hashes: `docs/EVIDENCE-INDEX.md` only.
- Migration/restore/browser measurement: read `LESSONS.md` first.

## Resume protocol

Read `AGENTS.md` → this file → active packet/checklist. Compare cwd, branch, HEAD, status and index. Preserve inherited dirty files. Keep one writer per cwd. After each verified wave record exact files, commands/results, evidence limits, owned processes, teardown, blockers and next step in the owning GSD checklist. MEMORY points; it does not duplicate campaign history.

## RA-06 checkpoint — validation green, review infrastructure blocked
- Service Run page projection/operational reads now use the shared fixed-count batch loader in `ServiceRunService.Projection.cs`; per-row EF reads were removed from `GetPageAsync`. Source-line lifecycle scope, intentional plan+shift operational fields/costs, DEFAULT issue ownership, FULLDAY matching, immutable snapshot replacement, ordering, and RA-07 status-filter behavior are preserved.
- SQL row volume is bounded before supplemental/audit lookup by candidate request IDs and before BOM lookup by candidate dish IDs/date range.
- SQLite interceptor gate: 1 and 20 rows execute equal SELECT counts; the fixture exercises the declaration/waiver phase, requires >10 fixed phases, <=18 total, and captures SQL `LIMIT`.
- Verified: ServiceRunLifecycleTests 17/17 PASS; `dotnet build backend/IPCManagement.slnx --no-restore` PASS 0 warnings/errors; diff check PASS; index empty.
- Final review is infrastructure-blocked: run `6cf06173-2b3f-4bc2-99ec-a22282d33538` timed out after 300000ms with no tracked child changes. Earlier completed review findings on same-date row volume and conditional test coverage were fixed; snapshot replacement was explicitly retained by project contract. Do not label REVIEW_OK until a same-protocol bounded retry succeeds.

## Current RA-07 checkpoint
- ServiceRun status-filter paging now uses a two-phase bulk read: lifecycle-status-only candidate batches through the shared canonical mapper, then full projection/operational hydration for current-page IDs only. Stored `ServiceRun.Status` remains unused for filtering.
- Relational coverage locks >page mixed status membership/count/order, page 2, CLOSED snapshots, off-page purchase hydration exclusion and equal 20-SELECT counts for 1 versus 20 candidates. Focused ServiceRun tests and backend build pass; independent review is still required.

## Post-remediation review pointer
- `C:/Users/Administrator/Downloads/IPCManagement_POST_REMEDIATION_REVIEW.md` is reconciled in sole ledger `.planning/notes/WHOLE-PAGE-BUSINESS-COMPOSITION-PLAN.md` §10A. RA-07/11/12/13/18 and RA-09 are `PASS_FOCUSED/REVIEW_OK`; RA-08 remains payload-evidence gated, RA-14 authority gated, RA-15/16 profiler/memory gated, RA-17 measured no fallback defect, RA-19 measured intentional. Final H01–H17 equality and current-source runtime evidence are in `.artifacts/goal-ui-ux/reaudit-async-performance-20260915-sol/synthesis.md`.

## RA-07 checkpoint — focused green, final review infrastructure blocked
- RA-06 independent review `73f4c1c1-0905-4d3f-95c9-bea7dd965987` completed with no issues; RA-06 is `PASS_FOCUSED/REVIEW_OK`.
- RA-07 now uses canonical status-only bulk evaluation for all filtered candidates and full operational hydration only for current-page runs; stored `ServiceRun.Status` is not trusted. Exact total/order/page, CLOSED snapshot normalization, off-page no-cost hydration and fixed SELECT-count gates were added.
- Verified after parent correction: `ServiceRunLifecycleTests` 19/19 PASS; `dotnet build backend/IPCManagement.slnx --no-restore` PASS 0 warnings/errors; `git diff --check` PASS; index empty.
- Final reviewer attempts `55776fba-940e-4950-a074-419d19044837` and same-protocol retry `3f7ef680-5caa-4d0c-b8b7-1e2c334d3a89` each timed out after 300000ms with no tracked child changes. RA-07 remains `PASS_FOCUSED; REVIEW_INFRA_BLOCKED`.
- Final residual processing completed serially: RA-08 authenticated measurement is `MEASURED_NOT_ACTIONABLE` (15 rows, 55,554 uncompressed JSON bytes, 82ms local); RA-14/H14 are external authority blocked; RA-15 was not reproduced as an actionable boundary cost and requires instrumentation authority for React attribution; RA-16 closed with the Warehouse owner fix rather than a global KeepAlive change; H16 remains conflict-blocked/not reproduced without fabricated business state. Independent final residual review was OK after stale execution-order text was corrected.
